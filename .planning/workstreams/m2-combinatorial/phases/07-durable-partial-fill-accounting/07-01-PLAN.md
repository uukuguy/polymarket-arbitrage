---
phase: 07-durable-partial-fill-accounting
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [H-005]
files_modified:
  - src/polyarb/routing/money.py
  - src/polyarb/routing/position_tracker.py
  - src/polyarb/execution/engine.py
  - tests/routing/test_position_tracker.py
  - tests/execution/test_engine.py
  - tests/cli/test_arbitrage_cli_process.py
  - docs/learning/16-部分成交如何不重不漏.md
must_haves:
  truths:
    - "Every immutable fill ID changes remaining quantity/cost basis and cash at most once across restart."
    - "Final fill consumes exact residual cost basis, leaving no rounding dust."
    - "Anonymous partial fill, overfill, zero fill, and identity conflict fail without mutation."
---

<objective>
Make partial position closes exact, idempotent, restart-safe, and observable without
introducing live order submission or venue fee reconciliation.
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Exact residual allocation and partial transition</name>
  <action>RED multi-fill BUY/SELL, remaining quantity/cost, final residual, zero/overfill/anonymous rejection. GREEN add Money proportional allocation and partial tracker mutation. Commit RED/GREEN.</action>
  <verify>uv run pytest tests/routing/test_position_tracker.py -q</verify>
</task>

<task type="tdd">
  <name>Task 2: Canonical fill identity and SQLite restart</name>
  <action>RED duplicate fill through different caller identity, response-loss replay, target conflict, restart residuals. GREEN canonical venue-fill operation IDs and atomic persistence. Commit RED/GREEN.</action>
  <verify>uv run pytest tests/routing/test_position_repository.py tests/routing/test_position_tracker.py -q</verify>
</task>

<task type="tdd">
  <name>Task 3: Engine/process proof and phase closure</name>
  <action>RED/GREEN fill-provider partial sequence and true subprocess recovery. Add teaching chapter 16, full gates, immediate SUMMARY, zero drift, learnings/ROADMAP/STATE/JOURNAL, then `make climb-cycle hypothesis=H-005`.</action>
  <verify>uv run pytest tests/models/test_slippage.py tests/routing tests/execution tests/cli -q &amp;&amp; uv run pytest tests/test_makefile.py -q &amp;&amp; make climb-check &amp;&amp; make planning-status &amp;&amp; git diff --check</verify>
</task>

</tasks>

<output>Create `07-01-SUMMARY.md` immediately after code commits.</output>

