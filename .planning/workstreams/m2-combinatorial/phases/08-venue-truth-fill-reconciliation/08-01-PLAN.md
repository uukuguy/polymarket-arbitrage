---
phase: 08-venue-truth-fill-reconciliation
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [H-006]
files_modified:
  - src/polyarb/routing/position_repository.py
  - src/polyarb/routing/position_tracker.py
  - src/polyarb/execution/engine.py
  - src/polyarb/cli_arbitrage.py
  - Makefile
  - tests/routing/test_position_repository.py
  - tests/routing/test_position_tracker.py
  - tests/execution/test_engine.py
  - tests/cli/test_arbitrage_cli.py
  - tests/cli/test_arbitrage_cli_process.py
  - tests/test_makefile.py
  - docs/learning/17-venue-truth-reconciliation.md
  - docs/learning/00-INDEX.md
  - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-01-SUMMARY.md
  - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-LEARNINGS.md
  - .planning/workstreams/m2-combinatorial/ROADMAP.md
  - .planning/workstreams/m2-combinatorial/STATE.md
  - .planning/JOURNAL.md
  - .planning/threads/execution-accounting.md
  - docs/status/climb/hypotheses.yaml
  - docs/status/climb/runs.csv
  - docs/status/climb/session-state.json
  - docs/status/climb/research-tree.json
  - docs/status/climb/research-tree.md
must_haves:
  truths:
    - "Only complete CONFIRMED venue facts with immutable fill identity can supersede modeled cash."
    - "Venue gross cash and fee determine exact net cash and PnL; exit price and fee-rate estimates cannot alter them."
    - "The same fill ID with different quantity/cash/fee/status/source fails atomically, including concurrent processes."
    - "Identical settlement retry after response loss returns one durable structured receipt without a second mutation."
    - "Legacy modeled Money/float receipts and paper fill behavior remain compatible."
  artifacts:
    - path: src/polyarb/routing/position_repository.py
      provides: Structured settlement receipt codec and fingerprinted atomic replay
    - path: src/polyarb/routing/position_tracker.py
      provides: Terminal venue settlement domain and exact tracker transition
    - path: tests/cli/test_arbitrage_cli_process.py
      provides: True subprocess lost-response and changed-payload conflict proof
---

<objective>
Create an exact reconciliation boundary where complete terminal venue share/cash/fee
facts replace paper price-delta accounting, while preserving H-005 remaining authority,
canonical fill identity, atomic SQLite state, and restart replay.
</objective>

<threat_model>

| ID | Threat | Severity | Required mitigation |
|---|---|---|---|
| T-08-01 | MATCHED/MINED/RETRYING cash is booked before terminal finality | HIGH | VenueSettlement accepts exactly CONFIRMED; rejection creates no receipt or mutation |
| T-08-02 | fee_rate_bps/local formula is mislabeled actual fee | HIGH | Settlement requires explicit gross Money and fee Money; no formula fallback |
| T-08-03 | same fill ID is retried with changed quantity/cash/fee/source | HIGH | versioned request fingerprint compared inside BEGIN IMMEDIATE before replay |
| T-08-04 | corrupt JSON uses missing/extra keys, bool-as-int, overflow, or inconsistent net | HIGH | exact tagged key set, strict integer types, Money construction, and net identity checks |
| T-08-05 | CLI pre-read bypasses fingerprint conflict | HIGH | venue path always calls repository apply; pre-read is display-only |

No secrets, auth material, network writes, order submission, or allowance mutation are
introduced by this plan.
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Structured settlement receipt and atomic request fingerprint</name>
  <read_first>
    - src/polyarb/routing/position_repository.py
    - tests/routing/test_position_repository.py
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-RESEARCH.md
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-PATTERNS.md
  </read_first>
  <files>
    - src/polyarb/routing/position_repository.py
    - tests/routing/test_position_repository.py
  </files>
  <action>
RED: add tests for frozen SettlementReceipt(gross_cash, fee, net_cash,
realized_pnl, source="venue-confirmed"), strict tagged JSON round-trip, legacy Money
compatibility, malformed exact-key/type/net rejection, additive
`request_fingerprint TEXT NOT NULL DEFAULT ''` migration, identical replay, and two
SQLite repository instances applying the same operation ID with different non-empty
fingerprints. Observe failures before production edits and commit RED.

GREEN: define SettlementReceipt in the repository-neutral accounting boundary used by
the codec; extend TransitionResult, validation, OperationReceipt, in-memory apply, and
SQLite apply. Encode exact integer micros with kind=settlement and fixed source. Add
request_fingerprint to schema/required columns and migrate legacy operation tables under
the existing initialization BEGIN IMMEDIATE. Extend repository `apply(...,
request_fingerprint="")`; compare operation type, target, and fingerprint inside
BEGIN IMMEDIATE before returning an old result: equal non-empty or empty+empty replay;
exactly one empty or unequal non-empty conflict. Never overwrite stored empty. Insert state, result,
and fingerprint atomically. Preserve empty legacy fingerprints and every old receipt.
Commit GREEN only after focused tests pass.
  </action>
  <verify>
    <automated>uv run pytest tests/routing/test_position_repository.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `m2_applied_operations` contains `request_fingerprint TEXT NOT NULL DEFAULT ''`.
    - Settlement JSON contains kind/source/gross_micros/fee_micros/net_micros/pnl_micros and no float cash authority.
    - Bool integer fields, missing/extra keys, overflow, and `net != gross-fee` raise RepositoryStateError.
    - An overlapping-writer test pauses writer A inside transition while writer B begins conflicting apply; after A commits, B conflicts with one row/one mutation.
    - Fingerprint tests cover equal, empty+empty, exactly-one-empty, and unequal-non-empty outcomes.
    - `uv run pytest tests/routing/test_position_repository.py -q` exits 0.
  </acceptance_criteria>
  <done>Structured settlement receipts round-trip exactly, legacy receipts remain readable, and fingerprint replay/conflict is serialized atomically under real writer overlap.</done>
</task>

<task type="tdd">
  <name>Task 2: Complete terminal venue truth supersedes modeled cash</name>
  <read_first>
    - src/polyarb/routing/position_tracker.py
    - src/polyarb/routing/money.py
    - src/polyarb/routing/quantity.py
    - src/polyarb/routing/position_repository.py
    - tests/routing/test_position_tracker.py
    - docs/superpowers/specs/2026-07-17-m2-venue-truth-reconciliation-design.md
  </read_first>
  <files>
    - src/polyarb/routing/position_tracker.py
    - tests/routing/test_position_tracker.py
  </files>
  <action>
RED: specify VenueSettlement with exact gross cash, exact fee, status CONFIRMED, and
non-empty source_ref. Add BUY 100 @ .40 vector: partial quantity 30, deliberately wrong
exit_price=.90, gross=13.80, fee=.30 yields allocated cost=12, net=13.50, PnL=1.50,
remaining q=70/cost=28, balance=973.50. Add identical restart replay and conflicting
same-fill fee/quantity/source tests. Add nonterminal, missing ID/source, negative
gross/fee, fee>gross, zero/overfill rollback tests and modeled-path compatibility.
Observe expected failures and commit RED.

GREEN: add frozen VenueSettlement validation and attach it optionally to Fill without
changing legacy constructors. Build a deterministic versioned fingerprint from market,
filled quantity micros, gross/fee micros, status, and source_ref. In
close_position_with_fill, retain H-005 validation/allocation. Venue path requires fill
ID, computes `net=gross-fee`, `pnl=net-allocated_cost`, books balance += net and exact
realized PnL, returns SettlementReceipt, and passes fingerprint to repository.apply.
It must not call Money.pnl_for or use exit_price for venue cash. Modeled path remains
unchanged. Return a public result that lets Engine/CLI distinguish structured venue
receipt from modeled Money without losing legacy float PnL compatibility. Commit GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/routing/test_position_tracker.py tests/routing/test_position_repository.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A wrong exit price cannot change the exact gross=.13.8/fee=.3/net=13.5/PnL=1.5 vector.
    - VenueSettlement rejects every status except CONFIRMED and every incomplete/invalid exact field before mutation.
    - Duplicate identical settlement after SQLite restart returns the same structured receipt and unchanged q/cost/balance.
    - Non-CONFIRMED status fails VenueSettlement validation without repository mutation.
    - Same fill ID with changed quantity, gross, fee, or source on a valid CONFIRMED settlement raises operation identity conflict.
    - Existing paper/model tests remain green.
  </acceptance_criteria>
  <done>Complete CONFIRMED venue facts override modeled cash while invalid/nonterminal facts and conflicting valid retries fail without mutation.</done>
</task>

<task type="tdd">
  <name>Task 3: Engine, CLI, Makefile, and subprocess proof</name>
  <read_first>
    - src/polyarb/execution/engine.py
    - src/polyarb/cli_arbitrage.py
    - Makefile
    - tests/execution/test_engine.py
    - tests/cli/test_arbitrage_cli.py
    - tests/cli/test_arbitrage_cli_process.py
    - tests/test_makefile.py
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-VALIDATION.md
  </read_first>
  <files>
    - src/polyarb/execution/engine.py
    - src/polyarb/cli_arbitrage.py
    - Makefile
    - tests/execution/test_engine.py
    - tests/cli/test_arbitrage_cli.py
    - tests/cli/test_arbitrage_cli_process.py
    - tests/test_makefile.py
  </files>
  <action>
RED: add Engine complete-settlement forwarding/restart and nonterminal leave-open tests.
Add CLI all-or-none options `--venue-cash`, `--venue-fee`, `--venue-status`,
`--venue-ref`, all requiring `--fill-id` and explicit original `--size`. Add true subprocess test: open BUY 100 @ .40;
close 30 with gross 13.80/fee .30/CONFIRMED/source trade-001 and discard stdout; replay
in a new process and assert stored structured output plus q=70/cost=28/balance=973.5;
retry fee=.31 and assert exit 2 with raw state/receipt unchanged. Add Makefile dry-run
assertions for all four venue flags. Observe failures and commit RED.

GREEN: keep Engine as a fact-forwarding boundary; no fee math. Make CLI venue options
plus explicit size all-or-none, construct complete Fill/VenueSettlement on first delivery and replay, and
always invoke tracker for venue retries so repository fingerprint adjudicates. Render
source/gross/fee/net/PnL from SettlementReceipt; preserve Money/float legacy output.
Extend existing `close-arb` target and help with quoted venue variables; add no command.
Commit GREEN and immediately create the initial 08-01-SUMMARY anchor before any later commit.
  </action>
  <verify>
    <automated>uv run pytest tests/execution/test_engine.py tests/cli/test_arbitrage_cli.py tests/cli/test_arbitrage_cli_process.py tests/test_makefile.py -q</automated>
  </verify>
  <acceptance_criteria>
    - CLI rejects any partial venue option set, omitted explicit size, or venue truth without fill ID with exit code 2 and no mutation.
    - Identical subprocess replay returns source/gross/fee/net/PnL from stored receipt; changed fee conflicts.
    - `make -n close-arb ... size=30 venue_cash=13.80 venue_fee=.30 venue_status=CONFIRMED venue_ref=trade-001 fill_id=fill-001` forwards every flag.
    - Focused Engine/CLI/process/Makefile command exits 0 and initial 08-01-SUMMARY exists immediately after code commit.
  </acceptance_criteria>
  <done>Engine forwards complete facts, CLI/Makefile require reconstructible exact venue inputs, and real subprocess replay/conflict proves one durable mutation.</done>
</task>

<task type="execute">
  <name>Task 4: Teaching, full gates, SUMMARY, learnings, and climb closure</name>
  <read_first>
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-VALIDATION.md
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-01-PLAN.md
    - docs/learning/16-部分成交如何不重不漏.md
    - .planning/workstreams/m2-combinatorial/phases/07-durable-partial-fill-accounting/07-01-SUMMARY.md
    - .planning/threads/execution-accounting.md
  </read_first>
  <files>
    - docs/learning/17-venue-truth-reconciliation.md
    - docs/learning/00-INDEX.md
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-01-SUMMARY.md
    - .planning/workstreams/m2-combinatorial/phases/08-venue-truth-fill-reconciliation/08-LEARNINGS.md
    - .planning/workstreams/m2-combinatorial/ROADMAP.md
    - .planning/workstreams/m2-combinatorial/STATE.md
    - .planning/JOURNAL.md
    - .planning/threads/execution-accounting.md
    - docs/status/climb/hypotheses.yaml
    - docs/status/climb/runs.csv
    - docs/status/climb/session-state.json
    - docs/status/climb/research-tree.json
    - docs/status/climb/research-tree.md
  </files>
  <action>
Confirm the initial 08-01-SUMMARY anchor exists immediately after Task 3 code commit.
Write teaching chapter 17 with 30-second model, file:line code map, finality vs fee-rate
truth, fingerprint concurrency, design trade-offs, five adversarial questions, and FAQ
increment area; update index. Run the complete validation commands including targeted
Ruff over every changed Python file. Fill SUMMARY with actual commit hashes/evidence,
extract 08-LEARNINGS, update ROADMAP/STATE/JOURNAL/execution-accounting thread, and run
`make planning-status` until zero drift. Then run `make climb-cycle hypothesis=H-006`,
verify five 100 subscores and deterministic research-tree state, and commit closure.
  </action>
  <verify>
    <automated>uv run pytest tests/models/test_slippage.py tests/routing tests/execution tests/cli -q &amp;&amp; uv run pytest tests/test_makefile.py tests/climb -q &amp;&amp; uv run ruff check src/polyarb/routing/position_repository.py src/polyarb/routing/position_tracker.py src/polyarb/execution/engine.py src/polyarb/cli_arbitrage.py tests/routing/test_position_repository.py tests/routing/test_position_tracker.py tests/execution/test_engine.py tests/cli/test_arbitrage_cli.py tests/cli/test_arbitrage_cli_process.py tests/test_makefile.py &amp;&amp; make planning-status &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `docs/learning/17-venue-truth-reconciliation.md`, 08-01-SUMMARY.md, and 08-LEARNINGS.md exist before Phase 8 is marked complete.
    - SUMMARY lists actual RED/GREEN commits and full test/Ruff/raw SQLite evidence.
    - ROADMAP/STATE/JOURNAL/thread agree that Phase 8 is complete and H-006 is adjudicated.
    - Full M2, Makefile, climb, planning-status, Ruff, and diff gates exit 0; H-006 cycle records five 100 subscores before confirmation.
  </acceptance_criteria>
  <done>Phase 8 has zero drift, durable teaching/learning/state artifacts, complete verification evidence, and H-006 confirmed by the climb adapter.</done>
</task>

</tasks>

<verification>

1. Every production behavior is preceded by a failing test and focused GREEN evidence.
2. Raw SQLite proves exact integer settlement receipt fields and non-empty fingerprint.
3. Formula-supersession vector uses a deliberately wrong price but exact venue cash.
4. Two-process identical replay and changed-payload conflict both preserve one mutation.
5. `make planning-status` reports zero drift immediately before and after H-006 cycle.

</verification>

<success_criteria>

- H-006 venue truth is complete, terminal, exact, fingerprinted, durable, and observable.
- Paper formulas remain explicit estimates and cannot fill missing venue cash/fee.
- H-005 fill identity, partial residuals, and restart behavior remain green.
- Phase 8 closes with teaching, SUMMARY, learnings, JOURNAL/thread state, and climb proof.

</success_criteria>

<output>Create `08-01-SUMMARY.md` immediately after Phase 8 code commits.</output>
