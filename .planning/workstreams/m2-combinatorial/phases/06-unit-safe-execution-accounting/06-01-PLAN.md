---
phase: 06-unit-safe-execution-accounting
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [H-004]
files_modified:
  - src/polyarb/routing/quantity.py
  - src/polyarb/routing/money.py
  - src/polyarb/models/signal.py
  - src/polyarb/routing/engine.py
  - src/polyarb/routing/orchestrator.py
  - src/polyarb/routing/position_tracker.py
  - src/polyarb/routing/position_repository.py
  - src/polyarb/execution/engine.py
  - src/polyarb/cli_arbitrage.py
  - tests/routing
  - tests/execution
  - tests/cli
  - docs/learning/15-成交数量与现金不是一回事.md
must_haves:
  truths:
    - "Share quantity and pUSD cash are distinct exact domain types and never compared as the same dimension."
    - "100-share BUY at 0.50 reserves 50 pUSD and closing at 0.60 yields final balance 1010 and PnL 10 from 1000."
    - "Phase 5 databases migrate transactionally to quantity and cost-basis authority with corrected open-position balance."
    - "Full-fill validation compares quantity; partial aggregation and live venue order submission remain deferred."
  artifacts:
    - path: src/polyarb/routing/quantity.py
      provides: Exact micro-share Quantity
    - path: src/polyarb/routing/position_repository.py
      provides: SQLite v3 quantity/cost-basis migration
    - path: docs/learning/15-成交数量与现金不是一回事.md
      provides: Unit-safe execution mental model
---

<objective>
Eliminate the share/cash unit collision across M2 routing, account state, full fills,
persistence, and operator views while preserving current executable entry points.
</objective>

<execution_context>
@docs/superpowers/specs/2026-07-17-m2-unit-safe-execution-accounting-design.md
@docs/superpowers/plans/2026-07-17-m2-unit-safe-execution-accounting.md
</execution_context>

<tasks>

<task type="tdd">
  <name>Task 1: Exact Quantity and cash conversion</name>
  <files>src/polyarb/routing/quantity.py, src/polyarb/routing/money.py, tests/routing/test_quantity.py, tests/routing/test_money.py</files>
  <action>RED exact six-decimal conversion, type separation, invalid/overflow, BUY/SELL collateral and PnL vectors. GREEN add Quantity and explicit Money conversion helpers. Commit RED and GREEN separately.</action>
  <verify>uv run pytest tests/routing/test_quantity.py tests/routing/test_money.py -q</verify>
  <done>Quantity and Money are exact, separate, and produce correct cash vectors.</done>
</task>

<task type="tdd">
  <name>Task 2: Explicit route, position, and full-fill contracts</name>
  <files>src/polyarb/models/signal.py, src/polyarb/routing/engine.py, src/polyarb/routing/orchestrator.py, src/polyarb/routing/position_tracker.py, src/polyarb/execution/engine.py, tests/routing, tests/execution</files>
  <action>RED explicit quantity/cost-basis fields, 100@.50 lifecycle, SELL collateral/PnL, full-fill quantity equality, and compatibility aliases. GREEN move all internal accounting reads to quantity/cost basis and keep old size/stake views read-only. Commit RED/GREEN separately.</action>
  <verify>uv run pytest tests/routing tests/execution -q</verify>
  <done>Routing and tracker cash flows are dimensionally correct with full-fill behavior preserved.</done>
</task>

<task type="tdd">
  <name>Task 3: SQLite v3 migration and restart authority</name>
  <files>src/polyarb/routing/position_repository.py, tests/routing/test_position_repository.py</files>
  <action>RED literal Phase 5 migration, open-position balance repair, integer dynamic types, idempotent restart, operations preservation, corrupt/mixed authority rollback. GREEN additive quantity/cost-basis columns, one-time transactional derivation/repair, exact reads/writes, legacy projections. Commit RED/GREEN separately.</action>
  <verify>uv run pytest tests/routing/test_position_repository.py -q &amp;&amp; git diff --check</verify>
  <done>Fresh and migrated DBs have one quantity/cash authority and correct equity.</done>
</task>

<task type="tdd">
  <name>Task 4: CLI/process compatibility, teaching, and closure</name>
  <files>src/polyarb/cli_arbitrage.py, tests/cli, tests/test_makefile.py, docs/learning, .planning, docs/status/climb</files>
  <action>RED/green explicit JSON quantity/cost_basis plus legacy stake/size compatibility and subprocess v2→v3 lifecycle. Add teaching chapter/index. Run corrected full M2, Makefile, Ruff, diff, climb-check, raw migration smoke. Immediately write 06-01-SUMMARY after code commits, require zero planning drift, extract learnings, close ROADMAP/STATE/JOURNAL, then run `make climb-cycle hypothesis=H-004`.</action>
  <verify>uv run pytest tests/models/test_slippage.py tests/routing tests/execution tests/cli -q &amp;&amp; uv run pytest tests/test_makefile.py -q &amp;&amp; make climb-check &amp;&amp; make planning-status &amp;&amp; git diff --check</verify>
  <done>H-004 is implemented, taught, summarized, drift-free, and evaluated.</done>
</task>

</tasks>

<output>
Create `06-01-SUMMARY.md` immediately after implementation commits with RED/GREEN hashes,
test counts, raw migration values, compatibility/deviation notes, and self-check.
</output>
