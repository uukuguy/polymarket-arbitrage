---
phase: 05
plan: 06
type: tdd
wave: 5
depends_on: [01, 02, 03, 04, 05]
revision: 3
autonomous: true
requirements:
  - PHASE05-R01
  - PHASE05-R02
  - PHASE05-R03
  - PHASE05-R04
  - PHASE05-R05
  - PHASE05-R06
  - PHASE05-R07
  - PHASE05-R08
files_modified:
  - tests/m1-perception/test_polywatch_healthz_watcher.py
  - scripts/polywatch/healthz_watcher.py
  - .github/workflows/polywatch-healthz.yml
  - Dockerfile
  - crontab
  - Makefile
  - docs/M1-市场感知平台使用手册.md
  - docs/learning/11-L3-K线.md
  - docs/learning/12-M1-持续运行.md
  - docs/learning/00-INDEX.md
  - .planning/workstreams/m1-perception/phases/05-ws-book-prices/05-SOAK-LOG.md
  - .planning/workstreams/m1-perception/phases/05-ws-book-prices/05-VALIDATION.md
must_haves:
  truths:
    - "The canonical production Dashboard URL resolves to the active Vercel project and never returns DEPLOYMENT_NOT_FOUND."
    - "Polywatch alerts on a failed opportunity quote feed, invalid opportunity response, L3 under-fill, L3 evidence failure, or unavailable Dashboard."
    - "A resident Fly cron machine runs Polywatch every two minutes; GitHub schedule is only a provider-independent fallback, not the primary timing SLA."
    - "Resident alerts fire on the first failure-set transition, suppress duplicate spam, remind after 30 minutes, and send one recovery notification."
    - "WAITING_FOR_EVENT alone remains an informational quiet state and does not page when event and L3 freshness pass."
    - "Production R2 read and snapshot upload succeed with r2:upload_recent_success=true."
    - "The opportunity quote worker has an exact interval of at least 24 hours with zero failed runs and max run gap <= 180 seconds."
    - "Phase 05.4 A7 remains the accepted L3 24-hour evidence; no obsolete spot-check re-soak is invented."
    - "Dashboard /status, /candidates, /signals, and one real /l3/[asset_id] route pass authenticated browser acceptance."
  artifacts:
    - path: "tests/m1-perception/test_polywatch_healthz_watcher.py"
      provides: "Decision and probe regression tests for the operational watcher."
    - path: "scripts/polywatch/healthz_watcher.py"
      provides: "One stateful watcher for L1, opportunity feed, L2/L3, and Dashboard availability."
    - path: "crontab"
      provides: "Two-minute primary production cadence on the existing Fly cron machine."
    - path: ".planning/workstreams/m1-perception/phases/05-ws-book-prices/05-SOAK-LOG.md"
      provides: "Reconciled Phase 05.4 A7 L3 verdict plus exact 24-hour quote-feed aggregate."
    - path: ".planning/workstreams/m1-perception/phases/05-ws-book-prices/05-VALIDATION.md"
      provides: "Signed Phase 05 validation."
    - path: "docs/learning/12-M1-持续运行.md"
      provides: "Operator mental model, commands, alert interpretation, recovery flow, self-check questions, and FAQ."
  key_links:
    - from: "scripts/polywatch/healthz_watcher.py"
      to: "polyarb-l1 /healthz and /arbitrage/opportunities"
      via: "scheduled read-only HTTP probes"
      pattern: "quote_feed:last_complete_age_seconds|arbitrage/opportunities"
    - from: "scripts/polywatch/healthz_watcher.py"
      to: "polyarb-l2 /healthz"
      via: "strict L3 sub-check decisions"
      pattern: "l3:active_count|l3:membership_convergence|l3:worst_market_freshness"
    - from: "scripts/polywatch/healthz_watcher.py"
      to: "Vercel production Dashboard"
      via: "non-following deployment probe"
      pattern: "POLYWATCH_DASHBOARD_URL"
---

<objective>
Close Phase 05 as an operational M1 platform, not merely a collection of
implemented data paths.

The old revision-1 Plan 06 is superseded because Phase 05.4 already replaced
six-hour spot checks with continuous 30-second process/per-market evidence and
passed an exact 24-hour A7 window. This revision consumes that evidence, repairs
the remaining production entry and archive problems, extends the existing
watcher, proves the newly shipped opportunity quote worker for 24 hours, and
signs the phase.

Design contract:
`docs/superpowers/specs/2026-07-26-m1-operational-closure-design.md`
</objective>

<tasks>

<task type="tdd">
  <name>Task 1: Add failing operational watcher and Dashboard contract tests</name>
  <files>
    tests/m1-perception/test_polywatch_healthz_watcher.py
    tests/m1-perception/test_makefile_contract.py
  </files>
  <action>
Create focused tests that import `scripts/polywatch/healthz_watcher.py` without
network access and prove these missing behaviours:

1. L1 quote age status `fail` returns `push`.
2. L1 collector state `error` or `stopped` returns `push`.
3. A valid opportunity payload with an empty list is healthy.
4. Missing/wrong strategy, wrong `profit_basis`, missing opportunity list, or
   unreachable opportunity endpoint returns `push`.
5. L2 `l3:active_count != 10` returns `push`.
6. Any failed strict evidence/convergence/freshness L3 key returns `push`.
7. `WAITING_FOR_EVENT` with fresh WS and all strict L3 checks passing remains
   `noop`.
8. Dashboard HTTP 200 and Vercel SSO 302/307 are healthy.
9. Dashboard 404 `DEPLOYMENT_NOT_FOUND`, 5xx, or transport failure returns
   `push`.
10. Makefile's default production Dashboard URL equals
    `https://polymarket-arbitrage-jiangwen-su-s-projects.vercel.app`.

Run only the new tests and confirm they fail because the new decisions/probe do
not exist, not because of import or fixture errors.
  </action>
  <verify>
`uv run pytest -q tests/m1-perception/test_polywatch_healthz_watcher.py
tests/m1-perception/test_makefile_contract.py`

Expected before implementation: new assertions fail for missing behaviour.
  </verify>
  <acceptance_criteria>
The RED test run is retained in the task summary, with each failure mapped to a
design requirement.
  </acceptance_criteria>
  <done>
Focused tests exist and have been observed failing for the intended missing
operational behaviours.
  </done>
</task>

<task type="tdd">
  <name>Task 2: Implement the unified production watcher and canonical Dashboard URL</name>
  <files>
    scripts/polywatch/healthz_watcher.py
    .github/workflows/polywatch-healthz.yml
    Dockerfile
    crontab
    Makefile
    docs/M1-市场感知平台使用手册.md
  </files>
  <action>
Implement the minimum code required by Task 1:

- Preserve existing snapshot auto-unpause exactly.
- Add an opportunity endpoint URL config with the production read-only default.
- Add a Dashboard URL config using the stable Vercel project domain.
- Fetch opportunity JSON and validate only its contract, not whether count is
  positive.
- Probe Dashboard without following redirects so deployment protection can be
  distinguished from deployment absence.
- Inspect explicit quote-feed and L3 sub-checks instead of relying only on
  top-level health.
- Aggregate all failing components into the existing one-message Telegram path.
- Add resident state so the first failure-set transition alerts immediately,
  duplicate ticks are suppressed, unresolved failures remind after 30 minutes,
  and recovery sends exactly one message.
- Package the watcher in the production image and schedule it every two minutes
  on the existing Fly cron process group. Keep GHA as a fallback only.
- Keep non-mutating failures alert-only.
- Update the workflow description and Makefile smoke defaults.
- Update the M1 manual with the canonical URL and exact alert interpretation.

Run the focused tests until GREEN, then run Ruff on changed Python files.
  </action>
  <verify>
`uv run pytest -q tests/m1-perception/test_polywatch_healthz_watcher.py
tests/m1-perception/test_makefile_contract.py`

`uv run ruff check scripts/polywatch/healthz_watcher.py
tests/m1-perception/test_polywatch_healthz_watcher.py`

`POLYWATCH_DRY_RUN=1 uv run python scripts/polywatch/healthz_watcher.py`

`make smoke-l2-dashboard`

`make -n polywatch-resident-status`
  </verify>
  <acceptance_criteria>
All focused tests and Ruff pass. The dry-run reports L1, opportunity feed, L2/L3,
and Dashboard decisions. The Dashboard smoke no longer uses the dead alias.
  </acceptance_criteria>
  <done>
The watcher and canonical Dashboard entry pass their focused tests and local
production dry-run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Repair R2 configuration and deploy the monitoring contract</name>
  <files>
    .planning/workstreams/m1-perception/phases/05-ws-book-prices/05-SOAK-LOG.md
  </files>
  <action>
Production operations are authorized by the user's M1 closure instruction.

1. Record the reproduced R2 failure: the bucket value contains literal quotes
   and trailing whitespace and fails botocore bucket validation.
2. Set only `POLYARB_R2_BUCKET=polyarb-snapshots` on `polyarb-l1`.
3. Re-resolve the started app machine after the Fly restart.
4. Run a read-only `list_objects_v2(MaxKeys=1)` probe without printing
   credentials or object names.
5. Run or await one normal subset snapshot and require a non-null
   `parquet_r2_url` plus `r2:upload_recent_success=true`.
6. Push the tested code commit to `origin/main`.
7. Require the GitHub CI, Fly deployment, Vercel deployment, and scheduled
   Polywatch workflow to complete successfully. Dispatch Polywatch once with
   `dry_run=true` and inspect all four decisions.

Do not delete R2 objects, rotate credentials, trade, or change alert recipients.
  </action>
  <verify>
`flyctl secrets list -a polyarb-l1`

Read-only R2 list probe exits 0.

`curl -fsS https://polyarb-l1.fly.dev/healthz`

`gh run list --branch main --limit 10`

`gh workflow run polywatch-healthz.yml -f dry_run=true`
  </verify>
  <acceptance_criteria>
R2 health is pass, production release identities match the pushed commit where
applicable, and the dry-run watcher reports no false alert on healthy production.
  </acceptance_criteria>
  <done>
R2 upload health is green and the pushed monitoring contract has passed its
production workflow checks.
  </done>
</task>

<task type="auto">
  <name>Task 3b: Make monitoring continuous instead of relying on GitHub schedule</name>
  <files>
    scripts/polywatch/healthz_watcher.py
    tests/m1-perception/test_polywatch_healthz_watcher.py
    Dockerfile
    crontab
    Makefile
    docs/M1-市场感知平台使用手册.md
    docs/learning/12-M1-持续运行.md
    .planning/threads/polywatch-architecture.md
  </files>
  <action>
Treat the observed GitHub schedule gaps as a production monitoring defect:

1. Preserve the actual run-history evidence, including the approximately
   3.5-hour `01:08Z → 04:43Z` gap despite `*/15`.
2. Test and implement state-transition alerting, duplicate suppression,
   30-minute reminders, failed-recovery retry, and one recovery notification.
3. Package the stdlib watcher and add a two-minute Supercronic entry.
4. Expose `make polywatch-resident-status` for cron machine, logs, and state.
5. Build and deploy only the existing `cron` process group. Do not restart the
   L1 app process or reset the active quote evidence window.
6. Require the app machine/image/start identity and run-78 anchor to remain
   unchanged.
7. Observe at least three resident ticks no more than 150 seconds apart; require
   all four decisions noop, an advancing state timestamp, and no Telegram spam.
8. Keep GHA enabled as a separate-provider fallback.
  </action>
  <verify>
`uv run pytest -q tests/m1-perception/test_polywatch_healthz_watcher.py`

`uv run ruff check scripts/polywatch/healthz_watcher.py
tests/m1-perception/test_polywatch_healthz_watcher.py`

`make polywatch-resident-status`

Read-only Fly status and quote SQL prove the app machine and run-78 anchor were
not restarted.
  </verify>
  <acceptance_criteria>
Primary monitoring runs every two minutes on the resident cron machine, state
transition/reminder/recovery behaviour is tested, three production ticks pass,
and the quote continuity identity is unchanged.
  </acceptance_criteria>
  <done>
The 24-hour quote window is actively monitored rather than passively awaited.
  </done>
</task>

<task type="auto">
  <name>Task 4: Seal 24-hour quote evidence, browser acceptance, and Phase 05 records</name>
  <files>
    .planning/workstreams/m1-perception/phases/05-ws-book-prices/05-SOAK-LOG.md
    .planning/workstreams/m1-perception/phases/05-ws-book-prices/05-VALIDATION.md
    docs/learning/11-L3-K线.md
    docs/learning/12-M1-持续运行.md
    docs/learning/00-INDEX.md
  </files>
  <action>
After the final L1 restart/deploy:

1. Query `neg_risk_quote_runs` on the exact started app machine.
2. Choose an interval of at least 24 hours beginning after the final restart.
3. Require every run to be `complete`, requested count to equal successful
   response count, no failed/collecting rows, and maximum start gap <= 180s.
4. Record first/last run ids, UTC boundaries, run count, failure count, minimum
   request/success counts, max gap, release, machine, and database path in
   `05-SOAK-LOG.md`.
5. Reconcile Phase 05.4 A7 as the authoritative L3 24-hour evidence and retain
   the earlier diagnostic-only windows as historical failures, not passes.
6. Use an authenticated browser to verify `/status`, `/candidates`, `/signals`,
   and `/l3/<real asset_id>`. Confirm application content and data timestamps,
   not merely the Vercel login page.
7. Set `05-VALIDATION.md` to `status: signed`,
   `nyquist_compliant: true`, and record every automated/manual proof.
8. Add `docs/learning/12-M1-持续运行.md` using the required 30-second mental
   model, code references, design trade-offs, operating commands, self-check
   questions, and FAQ-increment section. Update the index and existing L3 doc.
9. Run the full verification matrix and create `05-06-SUMMARY.md`.
10. Run learnings extraction, planning-status, update ROADMAP/STATE/JOURNAL and
    relevant threads, and mark Phase 05 complete only if every gate passes.
  </action>
  <verify>
`uv run pytest -q tests/`

`uv run ruff check src/ tests/`

`make dashboard-typecheck`

`make dashboard-build`

`make docs-m1-check`

`make planning-status`

Production health, opportunity, Dashboard, R2, quote interval, and authenticated
browser evidence all pass.
  </verify>
  <acceptance_criteria>
Phase 05 has a complete Summary, signed Validation, reconciled Soak Log,
operational teaching doc, clean planning status, green CI, and production
evidence meeting every truth in this plan.
  </acceptance_criteria>
  <done>
Every code, production, browser, evidence, documentation, and planning gate is
closed and reproducible.
  </done>
</task>

</tasks>

<verification>
The phase verdict is strict. Any unavailable canonical Dashboard, missing alert
coverage, R2 warning, failed/late quote run in the selected 24-hour interval,
failed browser route, red required test/build/CI gate, or unsigned validation
keeps Phase 05 open.
</verification>

<success_criteria>
- Production Dashboard is reachable at the canonical stable project URL.
- Polywatch automatically covers L1 snapshot, opportunity quote feed, L2/L3,
  and Dashboard deployment availability.
- R2 upload health is pass.
- Opportunity quote feed has a clean exact 24-hour interval.
- Existing Phase 05.4 L3 A7 evidence is reconciled without falsifying history.
- Authenticated browser routes render current production data.
- Tests, lint, build, docs, CI, planning-status, Summary, Validation, ROADMAP,
  STATE, JOURNAL, learning docs, and threads are all closed consistently.
</success_criteria>
