---
context: phase
workstream: m1-perception
phase: 05-ws-book-prices
plan: 06
task: 2
total_tasks: 4
status: blocked_before_production_migration
last_updated: 2026-07-20T09:53:35Z
---

# Next-session handoff — diagnose L3 `0/10` before the 24h soak

## Current state

Phase 05 Plans 01-05 are implemented and deployed. Inserted Phase 05.1 is now
complete: L1/L2 durable recovery, quiet-refresh mechanism, and current strict
health all passed on one unchanged instance. L2 currently has three candidates,
cursor lag zero, and strict HTTP 200.

The L3 prerequisites are repaired and fully verified locally in Phase 05.3:

- `l3:active_count = 0/10`;
- `l3:last_book_levels_write_at_s` is still cold-start/never written;
- only three current L2 candidates exist, all near-end/extreme-price markets;
- recent TOB recipe matches zero because spread is `~0.998` or incomplete;
- Alembic 006 adds nullable `no_token_id` and the mirror/temp projection carries
  complete Yes/No identity;
- `l3-seed` observes at most 100 deterministic liquid mid-market Yes books;
- promotion looks up complete pairs by `yes_token_id` and fails closed;
- the operator dry-run causes zero WS, Supabase, LKG, state, or freshness mutation;
- the locked Phase 05 gate is five markets / ten Yes+No tokens;
- the 24-hour soak has not started and cannot start from `0/10`.

## Constraints

- Preserve strict N=5 / 10 tokens; do not change recipe or health thresholds to
  turn under-fill into success.
- Diagnose first with read-only health, logs, and database queries.
- The climb adapter does not authorize deploy, restart, scale, config, secret,
  schema, or real-money actions.
- H-009 opportunity-feed production work remains separately pending.

## Next action

Obtain explicit authorization for production Alembic 006 and the L1/L2 deploy.
Then prove `l3:active_count == 10` and a real `l2_book_levels` write on the exact
deployed instance before starting the 24-hour soak. Local tests and the 100-book
sample are reachability evidence, not production readiness evidence.

```bash
$gsd-resume-work --ws m1-perception
```
