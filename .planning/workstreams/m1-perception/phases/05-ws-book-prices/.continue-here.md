---
context: phase
workstream: m1-perception
phase: 05-ws-book-prices
plan: 06
task: 2
total_tasks: 4
status: paused_before_24h_soak
last_updated: 2026-07-20T12:27:33.314Z
---

# Handoff — Phase 05 production soak is ready but not started

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual production failures. Acknowledge
> each one explicitly before starting or judging the soak.

- [ ] **Do not accept a startup-only 10/10.** Release 36 started at 10/10 and
  fell to 8/10 on the next promoter tick. The soak must cover the full 24 hours.
- [ ] **Do not query coverage from only the newest 1000 REST rows.** At handoff,
  that capped page contained four hot assets while SQL over the same interval
  proved 3840 rows across ten token assets.
- [ ] **Do not mix token count with market count.** Active health is ten tokens;
  D-12 market coverage is five promoted Yes markets after Yes/No identity mapping.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Single-sample readiness | Release 36's cold-start 10/10 regressed to 8/10 five minutes later. | blocking | Start a fresh 24h T+0 and retain every scheduled checkpoint; never backfill a formal soak from pre-soak observations. |
| Positional book semantics | Polymarket supplied farthest-first book arrays; index zero produced false spreads near 0.998. | blocking | Preserve shared BUY-descending/SELL-ascending normalization before TOB, depth, or level projection. |
| Row-limit identity leakage | Duplicate snapshots and then No-side rows consumed a five-market recipe LIMIT. | blocking | Preserve newest-row-per-asset collapse and authoritative Yes-side filtering before scanner LIMIT. |
| Capped-page coverage | A hot subset can fill PostgREST's 1000-row page and hide older active assets. | blocking | Use SQL `COUNT(DISTINCT ...)` over the exact soak interval and map Yes/No tokens explicitly. |

<current_state>

Phase 05 Plan 06 Task 1 is complete. Production Alembic is `006 (head)` and L1
release 127 mirrored snapshot 578 with 1946/1946 complete Yes/No projections.

L2 release 37 is the canonical soak baseline:

- machine: `85e647c4eed598`
- instance: `01KXZJKY9SKEJAY2DD8MMPNB2E`
- image: `deployment-01KXZJHS9QT8T6X0J33KVPTB5V`
- digest: `sha256:5da8e954897f60cf05f9d6664e99a15247d46a2bd4fd0edbb433c200af8b412c`
- deployed: `2026-07-20T10:55:01Z`

The release crossed its second real promoter tick at `11:00:20Z` with
`+0 -0 markets=5 tokens=10`. A later pre-soak read at `11:52:11Z` still returned
HTTP 200 with 10/10, promote age 108s, book-write age 5s, cursor lag 0, and 108
subscriptions. Direct SQL since release start showed 3840 book rows across all
ten token assets; all five promoted Yes markets had OHLC rows. These are strong
pre-soak baselines, but the formal 24-hour window has not started.

The root health body may say top-level `warn` while HTTP remains 200 because
`ws:connection_state=WAITING_FOR_EVENT` is informational during quiet periods.
Judge the named strict subchecks and HTTP contract, not the label in isolation.

</current_state>

<completed_work>

Completed Tasks:

- Task 1: Alembic 006 + L1/L2 deployment + production prerequisite proof — Done.
- Task 3: `docs/learning/11-L3-K线.md` and index maintenance — Done and updated
  with all three production ingestion lessons.
- Emergent fixes: `7ccd2da` book ranking, `57d3fc0` newest row per asset,
  `9451b4b` pre-LIMIT Yes-side filtering — deployed in release 37.
- Verification: 1433 passed, 1 skipped, 1 xfailed; Ruff/manual/climb/planning
  gates passed; working tree was clean before this handoff.

</completed_work>

<remaining_work>

- Task 2: Not started — create `05-SOAK-LOG.md` at a fresh T+0 and monitor for
  24 hours, sampling exact-instance health and SQL coverage about every 6 hours.
- Task 4: Not started — only after a fully green soak, sign validation, complete
  ROADMAP/STATE, create `05-06-SUMMARY.md`, extract learnings, and close Phase 05.
- Do not start H-009 opportunity-feed production work; it remains separately
  unauthorized and independent of this handoff.

</remaining_work>

<decisions_made>

- Preserve strict five markets / ten tokens and all recipe thresholds.
- Use release 37 as infrastructure baseline, but use resume time as formal soak T+0.
- Coverage queries must be SQL aggregates over the exact T+0/T+24 interval.
- Book coverage must be interpreted via the five promoted Yes market identities,
  even though `l2_book_levels` can contain all ten Yes/No token asset IDs.
- No real trading, funds, secrets, config changes, external submissions, or
  implicit soak occurred during the rollout session.

</decisions_made>

<blockers>

- Wall clock: the required 24-hour interval has not begun.
- Human action: resume this workstream to intentionally start the monitor.
- No technical production blocker is active at handoff; release 37 is healthy.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact resume state.
2. `.planning/workstreams/m1-perception/phases/05-ws-book-prices/05-06-PLAN.md` — strict D-12 acceptance gate.
3. `.planning/workstreams/m1-perception/STATE.md` — canonical M1 operational state.
4. `.planning/threads/market-observation-architecture.md` §2.9 — seed, ordering, dedupe, and identity lessons.
5. `docs/learning/11-L3-K线.md` — L3 mental model and production traps.
6. `.planning/JOURNAL.md` Sessions 72–73 — rollout timeline and handoff baseline.

## Critical Anti-Patterns (do NOT repeat these)

- Lowering thresholds to manufacture 10/10 → repair observation truth instead.
- Treating array index zero as best price → rank by side before projection.
- Letting repeated snapshots or No rows reach recipe LIMIT → normalize identity first.
- Treating `limit=1000` REST output as complete interval coverage → aggregate in SQL.
- Counting ten token asset IDs as ten markets → map back to five promoted Yes IDs.

## Infrastructure State

- Supabase schema: Alembic `006 (head)`.
- L1: release 127; snapshot 578; 1946 mirrored markets with complete pairs.
- L2: release 37 exact machine/instance/image/digest listed above; service running.
- Latest pre-soak production read: HTTP 200, 10/10, promote/book/cursor all pass.
- Local processes: none running for pytest, flyctl deploy, uvicorn, or polyarb daemon.
- Git: local `main` is 15 commits ahead of `origin/main` (`f4f095b`); production
  was built from local source. No push was performed.

<context>

The rollout is genuinely soak-ready after three production-only feedback defects
were found and repaired. The next agent should resist reopening implementation
unless a timestamped soak checkpoint supplies new evidence. The immediate job is
operations/evidence collection, not strategy changes or more threshold tuning.

</context>

<next_action>

Start with:

```bash
/gsd-execute-phase 05 --ws m1-perception
```

Then acknowledge the three blocking constraints above, create
`05-SOAK-LOG.md` with a fresh T+0, record exact release 37 identity, and capture
the first strict health plus SQL coverage reading. Do not backdate T+0 to the
deployment or handoff observations.

</next_action>
