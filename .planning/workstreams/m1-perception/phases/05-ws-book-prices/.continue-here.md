---
context: phase
workstream: m1-perception
phase: 05-ws-book-prices
plan: 06
task: 2
total_tasks: 4
status: production_prerequisites_passed_awaiting_24h_soak
last_updated: 2026-07-20T11:02:00Z
---

# Next-session handoff — start the strict 24h L3 soak

## Current state

Production Alembic is `006 (head)`. L1 release 127 mirrored snapshot 578 with
1946/1946 complete Yes/No projections. L2 release 37 is healthy on exact instance
`01KXZJKY9SKEJAY2DD8MMPNB2E` and image digest
`sha256:5da8e954897f60cf05f9d6664e99a15247d46a2bd4fd0edbb433c200af8b412c`.

Prerequisite proof through the second real tick at `2026-07-20T11:00:50Z`:

- `/health` HTTP 200, `l3:active_count = 10/10`;
- promoter age 30.0s and book-level write age 59.3s, both pass;
- the `11:00:20Z` tick logged `+0 -0 markets=5 tokens=10`;
- cursor lag 0 and 108 total WS subscriptions;
- five promoted Yes markets resolve to ten distinct Yes/No tokens;
- 280 real book-level rows since release, covering eight active tokens at the
  immediate read; level one contains executable adjacent prices;
- all five promoted Yes TOB rows have spread 0.01 and depth > $100k.

Three production-discovered fixes are deployed: price-ranked book arrays
(`7ccd2da`), newest-row-per-asset promotion input (`57d3fc0`), and authoritative
Yes-side filtering before LIMIT (`9451b4b`). Release 36 exposed the last bug by
falling from startup 10 to second-tick 8; release 37 stayed 10. Locked recipe
thresholds were unchanged. The 24-hour soak has not started.

## Constraints

- Preserve strict N=5 / 10 tokens; do not change recipe or health thresholds to
  turn under-fill into success.
- Record exact instance/release identity at every soak checkpoint.
- The climb adapter does not authorize deploy, restart, scale, config, secret,
  schema, or real-money actions.
- H-009 opportunity-feed production work remains separately pending.

## Next action

Start a fresh 24-hour Phase 05 Plan 06 soak. Create `05-SOAK-LOG.md` at T+0,
sample `/health`, distinct active book coverage, distinct Yes-side OHLC coverage,
and watchdog stale logs every ~6 hours. Phase 05 closes only if all strict D-12
sub-indicators remain green through T+24h.

```bash
$gsd-resume-work --ws m1-perception
```
