---
context: phase
workstream: m1-perception
phase: 05-ws-book-prices
plan: 06
task: 2
total_tasks: 4
status: paused_after_soak_window_evidence_incomplete
last_updated: 2026-07-21T14:01:34.372Z
---

# Handoff — Phase 05 soak window elapsed with missing checkpoints

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual production failures. Acknowledge
> each one explicitly before starting or judging the soak.

- [x] **Do not accept a startup-only 10/10.** Release 36 started at 10/10 and
  fell to 8/10 on the next promoter tick. The soak must cover the full 24 hours.
- [x] **Do not query coverage from only the newest 1000 REST rows.** At handoff,
  that capped page contained four hot assets while SQL over the same interval
  proved 3840 rows across ten token assets.
- [x] **Do not mix token count with market count.** Active health is ten tokens;
  D-12 market coverage is five promoted Yes markets after Yes/No identity mapping.

**Do not proceed until all boxes are checked.**

## Current handoff — 2026-07-21T14:01:34Z

The immutable formal window ended at `2026-07-21T13:30:55Z`, about 30 minutes
before this handoff. T+0 is valid and remains preserved, but T+6, T+12, and T+18
were not captured, and no T+24 observation had been taken when work paused.
Those missing health samples cannot be reconstructed by labelling a later probe
with an earlier timestamp.

This run is therefore **NOT-CLOSED due to incomplete evidence**. A fresh session
should first take a clearly labelled late read-only snapshot, query mapped
book/OHLC coverage over the literal completed window, and inspect watchdog logs.
That diagnostic can reveal production truth but cannot change this run to PASS;
the strict minimum-throughout-window claim requires a new 24-hour re-soak with
retained checkpoints.

## Formal soak start — 2026-07-20T13:30:55Z

A genuinely new forced-machine health response established formal T+0 at
`2026-07-20T13:30:55Z`; the end target is `2026-07-21T13:30:55Z`. This sample
returned HTTP 200 with active `10/10` pass, promoter age `20.0s` pass,
book-level write age `19.9s` pass, WS event age `0.0s` pass, TOB mirror age
`20.6s` pass, candidate age `44.3s` pass, listener `listening`, reconciliation
age `44.1s`, and cursor lag `0`. The top-level body was warn only because
`WAITING_FOR_EVENT` is an informational quiet-state label.

Exact identity was unchanged before and after the sample: release `37`, machine
`85e647c4eed598`, instance `01KXZJKY9SKEJAY2DD8MMPNB2E`, image
`deployment-01KXZJHS9QT8T6X0J33KVPTB5V`, and digest
`sha256:5da8e954897f60cf05f9d6664e99a15247d46a2bd4fd0edbb433c200af8b412c`.

The promoted set was re-resolved rather than copied. A pre-baseline query saw
market `908713`; the `13:30:35.609Z` promoter tick then logged
`+2 -2 markets=5 tokens=10`, and the post-tick authoritative mapping used for
T+0 contained markets `540819`, `562802`, `565064`, `601819`, and `665374`,
each with one Yes and one No token. Direct SQL over the literal initial interval
`[13:30:55Z,13:33:07.977Z)` found 40 book rows across two token assets / one
mapped market and one Yes-side OHLC row / one mapped market. This partial
boundary coverage is not the final verdict. The exact-machine log interval
`[13:30:55Z,13:33:22.894Z)` had 43 entries and zero `ws_watchdog: stale`.

The complete evidence and scheduled timeline live in `05-SOAK-LOG.md`:

- T+6: `2026-07-20T19:30:55Z`
- T+12: `2026-07-21T01:30:55Z`
- T+18: `2026-07-21T07:30:55Z`
- T+24: `2026-07-21T13:30:55Z`

Do not sign validation, create `05-06-SUMMARY.md`, or close the phase before the
T+24 strict three-indicator verdict.

## Rejected start attempt — 2026-07-20T12:49:25Z

The candidate T+0 was rejected and is **not** a formal soak start. Exact read-only
identity remained unchanged: release `37`, machine `85e647c4eed598`, instance
`01KXZJKY9SKEJAY2DD8MMPNB2E`, image
`deployment-01KXZJHS9QT8T6X0J33KVPTB5V`, and digest
`sha256:5da8e954897f60cf05f9d6664e99a15247d46a2bd4fd0edbb433c200af8b412c`.

The forced-machine `/health` probe at `12:49:28Z` returned HTTP 200. Active count
was `10/10` pass, promoter age was `240.9s` pass, WS event age was `0.0s` pass,
TOB mirror age was `2.9s` pass, candidate fetch age was `24.2s` pass, listener was
`listening`, and cursor lag was `0`. However,
`l3:last_book_levels_write_at_s` was `207.2s` with status `warn`, so the locked
strict `<120s` T+0 baseline was not green. The earlier `12:47:33Z` pass sample
was not stitched into this later candidate start.

All four blocking mechanisms were enforced in this bounded attempt:

- **Single-sample readiness:** the earlier green sample was rejected for the
  later candidate T+0; no observation was backdated or stitched.
- **Positional book semantics:** the exact release/digest carrying shared
  BUY-descending / SELL-ascending normalization was verified unchanged before
  interpreting TOB, depth, or book rows.
- **Row-limit identity leakage:** promoted identity came from the five current
  Yes-side `l2_candidates.l3_promoted_at_ts` rows and was joined to authoritative
  `markets_latest` Yes/No pairs before any coverage aggregate.
- **Capped-page coverage:** coverage came only from direct interval SQL; no
  PostgREST `limit=1000` page was used as evidence.

Direct read-only SQL over the pre-start diagnostic hour closed the identity and
coverage chain without a capped REST page: five promoted Yes token IDs mapped
uniquely to five `market_id` values and five No token IDs; mapped coverage was
five markets / ten token assets / 4240 `l2_book_levels` rows, plus OHLC rows for
all five Yes-side market identities:

| Market | Yes token | No token | Book rows (pre-start 1h) | Yes OHLC rows |
|---|---|---|---:|---:|
| `540819` | `90435811253665578014957380826505992530054077692143838383981805324273750424057` | `92388629082681805622801622703528982922543286352927708208755887536971583436902` | 400 | 10 |
| `562802` | `83247781037352156539108067944461291821683755894607244160607042790356561625563` | `33156410999665902694791064431724433042010245771106314074312009703157423879038` | 480 | 10 |
| `565064` | `67109747366255871599717338045111308888043498275177238365560740200996017915657` | `103845791232328975452762372781150730610824357544180691092497335946993481308222` | 120 | 3 |
| `601819` | `30630994248667897740988010928640156931882346081873066002335460180076741328029` | `79191939610100241429039499950443680906623179487184628479206155805558220344190` | 240 | 4 |
| `665374` | `55115078421062885512539156303747803058407616201213034911037320915726138659123` | `1910830010387565971650098373488592514702818137344973088263643820608151819241` | 3000 | 28 |

The exact rejected interval
`[12:49:25Z, 12:49:32.253Z)` contained zero book/OHLC rows, which is retained as
the honest boundary reading rather than backfilled. The exact-machine Fly rolling
log buffer contained two entries in that interval and zero
`ws_watchdog: stale` matches.

No `05-SOAK-LOG.md` was created, no formal T+0 exists, and no production state,
threshold, secret, deployment, or trading path was changed. Resume by taking a
new strict baseline; start a genuinely fresh T+0 only if the same exact identity
is still present and every locked baseline check, including book-level freshness,
is green in that single sample.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Single-sample readiness | Release 36's cold-start 10/10 regressed to 8/10 five minutes later. | blocking | Start a fresh 24h T+0 and retain every scheduled checkpoint; never backfill a formal soak from pre-soak observations. |
| Positional book semantics | Polymarket supplied farthest-first book arrays; index zero produced false spreads near 0.998. | blocking | Preserve shared BUY-descending/SELL-ascending normalization before TOB, depth, or level projection. |
| Row-limit identity leakage | Duplicate snapshots and then No-side rows consumed a five-market recipe LIMIT. | blocking | Preserve newest-row-per-asset collapse and authoritative Yes-side filtering before scanner LIMIT. |
| Capped-page coverage | A hot subset can fill PostgREST's 1000-row page and hide older active assets. | blocking | Use SQL `COUNT(DISTINCT ...)` over the exact soak interval and map Yes/No tokens explicitly. |

<current_state>

Phase 05 Plan 06 Task 1 is complete. Task 2's first formal window elapsed with
incomplete scheduled evidence and is NOT-CLOSED; a late diagnostic plus a new
re-soak remain. Production Alembic is `006 (head)` and L1 release 127 mirrored
snapshot 578 with 1946/1946 complete Yes/No projections.

L2 release 37 is the canonical soak baseline:

- machine: `85e647c4eed598`
- instance: `01KXZJKY9SKEJAY2DD8MMPNB2E`
- image: `deployment-01KXZJHS9QT8T6X0J33KVPTB5V`
- digest: `sha256:5da8e954897f60cf05f9d6664e99a15247d46a2bd4fd0edbb433c200af8b412c`
- deployed: `2026-07-20T10:55:01Z`

The release crossed its second real promoter tick at `11:00:20Z` with
`+0 -0 markets=5 tokens=10`. A later pre-soak read at `11:52:11Z` still returned
HTTP 200 with 10/10, promote age 108s, book-write age 5s, cursor lag 0, and 108
subscriptions. Direct SQL since release start showed 3840 book rows across all
ten token assets; all five promoted Yes markets had OHLC rows. These are strong
pre-soak baselines, but they are not part of the formal interval that began at
`2026-07-20T13:30:55Z`.

The root health body may say top-level `warn` while HTTP remains 200 because
`ws:connection_state=WAITING_FOR_EVENT` is informational during quiet periods.
Judge the named strict subchecks and HTTP contract, not the label in isolation.

</current_state>

<completed_work>

Completed Tasks:

- Task 1: Alembic 006 + L1/L2 deployment + production prerequisite proof — Done.
- Task 3: `docs/learning/11-L3-K线.md` and index maintenance — Done and updated
  with all three production ingestion lessons.
- Emergent fixes: `7ccd2da` book ranking, `57d3fc0` newest row per asset,
  `9451b4b` pre-LIMIT Yes-side filtering — deployed in release 37.
- Verification: 1433 passed, 1 skipped, 1 xfailed; Ruff/manual/climb/planning
  gates passed; working tree was clean before this handoff.

</completed_work>

<remaining_work>

- Task 2: In progress / NOT-CLOSED — formal T+0 was valid, but T+6/T+12/T+18
  were missed and T+24 was unobserved at handoff. Capture a labelled late
  diagnostic, then start a new 24-hour re-soak rather than backfilling.
- Task 4: Not started — only after a fully green soak, sign validation, complete
  ROADMAP/STATE, create `05-06-SUMMARY.md`, extract learnings, and close Phase 05.
- Do not start H-009 opportunity-feed production work; it remains separately
  unauthorized and independent of this handoff.

</remaining_work>

<decisions_made>

- Preserve strict five markets / ten tokens and all recipe thresholds.
- Use release 37 as infrastructure baseline and preserve
  `2026-07-20T13:30:55Z` as the immutable formal T+0.
- Coverage queries must be SQL aggregates over the exact T+0/T+24 interval.
- Book coverage must be interpreted via the five promoted Yes market identities,
  even though `l2_book_levels` can contain all ten Yes/No token asset IDs.
- No real trading, funds, secrets, config changes, external submissions, or
  implicit soak occurred during the rollout session.

</decisions_made>

<blockers>

- Evidence gap: T+6/T+12/T+18 were missed; the strict minimum observed market
  count throughout the window cannot be established from T+0 alone.
- T+24 was overdue by about 30 minutes and still unobserved at handoff.
- A new 24-hour re-soak is required for PASS after collecting a late diagnostic.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact resume state.
2. `.planning/workstreams/m1-perception/phases/05-ws-book-prices/05-SOAK-LOG.md` — valid T+0 plus explicitly missed checkpoints.
3. `.planning/workstreams/m1-perception/phases/05-ws-book-prices/05-06-PLAN.md` — strict D-12 acceptance gate.
4. `.planning/workstreams/m1-perception/STATE.md` — canonical M1 operational state.
5. `.planning/threads/market-observation-architecture.md` §2.9 — seed, ordering, identity, and observation-scheduling lessons.
6. `.planning/JOURNAL.md` Sessions 76–77 — formal start and incomplete-evidence handoff.
7. `docs/learning/11-L3-K线.md` — L3 mental model and production traps.

## Critical Anti-Patterns (do NOT repeat these)

- Lowering thresholds to manufacture 10/10 → repair observation truth instead.
- Treating array index zero as best price → rank by side before projection.
- Letting repeated snapshots or No rows reach recipe LIMIT → normalize identity first.
- Treating `limit=1000` REST output as complete interval coverage → aggregate in SQL.
- Counting ten token asset IDs as ten markets → map back to five promoted Yes IDs.

## Infrastructure State

- Supabase schema: Alembic `006 (head)`.
- L1: release 127; snapshot 578; 1946 mirrored markets with complete pairs.
- L2: release 37 exact machine/instance/image/digest listed above; service running.
- Latest pre-soak production read: HTTP 200, 10/10, promote/book/cursor all pass.
- Local processes: none running for pytest, flyctl deploy, uvicorn, or polyarb daemon.
- Git: pre-handoff local `main` was 22 commits ahead of `origin/main`; the WIP
  handoff commit makes it 23 ahead. Production was built from local source. No
  push was performed.
- Local workflow processes: none. The shared `flyctl agent` helper remains
  running; it is not a soak monitor and captured no scheduled checkpoints.

<context>

The production mechanism may still be healthy, but the observation protocol is
not complete. Separate system truth from evidence sufficiency: late SQL/log
diagnostics are still useful, while the strict 24-hour PASS must come from a new
window with retained checkpoints. Do not reopen thresholds or implementation
until the late diagnostic supplies a concrete technical failure.

</context>

<next_action>

Start with:

```bash
/gsd-execute-phase 05 --ws m1-perception
```

Then capture a clearly labelled late read-only exact-instance snapshot and SQL
coverage over `[2026-07-20T13:30:55Z, 2026-07-21T13:30:55Z)`, render this run
NOT-CLOSED for missing checkpoints, and start a new 24-hour re-soak. Do not
replace, backdate, or invent T+6/T+12/T+18 observations.

</next_action>
