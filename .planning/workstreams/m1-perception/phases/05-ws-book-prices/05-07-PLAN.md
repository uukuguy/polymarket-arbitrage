---
phase: 05
plan: 07
type: tdd
wave: 6
depends_on: [01, 02, 03, 04, 05]
autonomous: true
requirements:
  - PHASE05-R02
  - PHASE05-R03
  - PHASE05-R05
  - PHASE05-R08
files_modified:
  - src/polyarb/observation/l3_evidence.py
  - src/polyarb/daemon/ws_consumer.py
  - src/polyarb/observation/l3_promote.py
  - src/polyarb/observation/l3_sampler.py
  - tests/daemon/test_ws_quiet_refresh.py
  - tests/daemon/test_ws_resubscribe_transaction.py
  - tests/daemon/test_ws_book_evidence_chain.py
  - tests/m1-perception/test_l3_evidence_sampler.py
  - tests/m1-perception/test_l3_promoter.py
  - tests/chaos/test_l3_evidence_chain.py
must_haves:
  truths:
    - "A promoter never publishes or reports success with fewer than 10 target-token durable book evidence identities."
    - "A quiet-refresh timeout persists failure truth and closes only its captured WebSocket generation."
    - "The sampler observes either the old complete mapping or the new complete mapping, never an in-flight transition."
    - "Strict L3 freshness remains below 120 seconds without grace periods or threshold relaxation."
    - "L1 machine identity and the active quote evidence anchor survive the L2-only deployment."
  artifacts:
    - path: "src/polyarb/daemon/ws_consumer.py"
      provides: "Generation-scoped refresh recovery and prepared L3 target transaction."
    - path: "src/polyarb/observation/l3_promote.py"
      provides: "Exact 10/10 promoter success invariant."
    - path: "src/polyarb/observation/l3_sampler.py"
      provides: "Transition-serialized evidence sampling."
  key_links:
    - from: "src/polyarb/daemon/ws_consumer.py"
      to: "src/polyarb/observation/l3_evidence.py"
      via: "PreparedL3Target and bounded runtime events"
      pattern: "PreparedL3Target|SUBSCRIPTION_CONTROL_FAILED"
    - from: "src/polyarb/observation/l3_promote.py"
      to: "src/polyarb/observation/l3_sampler.py"
      via: "shared transition lock"
      pattern: "transition_lock"
---

<objective>
Repair the two production L3 continuity failures observed on 2026-07-26 while
keeping the locked 120-second freshness and first-failure alert contracts.

Design:
`docs/superpowers/specs/2026-07-26-l3-continuity-boundary-repair-design.md`

Implementation plan:
`docs/superpowers/plans/2026-07-26-l3-continuity-boundary-repair.md`
</objective>

<execution>
1. Make quiet-refresh evidence timeout persist a bounded event and compensate
   only the captured generation.
2. Add generation-scoped target preparation and atomic membership commit.
3. Make promoter success require exact desired=committed=evidenced=10.
4. Serialize sampler collection across the mapping/membership commit boundary.
5. Run focused, chaos, full-suite, production-image, and production health
   verification.
6. Deploy only polyarb-l2 and open a new immutable 24-hour L3 evidence window.
</execution>
