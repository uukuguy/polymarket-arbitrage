---
phase: 02.1
plan: 04
type: execute
wave: 3
depends_on: [02.1-01, 02.1-02, 02.1-03]
files_modified:
  - docs/learning/09-生产化运维.md
  - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md
  - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md
autonomous: false
subsystem: documentation-closure
tags: [teaching, ops, soak-log, phase-closure]
requires: [02.1-01-SUMMARY, 02.1-02-SUMMARY, 02.1-03-SUMMARY]
provides:
  - docs/learning/09-生产化运维.md (生产化运维教学)
  - 02-SOAK-LOG.md 三段 chaos verification 整合
  - Phase 02.1 closure 凭证
affects:
  - learning curve (用户认知更新)
  - phase 02.1 SUMMARY 闭环
tech-stack: [markdown, gsd-tooling]
key-files:
  - docs/learning/09-生产化运维.md
  - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md
duration_estimate: ~250 行 docs + Phase SUMMARY 收尾 + planning-status 验收

requirements: [CLAUDE.md-teaching, CLAUDE.md-makefile, CLAUDE.md-summary, D-07]

must_haves:
  truths:
    - "docs/learning/09-生产化运维.md 写完 — 用户可独立解释 /health vs /healthz, audit log + breadcrumb 设计, prod unpause flow"
    - "docs/learning/00-INDEX.md 加入 09 章节, 阅读顺序连续"
    - "02-SOAK-LOG.md 三段 (Inj 3-v2 + Inj 4 + #6-verification) 已分别由 Plan 02.1-01/02/03 追加, 本 plan verify 时间线连续"
    - "make planning-status zero drift, Phase 02.1 4 个 plan 全部 SUMMARY 就位"
    - "Phase 03 启动凭证三条 (CONTEXT.md § '关闭凭证') 全通过, 可 /gsd-extract_learnings 02.1"
    - "02.1-VALIDATION.md frontmatter 翻为 status=complete + nyquist_compliant=true + wave_0_complete=true (Nyquist gate 闭合凭证, ISSUE-03 修法)"
  artifacts:
    - path: docs/learning/09-生产化运维.md
      provides: "Phase 02.1 教学: 生产化运维 (fail-soft visibility + prod control endpoints + IETF vs Fly probe trade-off)"
      min_lines: 200
    - path: docs/learning/00-INDEX.md
      provides: "更新阅读顺序索引"
      contains: "09-生产化运维"
  key_links:
    - from: docs/learning/09-生产化运维.md
      to: src/polyarb/snapshot/orchestrator.py step 7.5 (file:line)
      via: "教学文档 contains 真 file:line 引用"
      pattern: 'orchestrator\\.py:\\d+'
    - from: docs/learning/09-生产化运维.md
      to: src/polyarb/http/control.py + health.py
      via: "教学文档 contains code 片段引用"
      pattern: 'control\\.py|healthz'
    - from: docs/learning/00-INDEX.md
      to: 09-生产化运维.md
      via: "INDEX 列出"
      pattern: '09-生产化运维'
---

<objective>
Phase 02.1 收尾 plan — 教学文档 + chaos verification 整合 + Phase SUMMARY 闭环。

Purpose: 兑现 CLAUDE.md "教学文档持续产出" + "每个 Plan 末 SUMMARY 纪律" + "命令入口约定" 三条规则。Phase 02.1 4 个 plan 完成后必须有教学增量, 否则用户的"理解曲线"被 agent 代码进度甩开 (CLAUDE.md 明示反模式).

Phase 02.1 引入 3 个新概念用户尚未在教学层吸收:
1. **fail-soft visibility** — 静默 vs audit-visible 的设计 (orchestrator step 7.5 与 Sentry breadcrumb 联动)
2. **prod control endpoints** — HMAC 保护的 daemon control plane (/control/unpause 与未来 pause/status stub)
3. **IETF strict 与 platform probe 的语义分离** — /health vs /healthz 的 trade-off

这 3 个概念是 L1 production-grade 工艺的核心, 不教用户独立维护 prod daemon 仍会有盲点。

Output:
- `docs/learning/09-生产化运维.md` (新教学文档, 体例严格 mirror Phase 01.1 落地的 docs/learning/01-06)
- `docs/learning/00-INDEX.md` 更新加入 09 章节
- 02-SOAK-LOG.md 完整审计 (Plan 01/02/03 已分别追加段, 本 plan verify 时间线 + 加 Phase 02.1 closure 段)
- 全 planning-status zero drift, 4 plan SUMMARY 全部就位
- (可选) Phase SUMMARY 雏形, 便于 `/gsd-extract_learnings 02.1 --ws m1-perception` 执行
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/ROADMAP.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-01-SUMMARY.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-02-SUMMARY.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-03-SUMMARY.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-LEARNINGS.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md
@docs/learning/00-INDEX.md
@docs/learning/07-观察市场.md
@CLAUDE.md

<interfaces>
<!-- 教学文档体例参考 (mirror 现有 01-07 的结构) -->

From docs/learning/07-观察市场.md (Phase 01.1 落地的 347 行教学, 体例 anchor):
```markdown
# 07-观察市场

## 30 秒心智模型
[一段比喻 + 一张 ASCII 图]

## 核心代码片段
[3-5 个关键 file:line 引用, 含代码块]

## 设计取舍
[2-3 个为什么这样设计, 不那样]

## 自检题
[5 道题, 答得上才算吸收]

## FAQ 增量区
[空 — 用户提问后追加]
```

From docs/learning/00-INDEX.md:
```markdown
# Learning Index

## 阅读顺序

01. [快照工具基础](01-...)
02. ...
07. [观察市场](07-观察市场.md)
08. [生产化部署](08-...)   # 如果 Phase 02 之前补过
09. [生产化运维](09-生产化运维.md)   # ← 本 plan 加
```

From CLAUDE.md "教学文档持续产出 (教练角色的具象化)" 段:
> agent 并行执行会让用户的"理解曲线"被代码进度甩开。每次 phase 末或重大功能落地后, 主动产出 / 补充教学文档到 docs/learning/, 不等用户问。
> ...
> 命名约定: docs/learning/NN-<topic>.md, 序号递增; docs/learning/00-INDEX.md 维护阅读顺序
```
</interfaces>
</context>

<threat_model>
<!-- 本 plan 是 documentation closure, 无新代码改动, 无新威胁 -->

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | 纯文档 plan, 不触代码 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-doc-01 | Information Disclosure | 教学文档若 leak secret/API endpoint pattern | accept | docs/learning/ 是 public knowledge 教学 (HMAC mechanism 不是 secret, 业内通用); 任何具体 secret 值不写入文档 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 写 docs/learning/09-生产化运维.md</name>
  <files>docs/learning/09-生产化运维.md</files>
  <read_first>
    - docs/learning/07-观察市场.md 全文 (体例 anchor, 347 行)
    - docs/learning/00-INDEX.md (确认编号 09 是下一个空位; 如 08 已存在则 09, 如 08 也未写本 plan 顺手也加 08 stub or 调整命名)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-01-SUMMARY.md / 02.1-02-SUMMARY.md / 02.1-03-SUMMARY.md (引用 line range + chaos 验证结果)
    - src/polyarb/snapshot/orchestrator.py step 7.5 (实际 line range, 不要从 RESEARCH.md 复制估算的 line number)
    - src/polyarb/http/control.py (实际 line range, file:line 引用)
    - src/polyarb/http/health.py refactored 版本 (实际 _build_health_checks / health / healthz 行号)
    - fly.toml [[http_service.checks]] 段实际行号
  </read_first>
  <action>
    Per CLAUDE.md "教学文档持续产出" — 体例严格 mirror docs/learning/07-观察市场.md (347 行, Phase 01.1 落地 anchor)。
    
    **文档结构** (5 大段, 每段必须有, 不允许跳):
    
    ### 1. 30 秒心智模型 (~30 行)
    
    用一个比喻 + 一张 ASCII 图说清: Phase 02.1 解决了什么, 抽象层是什么。建议比喻:
    
    > 想象 daemon 是一艘船:
    > - **Phase 02** 让船能 7×24 在大洋里跑 + 三种告警雷达 (Sentry + Telegram + Better Stack)
    > - **Phase 02.1** 解决了三个船员痛点:
    >   1. 静默故障 (#7): 撤了 mirror 凭证, 船仍跑但所有 mirror 操作消失, 监控雷达看不见
    >   2. 远程急救 (#8): 船 stuck PAUSED, 唯一恢复路径是 SSH + sqlite3 + restart 三步
    >   3. 监控雷达自伤 (#6): 一台监控雷达 (Fly proxy) 看 503 后切自己的流量, 让所有人都联系不上船
    
    ASCII 图 (建议 — 不强制但很有效):
    ```
    [外部 Better Stack 探针]
            ↓ GET
        /health  ──→  IETF 严格 (fail → 503, 告警信号, 是好事)
    
    [Fly platform probe]
            ↓ GET
        /healthz ──→  永远 200 (Fly proxy 不切流量, body status 反映真状态)
    
    [运维 (本地 shell)]
            ↓ make unpause-prod (HMAC POST)
        /control/unpause ──→ scheduler.unpause() (state→RUNNING + counter→0)
    
    [orchestrator step 7.5]
        ┌── mirror_enabled=True + valid  → 真 push (try/except 包)
        ├── mirror_enabled=True + invalid → F-05 guard log
        └── mirror_enabled=False (撤 secret) → audit log + Sentry breadcrumb (Phase 02.1 D-01 修)
    ```
    
    ### 2. 核心代码片段 (~60 行, 3 个块)
    
    每个块: 一句"为什么这块重要" + 真 file:line 引用 + 5-15 行代码片段 (从实际文件读, 不要 RESEARCH.md 估算行号).
    
    **块 1**: orchestrator step 7.5 三分支 (D-01 修)
    - `src/polyarb/snapshot/orchestrator.py:<真实行号>`
    - 完整 if/elif/else 三分支代码 (verbatim 复制 Plan 02.1-01 落地版本)
    - 一句解释: "config-disabled else 是 Phase 02 chaos Inj 3 暴露的盲点 — 之前没有这条 else, 撤 secret 整段 mirror 路径都消失, 0 log + 0 breadcrumb"
    
    **块 2**: control.py 的 HMAC 中间件独立设计 (D-03)
    - `src/polyarb/http/control.py:<真实行号>` (control_auth_middleware path guard)
    - 一句解释: "path guard 用 startswith('/control') 而不是 scan.py 的 != '/scan' — 这是关键差别, 让 /healthz 等 public 路径正确 bypass HMAC"
    - 引用 RESEARCH Pitfall 1 + 2 警示
    
    **块 3**: health.py refactor 抽 helper (D-05)
    - `src/polyarb/http/health.py:<真实行号>` (`_build_health_checks` 签名)
    - `src/polyarb/http/health.py:<真实行号>` (healthz handler 的强制 200)
    - 一句解释: "/health 和 /healthz 共享 _build_health_checks (4 个 check 逻辑不分叉), 区别只在最后一行 status_code — IETF strict 用 503 if fail, Fly-friendly 永远 200"
    
    ### 3. 设计取舍 (~50 行, 3-4 个)
    
    每个取舍: 标题 (一句) + 候选方案 + 选了哪个 + 为什么。引用 CONTEXT.md decision ID (D-01..D-07).
    
    取舍 1: **fail-soft visibility 选 audit-log 不选 settings 拆分** (D-01 拒绝 option (a))
    - 候选 a: 把 supabase_mirror_enabled 拆成 config_disabled vs invalid_creds 两态
    - 候选 b (选): 不动 settings 语义, 加 audit log + breadcrumb
    - 候选 c: 加 /health drift check
    - 选 b 理由: 工作量 ~30 行 vs (a) ~80 行 + 回归风险; (c) 与 #6 决策耦合不必要
    
    取舍 2: **unpause endpoint 选独立 /control/unpause 不选 /scan?action=unpause** (D-03 拒绝 option (a))
    - 候选 a: 复载 /scan action 语义
    - 候选 b (选): 新建 /control/unpause 专语义 endpoint
    - 选 b 理由: action 复载 → /scan 语义脏 (既"触发扫描"又"控制 daemon"); 独立 endpoint future-proof, 顺手搭 /control/pause + /control/status stub
    
    取舍 3: **/health 与 /healthz 拆 vs /health 降级总 200** (D-05 选 option (c))
    - 候选 a: /health 永远 200 (status in body)
    - 候选 b: 用 TCP probe 替代 HTTP probe
    - 候选 c (选): 拆两 endpoint, IETF strict 不让步
    - 选 c 理由: (a) 破坏 Better Stack 告警语义 — 外探针 200 ≠ 系统正常; (b) TCP probe 不验证 HTTP 服务真健康
    
    取舍 4: **HMAC middleware 独立 (control.py) vs 复用 scan.py 中间件** (Agent's discretion, planner 选)
    - 候选 a: 改 scan_auth_middleware path guard 加 /control/*
    - 候选 b (选): 独立 ControlAuthMiddleware
    - 选 b 理由: scan.py 保持不变 (P11 模板, 不引入回归); 关注点分离; future /control/* 可能用不同 secret 时演化空间
    
    ### 4. 自检题 (~30 行, 5 道)
    
    实战决策题 (CLAUDE.md "教练角色"), 不是单纯知识题。每题: 题目 + 期望答题方向 (1-2 句, 不直接给答案 — 让用户思考).
    
    建议题:
    1. **如果 prod 上 daemon 是 PAUSED 状态, 但你忘了 secret 的本地副本, 怎么 unpause?** (期望: ssh console + sqlite3 直接改 state 表; 或者从 Fly secrets list + flyctl secrets list + 重新 issue 一个 secret 给本地 .env)
    
    2. **如果 chaos Inj 3 (撤 mirror secret) 跑完, daemon log 里看不到 "mirror disabled" 字串, 三种最可能的故障原因是什么?** (期望: (a) D-01 修法部署到 prod 时 deploy 失败回滚; (b) `else` 分支位置错误 — RESEARCH Pitfall 3 — 加在了 F-05 branch 内; (c) Sentry/log buffer flush 没等够时间)
    
    3. **/healthz 永远返 200 会不会让外部探针误判系统总健康?** (期望: 不会 — 因为 Better Stack 监控的是 /health (IETF strict), 不是 /healthz; 监控告警与平台路由探针的 endpoint 必须分离, 这正是本 Phase 的核心设计)
    
    4. **`make unpause-prod` 的 HMAC body 是空字符串. 如果攻击者抓到一次 unpause 的 HTTP 请求, 能否 replay?** (期望: 能 — unpause 是幂等操作 (T-02.1-8-04 disposition=accept), 重放 N 次效果与 1 次同样; 是接受的风险, 没加 nonce 因为 unpause 无放大效应; 若 Phase 03+ 加破坏性操作 (e.g. /control/wipe) 必须加 nonce)
    
    5. **Phase 02 LEARNINGS L7 "测试按钮不算 verified" 在 Phase 02.1 体现在哪?** (期望: 每个 bug 修法的 verification 凭证都不是 unit test, 而是 chaos Inj 3-v2 / Inj 4 / fail-inject 期间的 fly checks list + curl 实测; Phase 02.1 三个 plan 都各自有 prod chaos 段并写入 02-SOAK-LOG.md, 这正是吸收 L7 教训的执行落地)
    
    ### 5. FAQ 增量区 (~10 行, 空 placeholder)
    
    ```markdown
    ## FAQ 增量区
    
    > 用户阅读后提的问题在此累积. 同一问题 ≥ 3 次出现后 promote 进正文.
    
    *(暂无问答 — 待积累)*
    ```
    
    **关键约束**:
    - 中文 (CLAUDE.md § 4 "docs/ 是内部团队材料, Chinese ensures better comprehension")
    - 必须 ≥ 200 行 (与 docs/learning/07-观察市场.md 体量相当)
    - file:line 引用必须真 — 不要从 RESEARCH.md 估算的 487/192/56 等行号复制, 要从落地后的 src/* 实际 grep 行号
    - 提及 D-01..D-07 时引用 decision ID, 不只说"决策"
    - 5 道自检题必须是决策题, 不是单纯 "X 是什么" 的知识题
    
    提交: `docs(02.1-04): docs/learning/09-生产化运维.md (Phase 02.1 教学增量)`
  </action>
  <verify>
    <automated>test -f docs/learning/09-生产化运维.md && wc -l docs/learning/09-生产化运维.md | awk '{print $1}' | xargs -I N test N -ge 200 && grep -c 'D-0[1-7]' docs/learning/09-生产化运维.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/learning/09-生产化运维.md`
    - Min length: `wc -l docs/learning/09-生产化运维.md | awk '{print $1}'` ≥ 200
    - 5 sections present: `grep -cE "^## (30 秒心智模型|核心代码片段|设计取舍|自检题|FAQ 增量区)" docs/learning/09-生产化运维.md` == 5
    - Decision IDs referenced: `grep -c 'D-0[1-7]' docs/learning/09-生产化运维.md` ≥ 4 (引用 ≥ 4 个 decisions)
    - File:line refs to real code: `grep -cE '(orchestrator|control|health|healthz)\\.py:[0-9]+' docs/learning/09-生产化运维.md` ≥ 3
    - 5 self-check questions: `grep -cE "^[0-9]\\.\\s|^[0-9]\\)" docs/learning/09-生产化运维.md` ≥ 5
    - Commit message starts with `docs(02.1-04):`
  </acceptance_criteria>
  <done>09-生产化运维.md 落地 ≥ 200 行, 5 段齐全, 引用 ≥ 4 个 decision ID 和 ≥ 3 个真 file:line.</done>
</task>

<task type="auto">
  <name>Task 2: docs/learning/00-INDEX.md 加入 09 章节</name>
  <files>docs/learning/00-INDEX.md</files>
  <read_first>
    - docs/learning/00-INDEX.md 全文
    - docs/learning/ 目录 ls (确认 08 是否已存在 — 若存在则正常加 09, 若不存在 planner 决定: 顺手加 08 stub 也可, 或保持 09 跳号但 INDEX 说明)
  </read_first>
  <action>
    更新 `docs/learning/00-INDEX.md`:
    
    1. 在 "阅读顺序" 列表中加 09 条目:
       ```markdown
       09. [生产化运维](09-生产化运维.md) — fail-soft visibility / prod control endpoints / IETF vs Fly probe trade-off (Phase 02.1)
       ```
    
    2. 如果 08 不存在 (Phase 02 当时没补教学), 在 INDEX 加注释:
       ```markdown
       <!-- 08-生产化部署 (Phase 02 Wave 3-5 教学) — 待补 -->
       ```
       但**不要** stub 08 文件 (D-07 严格 fix-up scope, Phase 02 的教学债不归本 plan).
    
    3. 加一段 "Phase 02.1 教学增量" 简介 (~3 行):
       > Phase 02.1 在 09-生产化运维.md 加入了三个生产化运维核心概念: 静默故障 (fail-soft visibility) 的修法, prod control endpoints 的 HMAC 设计, 以及 IETF strict 监控与 platform probe 在路由层面的语义分离。
    
    提交: `docs(02.1-04): INDEX 加入 09-生产化运维 + Phase 02.1 教学增量简介`
  </action>
  <verify>
    <automated>grep -c "09-生产化运维" docs/learning/00-INDEX.md</automated>
  </verify>
  <acceptance_criteria>
    - INDEX contains 09 entry: `grep -c "09-生产化运维" docs/learning/00-INDEX.md` ≥ 1
    - Phase 02.1 introduction paragraph present (≥ 3 lines mention Phase 02.1): `grep -c "Phase 02.1" docs/learning/00-INDEX.md` ≥ 1
    - INDEX still well-formed (no broken markdown links): `grep -cE '\\[.*\\]\\(.*\\)' docs/learning/00-INDEX.md` ≥ original count + 1
  </acceptance_criteria>
  <done>00-INDEX.md 列出 09 章节, 阅读顺序连续.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 02.1 closure verification — planning-status zero drift + 三个关闭凭证 reflection</name>
  <what-built>
    Phase 02.1 4 个 plan (01/02/03/04) 全部 ship:
    - Plan 01: orchestrator step 7.5 audit log + breadcrumb + chaos Inj 3-v2 verified
    - Plan 02: /control/unpause endpoint + make unpause-prod + chaos Inj 4 verified
    - Plan 03: /healthz endpoint + fly.toml probe 切换 + Fly checks smoke verified
    - Plan 04 (本 plan): docs/learning/09 + INDEX 更新
    
    需要 human gate 确认 Phase 02.1 closure 凭证全过.
  </what-built>
  <how-to-verify>
    **Step 0 — VALIDATION.md frontmatter closure (ISSUE-03 fix, per Nyquist gate)**:
    
    Update `02.1-VALIDATION.md` frontmatter (lines 3-6) to reflect Phase 02.1 closure state. This unflips the Nyquist gate flags so future `make planning-status` / nyquist compliance checks stop reporting non-compliance even though the work is done.
    
    Edit `.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md` frontmatter:
    ```yaml
    ---
    phase: 02.1
    slug: phase-02-fix-up-2-p1-backlog-health-503-trade-off
    status: complete          # ← was: draft
    nyquist_compliant: true   # ← was: false
    wave_0_complete: true     # ← was: false
    created: 2026-05-20
    ---
    ```
    
    Also update the "Validation Sign-Off" section (bottom of file) to flip the `[ ]` to `[x]` for both `nyquist_compliant: true` and `wave_0_complete: true` lines, and change `**Approval:** pending` to `**Approval:** complete (Phase 02.1 closed YYYY-MM-DD)`.
    
    Acceptance: `grep -E '^(status|nyquist_compliant|wave_0_complete):' .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md` 显示 `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true` (3 lines exactly).
    
    Commit the VALIDATION.md frontmatter flip as a separate commit BEFORE Step 1 planning-status check: `chore(02.1-04): VALIDATION.md frontmatter → complete + nyquist_compliant true + wave_0_complete true`.
    
    **Step 1 — Planning hygiene 零漂移**:
    ```bash
    make planning-status
    ```
    期望:
    - Phase 02.1 4 个 plan 全列, 全部 ✅ (PLAN.md + SUMMARY.md + commit all present)
    - 没有 DRIFT (代码已 commit 但 SUMMARY 缺失) 
    - 没有 ORPHAN (SUMMARY 存在但 PLAN.md 缺失)
    
    **Step 2 — 三个 Phase 02.1 关闭凭证 (per 02.1-CONTEXT.md § "Phase 02.1 关闭凭证")**:
    
    凭证 #1 (#7 修): 撤 Supabase secret → daemon log 出现 "mirror disabled" + Sentry event 携带 breadcrumb
    - 验证: 翻 02-SOAK-LOG.md, 应有 "Inj 3-v2" 段, 含: secret 撤回 timestamp / log line 截图 / Sentry event URL / breadcrumb 详情
    - 检查: `grep -c "Inj 3-v2\\|Inj 3 v2\\|mirror disabled" .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md` ≥ 1
    
    凭证 #2 (#8 修): `/control/unpause` POST (HMAC) → scheduler RUNNING + counter=0, `/health` overall=pass
    - 验证: 02-SOAK-LOG.md 应有 "Inj 4" 段, 含: PAUSED 触发命令 + `make unpause-prod` 输出 (JSON 含 state=RUNNING + failure_counter=0) + 401 验证 + 恢复
    - 检查: `grep -c "Inj 4\\|unpause-prod\\|already_running" .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md` ≥ 1
    
    凭证 #3 (#6 修): `flyctl machines stop` (或注入 fail) 期间 `/healthz` 仍 200, Fly proxy 不切流量
    - 验证: 02-SOAK-LOG.md 应有 "#6-verification" 段, 含: flyctl checks list 输出 / 注入 fail 期间 curl /health (503) + curl /healthz (200) 对比 / Fly checks 仍 passing
    - 检查: `grep -c "healthz\\|Fly proxy\\|#6-verification" .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md` ≥ 1
    
    **Step 3 — Phase 02.1 SUMMARY 雏形** (本 plan 不强制写 Phase SUMMARY — 那是 `/gsd-extract_learnings 02.1` 的工作; 但确认下一步是它):
    
    本 plan 完成后立即下一步:
    ```bash
    /gsd-extract_learnings 02.1 --ws m1-perception
    ```
    这会从 4 个 plan SUMMARY + 02-SOAK-LOG.md 提 Phase 02.1 LEARNINGS.md。
    
    **Step 4 — 用户对手测试 (CLAUDE.md "Phase 末" 纪律)**:
    
    Phase 02.1 末用户必须答得上以下 3-5 个对手题 (从 docs/learning/09 自检题选 + 加 2 个 dev 实战题), 否则 Claude 教学有 gap:
    
    1. (从教学 Q3) "/healthz 永远返 200 会不会让外部探针误判系统总健康?" → 期望提到 Better Stack 看 /health, 不是 /healthz, 监控告警与路由探针 endpoint 必须分离
    
    2. "如果 Phase 03 启动后第一周 daemon 又进 PAUSED, 你的 ops 流程是什么? 列出从发现到恢复的具体命令链." → 期望: 收 Telegram 告警 → curl /health 看 status=fail → flyctl logs 看 PAUSED 原因 → 修根因 (若是 Gamma URL 漂移则 flyctl secrets set) → make unpause-prod → 等 1 tick 看 /health overall=pass
    
    3. "Phase 02.1 修了 3 个 bug 但 D-07 严格拒绝把 7-day soak 也并进来. 为什么? 把 7-day soak 放 Phase 03 启动期处理是不是会让 Phase 03 启动延迟?" → 期望: 严格 fix-up scope 防止 phase 蔓延 (Phase 02 LEARNINGS 教训); 7-day soak 涉及 Supabase Pro $25/月用户决策, 是独立决策点; 与 L2 orderbook 工作并行不阻塞
    
    **Resume signal**: 用户回答这 3 题 OK → "approved" + Phase 02.1 closure 通过 → 下一步 `/gsd-extract_learnings 02.1`. 答不上 → "learning gap: <题号>" → Claude 立即在 09-生产化运维.md FAQ 增量区追加.
  </how-to-verify>
  <resume-signal>
    Type "approved" 触发 Phase 02.1 closure (下一步 /gsd-extract_learnings), 或 "learning gap: <Q号>" 说明用户哪些教学盲点未消除 (Claude 在 09 FAQ 区追加), 或 "drift: <plan>" 说明哪个 plan SUMMARY 还有问题.
  </resume-signal>
</task>

</tasks>

<verification>
**Plan-level invariants (Phase 02.1 closure 凭证)**:

1. docs/learning/09-生产化运维.md 落地 ≥ 200 行, 5 段齐全
2. docs/learning/00-INDEX.md 加入 09 章节, 阅读顺序连续
3. `make planning-status` zero drift — Phase 02.1 4 plan 全 ✅
4. 02-SOAK-LOG.md 三段 chaos verification 全部 commit:
   - Inj 3-v2 (Plan 01 追加) — #7 closure 凭证
   - Inj 4 (Plan 02 追加) — #8 closure 凭证
   - #6-verification (Plan 03 追加) — #6 closure 凭证
5. 用户对手测试通过 (3 题答得上 — 教学 gap 没遗留)
6. Phase SUMMARY 雏形可立即由 `/gsd-extract_learnings 02.1 --ws m1-perception` 提
7. 02.1-VALIDATION.md frontmatter 已翻 (Step 0): `grep -E '^(status|nyquist_compliant|wave_0_complete):' .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md` 显示 complete/true/true

**Negative tests**:
- 不要在本 plan 写 02.1-PHASE-SUMMARY.md — `/gsd-extract_learnings` 是专用工具, 本 plan 只是为它打好基础
- 不要在本 plan 改 03 phase ROADMAP entry — D-07 严格 scope, 那是 `/gsd-discuss-phase 03` 的工作
- 不要补 Phase 02 漏掉的 docs/learning/08 (Phase 02 教学债不在本 plan scope; INDEX 注释提一句即可)
</verification>

<success_criteria>
- Phase 02.1 closure 凭证三条全过 (CONTEXT.md § "Phase 02.1 关闭凭证" 列表完整勾选)
- 用户对手测试 3 题答得上 (CLAUDE.md "Phase 末" 纪律)
- docs/learning/09 教学增量完整, 用户能独立解释 Phase 02.1 三个核心概念
- Plan SUMMARY (`02.1-04-SUMMARY.md`) 落地, planning-status zero drift
- Phase 03 启动条件 unblocked — 下一步 `/gsd-extract_learnings 02.1 --ws m1-perception` → `/gsd-discuss-phase 03 --ws m1-perception`
</success_criteria>

<plan_check>
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md

本 plan 是 documentation closure, VALIDATION.md 不直接对应 unit test task. 验证手段:
- `wc -l docs/learning/09-生产化运维.md` ≥ 200
- `make planning-status` zero drift
- 用户 checkpoint approval
</plan_check>

<output>
After Tasks 1-3 完成, create `.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-04-SUMMARY.md` (CLAUDE.md 纪律). pre-commit hook 守门.

Phase 02.1 4 plan 全部 SUMMARY 就位后, 下一步:
```bash
/gsd-extract_learnings 02.1 --ws m1-perception
```
提 Phase 02.1 LEARNINGS.md, 然后 Phase 03 unblocked.
</output>

---

## Plan Boundary (D-07 严格 fix-up scope reminder)

**本 plan 只做**: Phase 02.1 documentation closure + 教学增量 + Phase closure 凭证 verification.

**本 plan 不做**:
- 不写 02.1-PHASE-SUMMARY.md (那是 `/gsd-extract_learnings` 的工作)
- 不改 ROADMAP.md Phase 03 entry (那是 `/gsd-discuss-phase 03` 的工作)
- 不补 Phase 02 漏掉的 docs/learning/08 (Phase 02 教学债, 不归本 plan)
- 不动 src/ 任何代码 (本 plan 是文档 + verification, 不触实现)
- 不写 7-day soak 启动计划 (推到 Phase 03)
- 不动 Vercel dashboard "Unpause Daemon" UI (推独立 plan)

如 executor 想补 Phase 02 教学债或顺手做 Phase 03 准备, stop and ask. CLAUDE.md "不蔓延" 纪律严守。
