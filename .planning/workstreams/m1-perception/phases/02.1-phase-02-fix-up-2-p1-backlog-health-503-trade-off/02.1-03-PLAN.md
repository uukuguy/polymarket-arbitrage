---
phase: 02.1
plan: 03
type: execute
wave: 2
depends_on: [02.1-02]
files_modified:
  - src/polyarb/http/health.py
  - src/polyarb/http/app.py
  - fly.toml
  - Makefile
  - tests/m1-perception/test_health_endpoint.py
autonomous: false
subsystem: daemon-http-health
tags: [http, ietf, fly-platform, health-probe]
requires: [D-22]
provides:
  - /healthz endpoint (always-200)
  - _build_health_checks() shared helper
  - fly.toml probe path migration
  - make smoke-healthz
affects:
  - http-routes
  - fly-machine-health-probe
tech-stack: [starlette, ietf-health-check-06, fly-toml]
key-files:
  - src/polyarb/http/health.py
  - fly.toml
duration_estimate: ~80 行 code (refactor + new handler) + 4 unit tests + fly deploy smoke

requirements: [D-05, D-06, BUG-6]

must_haves:
  truths:
    - "GET /healthz 永远返 HTTP 200 — 即便 underlying checks 显示 fail (Fly proxy 不切外部流量)"
    - "GET /health 仍遵守 IETF strict — fail → 503 (Better Stack 外探针读, 是告警信号)"
    - "fly.toml [http_service.checks] path 指向 `/healthz` (deploy 后 Fly probe 看 /healthz)"
    - "/healthz body 与 /health 共享 schema (4 checks dict + status + serviceId + version + releaseId) per D-06"
    - "/healthz 和 /health 都是 public (no HMAC) — D-22 amendment 保持"
    - "_build_health_checks() shared helper 抽出, /health 和 /healthz 都调它 (check 逻辑不分叉)"
  artifacts:
    - path: src/polyarb/http/health.py
      provides: "refactor: _build_health_checks() + healthz() handler + health() 调 helper"
      contains: "async def healthz"
      contains: "def _build_health_checks"
    - path: src/polyarb/http/app.py
      provides: "register /healthz route"
      contains: '"/healthz"'
    - path: fly.toml
      provides: "[[http_service.checks]] path = \"/healthz\""
      contains: 'path = "/healthz"'
    - path: Makefile
      provides: "make smoke-healthz target — public GET 验证 200"
      contains: "smoke-healthz:"
    - path: tests/m1-perception/test_health_endpoint.py
      provides: "4 new healthz tests (fresh/stale/failed/body-status)"
  key_links:
    - from: GET /healthz
      to: _build_health_checks(store, settings, now_s)
      via: "shared helper, force HTTP 200"
      pattern: '_build_health_checks\\('
    - from: GET /health
      to: _build_health_checks(store, settings, now_s)
      via: "shared helper, HTTP 503 if overall==fail"
      pattern: 'http_status = 503 if overall'
    - from: Fly platform probe
      to: /healthz endpoint
      via: "fly.toml [[http_service.checks]] path"
      pattern: 'path = "/healthz"'
---

<objective>
修 Bug #6 (`/health` 503 触发 Fly proxy 切外部流量) — RESEARCH Area 1 已 VERIFIED fly.io docs: "If a Machine fails its service check, it will be marked as unhealthy by the proxy, and it won't be routed to until its checks start passing". 这是 Phase 02 Inj 2 期间 `curl /health` timeout 的根因 — daemon 内部正常但外部完全不可达。

Purpose: 分离 IETF strict reporting (Better Stack) 与 Fly platform probe (流量路由) 两个语义. 用户 D-05 拒绝 option (a) "降级 /health 总 200" (会破坏 Better Stack 告警语义), 拒绝 option (b) "TCP probe" (不验证 HTTP 服务真健康), 选 option (c) "拆两 endpoint".

Output:
- `health.py` 重构: 抽 `_build_health_checks(store, settings, now_s) -> (checks_dict, overall_str)` helper, `/health` 和 `/healthz` 共享 underlying 逻辑 (RESEARCH recommendation: full mirror, not simplified — 不分叉 check 逻辑)
- 新 `/healthz` handler — 永远 200, 与 `/health` 同 body schema (per D-06)
- `app.py` 注册 `/healthz` route
- `fly.toml` `[[http_service.checks]]` path: `/health` → `/healthz`
- 4 新 unit test in `test_health_endpoint.py` (healthz 在 fresh/stale/failed 三态下都 200 + body has status field)
- `make smoke-healthz` target
- Post-deploy smoke: `flyctl checks list -a polyarb-l1` 显示 check passing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/ROADMAP.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-RESEARCH.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-LEARNINGS.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-02-SUMMARY.md
@src/polyarb/http/health.py
@src/polyarb/http/app.py
@fly.toml
@tests/m1-perception/test_health_endpoint.py

<interfaces>
<!-- Critical context: /health current shape + refactor target -->

From src/polyarb/http/health.py (current — lines 1-192):
```python
HEALTH_CONTENT_TYPE = "application/health+json"   # ← reuse in /healthz too

def _severity(a: str, b: str) -> str:
    """Combine two statuses by worst-of: pass < warn < fail."""
    ...

async def health(request: Request) -> JSONResponse:
    """IETF draft-inadarei-api-health-check-06 strict.
    Currently includes check 1 (snapshot age), 2 (status), 3 (supabase mirror), 4 (r2 upload).
    Returns 200 for pass/warn, 503 for fail.
    """
    # ... check 1-4 building blocks ...
    body = {
        "status": overall,
        "version": settings.version,
        "releaseId": settings.release_id,
        "serviceId": "polyarb-l1",
        "description": "Polymarket L1 observation daemon ...",
        "checks": checks,
    }
    http_status = 503 if overall == "fail" else 200
    return JSONResponse(body, status_code=http_status, media_type=HEALTH_CONTENT_TYPE)
```

From src/polyarb/http/app.py (after Plan 02.1-02 lands — anticipated state):
```python
from polyarb.http.health import health           # ← extend to: from polyarb.http.health import health, healthz
# routes list has 5 entries after Plan 02:
#   /health, /scan, /control/unpause, /control/pause, /control/status
# This plan adds 1 more: /healthz
```

From fly.toml (current — lines 34-39):
```toml
[[http_service.checks]]
  grace_period = "120s"
  interval = "30s"
  method = "GET"
  path = "/health"      # ← change to "/healthz"
  timeout = "10s"
```

From tests/m1-perception/test_health_endpoint.py (analog for new tests):
```python
def test_fail_when_very_stale(daemon_settings_for_test, http_test_client):
    """Snapshot taken 26h ago → status=fail, HTTP 503."""
    twenty_six_hours_ago_ms = ...
    _insert_snapshot(daemon_settings_for_test.db_path, taken_at_ms=...)
    resp = http_test_client.get("/health")
    assert resp.status_code == 503
    assert resp.json()["status"] == "fail"
```
Helper `_insert_snapshot` already exists in test_health_endpoint.py — reuse.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /healthz (public, no HMAC) | 任何客户端可 GET; body 暴露 snapshot age + mirror age + r2 age + status (与 /health 同等暴露面) |
| Fly platform probe → /healthz | machine 内 Fly-managed, 信任 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-6-01 | Information Disclosure | /healthz body 暴露 internal check status | accept | 与 /health 同 schema (per D-06 + D-22 — 两者都 public, 既定决策); 不暴露 IP/secret/PII; 与 Phase 02 落地等同暴露面 |
| T-02.1-6-02 | Denial of Service | /healthz 高频请求触发 SQLite read (Fly probe 30s 一次, 外部探针潜在更高频) | accept | SQLite read ≪ 1ms (RESEARCH A3 LOW); rate limiting 由 Fly proxy 层处理; 现状下未观察到 abuse |
| T-02.1-6-03 | Spoofing | Fly probe vs malicious external GET /healthz 行为等同 | accept | /healthz body 是 read-only; 无 state change; 等同 /health 安全等级 |
| T-02.1-6-04 | Misconfiguration | fly.toml path 改后 probe 失效 | mitigate | post-deploy `flyctl checks list -a polyarb-l1` 必须显示 check passing (Task 5 verification); rollback 路径已知 (`fly.toml path = "/health"` 即可恢复) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 — RED): 加 4 个 healthz failing tests 到 test_health_endpoint.py</name>
  <files>tests/m1-perception/test_health_endpoint.py</files>
  <read_first>
    - tests/m1-perception/test_health_endpoint.py 全文 (包括 `_insert_snapshot` helper 实际定义 + 现有 test_fail_when_very_stale 模式)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md § "tests/m1-perception/test_health_endpoint.py" 段 (lines 422-465)
    - tests/m1-perception/conftest.py (http_test_client + daemon_settings_for_test fixtures)
  </read_first>
  <behavior>
    Test 1 — `test_healthz_returns_200_when_fresh`:
    - Setup: insert snapshot 1h ago (default fresh)
    - Action: `http_test_client.get("/healthz")`
    - Assert: status_code == 200
    
    Test 2 — `test_healthz_returns_200_when_stale`:
    - Setup: insert snapshot 15h ago (would trigger /health warn, but still warn → 200, parallel sanity check)
    - Action: `http_test_client.get("/healthz")`
    - Assert: status_code == 200
    
    Test 3 — `test_healthz_returns_200_when_failed`:
    - Setup: insert snapshot 26h ago (would trigger /health 503)
    - Action: `http_test_client.get("/healthz")`
    - Assert: status_code == 200 (KEY: 不是 503, 这是 /healthz 与 /health 的核心差别)
    
    Test 4 — `test_healthz_body_has_status_field_and_content_type`:
    - Setup: insert snapshot 26h ago (fail state)
    - Action: `http_test_client.get("/healthz")`
    - Assert:
      - status_code == 200
      - body["status"] in ("pass", "warn", "fail")
      - body["status"] == "fail" (因为 underlying state is fail, only HTTP code wrapping differs)
      - Content-Type 头包含 "health+json" (per RESEARCH Pitfall 5)
      - body 含 "checks" dict (per D-06: 与 /health 同 schema)
  </behavior>
  <action>
    在 `tests/m1-perception/test_health_endpoint.py` 末尾追加 4 个新 test function. 完全 mirror PATTERNS.md 第 422-465 行的代码模板, 但加 Test 4 的 Content-Type 断言 + body schema 检查 (per D-06).
    
    使用现有 `_insert_snapshot` helper (在 test_health_endpoint.py 顶部, 不要重新定义). 使用现有 `http_test_client` + `daemon_settings_for_test` fixtures.
    
    Test 4 完整代码 (扩展 PATTERNS.md 给的 stub):
    ```python
    def test_healthz_body_has_status_field_and_content_type(
        daemon_settings_for_test: Any, http_test_client: TestClient
    ) -> None:
        """GET /healthz body has status field + application/health+json content-type (D-06)."""
        _insert_snapshot(
            daemon_settings_for_test.db_path,
            taken_at_ms=int(time.time() * 1000) - int(26 * 3600 * 1000),
        )
        resp = http_test_client.get("/healthz")
        assert resp.status_code == 200
        body = resp.json()
        assert "status" in body
        assert body["status"] in ("pass", "warn", "fail")
        assert body["status"] == "fail"   # underlying state is fail, only HTTP wrapping differs
        assert "checks" in body          # D-06: same schema as /health
        ct = resp.headers.get("content-type", "")
        assert "health+json" in ct       # RESEARCH Pitfall 5
    ```
    
    跑测试: `uv run pytest tests/m1-perception/test_health_endpoint.py -xvs -k healthz` — 必须 4/4 RED (route 不存在 → 404).
    
    提交: `test(02.1-03): add failing tests for /healthz always-200 endpoint (D-05/D-06)`
  </action>
  <verify>
    <automated>uv run pytest tests/m1-perception/test_health_endpoint.py -xvs -k healthz 2>&1 | grep -E "FAIL|ERROR|PASS|test_healthz" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - 4 healthz tests present: `grep -c "^def test_healthz" tests/m1-perception/test_health_endpoint.py` ≥ 4
    - Test names match: test_healthz_returns_200_when_fresh, test_healthz_returns_200_when_stale, test_healthz_returns_200_when_failed, test_healthz_body_has_status_field_and_content_type
    - RED state: 4 healthz tests fail (route 不存在 → 404), 不是 fixture / import 错误
    - Existing /health tests still in file (no accidental delete): `grep -c "^def test_" tests/m1-perception/test_health_endpoint.py` ≥ original count + 4
  </acceptance_criteria>
  <done>4 失败测试存在, 404 报错符合预期 (route not registered yet).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1 — GREEN): health.py 重构 — 抽 _build_health_checks() helper + 新 healthz() handler</name>
  <files>src/polyarb/http/health.py</files>
  <read_first>
    - src/polyarb/http/health.py 全文 (192 行, 必读以理解 check 1-4 完整逻辑)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md § "src/polyarb/http/health.py" 段 (lines 118-177) — refactor 代码模板
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-RESEARCH.md Area 2 (IETF 合规 + body shape full-mirror 决定)
  </read_first>
  <action>
    **重构步骤** (per PATTERNS.md 第 118-177 行, RESEARCH 推荐 full mirror not simplified):
    
    **Step A**: 抽 helper `_build_health_checks(store, settings, now_s) -> tuple[dict, str]`
    
    把现有 `health()` body 中 lines 56-180(ish) 的整段 check-building 逻辑搬到这个 helper. 完全 verbatim 搬运 — 不改任何 check 行为。Helper 返回 `(checks_dict, overall_str)`.
    
    Helper 签名 (per PATTERNS.md 第 122-137 行):
    ```python
    def _build_health_checks(
        store: Any,
        settings: Any,
        now_s: float,
    ) -> tuple[dict, str]:
        """Compute all health checks. Returns (checks_dict, overall_status).
        Shared by /health (IETF strict 503) and /healthz (always 200).
        """
        checks: dict[str, list[dict]] = {}
        overall = "pass"
        # ... existing check 1 (snapshot age) — verbatim move ...
        # ... existing check 2 (status) — verbatim move ...
        # ... existing check 3 (supabase mirror) — verbatim move ...
        # ... existing check 4 (r2 upload) — verbatim move ...
        return checks, overall
    ```
    
    **Step B**: `health()` handler 简化为薄 wrapper (调 helper, 包 503/200 渲染):
    ```python
    async def health(request: Request) -> JSONResponse:
        """IETF draft-inadarei-api-health-check-06 strict.
        Returns 200 for pass/warn, 503 for fail.
        Better Stack 外探针 read /health (503 = 告警信号).
        Fly platform probe 改读 /healthz (per Phase 02.1 D-05).
        """
        import time
        from polyarb.storage.sqlite_store import SQLiteStore
        store: SQLiteStore = request.app.state.sqlite_store
        settings = request.app.state.settings
        checks, overall = _build_health_checks(store, settings, time.time())
        body = {
            "status": overall,
            "version": settings.version,
            "releaseId": settings.release_id,
            "serviceId": "polyarb-l1",
            "description": "Polymarket L1 observation daemon — subset 2x/day, full 1/week",
            "checks": checks,
        }
        http_status = 503 if overall == "fail" else 200
        return JSONResponse(body, status_code=http_status, media_type=HEALTH_CONTENT_TYPE)
    ```
    
    **Step C**: 新 `healthz()` handler — 与 health() 同 body, 仅 HTTP code 不同 (永远 200):
    ```python
    async def healthz(request: Request) -> JSONResponse:
        """GET /healthz — Fly-friendly probe. Always returns HTTP 200.
        
        Status is in body; Fly [http_service.checks] reads only HTTP code.
        /health (IETF strict 503) continues to serve Better Stack external probe.
        D-22 + D-06: public endpoint, no HMAC, same body schema as /health.
        """
        import time
        from polyarb.storage.sqlite_store import SQLiteStore
        store: SQLiteStore = request.app.state.sqlite_store
        settings = request.app.state.settings
        checks, overall = _build_health_checks(store, settings, time.time())
        body = {
            "status": overall,
            "version": settings.version,
            "releaseId": settings.release_id,
            "serviceId": "polyarb-l1",
            "description": "Polymarket L1 observation daemon — subset 2x/day, full 1/week",
            "checks": checks,
        }
        return JSONResponse(body, status_code=200, media_type=HEALTH_CONTENT_TYPE)
    ```
    
    **关键约束**:
    - 不改 check 1-4 任何业务逻辑 — 纯位置搬运 (lines 56-180 移到 helper)
    - 不改 HEALTH_CONTENT_TYPE / _severity 函数
    - 不改 body schema (D-06 锁定)
    - `media_type=HEALTH_CONTENT_TYPE` 必须显式传 (per RESEARCH Pitfall 5 — 不要默认 application/json)
    - `_build_health_checks` 是 module-private (前导下划线), 不导出
    - `healthz` 在 `__all__` 或不显式导出都可, 由 app.py import 即可
    
    跑测试: `uv run pytest tests/m1-perception/test_health_endpoint.py -x --tb=short` — 现有 /health 测试必须全 green (refactor 不改行为) + 4 新 healthz tests 仍 RED (route 未 register).
    
    提交: `refactor(02.1-03): extract _build_health_checks() helper + add healthz() handler (D-05/D-06)`
  </action>
  <verify>
    <automated>uv run pytest tests/m1-perception/test_health_endpoint.py -x --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Existing /health tests still green (refactor 不改行为): `uv run pytest tests/m1-perception/test_health_endpoint.py -x -k "not healthz"` exit 0
    - New helper exists: `grep -c "^def _build_health_checks" src/polyarb/http/health.py` == 1
    - New handler exists: `grep -c "^async def healthz" src/polyarb/http/health.py` == 1
    - Existing health() handler still exists: `grep -c "^async def health" src/polyarb/http/health.py` == 1 (note: this matches both `health` and `healthz`. Use `grep -cE "^async def health\\(" src/polyarb/http/health.py` == 1)
    - HEALTH_CONTENT_TYPE used in /healthz: `grep -c "media_type=HEALTH_CONTENT_TYPE" src/polyarb/http/health.py` ≥ 2 (one for /health, one for /healthz)
    - Grep gate: `grep -c "status_code=200" src/polyarb/http/health.py | tr -d '\n'` should show /healthz forces 200 — specifically `grep -B2 'status_code=200.*media_type' src/polyarb/http/health.py | grep -c "healthz"` ≥ 1
    - 4 healthz tests still RED (because app.py not yet wired): `uv run pytest tests/m1-perception/test_health_endpoint.py -x -k healthz 2>&1 | grep -c "FAILED"` ≥ 4
  </acceptance_criteria>
  <done>helper 抽出, /health 行为不变 (regression-free), healthz 函数存在但路由未 register (测试仍 RED 直到 Task 3).</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 1 — GREEN): app.py 注册 /healthz route</name>
  <files>src/polyarb/http/app.py</files>
  <read_first>
    - src/polyarb/http/app.py 全文 (注意: Plan 02.1-02 已加 /control/* routes, 当前 routes list 应有 5 条)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md § "src/polyarb/http/app.py" 段
  </read_first>
  <action>
    修改 `src/polyarb/http/app.py`:
    
    **修改 1**: import 扩展. 当前 (after Plan 02.1-02):
    ```python
    from polyarb.http.health import health
    ```
    改为:
    ```python
    from polyarb.http.health import health, healthz
    ```
    
    **修改 2**: routes list 加一条 `/healthz`. 在 `/health` 紧邻处加 (per PATTERNS.md 第 209 行):
    ```python
    routes = [
        Route("/health", health, methods=["GET"]),
        Route("/healthz", healthz, methods=["GET"]),                        # ← new D-05
        Route("/scan", scan, methods=["POST"]),
        Route("/control/unpause", unpause, methods=["POST"]),
        Route("/control/pause", pause, methods=["POST"]),
        Route("/control/status", control_status, methods=["GET"]),
    ]
    ```
    
    **不要**:
    - 不要改 middleware list — `/healthz` 是 public no-HMAC, ScanAuthMiddleware/ControlAuthMiddleware 的 path guard 都正确 bypass /healthz (per RESEARCH Pitfall 2)
    - 不要新增 middleware 给 /healthz
    
    跑 Wave 0 测试: `uv run pytest tests/m1-perception/test_health_endpoint.py -x` — 现在 4 healthz tests + 现有 /health tests 全 green.
    回归: `uv run pytest tests/m1-perception/test_http_scan.py tests/m1-perception/test_control_unpause.py -x` — 现有 /scan + /control/unpause tests 仍 green (新 route 不影响).
    
    提交: `feat(02.1-03): app.py register /healthz route (D-05)`
  </action>
  <verify>
    <automated>uv run pytest tests/m1-perception/test_health_endpoint.py tests/m1-perception/test_http_scan.py tests/m1-perception/test_control_unpause.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 healthz tests green + all existing health/scan/control tests green
    - Grep gate: `grep -c '"/healthz"' src/polyarb/http/app.py` ≥ 1
    - Grep gate: `grep -c 'healthz' src/polyarb/http/app.py` ≥ 2 (import + Route)
    - Negative: `grep -v '^#' src/polyarb/http/app.py | grep -c 'ScanAuthMiddleware.*healthz'` == 0 (no middleware misconfig touching /healthz)
    - Route order: `/healthz` appears in routes list before `/scan` (visual readability, /health* clustered)
  </acceptance_criteria>
  <done>/healthz 路由 live, 12 个 healthz tests + 现有所有 HTTP tests 全 green.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 1 — Platform config): fly.toml 切换 probe path + Makefile smoke target</name>
  <files>fly.toml, Makefile</files>
  <read_first>
    - fly.toml 全文 (70 行)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md § "fly.toml" 段 + "Makefile" 段
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-RESEARCH.md Area 1 (Fly probe semantics)
  </read_first>
  <action>
    **Step A — fly.toml**: 单行修改 (per PATTERNS.md 第 275-281 行):
    
    在 `[[http_service.checks]]` 段 (~lines 34-39):
    ```toml
    [[http_service.checks]]
      grace_period = "120s"
      interval = "30s"
      method = "GET"
      path = "/healthz"     # ← was "/health"; D-05 Phase 02.1
      timeout = "10s"
    ```
    
    其他 fly.toml 内容**全部不动** — 仅这一行 string 替换。
    
    **Step B — Makefile**: 加 `make smoke-healthz` target (per PATTERNS.md 第 325-331 行, 放在 `make smoke-health-local` 附近):
    
    ```makefile
    ## smoke-healthz: Verify /healthz always returns 200 (Fly probe path)
    ## Usage: make smoke-healthz  (no auth required — public endpoint)
    smoke-healthz:
    	@echo ">> smoke-healthz — GET https://polyarb-l1.fly.dev/healthz"
    	@STATUS=$$(curl -s -o /tmp/healthz_body.json -w "%{http_code}" https://polyarb-l1.fly.dev/healthz); \
    	echo "HTTP $$STATUS"; cat /tmp/healthz_body.json | python -m json.tool; \
    	[ "$$STATUS" = "200" ] && echo "PASS: /healthz returned 200" || (echo "FAIL: expected 200 got $$STATUS"; exit 1)
    ```
    
    使用 TAB 缩进, `$$` 转义 (Makefile shell variable expansion).
    
    Dry-run: `make -n smoke-healthz` 必须打印命令 (无 syntax error).
    
    提交: `chore(02.1-03): fly.toml probe path → /healthz + Makefile smoke-healthz target (D-05)`
  </action>
  <verify>
    <automated>grep -A1 'http_service.checks' fly.toml | grep -E 'path = ' && make -n smoke-healthz 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - fly.toml exact: `grep -c 'path = "/healthz"' fly.toml` == 1
    - fly.toml negative: `grep -c 'path = "/health"$' fly.toml` == 0 (must use `grep -E 'path = "/health"\\s*$' fly.toml | grep -c .` to ensure not matching /healthz — explicitly: `grep -cE '^\\s*path = "/health"\\s*$' fly.toml` must be 0)
    - Makefile target exists: `grep -c "^smoke-healthz:" Makefile` == 1
    - Makefile dry-run smoke: `make -n smoke-healthz 2>&1 | grep -c "/healthz"` ≥ 1
    - Makefile help auto-discovery: `make help 2>&1 | grep -c "smoke-healthz"` ≥ 1
    - fly.toml line count unchanged: `wc -l fly.toml` returns 70 (only one string changed, no lines added/removed)
  </acceptance_criteria>
  <done>fly.toml probe 指向 /healthz, smoke target 在 Makefile, dry-run OK.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 (Wave 2 — Deploy + Fly checks smoke per D-05)</name>
  <what-built>
    `/healthz` endpoint live (永远 200) + `/health` 仍是 IETF strict (fail→503) + fly.toml probe 切到 `/healthz` + `make smoke-healthz` 命令入口。Wave 1 unit test 已 GREEN, 但 fly.toml 的修改需要 deploy 到 prod 才能验证 Fly platform probe 真正读到新 path。
  </what-built>
  <how-to-verify>
    Follow 02.1-VALIDATION.md § "Manual-Only Verifications" Fly probe smoke 步骤:
    
    **Deploy**:
    ```bash
    git push origin main     # 触发 GHA deploy workflow (Phase 02 落地)
    # 或手动: flyctl deploy -a polyarb-l1 --remote-only
    
    # 等 deploy 完成 (~3-5 min, watch GHA)
    ```
    
    **核心验证 1 — Fly platform 真的读了 /healthz**:
    ```bash
    flyctl checks list -a polyarb-l1
    ```
    期望输出: 至少一行 "passing" 状态的 check, name 包含 path 反映 (Fly UI 不一定显示 path 但状态 passing 即可). 0 个 failing check.
    
    **核心验证 2 — /healthz 真返 200**:
    ```bash
    make smoke-healthz
    ```
    期望: `HTTP 200` + JSON body (status: pass/warn/fail) + "PASS: /healthz returned 200".
    
    **核心验证 3 — /health 仍 IETF strict (regression sentinel)**:
    ```bash
    curl -sS -o /tmp/h.json -w "%{http_code}\n" https://polyarb-l1.fly.dev/health
    cat /tmp/h.json | python -m json.tool | head -20
    ```
    期望: 200 (current state pass) 或 503 (if 凑巧 stale/fail). 关键是 IETF spec 不变 — fail 时仍 503. body 含 4 checks (snapshot age / status / supabase mirror / r2 upload).
    
    **核心验证 4 — 故意制造 FAIL 状态看是否 Fly proxy 不切流量 (跨 verification 凭证)**:
    
    这步要 careful — Phase 02 Inj 2 已证明: 改 Gamma URL 会让 daemon 内 status 变 fail, 触发 /health 503. 现在 Fly probe 看 /healthz (永远 200) — 应 proxy 不切.
    
    ```bash
    # 备份 Gamma URL
    ORIG_GAMMA=$(flyctl ssh console -a polyarb-l1 -C 'printenv POLYARB_GAMMA_URL' 2>/dev/null | tail -1)
    test -n "$ORIG_GAMMA" || ORIG_GAMMA="https://gamma-api.polymarket.com"
    
    # 注入 fail
    flyctl secrets set POLYARB_GAMMA_URL=https://gamma-invalid.example.com -a polyarb-l1
    sleep 90    # 等 daemon 进 fail (但 NOT PAUSED — 只需 1 次 fail 让 /health 503)
    
    # 关键验证: 此时 /health 应 503, /healthz 应 200, Fly check 应仍 passing
    echo "--- /health (期望 503 or 200, body status 反映 underlying state) ---"
    curl -sS -o /tmp/h1.json -w "HTTP %{http_code}\n" https://polyarb-l1.fly.dev/health
    cat /tmp/h1.json | python -m json.tool | grep -E '"status"|overall' | head -5
    
    echo "--- /healthz (期望 200, body status 可以是 fail) ---"
    curl -sS -o /tmp/h2.json -w "HTTP %{http_code}\n" https://polyarb-l1.fly.dev/healthz
    cat /tmp/h2.json | python -m json.tool | grep -E '"status"' | head -5
    
    echo "--- Fly checks (期望仍 passing — Fly proxy 不切流量) ---"
    flyctl checks list -a polyarb-l1
    
    # 关键: 注入 fail 后 curl 仍可达 (不像 Phase 02 Inj 2 timeout)
    
    # 恢复
    flyctl secrets set POLYARB_GAMMA_URL="$ORIG_GAMMA" -a polyarb-l1
    sleep 90
    curl -sS https://polyarb-l1.fly.dev/health | python -m json.tool | grep -E "overall|status" | head -3
    ```
    
    期望:
    - `/health` body status 反映 underlying fail, HTTP 503
    - `/healthz` body status 同样 fail, **HTTP 200** (key differentiator)
    - `flyctl checks list` 显示 check 仍 passing
    - 整个验证期间 `curl https://polyarb-l1.fly.dev/...` 不 timeout (Phase 02 Inj 2 的 reproduction 证明 fix 生效)
    
    **追加到 02-SOAK-LOG.md**:
    Inj #6-verification 段 (新条目), 含 timestamp + 4 step 的命令输出 + fly checks 截图 + 验证 Phase 02 Inj 2 fix 凭证。
  </how-to-verify>
  <resume-signal>
    Type "approved" + 02-SOAK-LOG.md verification 段 commit SHA, 或 "smoke failed: <step>" 说明哪步 fail (例如 "flyctl checks failing" / "/healthz 不返 200" / "Fly proxy 仍切流量 — curl timeout").
  </resume-signal>
</task>

</tasks>

<verification>
**Plan-level invariants**:

1. Wave 0 + Wave 1 unit test 全 GREEN — `uv run pytest tests/m1-perception/test_health_endpoint.py tests/m1-perception/test_http_scan.py tests/m1-perception/test_control_unpause.py -x` exit 0
2. Helper 抽出 — `grep -c "^def _build_health_checks" src/polyarb/http/health.py` == 1
3. /healthz 永远 200 — `grep -B5 "status_code=200" src/polyarb/http/health.py | grep -c "async def healthz"` ≥ 1
4. /health IETF strict 不变 — `grep -c "http_status = 503 if overall" src/polyarb/http/health.py` ≥ 1
5. fly.toml — `grep -c 'path = "/healthz"' fly.toml` == 1 AND `grep -cE '^\s*path = "/health"\s*$' fly.toml` == 0
6. /healthz body schema D-06 — `test_healthz_body_has_status_field_and_content_type` 测试 verify "checks" key 在 body 内
7. Deploy + Fly checks smoke (Task 5 checkpoint approved, 02-SOAK-LOG.md 追加段已 commit)
8. SUMMARY.md 列出: health.py refactor diff range + fly.toml diff line + Fly checks list output + curl /health vs /healthz under failure 对比

**Negative tests (regression sentinels)**:
- D-06 schema 不变 — /healthz body 含 "checks" + "status" + "serviceId" + "version" + "releaseId" + "description"
- /scan + /control/* tests 全 green (不影响 ScanAuthMiddleware / ControlAuthMiddleware)
- /health 在 fail 时仍 503 (test_fail_when_very_stale 仍 green)
- Phase 02 Plan 03 落地的 supabase mirror check + r2 upload check 在 helper 内仍正确执行 — `grep -c "supabase_mirror_age_seconds" src/polyarb/http/health.py` ≥ 1 + `grep -c "r2.*age" src/polyarb/http/health.py` ≥ 1
</verification>

<success_criteria>
- prod deploy 后 `flyctl checks list -a polyarb-l1` 显示 check passing (per CONTEXT.md § "Phase 02.1 关闭凭证" #3)
- 注入 fail 期间 `/health` 503 + `/healthz` 200 + Fly proxy **不切流量** (curl 仍可达)
- D-12 fail-soft 路径 + IETF /health strict 行为均不变 (regression-free)
- Phase 02 LEARNINGS L7 "alert chain discipline + 不蔓延" — Better Stack 仍可监控 /health (有真告警), Fly platform 用 /healthz (不误切流量)
- 02-SOAK-LOG.md 追加 #6-verification 段, 时间线连续
- Plan SUMMARY (`02.1-03-SUMMARY.md`) 落地, planning-status zero drift
</success_criteria>

<plan_check>
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md

Covers VALIDATION.md task IDs: 02.1-03-01 + 02.1-03-02 + 02.1-03-03 + 02.1-03-04 (4 unit + 1 grep smoke + 1 deploy chaos).
Sampling per plan: `uv run pytest tests/m1-perception/test_health_endpoint.py -x`
</plan_check>

<output>
After Tasks 1-5 全部完成, create `.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-03-SUMMARY.md` (CLAUDE.md 纪律). pre-commit hook 守门, 不要 `--no-verify` 绕过.
</output>

---

## Plan Boundary (D-07 严格 fix-up scope reminder)

**本 plan 只做**: 拆 `/health` (IETF strict) + `/healthz` (always 200) + fly.toml probe 切换 (D-05 + D-06).

**本 plan 不做**:
- 不实现 simplified /healthz body shape — full mirror per RESEARCH 推荐 (理由: 不分叉 check 逻辑, future check 加自动跟)
- 不改 IETF /health 行为 (D-06 锁定)
- 不改 fly.toml 其他段 (machine size / regions / mounts 全不动)
- 不加额外 Fly top-level `[checks]` 段 (RESEARCH Area 1 评估后 D-05 不选这条路)
- 不动 ScanAuthMiddleware / ControlAuthMiddleware (Plan 02.1-02 的 scope)
- 不动 orchestrator.py / scheduler.py (其它 plan scope)
- 不加 7-day soak (推到 Phase 03)
- 不加 Axiom log shipping (P2 backlog)

如 executor 发现以上 scope 之外的改动需求, stop and ask 不自行扩展。
