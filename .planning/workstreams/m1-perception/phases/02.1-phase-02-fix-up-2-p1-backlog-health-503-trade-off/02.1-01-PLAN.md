---
phase: 02.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polyarb/snapshot/orchestrator.py
  - tests/m1-perception/test_orchestrator_mirror_skip.py
autonomous: true
subsystem: snapshot-orchestrator
tags: [observability, fail-soft, sentry, audit-log]
requires: [D-12, D-22]
provides: [mirror-disabled-audit-log, mirror-disabled-breadcrumb]
affects:
  - orchestrator.step-7.5
  - sentry-breadcrumb-trail
tech-stack: [python-3.12, sentry-sdk, loguru]
key-files:
  - src/polyarb/snapshot/orchestrator.py
duration_estimate: ~30 lines code + 2 tests + chaos Inj 3 复跑

requirements: [D-01, D-02, BUG-7]

must_haves:
  truths:
    - "撤 Supabase secret 场景下 daemon log 出现 'mirror disabled' / 'config-disabled' 字样"
    - "撤 Supabase secret 场景下 Sentry breadcrumb (category='mirror', level='info') 出现在下一次 Sentry event 上"
    - "D-12 fail-soft 主契约不变 — snapshot 仍继续完成, mirror skip 不阻断"
    - "Phase 02 LEARNINGS L7 verification 纪律遵守 — 必须 chaos Inj 3 复跑, 不允许只靠 unit test"
  artifacts:
    - path: src/polyarb/snapshot/orchestrator.py
      provides: "step 7.5 入口 else 分支: audit log + sentry_sdk.add_breadcrumb"
      contains: "mirror disabled — reason=config-disabled"
    - path: tests/m1-perception/test_orchestrator_mirror_skip.py
      provides: "2 unit tests: log assertion + breadcrumb assertion"
      contains: "def test_mirror_disabled_logs_audit_entry"
  key_links:
    - from: src/polyarb/snapshot/orchestrator.py step 7.5
      to: sentry_sdk.add_breadcrumb
      via: import + 直接调用 (orchestrator.py 顶部已 import sentry_sdk)
      pattern: 'sentry_sdk\\.add_breadcrumb.*category="mirror"'
    - from: src/polyarb/snapshot/orchestrator.py step 7.5
      to: loguru logger
      via: "logger.info(\"step 7.5: mirror disabled — reason=config-disabled ...\")"
      pattern: 'mirror disabled.*config-disabled'
---

<objective>
修 Bug #7 (fail-soft 互相抵消静默) — 让 `supabase_mirror_enabled=False` 路径从黑洞变成 audit-visible. 当 secret 撤销/未配 → pydantic 把 `mirror_enabled` 设为 False → step 7.5 完全跳过 mirror 路径, 此前 0 log / 0 breadcrumb / `/health supabase` check 整段消失 (Phase 02 chaos Inj 3 暴露).

Purpose: 兑现 Phase 02 LEARNINGS L6 "HTTP 200 ≠ user notified" + L7 "不允许测试按钮自我欺骗" 纪律. 任何 fail-soft 路径必须留痕, 否则下一次出事时 Sentry event 没有 context, debug 全靠脑补.

Output:
- orchestrator.py step 7.5 加 `else` 分支 (mirror_enabled=False 路径) 发 `logger.info` + `sentry_sdk.add_breadcrumb(category="mirror", level="info")`
- 2 unit test 验证 audit log + breadcrumb 真发出
- chaos Inj 3-v2 复跑确认 prod-grade visibility (manual verification, 不在 pytest suite 内)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/ROADMAP.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-RESEARCH.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-LEARNINGS.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-07-SUMMARY.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-SOAK-LOG.md
@src/polyarb/snapshot/orchestrator.py
@tests/m1-perception/conftest.py

<interfaces>
<!-- Critical context: orchestrator.py step 7.5 三分支结构 (per RESEARCH.md Pitfall 3) -->

From src/polyarb/snapshot/orchestrator.py (~line 487-548):
```python
# 当前 step 7.5 结构 (verified via RESEARCH.md code read):
if settings.supabase_mirror_enabled and not is_valid:     # branch A: F-05 guard
    logger.info("step 7.5: skip Supabase mirror — snapshot is_valid=False ...")
elif settings.supabase_mirror_enabled:                    # branch B: real mirror path
    try:
        mirror.push_snapshot(...)
    except Exception as e:
        sentry_sdk.add_breadcrumb(
            category="storage",
            message=f"supabase_mirror_failed snapshot_id={snapshot_id}",
            level="warning",
            data={"error": str(e)[:200]},
        )
        logger.warning(...)
# ← branch C 当前完全缺失 (mirror_enabled=False) — 这是 Bug #7 根因
```

From conftest.py (lines 461-499, already exists):
```python
@pytest.fixture
def mocked_sentry(monkeypatch: pytest.MonkeyPatch) -> Any:
    """Monkeypatch sentry_sdk.init / capture_message / add_breadcrumb.
    Returns SimpleNamespace with .add_breadcrumb MagicMock (call_args_list inspectable)."""
```

From orchestrator.py top of file:
```python
import sentry_sdk     # already imported, no new import needed
from loguru import logger
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon → Sentry SaaS | breadcrumb data 出网到 Sentry, 不含 PII (snapshot_id 是 ULID) |
| daemon → stdout/stderr log | Fly log shipping (5 day retention), 不含 secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-7-01 | Information Disclosure | orchestrator step 7.5 breadcrumb data | accept | data dict 只含 `supabase_mirror_enabled: bool` + `snapshot_id: str` (ULID), 无 secret/PII; Sentry SDK before_send 已 redact 已知 PII patterns |
| T-02.1-7-02 | Repudiation | daemon log "mirror disabled" 字串 | mitigate | logger.info 走 loguru JSON sink → Fly logs, 带 timestamp + module/function/line, 5 天可审计 (Phase 02 L5 落地) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 — RED): 新建 test_orchestrator_mirror_skip.py with 2 failing tests</name>
  <files>tests/m1-perception/test_orchestrator_mirror_skip.py</files>
  <read_first>
    - tests/m1-perception/test_orchestrator.py (现有 patch + asyncio.run 模式, RESEARCH.md Area 5 Pitfall 3 引用)
    - tests/m1-perception/conftest.py 第 461-499 行 (mocked_sentry fixture 已存在)
    - tests/m1-perception/conftest.py 第 1-100 行 (`_make_settings`, `mocked_gamma_orchestrator`, `mocked_clob` 等通用 fixture)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md § "tests/m1-perception/test_orchestrator.py" 段
  </read_first>
  <behavior>
    Test 1 — `test_mirror_disabled_logs_audit_entry`:
    - Setup: `_make_settings(tmp_path)` 默认 `supabase_mirror_enabled=False` (无 secret)
    - Action: `asyncio.run(run_snapshot(settings=settings))`
    - Assert: `caplog.records` 中至少一条 record 的 message 包含 "mirror disabled" 或 "config-disabled" 字串
    Test 2 — `test_mirror_disabled_adds_sentry_breadcrumb`:
    - Setup: 同上 + `mocked_sentry` fixture
    - Action: 同上 asyncio.run
    - Assert: `mocked_sentry.add_breadcrumb.call_args_list` 中至少一个 call kwargs 满足:
      - `category == "mirror"`
      - `level == "info"`
      - `"config-disabled" in message`
  </behavior>
  <action>
    新建 `tests/m1-perception/test_orchestrator_mirror_skip.py` (per D-01)。完全 mirror PATTERNS.md § "tests/m1-perception/test_orchestrator.py" 给的代码 (lines 493-524), 不要自己重新设计 fixture 用法。
    
    具体步骤:
    1. import block (顶部):
       ```python
       from __future__ import annotations
       import asyncio
       import logging
       from pathlib import Path
       from typing import Any
       
       import pytest
       
       from polyarb.snapshot.orchestrator import run_snapshot
       ```
    2. 复用 conftest.py 已有 `_make_settings` (注: 如果它定义在 test_orchestrator.py 而非 conftest, 从那里 import 或在 conftest 提取 — 检查后再定)
    3. 写两个 test function (function bodies 严格 mirror PATTERNS.md 第 493-524 行)。注意:
       - 默认 settings 应已使 `supabase_mirror_enabled=False` (没有 service_key)
       - test 1 用 `caplog.at_level(logging.INFO, logger="polyarb")`
       - test 2 用 `mocked_sentry` fixture (已在 conftest)
    4. 先跑 `uv run pytest tests/m1-perception/test_orchestrator_mirror_skip.py -xvs` — 必须 RED (orchestrator step 7.5 当前没 else 分支, 既无 log 也无 breadcrumb)
    5. 提交: `test(02.1-01): add failing tests for mirror disabled audit log + breadcrumb (D-01)`
  </action>
  <verify>
    <automated>uv run pytest tests/m1-perception/test_orchestrator_mirror_skip.py -xvs 2>&1 | grep -E "FAIL|ERROR" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/m1-perception/test_orchestrator_mirror_skip.py`
    - Test functions: `grep -c "def test_mirror_disabled" tests/m1-perception/test_orchestrator_mirror_skip.py` ≥ 2
    - RED state: tests fail with AssertionError on log/breadcrumb missing (not import error / fixture error)
    - Commit message starts with `test(02.1-01):`
  </acceptance_criteria>
  <done>2 失败测试存在, 错误信息是 "no record found containing 'mirror disabled'" 或 "add_breadcrumb not called with category=mirror" (而非 ImportError / fixture KeyError).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 1 — GREEN): orchestrator.py step 7.5 加 `else` 分支 audit log + breadcrumb</name>
  <files>src/polyarb/snapshot/orchestrator.py</files>
  <read_first>
    - src/polyarb/snapshot/orchestrator.py 全文 (确认 step 7.5 在 ~487-548 行的真实结构, RESEARCH.md 给的是参考)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-PATTERNS.md § "src/polyarb/snapshot/orchestrator.py" 段 (新 else block 代码模板, 第 244-260 行)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-RESEARCH.md Area 5 (breadcrumb level=info 设计依据 + Sentry LoguruIntegration 不影响 add_breadcrumb)
    - .planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-RESEARCH.md "Common Pitfalls" Pitfall 3 (改动位置错误)
  </read_first>
  <action>
    Per D-01: 在 orchestrator.py step 7.5 的 `if settings.supabase_mirror_enabled and not is_valid:` / `elif settings.supabase_mirror_enabled:` 链末尾加 `else:` 分支。
    
    具体修改 (verbatim 复用 PATTERNS.md 第 244-260 行的 else block):
    ```python
    else:
        # D-01 (Phase 02.1): audit log + breadcrumb for config-disabled skip.
        # Previously silent — now visible in daemon logs and next Sentry event.
        logger.info(
            f"step 7.5: mirror disabled — reason=config-disabled "
            f"(snapshot_id={snapshot_id}). "
            "Supabase dashboard will not update until mirror is re-enabled."
        )
        sentry_sdk.add_breadcrumb(
            category="mirror",
            level="info",
            message="mirror skipped: reason=config-disabled",
            data={"supabase_mirror_enabled": False, "snapshot_id": snapshot_id},
        )
    ```
    
    关键约束 (per RESEARCH.md Pitfall 3 + D-12):
    - `else` 必须与 `if settings.supabase_mirror_enabled and not is_valid:` 同缩进 (顶级 if-elif-else 链, 不是嵌套)
    - 不动 lines 493-548 的 fail-soft try/except 路径 (D-12 fail-soft 主契约不变)
    - `sentry_sdk` + `logger` 在 orchestrator.py 顶部已 import — 不要重复 import
    - `category="mirror"` 与现有 `category="storage"` 区分 (failure vs config-disabled 不同 context, per PATTERNS.md "Sentry Breadcrumb" 段)
    - 不动 settings 语义 (per D-01 拒绝 option (a) settings 拆分)
    
    跑测试: `uv run pytest tests/m1-perception/test_orchestrator_mirror_skip.py -xvs` 必须 GREEN (2 tests pass)。
    回归: `uv run pytest tests/m1-perception/test_orchestrator.py -xvs --tb=short` 必须仍 green (D-12 fail-soft 路径不变).
    
    提交: `feat(02.1-01): orchestrator step 7.5 emits audit log + breadcrumb when mirror disabled (D-01)`
  </action>
  <verify>
    <automated>uv run pytest tests/m1-perception/test_orchestrator_mirror_skip.py tests/m1-perception/test_orchestrator.py -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - GREEN: Wave 0 tests pass (`pytest test_orchestrator_mirror_skip.py -x` exits 0)
    - Regression: `uv run pytest tests/m1-perception/test_orchestrator.py -x` exits 0
    - Grep gate: `grep -n 'reason=config-disabled' src/polyarb/snapshot/orchestrator.py | wc -l` ≥ 2 (logger.info + add_breadcrumb)
    - Grep gate: `grep -c 'category="mirror"' src/polyarb/snapshot/orchestrator.py` ≥ 1
    - D-12 invariant: `grep -c 'supabase_mirror_failed' src/polyarb/snapshot/orchestrator.py` ≥ 1 (existing failure breadcrumb 未被删)
    - Commit message starts with `feat(02.1-01):` and references D-01
  </acceptance_criteria>
  <done>2 unit test 全 green, orchestrator.py 其他测试无回归, step 7.5 三个分支 (F-05 / mirror-enabled / config-disabled) 全部 visible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Wave 2 — chaos Inj 3-v2 manual verification per D-02)</name>
  <what-built>
    orchestrator step 7.5 在 `supabase_mirror_enabled=False` 路径加了 audit log + Sentry breadcrumb。Wave 1 unit test 已 GREEN, 但 D-02 明确要求 chaos injection 重跑 (Phase 02 L7 "不允许测试按钮自我欺骗" 纪律)。
  </what-built>
  <how-to-verify>
    Follow 02.1-VALIDATION.md § "Manual-Only Verifications" Chaos Inj 3 步骤:
    
    **预备 (本地 shell)**:
    ```bash
    # 备份当前 secret (本地变量, 不写文件)
    ORIG_KEY=$(flyctl ssh console -a polyarb-l1 -C 'printenv POLYARB_SUPABASE_SERVICE_KEY' 2>/dev/null | tail -1)
    test -n "$ORIG_KEY" || { echo "ERROR: backup key empty, aborting chaos"; exit 1; }
    echo "Backup secured (${#ORIG_KEY} chars)"
    
    # (可选) 加速 scheduler interval 到 60s 便于观察
    flyctl secrets set POLYARB_SCHEDULER_INTERVAL_S=60 -a polyarb-l1
    ```
    
    **注入 chaos**:
    ```bash
    # 撤 secret → pydantic mirror_enabled=False
    flyctl secrets unset POLYARB_SUPABASE_SERVICE_KEY -a polyarb-l1
    # daemon 会自动 restart, 等 ~30s
    sleep 60
    ```
    
    **观察 1 — daemon log 必须出现 audit entry**:
    ```bash
    flyctl logs -a polyarb-l1 --no-tail | grep -E "mirror disabled|config-disabled" | tail -5
    ```
    期望: ≥ 1 行包含 "mirror disabled — reason=config-disabled (snapshot_id=...)"。0 行 = FAIL。
    
    **观察 2 — Sentry event 必须 carry breadcrumb**:
    1. 触发一次 Sentry event: `POLYARB_SCAN_SHARED_SECRET=$(...) make alerts-test` (现有 Phase 02 Makefile target)
    2. 在 Sentry dashboard (https://sentry.io/organizations/...) 打开最新 event
    3. 看 Breadcrumbs 段, 必须看到一条 `category=mirror, level=info, message=mirror skipped: reason=config-disabled`, data 含 `supabase_mirror_enabled: false`
    
    **恢复**:
    ```bash
    flyctl secrets set POLYARB_SUPABASE_SERVICE_KEY="$ORIG_KEY" -a polyarb-l1
    flyctl secrets unset POLYARB_SCHEDULER_INTERVAL_S -a polyarb-l1     # 若之前加速
    sleep 60
    curl -sS https://polyarb-l1.fly.dev/health | python -m json.tool | grep -E 'overall|supabase'
    ```
    期望: `/health` overall=pass, supabase check 回归 (因为 secret 恢复, mirror_enabled 重新 True)。
    
    **记录到 02-SOAK-LOG.md**:
    在 02-SOAK-LOG.md 末尾追加 Inj 3-v2 段, 含 timestamp + secret 撤回时间 + 观察到的 log line 截图 + Sentry event URL + breadcrumb 截图。这是 SUMMARY closure 凭证。
  </how-to-verify>
  <resume-signal>
    Type "approved" 后并提供 02-SOAK-LOG.md 追加段的 commit SHA, 或 "chaos failed: <reason>" 说明哪一步 fail (例如: "log 缺失" / "breadcrumb 未出现 in Sentry event")。
  </resume-signal>
</task>

</tasks>

<verification>
**Plan-level invariants (goal-backward 必须都满足才能 SUMMARY 闭环)**:

1. Wave 0 + Wave 1 unit test 全 GREEN — `uv run pytest tests/m1-perception/test_orchestrator_mirror_skip.py tests/m1-perception/test_orchestrator.py -x --tb=short` exit 0
2. Grep gate — `grep -c 'reason=config-disabled' src/polyarb/snapshot/orchestrator.py` ≥ 2 (logger + breadcrumb message)
3. D-12 invariant — fail-soft 主契约不变, `grep -c 'supabase_mirror_failed' src/polyarb/snapshot/orchestrator.py` ≥ 1
4. Chaos Inj 3-v2 verification 通过 (Task 3 checkpoint approved, 02-SOAK-LOG.md 追加段已 commit)
5. SUMMARY.md 列出: which lines in orchestrator.py changed (line range) + Inj 3-v2 timestamp + Sentry event URL + chaos 观察的 log line 完整内容

**Negative tests (regression sentinels)**:
- 现有 Phase 02 orchestrator try/except 路径 lines 493-548 不动 — `git diff` 应只显示新增 else 分支 + 注释, 不应有 `try:` / `except:` 修改
- `grep -c 'category="storage"' src/polyarb/snapshot/orchestrator.py` ≥ 1 (existing failure breadcrumb 仍在)
</verification>

<success_criteria>
- 撤 Supabase secret → daemon 不静默, log 出现 "mirror disabled — reason=config-disabled" + Sentry event 携带 breadcrumb (per CONTEXT.md § "Phase 02.1 关闭凭证" #1)
- Phase 02 LEARNINGS L6/L7 纪律遵守 — 不靠测试按钮自我欺骗, 真 chaos 验证
- 02-SOAK-LOG.md 追加 Inj 3-v2 段, 时间线连续
- Plan SUMMARY (`02.1-01-SUMMARY.md`) 落地, planning-status zero drift
</success_criteria>

<plan_check>
@.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-VALIDATION.md

This plan covers VALIDATION.md task IDs: 02.1-01-01 (unit) + 02.1-01-02 (chaos Inj 3).
Sampling per plan: `uv run pytest tests/m1-perception/test_orchestrator_mirror_skip.py tests/m1-perception/test_orchestrator.py -x`
</plan_check>

<output>
After Task 1 + Task 2 + Task 3 全部完成, create `.planning/workstreams/m1-perception/phases/02.1-phase-02-fix-up-2-p1-backlog-health-503-trade-off/02.1-01-SUMMARY.md` (per CLAUDE.md "每个 Plan 末" 纪律). pre-commit hook (`.githooks/pre-commit`) 会拦截缺 SUMMARY 的 plan-scoped commit, 不要 `--no-verify` 绕过.
</output>

---

## Plan Boundary (D-07 严格 fix-up scope reminder)

**本 plan 只做**: orchestrator step 7.5 的 `mirror_enabled=False` 路径 visibility 修复 (D-01 + D-02).

**本 plan 不做** (planner 拒绝任何 scope 蔓延的执行者建议):
- 不动 `settings.supabase_mirror_enabled` 语义 (D-01 明确拒绝 option (a) settings 拆分)
- 不加 #6 /healthz drift check (D-01 明确拒绝 option (c))
- 不修 3 pre-existing test failure (D-07 推到独立 chore commit)
- 不动 D-12 fail-soft 主契约的 try/except 路径
- 不动 R2 step 7.6 (Bug #7 只在 Supabase mirror 路径出现, R2 step 7.6 已有 breadcrumb)
- 不动 dashboard "Unpause Daemon" 按钮 (D-07 推独立 plan)

如果 executor 在执行中发现需要超 scope 的改动, 必须 stop and ask, 不允许自行扩展。
