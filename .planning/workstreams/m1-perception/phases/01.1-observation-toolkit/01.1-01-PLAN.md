---
phase: 01.1-observation-toolkit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/polyarb/storage/schemas.py
  - src/polyarb/snapshot/normalizer.py
  - tests/m1-perception/test_normalizer.py
  - tests/m1-perception/test_sqlite_store.py
  - tests/m1-perception/test_schema_lockstep.py
  - .gitignore
autonomous: false  # 末尾有 checkpoint:human-action — 用户手动跑 snapshot-markets-v 重建基线
requirements: []  # workstream-level phase, no central REQUIREMENTS
tags: [polyarb, m1-perception, schema, gamma-extraction, baseline-reset]

must_haves:
  truths:
    - "markets 表 schema 含 category TEXT + tags TEXT 列"
    - "question_translations 表存在，schema 与 CONTEXT.md 锁定一致"
    - "Gamma 抓取流程把 raw['category'] 与 raw['tags'] 落入 markets 行（normalizer 已提取）"
    - "openai 与 rich 出现在 pyproject.toml [project.dependencies]"
    - "schemas.py 5 处同步点（DDL/MARKETS_COLUMN_ORDER/MARKETS_INSERT_SQL/SNAPSHOT_SCHEMA + 新表 DDL）通过一致性测试"
    - "老 SQLite + 老 parquet 清空后，重新跑 snapshot 得到一份带 category/tags 的新基线"
  artifacts:
    - path: "src/polyarb/storage/schemas.py"
      provides: "markets 加列 + question_translations 表 DDL"
      contains: "category TEXT"
    - path: "src/polyarb/storage/schemas.py"
      provides: "question_translations DDL"
      contains: "CREATE TABLE IF NOT EXISTS question_translations"
    - path: "src/polyarb/snapshot/normalizer.py"
      provides: "category/tags 字段提取"
      contains: "raw.get(\"category\")"
    - path: "pyproject.toml"
      provides: "openai + rich 依赖"
      contains: "openai>=2.0"
    - path: "tests/m1-perception/test_schema_lockstep.py"
      provides: "5 处同步点一致性测试"
  key_links:
    - from: "src/polyarb/snapshot/normalizer.py"
      to: "src/polyarb/storage/schemas.py:MARKETS_COLUMN_ORDER"
      via: "返回 dict 的 key 顺序必须可被 _row_to_tuple 通过 MARKETS_COLUMN_ORDER 索引"
      pattern: "category.*tags"
    - from: "src/polyarb/storage/sqlite_store.py:_row_to_tuple"
      to: "MARKETS_INSERT_SQL"
      via: "placeholder 数从 21 升到 23"
      pattern: "VALUES \\([?,]+\\)"
---

<objective>
Phase 1.1 的根基：把 Gamma API 已经返回的 `category` + `tags` 字段从 RAW 抓回 normalize 进库；新建 `question_translations` 表为 T2 翻译做底；引入 `openai` + `rich` 两个新主依赖；最后清空老数据并由用户手动重建一份带新字段的基线 snapshot。

Purpose: 没有这一步，所有 scan/translation/show 都拿不到 category 字段。schema 是 single source of truth（schemas.py 顶部 docstring 警告），加列必须 5 处同步——这是历史踩坑（SESSION 11 的 cli.py vs cli/ silent shadow 同源教训）。

Output: schema 升级 commit、normalizer commit、deps commit、清空旧基线后由用户跑一次 `make snapshot-markets-v` 得到新基线。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-PATTERNS.md
@.planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-RESEARCH.md
@CLAUDE.md
@src/polyarb/storage/schemas.py
@src/polyarb/snapshot/normalizer.py
@src/polyarb/storage/sqlite_store.py
@pyproject.toml

<interfaces>
<!-- schemas.py 现有 21 列锚点（从 schemas.py:79-110 摘出）-->
MARKETS_COLUMN_ORDER 当前 21 个元素，结尾 `("fetched_at_ms", "snapshot_id", "incomplete")`
MARKETS_INSERT_SQL 当前 21 个 placeholder
SNAPSHOT_SCHEMA 当前 22 字段（含 snapshot_taken_at_ms 1 个 parquet-only）

<!-- 新增后的目标 -->
MARKETS_COLUMN_ORDER 23 个元素，加 ("category", "tags") 在 incomplete 之后
MARKETS_INSERT_SQL 23 个 placeholder
SNAPSHOT_SCHEMA 24 字段，加 pa.field("category", pa.string(), nullable=True) + pa.field("tags", pa.string(), nullable=True)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gamma API → normalizer | category/tags 是 Polymarket 自报字段，可能是任意字符串/任意嵌套结构 |
| .env → pyproject.toml | 新依赖 openai 引入网络出口能力（Plan 02 用），但本 plan 只装不调 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | normalizer raw['tags'] 是 list[str]，可能含恶意字符 | mitigate | 用 json.dumps(raw.get("tags") or [], ensure_ascii=False) 序列化为字符串列存；不在本 phase 解析/渲染 |
| T-01.1-02 | Information Disclosure | category/tags 可能泄漏 Polymarket 内部分类逻辑 | accept | 公开数据，无敏感 |
| T-01.1-03 | DoS | 巨大 tags 数组（10000 元素）导致 SQLite 单行膨胀 | accept | Polymarket 实际 tags 数 ≤ 10，攻击概率低；F-5 truncation 已在 Phase 1 防过 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 升级 schemas.py（5 处同步）+ 加 question_translations DDL + lockstep 测试</name>
  <files>
    src/polyarb/storage/schemas.py
    tests/m1-perception/test_schema_lockstep.py
  </files>
  <read_first>
    - src/polyarb/storage/schemas.py（**全部** — 顶部 docstring 警告 5 处同步）
    - .planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-PATTERNS.md §2.13（schemas.py 修改 5 处枚举）
    - .planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-CONTEXT.md `### T1` + `### T2` 数据模型
    - src/polyarb/storage/sqlite_store.py:_row_to_tuple（验证它通过 MARKETS_COLUMN_ORDER 索引，schemas 改完它零动）
    - tests/m1-perception/test_sqlite_store.py（看现有 schema 一致性测试模板）
  </read_first>
  <behavior>
    - test_lockstep_column_count_matches: len(MARKETS_COLUMN_ORDER) == placeholder count in MARKETS_INSERT_SQL == DDL field count == SNAPSHOT_SCHEMA non-parquet-only field count
    - test_category_field_in_all_5_sync_points: "category" in MARKETS_COLUMN_ORDER 且 INSERT SQL placeholder 数对、DDL 含 category TEXT、SNAPSHOT_SCHEMA 有 category
    - test_tags_field_in_all_5_sync_points: 同上 for "tags"
    - test_question_translations_ddl_exists: init_schema 后 sqlite3 .schema 含 question_translations
    - test_question_translations_columns: question_hash/question_en/question_zh/translator_model/translated_at_ms/token_cost/retry_count/is_dead 八列齐全
  </behavior>
  <action>
    1. 修改 `src/polyarb/storage/schemas.py:DDL` 字符串：
       - 在 `markets` 表 `incomplete INTEGER NOT NULL DEFAULT 0` 之后加两行：
         ```
         category           TEXT,
         tags               TEXT,
         ```
       - 在文件末尾 `validation_issues` index 后追加 `question_translations` 表 DDL（精确按 CONTEXT.md `### T2` 数据模型）：
         ```sql
         CREATE TABLE IF NOT EXISTS question_translations (
           question_hash    TEXT PRIMARY KEY,
           question_en      TEXT NOT NULL,
           question_zh      TEXT NOT NULL,
           translator_model TEXT NOT NULL,
           translated_at_ms INTEGER NOT NULL,
           token_cost       INTEGER,
           retry_count      INTEGER NOT NULL DEFAULT 0,
           is_dead          INTEGER NOT NULL DEFAULT 0
         );
         CREATE INDEX IF NOT EXISTS idx_qt_dead ON question_translations(is_dead);
         CREATE UNIQUE INDEX IF NOT EXISTS idx_qt_question_en ON question_translations(question_en);
         ```
         注：`idx_qt_question_en` 唯一索引保护 plan 02 的 `LEFT JOIN ON m.question = qt.question_en`（Open Question #1 的方案 c）防止重复行。
    2. 在 `MARKETS_COLUMN_ORDER` tuple 末尾加 `"category", "tags"`，保持顺序与 DDL 一致。
    3. 修改 `MARKETS_INSERT_SQL`：列名末尾加 `,category,tags`；VALUES placeholder 从 21 个 `?` 升到 23 个。
    4. 修改 `SNAPSHOT_SCHEMA`：在 `incomplete` 之后追加：
       ```python
       pa.field("category", pa.string(), nullable=True),
       pa.field("tags", pa.string(), nullable=True),
       ```
    5. 顶部 docstring 在 "Tables:" 列表加一行 `- question_translations — append-only translation cache (T2)`，并把"Adding a column requires updating ALL THREE plus the DDL." 改为 "ALL FOUR (DDL/MARKETS_COLUMN_ORDER/MARKETS_INSERT_SQL/SNAPSHOT_SCHEMA)"。
    6. 创建 `tests/m1-perception/test_schema_lockstep.py`，遵循上面 behavior 5 个测试。**先写测试看红 → 改 schemas.py 看绿**。

    **关键 invariant**（写进新测试 docstring）："The 5 sync points (DDL markets cols / DDL question_translations / MARKETS_COLUMN_ORDER / MARKETS_INSERT_SQL placeholder count / SNAPSHOT_SCHEMA non-parquet-only fields) must agree on column count and names."
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/m1-perception/test_schema_lockstep.py tests/m1-perception/test_sqlite_store.py -x -q</automated>
  </verify>
  <done>
    - `grep -c '"category"' src/polyarb/storage/schemas.py | grep -v '^#'` 至少 3（COLUMN_ORDER + INSERT_SQL + SNAPSHOT_SCHEMA + DDL = 4）
    - `grep -c 'CREATE TABLE.*question_translations' src/polyarb/storage/schemas.py` == 1
    - `PYTHONPATH=src python -c "from polyarb.storage.schemas import MARKETS_COLUMN_ORDER; assert len(MARKETS_COLUMN_ORDER) == 23, len(MARKETS_COLUMN_ORDER); assert MARKETS_COLUMN_ORDER[-2:] == ('category','tags')"` exit 0
    - `python -c "import re; sql = open('src/polyarb/storage/schemas.py').read(); m = re.search(r'VALUES \\(([?,]+)\\)', sql); assert m.group(1).count('?') == 23, m.group(1).count('?')"` exit 0
    - 新测试 5 个全绿
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: normalizer 提取 category + tags + 测试 + 依赖更新</name>
  <files>
    src/polyarb/snapshot/normalizer.py
    tests/m1-perception/test_normalizer.py
    pyproject.toml
    .gitignore
  </files>
  <read_first>
    - src/polyarb/snapshot/normalizer.py（**全部** — Phase 1 已建立的 normalize 风格）
    - tests/m1-perception/test_normalizer.py（前两个测试模板）
    - .planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-PATTERNS.md §2.12（修改 normalizer 的精确 diff）
    - .planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-RESEARCH.md `## Environment Availability`（依赖版本 pin）
    - tests/m1-perception/conftest.py:1-50（gamma_fixture 长什么样）
  </read_first>
  <behavior>
    - test_category_extracted: normalize_market({"category": "Politics", ...}) 返回 dict 含 "category": "Politics"
    - test_category_missing_returns_none: normalize_market({}) 返回 dict 含 "category": None
    - test_tags_serialized_as_json: normalize_market({"tags": ["a","b"], ...}) 返回 dict 含 "tags": '["a", "b"]'（json.dumps 字符串）
    - test_tags_missing_returns_empty_array_string: normalize_market({}) → "tags": "[]"
    - test_tags_unicode_preserved: normalize_market({"tags": ["体育"], ...}) → "tags" 含 "体育"（ensure_ascii=False）
  </behavior>
  <action>
    1. 修改 `src/polyarb/snapshot/normalizer.py` 末尾 `return {...}` dict：在 `"incomplete": False,` 之后加两行（精确 PATTERNS §2.12 给出的 diff）：
       ```python
       "category": raw.get("category"),
       "tags": json.dumps(raw.get("tags") or [], ensure_ascii=False),
       ```
       检查文件顶部 `import json` 是否已存在；若无则加。
    2. 写 `tests/m1-perception/test_normalizer.py` 加 5 个新测试（参考 behavior），用 `tests/m1-perception/conftest.py` 已有的 `gamma_fixture` 风格构造 raw dict。
    3. 修改 `pyproject.toml` `[project.dependencies]`：
       - 加 `"openai>=2.0,<3",`
       - 加 `"rich>=13,<14",`
       - **把 `duckdb>=1.0,<2` 从 `[project.optional-dependencies.dev]` 移到 `[project.dependencies]`**（production 路径需要它）
       - 注释行解释（参考 click 8.2 锁版本注释风格）：
         ```
         # openai 2.x: T2 translation client (AsyncOpenAI). Pinning <3 to avoid
         # major-version drift in retry/timeout interface.
         # rich 13.x: terminal table rendering for scan output.
         # duckdb 1.x: cross-snapshot parquet queries (T4 diff/tracker).
         ```
    4. 修改 `.gitignore` 末尾追加（为后续 plan 准备）：
       ```
       # Phase 1.1 user-local config
       watchlist.yaml
       # Phase 1.1 scan outputs (large, regenerable)
       data/scans/
       ```
       （`watchlist.yaml.example` 是模板会 commit，actual 是用户私有）
  </action>
  <verify>
    <automated>PYTHONPATH=src python -m pytest tests/m1-perception/test_normalizer.py -x -q</automated>
  </verify>
  <done>
    - `grep -c 'raw.get("category")' src/polyarb/snapshot/normalizer.py` >= 1
    - `grep -c 'json.dumps(raw.get("tags")' src/polyarb/snapshot/normalizer.py` >= 1
    - `grep -E '^  "openai' pyproject.toml` exit 0
    - `grep -E '^  "rich' pyproject.toml` exit 0
    - `grep -E '^  "duckdb' pyproject.toml` exit 0（在 main deps）
    - `grep -E 'watchlist.yaml$' .gitignore` exit 0
    - 新增 5 个 test_normalizer 全绿
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    - schemas.py 升级（markets 加 category/tags + question_translations 新表）
    - normalizer 提取 category/tags
    - pyproject.toml 加 openai + rich + duckdb 主依赖
    - .gitignore 加 watchlist.yaml + data/scans/
  </what-built>
  <how-to-verify>
    用户必须执行下列步骤（Claude 不能代跑，因为这是有意的人工 reset gate）：

    1. **同步依赖**（pip 或 uv 任选其一，与 Phase 1 一致）：
       ```bash
       pip install -e .
       # 或 uv pip install -e .
       ```
       确认 `python -c "import openai, rich, duckdb"` 不报 ImportError。

    2. **清空老 SQLite + 老 parquet**（CONTEXT.md `## Phase 1.1 收尾动作` 锁定）：
       ```bash
       sqlite3 data/state.db <<EOF
       DELETE FROM validation_issues;
       DELETE FROM markets;
       DELETE FROM snapshots;
       DELETE FROM sqlite_sequence;
       EOF
       rm -rf data/snapshots/2026
       ```
       不删 `data/state.db` 文件本身（保留 schema + index + WAL 配置）。
       **注：** 重新跑 snapshot 时 init_schema() 会幂等地补上 question_translations 表 + category/tags 列；如果 ALTER TABLE 失败（因为旧 schema 已定型），运行：
       ```bash
       rm data/state.db data/state.db-wal data/state.db-shm 2>/dev/null
       ```
       让下一次 snapshot 完全重建（这是 acceptable，因为本 phase 决定全清基线）。

    3. **重建基线 snapshot**：
       ```bash
       make snapshot-markets-v
       ```
       预期：~6-26 分钟（取决于 Gamma API 时段），最后输出 `OK | <N> markets | mode=subset | <K> issues | -> data/snapshots/...`

    4. **验证新字段落库**：
       ```bash
       sqlite3 data/state.db ".schema markets" | grep -E "category|tags"
       sqlite3 data/state.db "SELECT category, tags FROM markets WHERE category IS NOT NULL LIMIT 5;"
       sqlite3 data/state.db ".schema question_translations"
       ```
       第一行应输出 `category TEXT,` 和 `tags TEXT,`；第二行应有 5 行真实 category 数据（"Politics" / "Sports" / "Crypto" 等）；第三行应输出完整 question_translations CREATE TABLE。

    5. **验证 Phase 1 测试仍绿**：
       ```bash
       PYTHONPATH=src python -m pytest tests/m1-perception -q
       ```
       119 + 6 (新增) ≈ 125 测试全绿。

    把上述 5 步的输出粘贴回会话，Claude 据此判定 plan 完成。
  </how-to-verify>
  <resume-signal>粘贴 5 步输出，或描述失败点</resume-signal>
</task>

</tasks>

<verification>
- 5 处 schemas.py 同步点 lockstep test 绿
- normalizer test 绿（category/tags 5 个新测试）
- `make snapshot-markets-v` 成功跑完，新基线 SQLite 含 category/tags 数据
- question_translations 表存在且 schema 正确
- 现有 Phase 1 119 测试全绿（无 regression）
</verification>

<success_criteria>
- [ ] schemas.py 5 处同步通过 lockstep 测试
- [ ] question_translations 表 DDL 落地
- [ ] normalizer 落 category 单字符串 + tags JSON 字符串
- [ ] openai/rich/duckdb 进入主 dependencies
- [ ] 新基线 SQLite 至少 1000 行 markets 含非空 category
- [ ] tests/m1-perception 全绿（含新增 ~10 测试）
</success_criteria>

<output>
After completion, create `.planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-01-SUMMARY.md` 记录：
- 5 处同步点的 commit hash
- 新基线 snapshot ID（SQLite snapshots 表新行）
- category 分布前 10（`SELECT category, COUNT(*) FROM markets GROUP BY category ORDER BY 2 DESC LIMIT 10;` 输出）— 这是后续 plan 03 scan-by-category 的 ground truth
- 任何 ALTER TABLE / db reset 决策
</output>
