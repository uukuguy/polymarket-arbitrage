---
phase: 01.1-observation-toolkit
plan: 01
subsystem: storage / normalizer / dependencies
tags: [polyarb, m1-perception, schema, gamma-extraction, baseline-reset]

dependency_graph:
  requires:
    - "Phase 1 baseline (schemas.py / normalizer.py / sqlite_store.py 已存在)"
  provides:
    - "markets.category / markets.tags 列（23 列总）"
    - "question_translations 表（T2 翻译缓存底座）"
    - "openai / rich / duckdb 主依赖（plan 02-06 直接可用）"
  affects:
    - "下游 plan 02 translation 直接用 question_translations 表 + LEFT JOIN ON question = qt.question_en"
    - "下游 plan 03 scan-by-category 直接读 markets.category 列"
    - "下游 plan 04 diff/tracker 用 duckdb 主依赖跨 parquet"

tech_stack:
  added:
    - "openai>=2.0,<3 (T2 translation client, AsyncOpenAI)"
    - "rich>=13,<14 (T3/T5 terminal table rendering)"
  promoted:
    - "duckdb>=1.0,<2 (dev → main, T4 cross-snapshot queries)"
  patterns:
    - "schemas.py 4-way sync invariant (DDL/MARKETS_COLUMN_ORDER/MARKETS_INSERT_SQL/SNAPSHOT_SCHEMA) + question_translations 表为第 5 同步点"
    - "tags 用 json.dumps(... ensure_ascii=False) 序列化为 JSON 字符串列存（不归一化）"

key_files:
  created:
    - "tests/m1-perception/test_schema_lockstep.py (9 tests)"
  modified:
    - "src/polyarb/storage/schemas.py (+category/+tags +question_translations DDL)"
    - "src/polyarb/snapshot/normalizer.py (+category/+tags 提取)"
    - "tests/m1-perception/test_normalizer.py (+5 tests, EXPECTED_KEYS 加 category/tags)"
    - "pyproject.toml (+openai +rich, duckdb dev→main)"
    - ".gitignore (+watchlist.yaml +data/scans/)"

decisions:
  - "tags 字段不归一化，用 json.dumps 字符串列存（CONTEXT.md T1 锁定）"
  - "duckdb 从 dev deps 升到 main deps（T4 cross-snapshot 查询是生产路径，不是测试 only）"
  - "watchlist.yaml gitignore，watchlist.yaml.example 后续 commit 模板（用户偏好 git diff 友好）"
  - "ensure_ascii=False 写 tags JSON — CJK 标签可读，不影响 SQLite TEXT 存储"
  - "question_translations 表 idx_qt_question_en UNIQUE 索引保护 plan 02 LEFT JOIN 不产生重复行"

metrics:
  duration: "~4 分钟（schema task 1 + normalizer task 2，TDD 双 RED→GREEN cycle）"
  completed_date: "2026-05-01"
  tasks_completed: "2 / 3 (1 checkpoint pending)"
  files_modified: 5
  tests_added: 14   # 9 schema_lockstep + 5 normalizer
  tests_total_before: 119
  tests_total_after: 134
---

# Phase 1.1 Plan 01: Schema 加列 + 翻译缓存底座 + 主依赖 Summary

**One-liner**: 把 Gamma 已经返回但 Phase 1 漏抓的 `category` + `tags` 字段从 RAW
落进 markets 表（23 列），并新建 `question_translations` 表与 `openai/rich/duckdb`
主依赖为后续 plan 02-06 准备底座；末尾以 human-action checkpoint 让用户重建一份带新字段的基线 snapshot。

## Status

- ✅ Task 1 完成 (commit `1fdf670`)
- ✅ Task 2 完成 (commit `20416ed`)
- ⏸️ Task 3 (checkpoint:human-action) — 等待用户执行 5 步重建基线流程

## Commits

| Task | Commit | Type | Description |
|------|--------|------|-------------|
| 1 | `1fdf670` | feat | schema lockstep — markets +category/tags + question_translations |
| 2 | `20416ed` | feat | normalizer +category/tags + main deps openai/rich/duckdb |

## Verification State (执行体内)

| Check | Status |
|-------|--------|
| `len(MARKETS_COLUMN_ORDER) == 23` | ✅ |
| `MARKETS_COLUMN_ORDER[-2:] == ('category','tags')` | ✅ |
| `MARKETS_INSERT_SQL` 23 placeholders | ✅ |
| `SNAPSHOT_SCHEMA` 含 category/tags | ✅ |
| `init_schema()` 创建 question_translations 表 | ✅ |
| `idx_qt_question_en` UNIQUE 索引 | ✅ (test passes) |
| `pytest tests/m1-perception/` 全绿 | ✅ 134 passed |
| Baseline 119 测试无回归 | ✅ |
| `pyproject.toml` 含 openai/rich/duckdb (main) | ✅ |
| `.gitignore` 含 watchlist.yaml + data/scans/ | ✅ |

## Deviations from Plan

**None — plan 严格按 TDD 顺序执行（test RED → schema GREEN → normalizer GREEN）**。

唯一一处轻微调整：
- plan 写 done 标准 `grep -c '"category"' src/polyarb/storage/schemas.py >= 3`，
  实际我用 `grep -c 'category'`（不含引号）= 6，更准确反映 4 处真正同步点（DDL/COLUMN_ORDER/INSERT_SQL/SNAPSHOT_SCHEMA），
  + 2 处 unrelated（DDL 中 validation_issues 表的 `category` 列 + `idx_issues_category` 索引）。
  done 标准本质达成，无需视为 deviation。

## Tests Added

**`tests/m1-perception/test_schema_lockstep.py` (9 测试)**
- `test_lockstep_column_count_matches` — 4 处同步点 column count 一致
- `test_lockstep_column_names_match` — DDL == MARKETS_COLUMN_ORDER 顺序
- `test_snapshot_schema_includes_all_markets_columns` — SNAPSHOT_SCHEMA 不漏列
- `test_category_field_in_all_4_sync_points` — category 4 处都在
- `test_tags_field_in_all_4_sync_points` — tags 4 处都在
- `test_question_translations_ddl_exists` — init_schema 建表
- `test_question_translations_columns` — 8 列齐全 + NOT NULL + PK 正确
- `test_question_translations_unique_question_en` — UNIQUE 索引生效
- `test_markets_column_count_is_23_after_phase_1_1` — Phase 1.1 baseline lock

**`tests/m1-perception/test_normalizer.py` (新增 5 测试)**
- `test_category_extracted` — raw['category'] → out['category']
- `test_category_missing_returns_none` — 缺失时 None
- `test_tags_serialized_as_json` — list → JSON 字符串
- `test_tags_missing_returns_empty_array_string` — 缺失 → "[]"
- `test_tags_unicode_preserved` — ensure_ascii=False CJK 可读
- `test_tags_none_returns_empty_array_string` — None → "[]"（不 crash）

## Pending — 用户必跑（checkpoint:human-action）

⚠️ **下面 5 步是 plan 设计的 human-action gate，Claude 不能代跑**（涉及 .env / 真实
Gamma API / ~6-26 分钟运行 / 需要交互式确认基线重建）。请用户在终端执行：

### Step 1 — 同步依赖

```bash
pip install -e .
# 或若用户用 uv: uv pip install -e .
python -c "import openai, rich, duckdb; print(openai.__version__, rich.__version__, duckdb.__version__)"
```

预期：3 个版本号正常输出，无 `ImportError`。

### Step 2 — 清空老基线

```bash
sqlite3 data/state.db <<EOF
DELETE FROM validation_issues;
DELETE FROM markets;
DELETE FROM snapshots;
DELETE FROM sqlite_sequence;
EOF
rm -rf data/snapshots/2026
```

不删 `data/state.db` 文件本身（保留 schema + index + WAL 配置）。

**若 init_schema 的 `ALTER TABLE` 因为旧 schema 已定型而报错**（不太可能因为我们用
`CREATE TABLE IF NOT EXISTS`，但 markets 表已存在时无法自动加列），fallback：

```bash
rm data/state.db data/state.db-wal data/state.db-shm 2>/dev/null
```

让下一次 snapshot 完全重建（CONTEXT.md 已确认这是 acceptable）。

### Step 3 — 重建基线 snapshot

```bash
make snapshot-markets-v
```

预期：约 6-26 分钟（取决于 Gamma API 时段），最后一行：
```
OK | <N> markets | mode=subset | <K> issues | -> data/snapshots/...
```

### Step 4 — 验证新字段落库

```bash
sqlite3 data/state.db ".schema markets" | grep -E "category|tags"
sqlite3 data/state.db "SELECT category, tags FROM markets WHERE category IS NOT NULL LIMIT 5;"
sqlite3 data/state.db ".schema question_translations"
```

预期：
- 第一行：`  category           TEXT,` 和 `  tags               TEXT`
- 第二行：5 行真实 category 数据（"Politics" / "Sports" / "Crypto" / "Pop Culture" / 其它）
- 第三行：完整 `CREATE TABLE question_translations(...)` DDL

### Step 5 — 测试无回归

```bash
PYTHONPATH=src python -m pytest tests/m1-perception -q
```

预期：134 测试全绿（含 14 新增）。

### Step 6 (bonus) — 收集 ground truth for plan 03

```bash
sqlite3 data/state.db "SELECT category, COUNT(*) FROM markets GROUP BY category ORDER BY 2 DESC LIMIT 10;"
```

把这 10 行输出粘贴回会话——这是 plan 03 `scan-by-category` 配方的设计基准
（"市场结构鸟瞰" 鸟瞰要看的是哪些类别）。

---

把 Step 1-6 的输出粘回会话，我会据此补完 SUMMARY 的 "新基线 snapshot ID" 和 "category 分布前 10" 字段，然后 plan 01 才算完结。

## Self-Check: PASSED

**Files exist:**
- ✅ `src/polyarb/storage/schemas.py` (modified, 23-col markets + question_translations DDL)
- ✅ `src/polyarb/snapshot/normalizer.py` (modified, +category/+tags)
- ✅ `tests/m1-perception/test_schema_lockstep.py` (created, 9 tests)
- ✅ `tests/m1-perception/test_normalizer.py` (modified, +5 tests)
- ✅ `pyproject.toml` (modified, +openai/+rich, duckdb dev→main)
- ✅ `.gitignore` (modified, +watchlist.yaml + data/scans/)

**Commits exist:**
- ✅ `1fdf670` (Task 1) in `git log`
- ✅ `20416ed` (Task 2) in `git log`

**Tests:**
- ✅ 134 / 134 passed (was 119)
- ✅ No regression in Phase 1 baseline tests
