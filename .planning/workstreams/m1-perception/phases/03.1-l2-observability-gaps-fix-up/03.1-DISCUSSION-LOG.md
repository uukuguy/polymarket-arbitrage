# Phase 03.1: L2 Observability Gaps Fix-up - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-26 (SESSION 28)
**Phase:** 03.1-l2-observability-gaps-fix-up
**Areas discussed:** GAP-100 Fly DNS / GAP-101 failure_threshold / GAP-102 Sentry alert audit / Inj L2-4 cross-bug storm

---

## Gray Area Selection

Selected (multiSelect): GAP-100, GAP-101, GAP-102, Inj L2-4 (all 4 offered).
Skipped (mechanical, no ambiguity, decided directly by Claude in CONTEXT): GAP-2/3/4/5/103, PROCESS-1/2, Inj L2-3b, Inj L2-5.

---

## GAP-100 — Fly DNS chronic failure (Sentry 121111789)

| Option | Description | Selected |
|---|---|---|
| A+D+C (推荐) | tenacity retry 抹平秒级 jitter + GAP-101 调高 threshold + Fly ticket 并行诊断。不盲改 transport,有诊断数据再决定是否补 B | ✓ |
| 只 A (tenacity retry) | 最小象,5 行 retry。适合 "先简单看看" | |
| A+B 都上 | retry + IPv4 only transport。换"两道防线"但赌是 IPv6 问题 | |
| 只 C (先诊断) | 不改代码先拿 Fly 数据。代价: 这期间 PAUSE 还会发生,靠 healthz-watcher 兜底 | |

**User's choice:** A+D+C (推荐)
**Notes:** B 风险是没先验证 root cause 就盲改 transport。 healthz-watcher 已经能在 16min 内自动 unpause,给出诊断窗口。

---

## GAP-101 — failure_threshold 策略

| Option | Description | Selected |
|---|---|---|
| A (3→5, 推荐) | 配合 GAP-100 A retry, 5 次累计需 ≥3h 才 trip。 healthz-watcher 15min 轮询 → 几乎不会真 PAUSE。 实现 1 行 | ✓ |
| B (transient 分类) | 最优但需梳理 Issue layer/category, ≈4-6h 工量 | |
| C (时间窗 10min N-of-M) | 概念优雅但实施复杂, healthz-watcher 已兜底 | |
| A 现在+B 进 backlog | 先上 A 锁住; B 推 backlog | |

**User's choice:** A (3→5, 推荐)
**Notes:** B 推到 m1-perception backlog,触发条件 "出现新 fail 类型需要再衡量分类"。

---

## GAP-102 — Sentry alert routing audit + 修法

| Option | Description | Selected |
|---|---|---|
| A + audit + C->backlog (推荐) | audit 现状 → 修 environment=prod 代码 + 调 alert rule → PagerDuty/escalation 推 m5 polywatch backlog | ✓ |
| A+B 同时上 | 改代码 + 改 alert rule "不 filter env"。 双保险 但可能让 local test 误触 | |
| A+B+C 三者都上 | 一步到位, 但 PagerDuty 要钱+时间,不适合装进 03.1 | |
| 只 audit 不动手修 | 先查清现状写进 LEARNINGS, 修法独立 phase 决 — 但这【punt】了真问题 | |

**User's choice:** A + audit + C->backlog (推荐)
**Notes:** polyarb-l1/l2 都是 prod app, environment="dev" 是历史残留,名实不符必导致 routing 漂移。

---

## Inj L2-4 — cross-bug storm 设计

预决: 设计采 B sustained (100msg/s × 30s) + B 环境变量作用域 + A 双故障叠加。 子问题: 跑的时机。

| Option | Description | Selected |
|---|---|---|
| 推荐: 03.1 里跑, 低负载下验证逻辑 | 3 个 candidate 状态下验证 daemon storm 处理逻辑。 拿到 "storm 下 WS 合函数会不会崩" 的答案。 不能验证 throughput, 但这不是 Inj 的重点 | ✓ |
| 推 Phase 04 (candidate refresh 后) | 等真跳 100+ asset 负载下验证。 Inj 更 realistic, 但 03.1 少一项 carry-over 清账 | |
| B 只验证逆压机制, 不叠 Supabase pause | 验证点: storm 下 WsWatchdog/storm cap 工作。 不验双故障 — 但这就重复 L2-3a 了 | |

**User's choice:** 推荐: 03.1 里跑, 低负载下验证逻辑
**Notes:** 验证逻辑正确性是 Inj L2-4 的核心目的; throughput 类验证留 Phase 04 candidate refresh 真做完后补。

---

## the agent's Discretion

User 在 area selection 阶段明确 "Mechanical 修法 (GAP-2/3/4/5/103, PROCESS-1/2) 我直接 plan 不问"。
Claude 据此对以下项不开 discuss,在 CONTEXT.md 直接决:
- GAP-2 (SqliteStore getter), GAP-3 (mirror persist), GAP-4 (FLY_API_TOKEN unset), GAP-5 (re-run Inj L2-2), GAP-103 (snapshots.notes)
- PROCESS-1 (chain-truth lint), PROCESS-2 (chaos image hygiene)
- Inj L2-3b 时机 (低流量窗口), Inj L2-5 (mock fixture, 实发再跑)

Plan wave 切分、commit boundary、test 覆盖度也归 the agent's discretion (沿用 Phase 03 既有 pattern)。

## Deferred Ideas

- **GAP-101 B (transient 分类)** → m1-perception backlog
- **GAP-100 B (IPv4 only)** → 等 D-01 C Fly ticket 诊断数据回来再定
- **GAP-102 C (PagerDuty / SMS)** → m5-industrialize Polywatch phase 01 backlog (作为 trial-5 候选)
- **POLYARB_WS_TEST_KILL 进阶** → m5 polywatch trial-2 (chaos-inj-replay) 落地后可纳入 nightly cron
- **Supabase Pro / Neon 升级** → D-01 反 research 风险预案触发条件未到,继续观察
