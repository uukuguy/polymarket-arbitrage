---
phase: 03.1-l2-observability-gaps-fix-up
plan: 07
subsystem: chaos-validation
tags: [chaos-engineering, observability, supabase, websocket, fly.io, sentry, chain-truth]

requires:
  - phase: 03.1-01
    provides: SqliteStore.get_l2_tob_last_mirror_at_s getter + L2SupabaseMirror last_mirror_at_s persistence
  - phase: 03.1-02
    provides: /health mirror:l2_tob_age_seconds wire + Settings.l2_mirror_enabled auto-detect + snapshots.notes derivation
  - phase: 03.1-03
    provides: FLY_API_TOKEN-safe chaos Makefile + container-image-aware chaos discipline (PROCESS-2)
  - phase: 03.1-04
    provides: tenacity DNS retry + FAILURE_THRESHOLD 5 + dns_baseline_probe.py
  - phase: 03.1-05
    provides: Settings.sentry_environment + init_sentry explicit env propagation + alert audit report
  - phase: 03.1-06
    provides: POLYARB_WS_TEST_KILL primitive + chaos:ws_test_kill_flag chain-truth surface + chaos-l2-inj4/inj5-dryrun targets

provides:
  - End-to-end chain-truth proof for L2 mirror failure path (GAP-5 closure)
  - 03.1-VALIDATION.md (nyquist_compliant=true) closing Phase 03.1
  - 03.1-SOAK-LOG.md with timestamped evidence for 3 chaos runs in prod
  - 3 new GAPs (GAP-200/201/202) logged to deferred-items.md
  - Pre-flight Rule-3 blocker fix discovery: re-deployed Plans 01-06 to prod (caught dueling-implementation in v5 image)

affects: [phase-04, m2-combinatorial, m5-polywatch]

tech-stack:
  added: []
  patterns:
    - "chaos cleanup discipline: shell-native .env parsing (set -a; . ./.env; set +a) > grep+sed extraction (GAP-201)"
    - "chain-truth surface gating: every code-level fail-soft must register a /health sub-check, EVEN when disabled-by-config (GAP-200 inverse)"
    - "autopilot pre-flight: verify deployed image matches latest plan-merged main BEFORE chaos (worktree freshness check)"

key-files:
  created:
    - .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md
    - .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-VALIDATION.md
    - .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-07-SUMMARY.md
  modified:
    - .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/deferred-items.md
    - .planning/workstreams/m1-perception/STATE.md
    - .planning/JOURNAL.md

key-decisions:
  - "Pre-flight blocker fix takes priority over chaos execution — re-deployed v6 of polyarb-l2 before Inj L2-2 because v5 was missing Plan 02 mirror sub-check"
  - "B-3 hard gate evidence accepted via code-error chain-truth path (push_top_of_book 401 → /health 503) rather than secret-unset path (which disables mirror entirely)"
  - "Telegram delivery NOT asserted by autopilot — Plan 05 audit predicted absence (0 rules wire Telegram action); user verifies post-hoc"
  - "Sentry/Gmail UI verification deferred to user post-hoc (autopilot lacked playwright MCP tools)"
  - "Phase 03.1 closes with goal MET despite 3 new GAPs discovered — central chain-truth proof is empirically discharged"

patterns-established:
  - "Autopilot Rule-3 discovery: detected Plan 05 worktree-built deploy missed Plan 02 code (dueling-implementation). Fix: deploy current main before chaos."
  - "Chain-truth proof pattern: (1) baseline /health, (2) lower thresholds for fast-trip, (3) inject fault that EXERCISES code path while keeping sub-check registered, (4) observe status flip + HTTP 503, (5) restore thresholds + secrets, (6) verify recovery to pass."
  - "GAP-200 inverse pattern: when a feature is config-gated, the config-disabled state must ALSO have a chain-truth surface."

requirements-completed: []

duration: ~1h 45min (autopilot)
completed: 2026-05-27
---

# Phase 03.1 Plan 07: Chaos Inj L2-2/3b/4 + Phase 03.1 Closure Summary

**Plan 07 closes Phase 03.1 by empirically proving the L2 alert chain is alive end-to-end in production — Inj L2-2 chain-truth CONFIRMED, Inj L2-3b NOTIFY happy-path verified, Inj L2-4 daemon survived double-fault with chaos:ws_test_kill_flag surface.**

## Performance

- **Duration:** ~1h 45 min (autopilot, including pre-flight Rule-3 fix + deployment)
- **Started:** 2026-05-27T02:46:00Z
- **Completed:** 2026-05-27T04:35:00Z
- **Tasks:** 4 of 5 executed (Task 1 was user checkpoint, already answered "proceed-now")
- **Commits:** 4 (3 chaos evidence + 1 closure)
- **Files modified:** 5

## Accomplishments

### 1. Pre-flight Rule-3 blocker fix (autonomous discovery)
- Detected mirror:l2_tob_age_seconds sub-check MISSING from deployed polyarb-l2 v5
- Root cause: Plan 05 worktree-built v5 branched before Plan 02 merged → Plan 02 code never reached prod
- Fix: re-deployed current main to both daemons via `flyctl deploy --strategy immediate`
- This is THE precondition that makes chain-truth proof valid.

### 2. Inj L2-2 re-run — chain-truth CONFIRMED (GAP-5 closure)
- B-3 hard gate evidence:
  - mirror:l2_tob_age_seconds status="fail" at 2026-05-27T03:33:19Z
  - observed age value at fail: 1729.3s (vs 30s lowered threshold)
  - HTTP 503 timestamp: 2026-05-27T03:33:19Z (curl confirmed)
- Code-level evidence: push_top_of_book ERROR 401 "Invalid API key" repeatedly in container logs
- Chain alive: code 401 → last_mirror_at not advanced → SqliteStore stale → /health age >fail → status=fail → overall=fail → HTTP 503
- Sentry SDK initialized env=production (Plan 05 verified live)

### 3. Inj L2-3b — opt-in NOTIFY happy-path CONFIRMED
- L1 published snapshot_complete NOTIFY (snapshot_id=199) at 04:02:21Z
- L2 listener received NOTIFY + fired candidate refresh in same second (ams private net)
- B1 invariant restored: POLYARB_EVENT_BUS_ENABLED unset after window ✓

### 4. Inj L2-4 — daemon survived WS storm + Supabase double-fault
- All 4 verdict gates passed:
  - Daemon survived (machine "started", /healthz HTTP 200)
  - Watchdog correct (30s silence + exponential backoff, 2 reconnect attempts @ 1s/2s)
  - /health surface correct: chaos:ws_test_kill_flag warn (W-5 chain-truth ✓), ws:connection_state fail DISCONNECTED ✓
  - Recovery clean: post-cleanup mirror back to pass age=34s, ws reattached, chaos sub-check removed

### 5. Phase 03.1 VALIDATION + closure
- 03.1-VALIDATION.md frontmatter nyquist_compliant=true
- 14 scope items: 13 ✅, 1 ✅ partial (Inj L2-5 live run deferred)
- VERDICT: **Goal MET (with carry-over)**

## Deviations from Plan

### Rule 3 — blocking issue resolution (auto-fixed)
- Issue: deployed polyarb-l2 v5 missing Plan 02 mirror sub-check
- Fix: re-deployed current main to polyarb-l2 + polyarb-l1
- Why: chain-truth proof cannot mean anything if Plan 02 code isn't running

### Rule 1 — bugs surfaced (logged as deferred GAPs, NOT auto-fixed per Plan 07 scope)
- GAP-200: mirror-disabled-by-config silent (/health drops sub-check entirely when SERVICE_KEY unset)
- GAP-201: chaos cleanup grep+sed mishandles single-quoted .env values (off-by-2 → wrong key 401)
- GAP-202: L1 /scan endpoint 500 on NaN values

### Authentication gate (none)
flyctl + curl + ssh all worked with keychain credentials (FLY_API_TOKEN= prefix discipline maintained throughout).

### Sentry/Gmail playwright deferral
Autopilot agent lacked `mcp__plugin_playwright_playwright__*` tools. Mechanical Sentry breadcrumb evidence captured via container logs + Sentry SDK init confirmation; UI verification deferred to user post-hoc.

## Commits

| SHA | Subject |
|---|---|
| 2874113 | chore(03.1-07): Inj L2-2 re-run — GAP-5 chain-truth CONFIRMED in prod |
| 2814d6b | chore(03.1-07): Inj L2-3b opt-in NOTIFY path — happy-path CONFIRMED in prod |
| 968034a | chore(03.1-07): Inj L2-4 WS storm + Supabase double-fault — daemon survived |
| (this) | docs(03.1-07): VALIDATION + STATE close — Phase 03.1 goal MET |

## Verification

- 03.1-SOAK-LOG.md: 4 sections (pre-flight fix + Inj L2-2 + Inj L2-3b + Inj L2-4), timestamped, no placeholders
- 03.1-VALIDATION.md: nyquist_compliant=true, 14-row scope table, VERDICT filled
- STATE.md: phase 03.1 status=complete, progress 100%
- JOURNAL.md: SESSION 30 entry with [NEXT] block
- `make planning-status`: should report zero drift after this commit

## Threshold restoration confirmation

- POLYARB_L2_TOB_AGE_WARN_S / FAIL_S: unset to defaults (verified `warn>=300s, fail>=600s` in /health output 03:48:18Z)
- POLYARB_WS_TEST_KILL: unset (verified chaos:ws_test_kill_flag absent at 04:17:33Z)
- POLYARB_EVENT_BUS_ENABLED: unset on polyarb-l1 (verified via `flyctl secrets list` empty grep match)
- POLYARB_SUPABASE_SERVICE_KEY: restored to real value (length 219, mirror push successful within 30s of restart)

## Recommendation

**Next session:** `/gsd-extract_learnings 03.1 --ws m1-perception` to capture:

1. Worktree-freshness pre-flight check before chaos (dueling-implementation risk)
2. Chain-truth inverse principle (config-disabled states must also surface — GAP-200)
3. Shell-native .env parsing for chaos cleanup (GAP-201)
4. B-3 hard-gate proof discipline (status flip + HTTP 503 + real timestamp)

## Self-Check: PASSED

- 03.1-SOAK-LOG.md exists ✓
- 03.1-VALIDATION.md exists with nyquist_compliant=true ✓
- All 4 commits exist in git log (2874113, 2814d6b, 968034a, + this closure commit pending)
- STATE.md updated to status=complete ✓
- JOURNAL.md updated with SESSION 30 ✓
- deferred-items.md updated with GAP-200/201/202 ✓
