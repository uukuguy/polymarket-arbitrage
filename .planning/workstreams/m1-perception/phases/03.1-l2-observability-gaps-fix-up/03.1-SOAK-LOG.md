
## Inj L2-2 Re-run (GAP-5 closure) — 2026-05-27T03:05:32Z – 03:52:29Z

### Baseline (2026-05-27T03:03:19Z, BEFORE threshold lowering)
- /healthz: HTTP 200
- /health overall: warn (ws:connection_state WAITING_FOR_EVENT — low-traffic 3-asset bootstrap)
- mirror:l2_tob_age_seconds: status=pass, observedValue=254s (warn>=300s, fail>=600s defaults)
- l2_top_of_book table: rows present (mirror was healthy pre-injection)

### Threshold override (B-3 fix — Plan 02 fast-trip path)
- Pre-step T+0 = 2026-05-27T03:04:00Z:
  `FLY_API_TOKEN= flyctl secrets set POLYARB_L2_TOB_AGE_WARN_S=15 POLYARB_L2_TOB_AGE_FAIL_S=30 -a polyarb-l2`
- Confirmed /health threshold reflected ("warn>=15s, fail>=30s" in output): **yes** (verified at 03:04:53Z)

### Fault injection (T0 = 2026-05-27T03:05:32Z)
- Command: `FLY_API_TOKEN= flyctl secrets unset POLYARB_SUPABASE_SERVICE_KEY -a polyarb-l2` (manual; chaos-l2-inj2 target's set-with-restore path was decomposed for finer control)
- Action: revoked POLYARB_SUPABASE_SERVICE_KEY → daemon restart at 03:06:34Z

### Observation window (60s + extended diagnostic)
- t+0–60s: /health overall=warn, HTTP 200, BUT `mirror:l2_tob_age_seconds` sub-check was **MISSING from .checks** (not just stale).
- **Diagnostic outcome (Rule 1 — bug surfaced)**: when SUPABASE_SERVICE_KEY is unset, daemon boots with `l2_mirror_enabled=False`, so the `/health mirror:l2_tob_age_seconds` sub-check is never registered → status cannot flip to fail via this path. This is a real chain-truth gap — but a different one than Phase 03's Inj L2-2 root cause (Phase 03 was about silent fail-soft mid-flight). See "Deferred follow-up" below.

### Recovery to chain-truth ACTIVE state (T+~28 min)
- Discovery: my first key-restore attempt (sed-based extraction) stripped `'` quoting incorrectly → 221-char value vs 219-char real key → daemon booted with mirror enabled but pushes 401 Unauthorized
- Fix: `set -a; . ./.env; set +a; FLY_API_TOKEN= flyctl secrets set POLYARB_SUPABASE_SERVICE_KEY="$POLYARB_SUPABASE_SERVICE_KEY" -a polyarb-l2` (length 219, correct)
- After daemon restart at 03:47:52Z with WRONG key (length 221, double-quoted residue): `l2-mirror enabled (Supabase REST URL + service_key present)` but every push: `ERROR push_top_of_book failed: 401 Unauthorized "Invalid API key"`
- /health mirror sub-check status=fail, age=2628.7s, **HTTP 503 returned** at 03:48:18Z

### CHAIN-TRUTH PROOF (the meat of GAP-5 closure)
- **mirror:l2_tob_age_seconds status reached "fail": YES** at 2026-05-27T03:33:19Z (first poll showing fail after thresholds reflected)
- **observed age value at fail: 1729.3s** (vs 30s fail threshold = >57x over)
- **HTTP 503 timestamp: 2026-05-27T03:33:19Z** (curl -o /dev/null -w '%{http_code}' confirmed)
- **Code-level error visible in /app logs**: `polyarb.storage.l2_supabase_mirror:push_top_of_book:193 - l2-mirror push_top_of_book failed rows=1: ... code 401 "Invalid API key"`
- **Chain alive**: code 401 error → `last_mirror_at` NOT advanced → SqliteStore.get_l2_tob_last_mirror_at_s returns stale → /health age check >fail threshold → status=fail → overall=fail → HTTP 503

### Sentry breadcrumb evidence
- Source: container error logs include `l2-mirror push_top_of_book failed` ERROR-level events emitted via loguru
- Sentry SDK is wired (init log: `sentry initialized — release=dev env=production [explicit POLYARB_SENTRY_ENV]` at 03:12:49Z)
- Sentry routes ERROR-level loguru events as breadcrumbs/issues automatically per Plan 05's `LoguruIntegration`
- **Verification approach**: Playwright-cli navigation to Sentry deferred to post-task review block below (full UI inspection)

### Telegram delivery
- **EXPECTED ABSENT per Plan 05 audit (sentry-audit-report.md REFINED verdict)**: 0/2 alert rules wire Telegram action. Audit confirmed dominant root cause for "alerts arrived but Telegram silent" = no Telegram action wired, NOT env=dev tag filtering.
- **Verification by user**: post-hoc on Telegram client (user-authorized; this autopilot run does not assert Telegram delivery).

### Threshold cleanup (post-recovery)
- Command: `FLY_API_TOKEN= flyctl secrets unset POLYARB_L2_TOB_AGE_WARN_S POLYARB_L2_TOB_AGE_FAIL_S -a polyarb-l2`
- Timestamp: 2026-05-27T03:45:42Z (approximately)
- Verified: /health output at 03:48:18Z shows `(warn>=300s, fail>=600s)` — defaults restored: **YES**

### Mirror recovery to pass
- Real service_key restored at 2026-05-27T03:50:08Z (length 219 verified)
- Daemon restart + first successful push at ~03:52:30Z
- Final /health: status=warn (ws WAITING_FOR_EVENT — low-traffic), `mirror:l2_tob_age_seconds status=pass observedValue=23s output="last mirror push 23s ago (warn>=300s, fail>=600s)"`
- l2_top_of_book table: new rows landing (verified via subsequent age decay 23s → small)

### Verdict (GAP-5)
- **chain-truth: CONFIRMED**
- Plan 02 fix proven effective: **YES** — the wiring `L2SupabaseMirror.push → SqliteStore.l2_mirror_state → /health mirror:l2_tob_age_seconds → overall=fail → HTTP 503` is alive end-to-end in production
- The 401-Unauthorized regime (wrong key, mirror enabled) is the cleaner test case: mirror code is RUNNING but failing → chain surfaces. The unset-key regime (mirror disabled entirely) reveals a **secondary gap**: if mirror is disabled at boot, the failure is invisible. → see deferred-items.md for GAP-200 follow-up.

### Deferred follow-up (new GAP-200 — Phase 04 candidate)
- **Observation**: when `POLYARB_SUPABASE_SERVICE_KEY` is unset, daemon disables mirror entirely; /health drops the sub-check from `.checks` (no failure surface).
- **Risk**: production operator unsetting key by accident → silent loss of mirror writes (only visible in container startup log)
- **Possible fix (NOT in scope of Plan 07)**: when `supabase_url` is set but `service_key` is empty, /health should surface `mirror:l2_tob_age_seconds` with status=fail+output="mirror disabled by config" — turn "disabled" into a chain-truth signal too.
- **Logged to deferred-items.md as GAP-200**

### Sentry / Gmail playwright verification (DEFERRED to user post-hoc)

**Tooling constraint**: This Plan 07 executor agent did NOT have `mcp__plugin_playwright_playwright__*`
tools available in its environment (MCP tool restriction on agent spawn). Playwright-cli CLI fallback
is feasible for headless screenshots but not robust for authenticated Sentry/Gmail dashboard navigation
within autopilot scope.

**What IS verified mechanically:**
- Sentry SDK initialized in v6 deploy: container log `sentry initialized — release=dev env=production [explicit POLYARB_SENTRY_ENV]`
- Sentry environment correctly tagged `production` (Plan 05 GAP-102 fix live in this deploy)
- ERROR-level loguru events were emitted during the chaos window (`push_top_of_book failed: 401 Invalid API key` x many)
- Loguru → Sentry SDK integration captures ERROR-level events as Sentry breadcrumbs/issues
  (per Plan 05 `LoguruIntegration` wiring confirmed in src/polyarb/observability/sentry.py)

**Pending user post-hoc verification (action items for next session):**
1. **Sentry dashboard**: navigate to speechlessai.sentry.io → python project → events filter
   `environment=production AND release=dev AND tag:category=l2-mirror`, last 1h. Confirm
   event(s) corresponding to `push_top_of_book failed code=401` in window 03:32:55Z–03:50:00Z.
2. **Gmail inbox**: search `from:noreply@sentry.io newer_than:1h` — confirm alert email delivered
   to uukuguy@gmail.com (the Sentry-account email). Plan 05 audit predicted Sentry-account email
   WOULD arrive (in-app + account-email routing always works regardless of rule wiring).
3. **Telegram**: per Plan 05 audit, NOT expected to receive (0 rules wire Telegram action).
   This run does NOT assert Telegram delivery; confirm expected-absence on the Telegram client.

These verifications are tracked as "GAP-5 follow-up — operator validation" and are NOT
prerequisites for closing GAP-5: the mechanical chain-truth proof (code error → /health 503)
is the phase goal. Sentry/Telegram delivery is a separate alert-routing concern that Plan 05
addressed independently.

## Inj L2-3b: opt-in NOTIFY happy-path — 2026-05-27T03:59:47Z – 04:09:22Z

Window: 2026-05-27 ~04:00 UTC (Asia early morning, low-traffic per CONTEXT.md guidance)
Baseline:
- L2 listener state at T-1: status=pass, observedValue="listening"
- L1 secrets: POLYARB_EVENT_BUS_ENABLED NOT present (B1 invariant default OFF) ✓
- L2 candidate set: 3 bootstrap asset_ids (POLYARB_BOOTSTRAP_ASSET_IDS)

### Trigger
- Command: `FLY_API_TOKEN= flyctl secrets set POLYARB_EVENT_BUS_ENABLED=1 -a polyarb-l1` at 2026-05-27T03:59:47Z
- L1 daemon restarted, scheduler resumed, natural snapshot tick fired at 04:00:29Z
- (Manual /scan POST also attempted but returned 500 due to NaN-in-response bug unrelated to this chaos — natural tick was sufficient to fire NOTIFY)

### Observations
- L1 published NOTIFY: **YES** — at 2026-05-27T04:02:21Z log:
  `polyarb.events.bus:publish_snapshot_complete:69 - event_bus published snapshot_complete snapshot_id=199 taken_at_ms=1779854429213`
- L2 received NOTIFY + fired candidate refresh: **YES** — at 2026-05-27T04:02:21Z (same second, ams region private network):
  `polyarb.observation.l2_candidate_refresh:on_snapshot_complete:262 - candidate refresh: +0 -3 total=0 (cap=500) snapshot_id=199`
- candidate refresh effect: removed 3 bootstrap candidates (no current `near-end` matches in low-traffic window)
- l2_event_cursor advance: implied by listener pickup of snapshot_id=199 (cursor logic in l2_candidate_refresh.on_snapshot_complete)

### B1 invariant restoration (POLYARB_EVENT_BUS_ENABLED back to OFF)
- Command: `FLY_API_TOKEN= flyctl secrets unset POLYARB_EVENT_BUS_ENABLED -a polyarb-l1` at 2026-05-27T04:08:00Z
- Verification: `flyctl secrets list -a polyarb-l1 | grep event_bus` → returns empty (no match)
- L2 listener still in 'listening' state after L1 restart: **YES** (verified at 04:09:22Z)

### Verdict (Inj L2-3b)
- opt-in path WORKS in prod: **YES** — full NOTIFY → LISTEN → candidate refresh chain proven
- B1 invariant restored: **YES** — POLYARB_EVENT_BUS_ENABLED unset, listener idle (no NOTIFY traffic going forward)

### Side discovery (unrelated to Inj L2-3b but logged here)
- L1 R2 archive failing during this window: `R2UploadError: Invalid bucket name "'polyarb-snapshots' "` — extra quotes in env var (same quoting trap as GAP-201). L1 snapshots still complete (mirror to Supabase OK) but R2 archive disabled. Logged to deferred-items.md as part of GAP-201 audit scope.
- L1 /scan endpoint returns 500 on `recipe_name=near-end` due to `ValueError: Out of range float values are not JSON compliant: nan` in scan response. This is a code bug separate from any chaos. Logged to deferred-items.md as GAP-202.

## Inj L2-4: WS storm + Supabase pause double-fault — 2026-05-27T04:13:08Z – 04:17:33Z

### Phase 1: fault injection (T+0 = 04:13:08Z)
- Action: `FLY_API_TOKEN= flyctl secrets set POLYARB_WS_TEST_KILL=1 -a polyarb-l2` + `... unset POLYARB_SUPABASE_SERVICE_KEY -a polyarb-l2`
- POLYARB_WS_TEST_KILL=1 set: **YES**
- POLYARB_SUPABASE_SERVICE_KEY unset: **YES**
- Daemon restarted at 04:13:56Z, mirror disabled at boot (GAP-200 silent path)

### Phase 2: observation (T+60s)
- Daemon survival: **YES** — /healthz HTTP 200, machine state "started" throughout
- WsTestKillRequested in logs: **YES** — at 04:13:57Z:
  `ws_consumer: POLYARB_WS_TEST_KILL=1 — synthetic WS close (Phase 03.1-06 D-04)` (WARNING level)
- Boot-time warning: **YES** — at 04:13:56Z:
  `⚠ POLYARB_WS_TEST_KILL=1 detected — CHAOS MODE; WS will drop on next message`
- Watchdog reconnect attempts: **2 attempts in first 73s**:
  - attempt=1 at 04:14:26Z (30s after WS close, backoff=1s)
  - attempt=2 at 04:14:57Z (30s after attempt 1, backoff=2s)
- Storm cap engaged: **NO** (only 2 attempts in 60s; D-03 storm cap is 10/hr — well below)
- /health overall after 60s: **status=fail**, HTTP 503
- /health surface (chain-truth alive):
  - `ws:connection_state` status=fail observedValue=DISCONNECTED
  - `chaos:ws_test_kill_flag` status=warn observedValue="1" output="POLYARB_WS_TEST_KILL=1 — CHAOS MODE active; should never appear in production"
  - `mirror:l2_tob_age_seconds`: **NULL** (sub-check absent — GAP-200 silent-disable path)
- mirror:l2_tob_age_seconds value: N/A (sub-check not registered when service_key unset)

### Phase 3: cleanup recovery (T+~5 min)
- Secrets restored: **YES** at 04:15:42Z (unset WS_TEST_KILL + set real SERVICE_KEY from .env)
- Daemon restarted at ~04:16Z
- /health overall after 90s recovery (at 04:17:33Z): **status=warn** (ws WAITING_FOR_EVENT — normal low-traffic state)
- l2_top_of_book new rows: **YES** — mirror age 34s at recovery check (push fired within 30s of restart)
- WS subscriptions reattached: **YES** — ws_consumer log "starting consume loop with 3 subscribed assets" post-restart
- chaos sub-check removed from /health: **YES** (`has_chaos: false`)

### Verdict (Inj L2-4)
- Daemon survived double-fault: **YES** (machine state stayed "started" throughout, /healthz never failed)
- Watchdog logic correct under storm: **YES** (30s silence detection + exponential backoff 1s, 2s, ...)
- Mirror failure correctly surfaced to /health: **PARTIAL** — chaos:ws_test_kill_flag surfaced (W-5 chain-truth ✓); ws:connection_state surfaced (fail-fast on storm ✓); mirror:l2_tob_age_seconds went absent (GAP-200 silent-disable path)
- Recovery clean: **YES** (mirror back to pass, ws reattached, chaos sub-check removed)

### Carry-over / observations
- **GAP-200 reproduced again**: same as Task 2, mirror code-disabled state → sub-check absent → no chain-truth signal on mirror lane. Workaround OK for now (chaos:ws_test_kill_flag and ws:connection_state cover the chaos-active state); proper fix in Phase 04+.
- POLYARB_WS_TEST_KILL flag cleanup verified: chaos:ws_test_kill_flag sub-check removed from /health post-cleanup. No persistence past chaos window. ✓
- Test environment: 3-asset bootstrap candidate set is small enough that "WS storm" is really "WS close + reconnect" (no genuine storm rate). Phase 04+ revisits storm test under realistic candidate scale.
