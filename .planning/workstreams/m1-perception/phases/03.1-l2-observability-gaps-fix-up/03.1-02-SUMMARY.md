---
phase: 03.1-l2-observability-gaps-fix-up
plan: 02
subsystem: observability
tags: [health-check, supabase-mirror, settings, snapshot-notes, chain-truth]

requires:
  - phase: 03.1-01
    provides: SQLiteStore.get_l2_tob_last_mirror_at_s getter + L2SupabaseMirror._refresh_freshness_cache write-side
provides:
  - Settings.l2_mirror_enabled auto-detect (when supabase secrets present)
  - Settings.l2_tob_age_warn_s (default 300s) + Settings.l2_tob_age_fail_s (default 600s) — env-overridable chaos knob
  - l2_main.py threads SQLiteStore into L2SupabaseMirror constructor (Plan 01 hook consumed in prod path)
  - /health mirror:l2_tob_age_seconds sub-check is now LIVE (was dead code gated on missing settings field)
  - snapshots.notes column populated from API_UNREACHABLE issues (GAP-103)
affects: [03.1-07 chaos Inj L2-2 re-run, dashboard-mirror-freshness, operator-alert-chain]

tech-stack:
  added: []
  patterns: ["dead-code → live-code activation via Settings auto-detect"]

key-files:
  created:
    - tests/m1-perception/test_l2_health_mirror_check.py
    - tests/m1-perception/test_orchestrator_notes_write.py
  modified:
    - src/polyarb/config.py
    - src/polyarb/daemon/l2_main.py
    - src/polyarb/http/l2_health.py
    - src/polyarb/snapshot/orchestrator.py

key-decisions:
  - "Plan 01 contract is authoritative — no new tables, no new setters; we only consume"
  - "Settings auto-detect mirrors supabase_mirror_enabled pattern (same secrets gate both L1 and L2)"
  - "_MIRROR_PASS_S / _MIRROR_WARN_S replaced by settings.l2_tob_age_warn_s/fail_s so Plan 07 chaos can flip status in 60s via env override"
  - "snapshots.notes derives ONLY from API_UNREACHABLE issues (operators want fail reasons, not L2 validation noise)"

patterns-established:
  - "Pattern: Settings field + model_validator auto-set when paired secrets present (l2_mirror_enabled follows supabase_mirror_enabled)"
  - "Pattern: env-overridable thresholds for chaos knobs (warn/fail seconds become Settings fields)"

requirements-completed: []

duration: TBD
completed: 2026-05-26
---

# Phase 03.1 Plan 02: /health l2_tob_age live wiring + snapshots.notes Summary

**(skeleton — will be replaced at end of execution)**

Activates the dead `l2_mirror_enabled`-gated mirror sub-check in /health by adding the Settings field auto-detect that was missing; wires SqliteStore through l2_main.py into L2SupabaseMirror so the freshness write side runs in prod; replaces hardcoded mirror age thresholds with Settings fields for Plan 07 chaos knob; and derives snapshots.notes from L1 API_UNREACHABLE issues so operators can group fail reasons by SQL.

## Performance

- **Duration:** TBD
- **Started:** TBD
- **Completed:** TBD
- **Tasks:** 3
- **Files modified:** 6

## Task Commits

To be filled.

## Files Created/Modified

To be filled.

## Decisions Made

To be filled.

## Deviations from Plan

To be filled.

## Issues Encountered

None yet.

## Next Phase Readiness

To be filled.

---
*Phase: 03.1-l2-observability-gaps-fix-up*
*Completed: 2026-05-26 (pending final commit)*
