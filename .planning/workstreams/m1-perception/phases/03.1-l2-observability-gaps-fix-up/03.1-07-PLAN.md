---
phase: 03.1
plan: 07
type: execute
wave: 5
depends_on: ["03.1-02", "03.1-03", "03.1-04", "03.1-05", "03.1-06"]
files_modified:
  - .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md
  - .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-VALIDATION.md
autonomous: false
requirements: []
makefile_targets: []
addresses_gaps: [GAP-5 (Inj L2-2 re-run), Inj L2-3b, Inj L2-4, GAP-1 chain-truth final verify]
user_setup: []

must_haves:
  truths:
    - "Inj L2-2 re-run with Plan 02 wiring active: revoke POLYARB_SUPABASE_SERVICE_KEY on polyarb-l2 → /health returns HTTP 503 within 10 min (mirror sub-check trips) → Sentry breadcrumb visible via API query → Telegram alert delivered"
    - "Inj L2-3b: low-traffic-window opt-in NOTIFY happy-path: enable POLYARB_EVENT_BUS_ENABLED=1 on polyarb-l1 → trigger snapshot → L2 listener receives NOTIFY → candidate refresh fires → restored to OFF"
    - "Inj L2-4: WS storm + Supabase pause double-fault: daemon survives, watchdog reconnects, /health correctly degraded then recovered"
    - "03.1-SOAK-LOG.md documents all 3 chaos runs with timestamped evidence (curl outputs, psql query results, Sentry issue IDs, Telegram screenshots paths)"
    - "Phase 03 L1 lesson 'code-truth vs chain-truth' is empirically DISCHARGED on the patched paths — Inj L2-2 re-run proves the chain alert is alive end-to-end (HTTP 503 → Sentry breadcrumb → Telegram delivery)"
  artifacts:
    - path: .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md
      provides: chronological evidence for all 3 chaos runs + chain-truth verification
    - path: .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-VALIDATION.md
      provides: Phase 03.1 hard-gate verification ledger (frontmatter nyquist_compliant=true)
  key_links: []

---

<objective>
**Final wave — execute all 3 chaos injections + close the phase:**

- **GAP-5 (Inj L2-2 re-run)**: PROOF the chain-truth fix actually works in prod. Revoke L2 Supabase service key → Plan 02's freshness chain MUST surface to /health 503. Sentry breadcrumb API query must show the failure event. Telegram must deliver. **This is the closure of Phase 03's biggest lesson.**
- **Inj L2-3b**: opt-in L1 NOTIFY happy-path during low-traffic window (Asia early morning UTC). Verify L2 listener receives NOTIFY + candidate refresh fires. Restore to default-OFF immediately.
- **Inj L2-4**: cross-bug WS storm + Supabase pause double-fault. Verify daemon survives, watchdog reconnects, mirror failure surfaces correctly.
- **Phase closure**: SOAK-LOG.md + VALIDATION.md (frontmatter nyquist_compliant=true).

**Why human checkpoint**: chaos runs touch production. Each Inj is bounded but has blast radius:
- Inj L2-2 re-run: 60-90s mirror outage (data accumulates in WS frames buffer)
- Inj L2-3b: enables NOTIFY in prod — must be unset within 5 min to preserve B1 invariant
- Inj L2-4: double-fault for 60s in prod — daemon must survive

Plan 07 SUMMARY closes Phase 03.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-02-SUMMARY.md
@.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-03-SUMMARY.md
@.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-04-SUMMARY.md
@.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-05-SUMMARY.md
@.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-06-SUMMARY.md
@.planning/workstreams/m1-perception/phases/03-l2-orderbook-tracking-daemon/03-SOAK-LOG.md
@.planning/workstreams/m1-perception/phases/03-l2-orderbook-tracking-daemon/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm prerequisites + low-traffic window scheduling</name>
  <decision>Pre-flight check + window confirmation before running prod chaos</decision>
  <context>
    Plans 01-06 should have shipped:
    - SqliteStore l2 freshness cache (Plan 01)
    - /health l2_tob_age_seconds wired live (Plan 02)
    - snapshots.notes populated (Plan 02)
    - FLY_API_TOKEN-safe chaos targets (Plan 03)
    - DNS retry + threshold 5 (Plan 04)
    - sentry_environment=production deployed (Plan 05 + user applied)
    - POLYARB_WS_TEST_KILL primitive + chaos-l2-inj4 Makefile (Plan 06)

    User must confirm:
    1. All above plans have SUMMARY.md and were deployed to prod (`flyctl deploy -c fly-l2.toml -a polyarb-l2`)
    2. Low-traffic window selected for Inj L2-3b (Asia early morning UTC = ~18:00-22:00 UTC weekday)
    3. User is at keyboard for the next ~30 min (chaos runs are short but require attention)
  </context>
  <options>
    <option id="proceed-now">
      <name>Proceed immediately</name>
      <pros>Phase closes fast</pros>
      <cons>None if all prereqs met</cons>
    </option>
    <option id="defer-inj3b">
      <name>Defer Inj L2-3b only (run L2-2 + L2-4 now)</name>
      <pros>If not in low-traffic window, L2-3b can wait without holding phase closure</pros>
      <cons>Adds a follow-up SUMMARY entry; phase technically open until L2-3b done</cons>
    </option>
    <option id="defer-all">
      <name>Defer all chaos runs to next session</name>
      <pros>If something feels off, no harm</pros>
      <cons>Plans 01-06 land but phase hard-gate not crossed</cons>
    </option>
  </options>
  <resume-signal>
    Reply with one of: "proceed-now" / "defer-inj3b" / "defer-all"
    For defer-inj3b: also specify the substitute window
    For defer-all: also specify the next session start signal
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Inj L2-2 re-run with Sentry breadcrumb verification (GAP-5)</name>
  <files>.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md</files>
  <read_first>
    - 03-SOAK-LOG.md Inj L2-2 section (original run + 5-layer root cause) — this re-run is its closure
    - Makefile chaos-l2-inj2 target (per Plan 03, now FLY_API_TOKEN-safe)
    - Plan 02 SUMMARY (confirms /health sub-check is live)
  </read_first>
  <action>
    1. Initialize 03.1-SOAK-LOG.md with header:
       ```markdown
       # Phase 03.1 SOAK Log — Chaos Verification Evidence

       Generated: <UTC timestamp>
       Phase: 03.1 L2 Observability Gaps Fix-up
       Plan: 07
       ```
    2. Run `make chaos-l2-baseline` first — capture baseline /health + l2_top_of_book row count. Append to SOAK-LOG.
    3. Run `make chaos-l2-inj2` — this revokes SUPABASE_SERVICE_KEY on polyarb-l2.
    4. **B-3 fix: option (c) "sub-check exists" is FORBIDDEN — it only proves Plan 02 added a field, NOT that the chain status flip → Sentry alert rule → Telegram completes. Phase goal demands end-to-end chain-truth, not field-presence.**

       **Pre-step (BEFORE running chaos-l2-inj2): lower the /health age thresholds via env so the chain trips within ≤60s instead of 600s.** Plan 02 made these Settings fields specifically for this purpose:
       ```bash
       FLY_API_TOKEN= flyctl secrets set POLYARB_L2_TOB_AGE_WARN_S=15 POLYARB_L2_TOB_AGE_FAIL_S=30 -a polyarb-l2
       # machine auto-restarts on secret set; wait ~30s for daemon to come back healthy
       @sleep 35
       @curl -sS https://polyarb-l2.fly.dev/health | jq '.checks["mirror:l2_tob_age_seconds"][0].output'  # confirm thresholds reflected
       ```

       Then run `make chaos-l2-inj2` (revokes POLYARB_SUPABASE_SERVICE_KEY).

       Within the 60s waiting window, observe:
       - `curl -sS https://polyarb-l2.fly.dev/health | jq '.status, .checks["mirror:l2_tob_age_seconds"][0]'`
       - This is the moment of truth: status MUST transition pass → warn (age > 15s) → fail (age > 30s) → HTTP 503 → Sentry breadcrumb → Telegram delivery.
       - **Approach (default, B-3 fix)**: lowered thresholds above. Expected transition: warn ~T+20s, fail ~T+35s, HTTP 503 ~T+35s.
       - **Approach (fallback, only if lowered-threshold has wrinkles e.g. POLYARB_L2_TOB_AGE_FAIL_S not picked up by hot-reload)**: wait the full 600s for default threshold to trip naturally. Document the wrinkle in SOAK-LOG if you fall back.

       **Post-cleanup (MANDATORY — runs after chaos cleanup):**
       ```bash
       FLY_API_TOKEN= flyctl secrets unset POLYARB_L2_TOB_AGE_WARN_S POLYARB_L2_TOB_AGE_FAIL_S -a polyarb-l2
       # confirms thresholds restored to default 300/600 (visible in next /health output)
       ```
       Include the unset in the cleanup section of SOAK-LOG. Failure to unset = production runs with chaos thresholds = future false-alerts.
    5. Query Sentry API for the latest breadcrumb on L2 daemon (use the existing project key + REST API):
       ```bash
       curl -sS "https://sentry.io/api/0/projects/speechlessai/<project-slug>/events/?statsPeriod=10m" \
         -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" | jq '.[] | select(.tags[]?.key=="category" and .tags[]?.value=="l2-mirror")'
       ```
       (If no SENTRY_AUTH_TOKEN — request from user or fall back to playwright-cli Sentry dashboard query)
    6. Check Telegram bot for delivery — open Telegram via playwright-cli or screenshot path.
    7. Run `make chaos-l2-cleanup` to restore SUPABASE_SERVICE_KEY (chaos-l2-inj2 should auto-cleanup but verify).
    8. Append findings to SOAK-LOG.md:
       ```markdown
       ## Inj L2-2 Re-run (GAP-5 closure) — <UTC timestamp>

       ### Baseline
       - /healthz: 200
       - /health overall: pass
       - l2_top_of_book rows in last 120s: <N>

       ### Fault injection
       - Command: `make chaos-l2-inj2`
       - Action: `FLY_API_TOKEN= flyctl secrets unset POLYARB_SUPABASE_SERVICE_KEY -a polyarb-l2`

       ### Threshold override (B-3 fix — option b)
       - Pre-step `flyctl secrets set POLYARB_L2_TOB_AGE_WARN_S=15 POLYARB_L2_TOB_AGE_FAIL_S=30 -a polyarb-l2`: <UTC>
       - Confirmed /health threshold reflected (output contains "warn>15s, fail>30s"): <yes/no>

       ### Observation window (60s — fast-trip via lowered thresholds)
       - /health overall after 20s: <status>  (expected: warn)
       - /health overall after 35s: <status>  (expected: fail / HTTP 503)
       - /health overall after 60s: <status, HTTP code>
       - mirror:l2_tob_age_seconds status reached "fail": <yes/no>  ← CHAIN TRUTH PROOF (B-3: NOT "sub-check exists" — status flip is the truth)
       - observed age value at fail: <N> seconds
       - HTTP 503 timestamp: <UTC>

       ### Threshold cleanup (post-recovery)
       - `flyctl secrets unset POLYARB_L2_TOB_AGE_WARN_S POLYARB_L2_TOB_AGE_FAIL_S`: <UTC>
       - /health output after restore shows defaults (warn>300s, fail>600s): <yes/no>

       ### Sentry breadcrumb evidence
       - Query: <command>
       - Found: <yes/no>
       - Event ID: <id>
       - category: l2-mirror, message: push_top_of_book failed ...

       ### Telegram delivery
       - Message received: <yes/no>
       - Delivery time: <UTC>

       ### Cleanup
       - Secrets restored: <yes>
       - /health back to pass: <yes/no>, <when>

       ### Verdict (GAP-5)
       - chain-truth: <CONFIRMED / GAP-REMAINS>
       - Plan 02 fix proven effective: <yes/no>
       ```
  </action>
  <verify>
    <automated>grep -c "Inj L2-2 Re-run\|chain-truth: CONFIRMED\|mirror:l2_tob_age_seconds status reached \"fail\": yes\|HTTP 503 timestamp" .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md</automated>
  </verify>
  <done>
    - SOAK-LOG.md has the Inj L2-2 Re-run section fully populated (no placeholders)
    - Verdict line is CONFIRMED (or GAP-REMAINS with concrete next-action recorded)
    - **B-3 hard gate: SOAK-LOG must show `mirror:l2_tob_age_seconds status reached "fail": yes` AND `HTTP 503 timestamp: <real UTC>`** — option (c) "sub-check exists" is NOT acceptable evidence
    - Threshold cleanup verified (POLYARB_L2_TOB_AGE_* unset back to default)
    - Sentry breadcrumb evidence is concrete (event ID OR explicit explanation if Sentry API not queryable)
  </done>
</task>

<task type="auto">
  <name>Task 3: Inj L2-3b — opt-in NOTIFY happy-path (low-traffic window)</name>
  <files>.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md</files>
  <read_first>
    - Makefile chaos-l2-inj3b target
    - 03-LEARNINGS.md B1 invariant (POLYARB_EVENT_BUS_ENABLED default FALSE)
    - 03-SOAK-LOG.md Inj L2-3b substitute evidence (the catchup + bootstrap behavior)
  </read_first>
  <action>
    1. Verify still in low-traffic window (per Task 1 decision).
    2. Capture baseline: `make chaos-l2-baseline` + log L2 listener state via `curl https://polyarb-l2.fly.dev/health | jq '.checks["event_bus:listener_state"]'`
    3. Run `make chaos-l2-inj3b`. Within 5 min:
       - Verify L2 logs show `NOTIFY received` (use `FLY_API_TOKEN= flyctl logs -a polyarb-l2 | grep -i notify`)
       - Verify candidate refresh fires (`l2_candidates` table — psql probe)
    4. Cleanup is automatic in chaos-l2-inj3b (unsets POLYARB_EVENT_BUS_ENABLED). VERIFY by:
       ```bash
       FLY_API_TOKEN= flyctl secrets list -a polyarb-l1 | grep -i event_bus
       ```
       Must return empty / confirm unset.
    5. Append to SOAK-LOG.md (similar template to Task 2):
       ```markdown
       ## Inj L2-3b: opt-in NOTIFY happy-path — <UTC>

       Window: <time window justification>
       Baseline: <listener state, candidate count>
       Trigger: `make chaos-l2-inj3b`

       ### Observations
       - L2 logs show NOTIFY received: <yes/no, sample line>
       - l2_candidates row count delta: <N>
       - candidate refresh logged: <yes/no, sample line>

       ### B1 invariant restoration
       - POLYARB_EVENT_BUS_ENABLED unset on polyarb-l1: <verified yes>
       - L2 listener still in 'listening' state: <yes>

       ### Verdict
       - opt-in path WORKS in prod: <yes/no>
       - B1 invariant restored: <yes/no>
       ```
  </action>
  <verify>
    <automated>grep -c "Inj L2-3b: opt-in NOTIFY\|B1 invariant restored: yes" .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md</automated>
  </verify>
  <done>
    - SOAK-LOG.md has Inj L2-3b section fully populated
    - B1 invariant restoration confirmed
    - Both questions in Verdict answered yes
  </done>
</task>

<task type="auto">
  <name>Task 4: Inj L2-4 — WS storm + Supabase pause double-fault</name>
  <files>.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md</files>
  <read_first>
    - Plan 06 SUMMARY (POLYARB_WS_TEST_KILL design + chaos-l2-inj4 Makefile sequence)
    - 03-LEARNINGS.md D-03 (watchdog 30s + storm cap)
  </read_first>
  <action>
    1. Capture baseline.
    2. Run `make chaos-l2-inj4`. Watch logs in parallel: `FLY_API_TOKEN= flyctl logs -a polyarb-l2`.
    3. Within the 60s window, observe:
       - WsTestKillRequested exception in logs
       - Watchdog reconnect attempts
       - Storm cap engagement (if reconnects exceed 10/hr threshold)
       - /health overall transitions through pass → warn → fail
       - mirror:l2_tob_age_seconds value climbing
    4. Cleanup is automatic in chaos-l2-inj4 (unsets WS_TEST_KILL + restores SUPABASE_SERVICE_KEY).
    5. Within 30s of cleanup, observe recovery:
       - /health back to pass
       - l2_top_of_book new rows resume
       - WS reconnect successful (no longer hitting kill flag)
    6. Append to SOAK-LOG.md:
       ```markdown
       ## Inj L2-4: WS storm + Supabase pause double-fault — <UTC>

       ### Phase 1: fault injection (T+0)
       - Action: `make chaos-l2-inj4`
       - POLYARB_WS_TEST_KILL=1 set: <yes>
       - POLYARB_SUPABASE_SERVICE_KEY unset: <yes>

       ### Phase 2: observation (T+60s)
       - Daemon survival: <yes/no — flyctl status alive>
       - WsTestKillRequested in logs: <yes, sample line>
       - Watchdog reconnect attempts: <N over 60s>
       - Storm cap engaged: <yes/no, if N > 10/hr>
       - /health overall after 60s: <status>, HTTP <code>
       - mirror:l2_tob_age_seconds value: <N>

       ### Phase 3: cleanup recovery (T+90s)
       - Secrets restored: <yes>
       - /health overall after 30s recovery: <status>
       - l2_top_of_book new rows in last 60s: <N> (must be >0)
       - WS subscriptions reattached: <evidence>

       ### Verdict
       - Daemon survived double-fault: <yes/no>
       - Watchdog logic correct under storm: <yes/no>
       - Mirror failure correctly surfaced to /health: <yes/no — this is the chain-truth proof under load>
       - Recovery clean: <yes/no>

       ### Carry-over / observations
       - <anything unexpected; if WS_TEST_KILL flag persists past cleanup, file as new GAP>
       ```
  </action>
  <verify>
    <automated>grep -c "Inj L2-4: WS storm\|Daemon survived double-fault: yes\|Mirror failure correctly surfaced" .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-SOAK-LOG.md</automated>
  </verify>
  <done>
    - SOAK-LOG.md has Inj L2-4 section fully populated
    - All 4 verdict questions answered
    - Recovery confirmed
  </done>
</task>

<task type="auto">
  <name>Task 5: Phase 03.1 VALIDATION.md + closure</name>
  <files>.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-VALIDATION.md</files>
  <read_first>
    - 03-VALIDATION.md (Phase 03 template — VALIDATION ledger format)
    - All 6 SUMMARY files from Plans 01-06 of this phase
    - 03.1-SOAK-LOG.md (just produced)
  </read_first>
  <action>
    1. Create 03.1-VALIDATION.md:
       ```markdown
       ---
       phase: 03.1
       plan: 07
       nyquist_compliant: true
       generated: <UTC>
       ---

       # Phase 03.1 VALIDATION Ledger

       ## Scope Closure (12 items from 03.1-CONTEXT.md)

       | Item | Plan | Status | Evidence |
       |---|---|---|---|
       | GAP-1 (l2_mirror_enabled flag → /health) | 03.1-02 | ✅ | 03.1-02-SUMMARY + 03.1-SOAK-LOG Inj L2-2 re-run chain-truth confirmed |
       | GAP-2 (SqliteStore.get_l2_tob_last_mirror_at_s getter) | 03.1-01 | ✅ | 03.1-01-SUMMARY |
       | GAP-3 (L2SupabaseMirror persist last_mirror_at_s) | 03.1-01 | ✅ | 03.1-01-SUMMARY |
       | GAP-4 (chaos Makefile FLY_API_TOKEN unset) | 03.1-03 | ✅ | 03.1-03-SUMMARY + verify cmd 0 bare flyctl |
       | GAP-5 (re-run Inj L2-2 with Sentry breadcrumb verify) | 03.1-07 | ✅ | 03.1-SOAK-LOG Task 2 |
       | GAP-100 (Fly DNS retry tenacity) | 03.1-04 | ✅ | 03.1-04-SUMMARY |
       | GAP-101 (failure_threshold 3→5) | 03.1-04 | ✅ | 03.1-04-SUMMARY |
       | GAP-102 (Sentry env=dev audit + migration) | 03.1-05 | ✅ | 03.1-05-SUMMARY + sentry-audit-report.md |
       | GAP-103 (snapshots.notes write fail reason) | 03.1-02 | ✅ | 03.1-02-SUMMARY |
       | Inj L2-3b (opt-in NOTIFY happy-path) | 03.1-07 | ✅ | 03.1-SOAK-LOG Task 3 |
       | Inj L2-4 (WS storm + Supabase pause double-fault) | 03.1-07 | ✅ | 03.1-SOAK-LOG Task 4 |
       | Inj L2-5 (Data API 429 fixture only) | 03.1-06 | ✅ partial | fixture + dry-run target; live run deferred |
       | PROCESS-1 (chain-truth discipline in thread) | 03.1-03 | ✅ | 03.1-03-SUMMARY |
       | PROCESS-2 (CLAUDE.md container-image-aware chaos) | 03.1-03 | ✅ | 03.1-03-SUMMARY |

       ## Carry-over to Future Phases

       - **D-01 修法 B (httpx IPv4-only transport)**: deferred pending DNS baseline probe data (Plan 04 produced script; Polywatch chaos-inj-replay cron will deploy)
       - **D-01 修法 C (Fly support ticket evidence cron-deploy)**: m5-polywatch backlog
       - **D-02 修法 B (transient error classification)**: m1-perception backlog (trigger: new fail types in Phase 04+ candidate load)
       - **D-03 修法 C (PagerDuty / SMS escalation)**: m5-polywatch backlog (Polywatch trial-5 candidate)
       - **Inj L2-5 live run** (real Polymarket 429): on naturally triggered occurrence
       - **POLYARB_WS_TEST_KILL → nightly cron**: m5-polywatch trial-2 (chaos-inj-replay) candidate

       ## Phase Goal Verification

       Goal (from 03.1-CONTEXT.md): "L1+L2 alert chain 真正能在分钟级被发现并修复, SESSION 27 那种 '3.5 天没人发现 PAUSE' 永久不再发生"

       Verification:
       - Chain-truth alive (Inj L2-2 re-run): <✓ / partial / ✗>
       - Sentry environment correctly tagged (Plan 05 + user rollout): <✓>
       - DNS jitter no longer trips PAUSE (Plan 04 + 5-fail threshold): <✓ logic, prod evidence accumulates>
       - healthz-watcher (already shipped) provides 16-min auto-unpause fallback: <✓ live since 2026-05-26>

       VERDICT: <Goal MET / partially met with carry-over / NOT MET>

       ## Test Counts

       - New tests landed: <N>
       - Pre-existing tests still GREEN: <N>
       - Skipped/xfail (intentional): <list>
       ```
    2. Update `.planning/workstreams/m1-perception/STATE.md` — set Phase 03.1 status to COMPLETE, last_activity = <today>.
    3. Update `.planning/JOURNAL.md` — append a SESSION entry summarizing Phase 03.1 closure.
    4. Recommend `/gsd-extract_learnings 03.1 --ws m1-perception` as next step in the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-VALIDATION.md && grep -c "nyquist_compliant: true\|VERDICT:" .planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-VALIDATION.md</automated>
  </verify>
  <done>
    - VALIDATION.md exists with frontmatter nyquist_compliant=true
    - All 14 scope items in the table have ✅ status (or documented partial)
    - VERDICT line filled
    - STATE.md + JOURNAL.md updated
  </done>
</task>

</tasks>

<verification>
- 03.1-SOAK-LOG.md has Inj L2-2 + L2-3b + L2-4 sections all populated (no placeholders)
- 03.1-VALIDATION.md exists with nyquist_compliant=true
- STATE.md shows Phase 03.1 COMPLETE
- JOURNAL.md has new SESSION entry
- `make planning-status` shows zero drift
</verification>

<success_criteria>
- All 3 chaos injections verified in prod
- chain-truth discipline EMPIRICALLY proven on the patched paths (Inj L2-2 re-run is the central evidence)
- B1 invariant restored after Inj L2-3b
- Daemon survived double-fault under Inj L2-4
- Phase 03.1 closes with full VALIDATION ledger
- Phase goal "L1+L2 alert chain 分钟级被发现并修复" MET (or partial-with-carryover documented)
</success_criteria>

<output>
After completion, create `.planning/workstreams/m1-perception/phases/03.1-l2-observability-gaps-fix-up/03.1-07-SUMMARY.md` per template.

Include: chaos run results matrix, VALIDATION verdict, deferred items list, recommendation to run `/gsd-extract_learnings 03.1 --ws m1-perception` next session.
</output>
