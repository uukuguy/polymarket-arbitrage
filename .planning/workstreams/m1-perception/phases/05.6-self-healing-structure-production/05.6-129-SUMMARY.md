# 05.6-129 Summary — Make isolated control-plane apps start without legacy secrets

## Delivered

- Added explicit non-secret `POLYARB_RUNTIME_ROLE=control-plane` to all three
  isolated Fly templates.
- Kept mandatory legacy `/scan` HMAC secret validation for `legacy-daemon`.
- Allowed control-plane API/data/alert processes to initialize without
  receiving that unrelated legacy secret.

## Verification

- RED: a control-plane role without scan credentials failed Settings startup.
- GREEN: settings and template contracts pass; a built production Docker image
  initializes Settings and both control-plane entrypoints with no scan secret.
- Full control-plane/transactional/settings regression: 119 passed.

## Boundary

No cloud resource was created, deployed, migrated, or contacted beyond prior
read-only identity audit. This is a local least-privilege startup contract.
