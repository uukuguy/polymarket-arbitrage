# 05.6-240 Summary — Ambiguous R2 write timeout recovery

## Outcome

- Added `r2-write-timeout` disposable commissioning for all seven R2-writing
  nodes: Structure fetch/materialize/normalize/certify, Quote admit/batch, and
  Opportunity certify.
- The injected failure models the hard provider boundary: immutable object
  bytes and SHA-256 metadata are committed, then the PUT response raises
  botocore `ReadTimeoutError`.
- The failed lease records one retryable fact/incident through the central
  policy and is proven to have no database business receipt, even though the
  unacknowledged object exists.
- The policy-due replacement retains the same job input identity, HEAD-verifies
  exact key/SHA-256, and only then commits one real terminal/business fact.
  It never uploads the object twice.
- Refactored read/write timeout commissioning onto one stage-exact lifecycle
  while retaining the six-node H-051 contracts.
- Added seven-database harness, Make entry, exact argv/help contracts, climb
  gates, H-052 state, learning FAQ, and this summary.

## Verification

- Seven-node real-PostgreSQL write adapter contract: passed.
- Seven isolated migrated database harness runs with zero residue: passed.
- Six-node read timeout regression: passed after shared lifecycle refactor.
- Make exact command and climb evaluator contracts: passed.
- Ruff/Pyright and planning-status are required again before commit.

## Boundaries

- Local disposable PostgreSQL and in-memory provider-shaped object boundary
  only.
- No production provider/database request, Fly action, deployment, Docker
  build/context mutation, Colima, canary, or qualification operation occurred.
- Exact runtime image rebuild remains a separate build-only step after H-052
  confirmation; no deployment authority is implied.
