# Plan 05.6-48 Summary — Dashboard Structure comparison truth

## Outcome

`/perception/producer-progress` now carries the active durable Structure
comparison cursor independently from the latest child attempt.  The Dashboard
renders its phase, committed row count, and checkpoint time, preventing a
checkpointed/cancelled child receipt from being mistaken for stalled
certification.

## Verification

- Perception producer-progress HTTP contract test passes.
- Dashboard TypeScript compilation and Prettier pass.
- The M1 manual describes the comparison-progress interpretation.

## Deployment follow-up

Deploy with the next daemon release, then read the active comparison directly
from the Fly operator surface while Structure completes its durable cursor.
