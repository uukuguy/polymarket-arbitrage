# 05.6-228 Summary — Record exact-image progress-stall proofs

## Outcome

- Command-scoped OrbStack built and pushed the non-deploying runtime image for
  exact HEAD `9b770d57c906dc792f640b0f9b02c347a7bb92c0`.
- Registry digest is
  `sha256:1452064dd351fccbd9c41abada2b993f74be8b08603c5e800e7f1b9e6a06fa8e`;
  independent pull/inspection proved linux/amd64, user `polyarb`, exact revision
  label and image ID
  `sha256:dfd70b310b88a01f09e4f8fc0be81c368ef68650a466776562b738eb55fc249a`.
- The exact image executed all eight progress-stall node attacks and returned
  eight completed proofs.

## Evidence and cleanup

- Each proof binds the exact release and observe-only/empty-allowlist config
  `sha256:f79dc5e1268d5b75f245e6c7d59f5483609ab6e32aff99fe25cb1cc12df70a90`.
- The evidence tree contains 72 append-only attack lifecycle files plus one
  independently written execution manifest. Every proof has block impact,
  cleanup confirmation and typed incident/action/recovery/business fact IDs.
- The port-unpublished PostgreSQL 16 container reported zero
  `m1_commissioning_%` databases and zero migration-created capability roles
  after the run. Evidence scanning found no DSN, password, authorization,
  API-key, token or provider body.
- The stopped attack container and disposable PostgreSQL container were removed
  after evidence extraction and zero-residue verification. Their test-only data
  is not recoverable; the required artifacts are tracked here.
- Temporary registry credentials used an isolated Docker config and were moved
  to Trash. The user-global Docker context remained `orbstack`; Colima was not
  installed, started or referenced.

## Release boundary

- These eight v26 proofs cannot be combined into one final commissioning
  envelope with the v25 stale-owner proofs because release identity is part of
  every proof. Both runs validate their adapters, but the final 66-proof
  envelope must rerun every required attack on one later immutable release.
- To avoid turning every adapter increment into another throwaway full release,
  implement and confirm the remaining shared disposable attacks first, then run
  the complete shared matrix together in one exact image before scoped provider
  canaries or qualification.
