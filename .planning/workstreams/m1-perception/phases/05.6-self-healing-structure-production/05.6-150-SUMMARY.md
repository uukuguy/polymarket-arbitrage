# 05.6-150 Summary — Quarantine a source window whose frozen member closes

## Delivered

- A scoped exact-ID market fetch that receives Gamma's explicit
  `exact-id market response is not open` result quarantines its complete
  source window as `StructureSourceMemberBecameInactiveError`.
- The existing retryable incident path remains unchanged for timeouts and all
  other pagination-integrity failures.

## Why

- A long live source traversal can outlast market availability. Retrying a
  frozen member that Gamma declares closed cannot recreate a coherent original
  scope, while omitting it would silently create an incomplete Structure
  bundle. Window quarantine preserves all evidence and releases the next
  cadence bucket for a fresh atomic scope.

## Verification

- RED/GREEN unit contract proves a closed frozen market returns `quarantined`
  without a retryable incident.
- Existing timeout contract still proves retryable incident behavior.
- Full focused source/scheduler/CLI/PostgreSQL suite and Ruff passed.
