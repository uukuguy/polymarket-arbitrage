# 05.6-135 Summary — Preserve scoped market inputs during materialization

## Delivered

- Made `structure_source_window_pages()` reconstruct page specifications through
  the canonical persisted-input reader, including immutable `market_ids` and
  their digest.
- Added a Postgres regression contract that seals a scoped source window and
  proves the materializer-facing page list retains the exact first market batch.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_postgres.py -k
  'only_last_scoped_market_batch_releases_materializer or
  terminal_event_page_atomically_admits_immutable_market_batches' -q` — 2 passed.
- `uv run ruff check src/polyarb/control_plane/postgres.py
  tests/m1-perception/test_control_plane_postgres.py` — passed.

## Boundary

This is a staging control-plane correctness repair only. It changes neither
Telegram delivery nor any production L1/L2 worker or credential.
