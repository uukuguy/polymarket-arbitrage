# 05.6-122 Summary — Make transactional collector failure visible

## Delivered

- Added one fenced Postgres operation that changes a leased job to retryable,
  closes its running attempt, records the incident event, and creates dashboard
  alert intent in one database transaction.
- Connected source-page fetch, source materialization, Quote admission,
  Structure range/certification, and Quote batch/certification failures to that
  operation.
- Scoped each open incident by job while retaining one idempotent failure event
  for every lease epoch; repeated recovery attempts cannot duplicate one
  outbox intent.
- Added learning document 67 for the retry-to-alert transaction boundary.

## Verification

- RED: the new real-Postgres test failed with missing
  `finish_retryable_with_incident`.
- `uv run pytest tests/m1-perception/test_control_plane_postgres.py
  tests/m1-perception/test_transactional_quote_worker.py
  tests/m1-perception/test_transactional_structure_worker.py
  tests/m1-perception/test_transactional_structure_source_worker.py
  tests/m1-perception/test_transactional_structure_source_materializer.py
  tests/m1-perception/test_transactional_quote_admission.py -q` → 47 passed.
- Targeted Ruff and `git diff --check` passed.

## Boundary

This produces durable dashboard alert intent only. No alert-delivery worker or
cloud runtime has been deployed, and no live worker-loss or 24-hour soak
evidence exists yet.
