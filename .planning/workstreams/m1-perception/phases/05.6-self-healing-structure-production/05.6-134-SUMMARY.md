# 05.6-134 Summary — Include Alembic assets in control-plane runtime image

## Delivered

- Added `alembic.ini` and the immutable `alembic/` revision chain to the
  non-root runtime image.
- This repairs the isolated staging migration execution path without exporting
  its DSN or modifying production worker configuration.

## Verification

- RED: the staging image executed `alembic upgrade 015` with exit 255 and log
  `No 'script_location' key found in configuration`; Dockerfile contract test
  failed because neither asset was copied.
- GREEN: `uv run pytest tests/m1-perception/test_control_plane_migration_image.py -q` — passed.

## Boundary

The first one-off staging migration command stopped safely due to this missing
asset. No database schema or source scheduler state changed in that attempt;
the corrected image must still be built and rerun against staging.
