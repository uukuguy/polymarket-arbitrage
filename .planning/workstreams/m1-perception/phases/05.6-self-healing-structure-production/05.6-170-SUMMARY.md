# 05.6-170 — lease 过期的服务存活性修复

## 完成内容

- `TransactionalWorkerLoop` 与 coordinator scheduler 现在将
  `StaleLeaseError` 记录为本轮 `stale-lease`，继续同一 tick 的其他有界 turn。
- 仍保留 Postgres lease fence：stale worker 既不会写入 receipt，也不会被误报为成功；其他
  异常仍按原语义上抛并可见。
- 新增回归测试覆盖 role-local loop 与 coordinator 在 stale lease 后的继续执行行为。

## 验证

- 目标 scheduler 测试：11 passed；Ruff 通过。
- M1 测试集已启动；其完整输出未在本终端会话中返回，不能将其记为完整 green。
- staging image `m1-stale-lease-amd64-4a8233f1@sha256:5ffe41a3…51f8` 已滚动到
  coordinator、两台 Structure pool、两台 Quote pool。
- 实际 staging 日志已出现 `outcome: stale-lease` 后的成功 receipt turn，五个角色均保持
  started；旧的 `control-plane command unavailable: StaleLeaseError` / exit 1 模式未在
  新镜像的这段证据中出现。

## 仍需完成

- 记录足够的连续 queue-health 样本，确认 backlog/age 行为；当前 API 样本为 Structure
  unfinished `3481`、Quote unfinished `43`。
- 继续进行已隔离的 Quote retry/R2 process-loss takeover 与 scoped Telegram receipt，最后
  开启 24 小时连续 soak。
