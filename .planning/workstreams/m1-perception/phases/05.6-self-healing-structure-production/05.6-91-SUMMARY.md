# 05.6-91 Summary — Expose a guarded Structure worker operator turn

## Delivered

- Added `control-plane structure-once --enable`, which invokes exactly one
  transactional Structure normalization worker turn.
- Added `make structure-control-plane-once enable=1`; the Make target is the
  supported operator entry point and verifies its acknowledgement before
  loading credentials or connecting to Postgres.
- Both CLI output and tests explicitly state `pointer_mutations=0`.  This
  command cannot export legacy SQLite data, schedule a loop, certify a
  generation, or switch either current pointer.

## Verification

- CLI, Structure worker, and control-plane Postgres contracts: 25 passed.
- Ruff passes and `make -n structure-control-plane-once enable=1` resolves
  the guarded command without executing it.

## Boundary

The operator can execute only already-admitted jobs.  Next is the separately
guarded legacy shadow exporter/admitter, followed by certification/parity and
only then a separately authorized pointer switch and live soak.
