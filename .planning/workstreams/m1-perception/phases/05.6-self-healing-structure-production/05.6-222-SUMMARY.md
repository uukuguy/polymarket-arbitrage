# 05.6-222 Summary — Prove stale-owner fencing across all eight nodes

## Outcome

- Every disposable normal turn can now stop immediately before its real
  terminal domain transaction and later complete under an explicitly supplied
  current lease.
- All eight production node types reclaim an expired lease through the public
  `claim_job` authority, reject the superseded owner's terminal write and let
  only the replacement epoch publish terminal and business facts.
- The existing one-call normal-turn API remains implemented over the two-phase
  primitive, so normal commissioning behavior is unchanged.

## Evidence boundary

- The attack does not directly update lease time/state. It advances the
  control-plane API's deterministic observation time beyond the persisted lease
  and lets the normal claim transaction create the replacement epoch.
- After the stale commit is rejected, PostgreSQL proves the old epoch has zero
  succeeded attempts and zero `job.succeeded` events.
- The final attempt ID is independently read back and must belong to the
  replacement epoch before its business postcondition is accepted.

## Verification

- 41 real-PostgreSQL runtime-coverage cases pass, including eight normal turns
  and eight stale-owner takeover/terminal-write cases.
- Ruff and Pyright pass; no arbitrary timeout or production mutation was added.
- H-038's focused climb cycle is required after this commit.

## Next boundary

- Wrap this primitive in the cleanup-safe attack adapter and produce the seven
  ordered append-only stage receipts for eight exact-image proof directories.
