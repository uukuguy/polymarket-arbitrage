# 05.6-163 Summary — Budget extra Structure range turns

## Delivered

- Added a default-zero `structure_range_turns` scheduler budget.
- A tick retains its existing bounded base-worker ordering, then performs only
  the configured number of extra serial `structure-range` turns through the
  existing timeout and Postgres lease path.
- Added the regression proving an eight-worker tick with budget three invokes
  Structure range exactly four times while no other worker receives extra work.
- Exposed the budget as default-zero `--structure-range-turns` for `serve`
  and `tick-once`, with non-negative validation and explicit forwarding to the
  scheduler.

## Verification

- `uv run pytest tests/m1-perception/test_transactional_control_plane_scheduler.py -q`
- `uv run ruff check src/polyarb/control_plane/scheduler.py tests/m1-perception/test_transactional_control_plane_scheduler.py`
- `uv run pytest tests/m1-perception/test_transactional_control_plane_scheduler.py tests/m1-perception/test_control_plane_cli.py tests/m1-perception/test_transactional_structure_worker.py -q`

## Next

- Deploy only to staging with budget eight and gather live throughput evidence.
