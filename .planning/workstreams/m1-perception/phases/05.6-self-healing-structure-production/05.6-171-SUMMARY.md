# 05.6-171 — receipt→finish lease 竞争修复

## 完成内容

- 找到并修复完整 Structure/Quote receipt 的状态竞争：之前 receipt 将 job 置为
  `checkpointed`，使另一 pool 在同一 lease 尚未 finish 时可以立即重领。
- 完整 receipt 现在保留 `leased` 状态和原 lease owner；当前 worker 在同一 epoch 内 terminal
  finish。若进程在其间丢失，lease 到期后仍由既有 claim/replay 路径安全接管。
- 通用 `checkpointed` 保持即时续跑，未破坏 Structure materializer 的分段 work semantics。

## 验证与 staging 证据

- `tests/m1-perception/test_control_plane_postgres.py`：52 passed；Ruff 通过。
- 新增 Structure 和 Quote 的“不在原 lease 到期前被接管”回归，保留已有 process-loss
  lease-expiry 接管与 materializer checkpoint 回归。
- staging image `m1-receipt-lease-amd64-004d1fb9@sha256:f289a2d9…bc83` 已滚动至
  coordinator、两台 Structure pool 与两台 Quote pool。更新后的 Structure pool 连续成功
  turns，未复现此前高频 `stale-lease` 竞争。

## 后续验收

- 继续观察五角色 queue-health；在自然产生的下一批 Quote job 上完成 exact-key R2
  process-loss takeover。
- scoped Telegram 回执仍受 Fly secrets write 授权阻断，和本修复无关。
