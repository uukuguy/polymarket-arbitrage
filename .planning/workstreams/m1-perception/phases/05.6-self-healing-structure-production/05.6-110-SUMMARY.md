# 05.6-110 Summary — Verify cloud fault takeover and sustained soak evidence

## Delivered

- Added a local-only cloud fault/soak verifier and
  `make control-plane-verify-fault-soak evidence=<json>`.
- PASS requires a Structure and Quote R2-upload-before-receipt loss/reclaim
  proof, reclaim inside a declared bounded (≤180 s) SLA, old certified truth
  and independent control API availability during both failures.
- It additionally requires an uninterrupted ≥24-hour soak with scheduler
  ticks, readable API, zero manual unlocks, silent stops and permanent
  degradations.

## Verification

- Contracts prove complete acceptance plus refusal for missing worker takeover,
  short soak and lost certified truth.
- CLI contract proves local-only evidence processing; Ruff and Make dry-run
  pass.
- Focused worker/scheduler/API/template/rollout/parity/fault suite runs before
  commit.

## Boundary

- This verifier does not inject or observe cloud failures by itself. The
  designated environment must supply the immutable fault/soak evidence before
  M1 can claim continuous online operation.
