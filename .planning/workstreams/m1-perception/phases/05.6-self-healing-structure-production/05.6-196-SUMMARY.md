# 05.6-196 Summary — Cloud-Resident Soak Evidence Ledger

## Delivered

- Added Alembic revision 017: immutable `m1_soak_runs` and
  `m1_soak_observations` on the existing transactional Postgres authority.
- Locked every run to its formal control API, exact machine identity set and
  canonical V2 baseline; database triggers reject updates and deletes.
- Added `PostgresControlPlane` methods to atomically create a run plus its
  baseline observation, append only exact-digest idempotent retransmissions,
  and read verifier-ordered observations.

## Verification

- `uv run pytest tests/alembic/test_017.py -q`
- `uv run pytest tests/m1-perception/test_control_plane_postgres.py -k cloud_soak_ledger -q`
- `uv run ruff check src/polyarb/control_plane/postgres.py tests/m1-perception/test_control_plane_postgres.py alembic/versions/017_m1_cloud_soak_evidence.py tests/alembic/test_017.py`

## Next

Implement the isolated Fly cloud sampler and remote verifier, then deploy a
fresh cloud-only 24-hour evidence window.
