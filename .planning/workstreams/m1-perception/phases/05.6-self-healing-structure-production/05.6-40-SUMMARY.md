# 05.6-40 — Structure Publication Reconciliation Lock Bound Summary

## Delivered

- Diagnosed the live Structure P1 timeout from the persisted cursor and child
  evidence: the generation remained in `issues` normalization, not in the
  terminal certification/seal transition.
- Closed the remaining unbounded per-chunk writer acquisition: contract
  reconciliation now receives the same capped five-second writer budget as a
  normalization chunk, instead of inheriting SQLite's 120-second default.
- Applied that bounded writer acquisition to the later count-seal transition
  as well, so it cannot reintroduce the same failure at certification entry.
- Added a regression contract that verifies a publication step supplies the
  bounded writer budget to contract reconciliation.

## Verification

- `uv run pytest tests/m1-perception/test_structure_generation_publication.py::test_publication_step_bounds_contract_reconciliation_writer_wait tests/m1-perception/test_structure_generation_publication.py::test_normalization_chunk_writer_wait_is_explicitly_bounded -q`
- `uv run ruff check src/polyarb/perception/structure_publication.py src/polyarb/storage/sqlite_store.py tests/m1-perception/test_structure_generation_publication.py`
- `git diff --check -- src/polyarb/perception/structure_publication.py src/polyarb/storage/sqlite_store.py tests/m1-perception/test_structure_generation_publication.py`

## Production acceptance still required

Deploy the hotfix to both L1 authority and cron machines, then prove a fresh
Structure publication advances beyond the prior `issues` cursor without a
75-second child timeout. The P1 incident must resolve only after a fresh
Structure pointer and a later certified Quote run; dashboard and Polywatch
must retain the fault and recovery evidence.
