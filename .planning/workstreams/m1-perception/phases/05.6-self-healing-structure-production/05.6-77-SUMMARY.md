# 05.6-77 Summary — Durable Quote batch inputs

## Delivered

- Added `m1_quote_batch_inputs` to additive Alembic 009. Each job persists its
  Structure receipt digest, universe hash, token-range digest, exact ordered
  token ids, and admission time.
- Made Quote generation admission transactional: batch jobs, their immutable
  inputs, and the certifier job commit together or not at all.
- Added `quote_batch_spec(job_key)` so a replacement worker can recover the
  exact batch after a lease expiry without consulting a mutable SQLite cache or
  newer Structure generation.

## Verification

- RED/GREEN migration contract and PostgreSQL integration proof cover durable
  input recovery after admission.
- `uv run pytest tests/alembic/test_009.py tests/m1-perception/test_control_plane_postgres.py -v` — 13 passed.
- Changed-file Ruff and targeted diff check passed.

## Boundary

No executable Quote batch worker is wired yet. This slice only makes future
fenced worker takeover semantically possible; it changes no production DB,
legacy pointer, deployment, or external service state.
