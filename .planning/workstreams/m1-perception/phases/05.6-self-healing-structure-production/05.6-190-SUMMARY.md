# 05.6-190 Summary — authenticated Quote opportunity computation

## Delivered

- Added pure fail-closed projection from frozen Quote batch legs plus authenticated
  Quote artifact rows to positive buy-all opportunity rows.
- Added digest-checked Quote artifact parsing without CLOB or SQLite access.

## Verification

- `uv run ruff check src/polyarb/control_plane/opportunity_projection.py tests/m1-perception/test_transactional_opportunity_projection.py`
- `uv run pytest tests/m1-perception/test_transactional_opportunity_projection.py -q` — 1 passed.

## Next

Read the current certified Quote batch artifacts from R2, join their frozen
leg inputs, and send the complete result to the atomic Postgres publisher.
