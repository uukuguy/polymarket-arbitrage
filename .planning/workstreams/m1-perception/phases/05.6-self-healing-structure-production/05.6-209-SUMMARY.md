---
phase: 05.6-self-healing-structure-production
plan: "209"
plan-source: docs/superpowers/plans/2026-08-28-m1-runtime-lifecycle-hardening.md
subsystem: runtime
tags: [runtime-policy, deadlines, dag, cancellation, checkpoints, recovery, qualification]

requires:
  - phase: 05.6-self-healing-structure-production
    provides: transactional eight-job runtime, observe-only recovery, rolling qualification, and Plan 208 liveness diagnosis
provides:
  - one closed runtime-v2 policy registry and validated eight-job DAG
  - absolute attempt/progress watchdogs without scheduler-owned turn timeout
  - bounded service stop with durable lease/checkpoint recovery facts
  - atomic expired-attempt closure and migration cleanup of leased orphans
  - resumable 1,117-range Structure and 231-shard Quote reducers
  - controller-only single circuit probe and identity-aware qualification cursor handoff
  - bounded qualification status projection that never transfers full epoch evidence
  - normalized qualification facts with paged restart replay and bounded certificate projection
  - fixed-round role preflight and healthy qualification ingestion
  - timeout-complete connection bootstrap and interruption-resumable climb gates
  - one-attempt provider envelopes and fixed-round interruptible proof loops
affects: [m1-perception, production-runtime, rolling-qualification, recovery-controller]

tech-stack:
  added: []
  patterns:
    - one lifecycle policy persisted with every attempt
    - scheduler cadence separated from worker lifetime authority
    - terminal-grace detach protected by lease fencing and checkpoints
    - numeric per-job checkpoint ordering
    - first-identity replay versus successor-release high-water handoff

key-files:
  created:
    - src/polyarb/control_plane/runtime_deadlines.py
    - src/polyarb/control_plane/service_lifecycle.py
    - src/polyarb/control_plane/blocking_bridge.py
    - src/polyarb/control_plane/db_deadlines.py
    - alembic/versions/028_m1_runtime_policy_snapshot.py
    - alembic/versions/029_m1_bounded_qualification_status.py
    - alembic/versions/030_m1_normalized_qualification_facts.py
    - docs/learning/93-任务生命周期为何不能由多个时钟共同决定.md
    - docs/learning/94-增长证据不能放进每轮状态行.md
    - docs/learning/95-超时任务序列与可恢复性审计.md
    - docs/superpowers/specs/2026-08-28-m1-runtime-lifecycle-hardening-design.md
    - docs/superpowers/plans/2026-08-28-m1-runtime-lifecycle-hardening.md
  modified:
    - src/polyarb/cli_control_plane.py
    - src/polyarb/control_plane/alert_delivery.py
    - src/polyarb/control_plane/scheduler.py
    - src/polyarb/control_plane/worker_loop.py
    - src/polyarb/control_plane/runtime_contract.py
    - src/polyarb/control_plane/postgres.py
    - src/polyarb/control_plane/reconciler.py
    - src/polyarb/control_plane/qualification_service.py
    - src/polyarb/control_plane/structure_worker.py
    - src/polyarb/control_plane/quote_admission.py
    - src/polyarb/control_plane/runtime_observe.py
    - tools/climb/eval_local.py

key-decisions:
  - "A claimed attempt has one lifecycle authority; scheduler and role loops own cadence, fairness, lanes, and bounded shutdown only."
  - "I/O timeout is derived below both progress deadline and terminal grace; no worker-local deadline profile may compete with runtime-v2."
  - "An open circuit owns recovery before the historical attempt's expired clocks and only a controller action may release one probe."
  - "The first qualification identity consumes existing breaker history; only a successor identity starts at current ledger high-water."
  - "Production rollout remains stopped until the exact revision-031 Plan 209 image and fresh reviewed preflight pass."
  - "Operator status reads project bounded summaries; append-only certificate evidence is never transferred through SELECT *."
  - "Qualification facts append as normalized rows; the epoch row stays fixed-size and certificates read only scalar evidence projections."
  - "DB deadlines are centrally ordered; first cancellation drains within terminal grace and second cancellation is authoritative."
  - "Scheduler DAG order is advisory; certifier eligibility is a durable waiting-to-runnable receipt barrier."
  - "A per-I/O timeout cannot bound an unbounded query loop; formal operations use fixed rounds or explicit pages/checkpoints."
  - "A connection is usable only after its startup packet has installed search path, statement timeout, and lock timeout."
  - "Gate orchestration owns resume identity, not a universal outer timeout; each gate owns its domain deadline."
  - "Formal provider SDKs perform one inner attempt below worker I/O; durable PostgreSQL retry/circuit state is the sole recovery controller."
  - "Proof and watchdog loops declare fixed bounded rounds and run off the signal loop; bounded leaf calls alone do not bound an operation."

requirements-completed:
  - Plan 05.6-209 Tasks 1-5
  - Plan 05.6-209 Task 6 local verification, lifecycle audit, and independent review
  - Plan 05.6-209 Task 7 operation-round, controller, alert, and gate-resume audit
  - Plan 05.6-209 Task 8 provider retry, barrier polling, watchdog, cloud-soak, and alert-clock audit

duration: "one extended implementation and verification session"
completed: 2026-08-28
---

# Plan 05.6-209 Summary: Runtime Lifecycle Hardening

**The local runtime no longer lets unrelated timeout layers kill the same
attempt. All eight job types now share one persisted policy, long reducers
resume from durable ordered checkpoints, service stop is bounded and
observable, and reclaim/circuit/qualification transitions have real PostgreSQL
end-to-end proof. Production rollout and the new 86,400-second epoch remain.**

## Root cause

Plan 208's 3,600-second Structure deadline fixed one symptom but a second
production failure proved the defect class was broader: `quote-admit` owned a
valid long attempt yet the coordinator cancelled its turn at 105 seconds. The
cancel bypassed normal finalization, left `running` attempts, and restarted all
231 shards from zero. Structure certification had the same zero-restart shape
over 1,117 ranges.

The audit found competing module-local policies, scheduler/role timeouts,
relative I/O budgets that reset on every call, no shutdown grace consumption,
timestamp-ordered checkpoints, an expired-lease reclaim gap, an open-circuit
decision-order contradiction, and new qualification releases replaying old
ledger history as if it were current evidence.

## What changed

- `runtime-v2` covers exactly the eight registered job types. It derives and
  validates lease, heartbeat, progress, absolute attempt, I/O, terminal grace,
  retry budget and checkpoint cadence. Claim persists the exact profile;
  migration defaults are dropped after backfill so future writers fail closed.
- Async runtime watchdogs enforce absolute attempt and no-progress deadlines.
  Structure's synchronous range calls consume remaining absolute attempt time
  instead of resetting a full timeout per operation.
- Scheduler lanes come from a validated acyclic DAG, run independently, and no
  longer own a turn timeout. Both multi-role and role-local services stop new
  claims, request/cancel current work, drain through central terminal grace and
  emit `service-stop-grace-expired` when durable takeover must finish recovery.
- Sync workers run through a daemon bridge so Python's default executor shutdown
  cannot turn one non-cooperative call into an infinite process join. Production
  workers still use client bounds, stop checks, checkpoint facts and epoch
  fencing; daemonization is not treated as data safety.
- Blocking worker calls, qualification ticks, watchdog reads and operator HTTP
  reads share one cancellation-aware daemon bridge. The first cancellation may
  drain only inside central grace; the second detaches and forbids cleanup from
  starting another terminal I/O. Quote's async reader follows the same rule.
- PostgreSQL connect/statement/lock bounds now come from one ordered registry.
  Session defaults cover every control-plane method; fenced transactions may
  only narrow them to remaining lease budget. HTTP envelopes are derived above
  one connect plus statement rather than contradicting the inner deadline.
- Structure and Quote certifiers are inserted `waiting`. The last terminal
  range/batch receipt atomically moves its certifier to `runnable`; the
  certifier then independently revalidates the complete input/receipt set.
- `claim_job` closes an expired old attempt and appends the authoritative
  retryable event before creating a new epoch. Revision 028 also repairs legacy
  current leased jobs that have no matching runtime row.
- Running checkpoints receive a locked per-job numeric sequence. Structure
  certification checkpoints every 25 ranges and Quote admission every 10
  shards, with generation/digest/policy verification before resume.
- Open circuits are reconciled before their historical attempt clocks. Ordinary
  workers cannot auto-claim a due circuit; the recovery controller releases one
  probe and advances `next_probe_at`.
- Qualification's first identity consumes current breaker history. A later
  release detects an existing predecessor identity and seeds its cursor at the
  live ledger high-water, so old facts cannot count as new release coverage.
- Qualification status no longer fetches or evaluates the full, monotonically
  growing `fact_records` and `fact_digests` JSON arrays. Revision 029 persists
  three `STORED GENERATED` write-time projections: final fact, recovery total,
  and at most 20 recent recovery summaries. Status reads only those fixed
  columns; predecessor breaker fallback reads only two scalar columns. The
  certificate and writer paths retain complete append-only evidence.
- Revision 026's PostgreSQL 16 downgrade SQL was repaired for dynamic database
  identifiers and composite row declarations; revision expectations now point
  to the real head 028 without restoring broad `service_role` certificate
  authority.
- The final operation-round audit found that individually bounded SQL still
  allowed unbounded total work. Role authority preflight now obtains schema,
  table and sequence matrices in fixed catalog rounds; production read-only
  verification completes in 11-13 seconds instead of exceeding 3m48s.
- Scoped connections use libpq startup options as defense-in-depth, then run
  exactly one autocommit `set_config`/readback round under a central
  cancellation deadline. This preserves active-session truth through Supabase
  Session Pooler without returning an unbounded or half-scoped connection.
- Qualification initializes history once per process rather than every tick.
  Ordinary accumulating batches append and verify facts in bulk with fixed SQL
  rounds; state-changing terminal/recovery batches retain exact sequential
  semantics. Cooperative stop finishes at most the current server-bounded SQL,
  rolls back, and refuses later I/O before terminal-grace detach.
- Climb removed its universal 120-second subprocess kill. It atomically records
  each completed gate against exact git head and full argv, resumes only an
  identical unfinished suffix, and rejects stale or malformed progress.
- Runtime controller observe-only turns no longer open one connection per
  candidate. Up to 100 decisions enter one lease-fenced bulk transaction, and
  the synchronous turn runs through cooperative stop plus recovery-DB grace
  isolation so PostgreSQL work cannot block service signals.
- Alert delivery's mixed HTTP/PostgreSQL turn now runs off the signal loop.
  Cooperative stop after claim/provider response forbids new finish SQL, while
  outbox lease expiry and database grace cover abandoned/non-cooperative turns.
- Formal Gamma, CLOB and R2 clients now make exactly one inner provider attempt
  under an explicit timeout below the worker I/O envelope. Both retryable
  transaction paths use the central runtime backoff function, so SDK retry and
  module-local formulas cannot multiply or contradict durable circuit state.
- Incomplete Quote and opportunity certifier barriers no longer poll every five
  seconds outside the circuit. They record a retryable incident and consume the
  same durable failure budget; unused `retry_delay` controls were removed.
- Cloud-soak samples run behind cooperative stop and recheck it before their DB
  append. Fly CLI reads have an operation-specific subprocess bound. Watchdog
  control/app snapshots execute in one parallel observation round; each bounded
  app uses one list read plus one parallel exact-Machine detail round so restart
  counts retain their authoritative semantics.
- Alert delivery now declares one executable clock policy: one provider attempt,
  provider timeout below DB-derived stop grace, stop grace below outbox lease,
  and retry cadence limited to future scheduling.

## Verification evidence

- Real PostgreSQL circuit chain: three failures, open circuit, ordinary claim
  denied, reconcile/schedule/execute `probe-circuit`, exactly one released claim.
- Real 027→028 migration: current leased orphan plus running attempt becomes
  retryable with bounded error evidence; all five runtime policy defaults are
  absent; 028→027→028 round trip passes.
- Production-shaped resume: 1,117 Structure ranges and 231 Quote shards resume
  after the last numeric checkpoint; stale epoch and mismatched proof fail.
- Non-cooperative sync and stalled async terminal tests prove both service loops
  return through terminal grace and emit immediate observable evidence.
- Final full `make test-m1`: **3,984 passed, 1 skipped, 1 expected xfail** in
  1,541.27 seconds. The large 120k/166,926-row performance/golden group completed
  naturally rather than being killed by an arbitrary wrapper timeout.
- Complete PostgreSQL control-plane file passes, including independent
  connections proving certifiers cannot claim before the final receipt and can
  claim immediately after it. Focused revision 026/028/029/030, runtime,
  scheduler, checkpoint, circuit and qualification suites pass.
- Ruff check for every changed Python file, format check for Plan 209 files,
  `git diff --check`, `make planning-status`, and `make climb-check` pass.
- The bounded-operator climb gate now selects the revision-030 bounded status
  regression instead of a removed revision-029 JSON test; exact node collection
  and the complete climb suite pass.
- Independent review rounds closed all three HIGH findings. The only subsequent
  MEDIUM (missing role-local grace-expired emission) was fixed with a regression;
  no CRITICAL/HIGH finding remains.

## Production boundary

The first coordinator canary exposed a second consumer of the same growth-bound
epoch JSON: `operational_snapshot()` still used `SELECT *`. The canary was
stopped immediately, restored to digest `sha256:bd21b2d…33df`, and returned to
its original stopped state; its Machine ID and non-image config hash remained
unchanged. Structure, Quote and qualification were never mutated.

Revision 030 closes the whole active path rather than adding another timeout.
Qualification facts now append to `m1_qualification_epoch_facts`; the epoch row
updates only scalar counters/state, restart replays facts in pages of 500, and
terminal certificate construction materializes history once in memory while
the database comparison reads fixed scalar projections. A 501-fact PostgreSQL regression proves the active
arrays remain empty, the append-only relation contains 501 rows, and a fresh
process reconstructs identical ordered state across the 500+1 page boundary.
`operational_snapshot` selects only fixed columns. At that revision-030 point,
the full M1 gate reported **3,963 passed, 1 skipped, 1 expected xfail** in
1,544.58 seconds.

The rollout image at commit `463e44b7` is superseded. Production is restored to
the pre-canary Machine topology. Revision 030 was later applied transactionally
without changing a Machine; the first revision-030 image at commit `13f2a9a6`
is now also superseded before deployment by the revision-031 compaction fix.

Coordinator Machine `e82d1220b2d138` and qualification Machine
`876077f0274598` remain stopped; Structure `683e46ea500dd8` and Quote
`4d895231f66748` remain started on the pre-Plan-209 runtime. Production is
revision 031. The failed coordinator canary was restored to digest
`sha256:bd21b2d…33df`, its original stopped state and unchanged non-image config;
no other worker was mutated.

Post-030 read-only audit found 6,572 normalized facts across 111 epochs, but the
active 2,500-fact epoch still retained its three legacy growth arrays. Revision
031 now validates normalized count, ordinal, payload, fact-ID and contained
recovery parity before clearing those arrays, then installs database CHECK
fences against regrowth. The full PostgreSQL suite also exposed the old
`qualification_store` compatibility writer; it now appends/verifies normalized
facts and writes only scalar epoch state. Alembic connect/statement/lock waits
are centrally bounded, and formal service shutdown no longer invents a missing
worker lease as three seconds.

The fresh final `make test-m1` after the provider/proof-loop follow-through
passed **3,984 tests, one skip and one expected xfail in 1,541.27 seconds**.
`make planning-status` has no drift and `make climb-check` passes. The complete
changed-file Ruff/format and `git diff --check` gates pass.

Revision 031 was applied transactionally with zero Machine changes. The
post-migration audit found 6,572 facts, zero legacy arrays, three compaction
constraints, one append-only trigger and zero scalar count mismatches. Image
`sha256:548cc4c37843dc239f2f04bfd7bbf0802fa2eb49d2f90cd3ef3fa96f9d0243f9`
is superseded before Machine rollout because the operation-round audit changed
executable bytes.

The subsequent image from commit `2b7c7817a5ab`, immutable Fly digest
`sha256:413e2b4ae3e9cb2989441bdf83a388a2c1ac185312ee49be915046552bad5d30`,
is also superseded before Machine rollout. Reverse factory-chain audit found its
formal Quote R2 client still inherited SDK retries, and proof-loop/watchdog/
alert lifecycle fixes changed executable bytes again. No Machine received that
image.

Commit `eb7e24c9` produced immutable digest `sha256:f5d624d1…be2e`. A
controller-first canary was necessary because the old controller's durable
decision semantics did not match the new deterministic replay gate. The canary
failed closed at `database-role.namespace-unsafe: active-search-path`: the
operation-round change had removed the post-connect bootstrap even though
production evidence already showed Supabase Session Pooler drops libpq startup
options. The same controller Machine was immediately restored to prior digest
`sha256:698344a7…3086`, observe-only mode, empty allowlist and identical
non-image config hash. No sibling Machine was updated; `f5d624d1…be2e` is
superseded.

A separate read-only SSH role-verifier preflight on the 256MB live controller
also exhausted its cgroup and caused Fly to restart the same Machine with exit
137. This did not mutate image or config, but it invalidated the continuity
window. Preflight now runs fixed-round catalog proof from the operator host and
never starts an extra interpreter inside a constrained live Machine.

After the bounded dual-layer repair and all connection-factory test doubles
were aligned with the real psycopg interface, the final complete M1 gate passed
**3,986 tests, one skip and one expected xfail in 1,554.43 seconds**. The
120k/166,926 scale and independent-oracle group completed naturally with no
orchestration timeout.

The next rollout must commit the bounded dual-layer bootstrap repair, build and
verify its exact immutable amd64 image, canary the unchanged controller, and
re-run the 1,800/90/90 observe-only gate from its new lease epoch against
revision 031.
Then roll coordinator → Structure range → Quote batch → qualification on the
same Machine IDs. Live proof must reach Structure certification, Quote
admission, Quote certification and opportunity publication without zero restart
or orphan attempts. Only then may a new 86,400-second qualification window
begin.
