---
phase: 05.6-self-healing-structure-production
plan: 08
subsystem: m1-m2-feed
tags: [quote, http, supervisor, recovery]
status: in_progress
requires:
  - phase: 05.6-self-healing-structure-production/07
    provides: strict production health
provides:
  - isolated Quote producer can hydrate the HTTP opportunity feed from a durable certified run
affects: [m1-production-closure, m2-live-data, opportunity-api]
key-files:
  modified:
    - src/polyarb/http/arbitrage.py
    - src/polyarb/daemon/quote_worker.py
    - src/polyarb/daemon/main.py
    - tests/m1-perception/test_arbitrage_opportunities_http.py
key-decisions:
  - "HTTP may hydrate only an already-certified SQLite projection; it never collects CLOB quotes."
  - "Hydration is bounded to one second and cached atomically in the parent runtime."
requirements-completed: []
updated: 2026-08-09
---

# Plan 05.6-08 Summary: Supervised Quote feed handoff

## Root cause

The production supervisor isolates Quote collection from the HTTP parent. The
child produced certified Quote runs and 17 candidate opportunities, but the
HTTP parent had no resident `QuoteWorkerRuntime`; `/arbitrage/opportunities`
therefore returned 503 despite durable source evidence.

## Delivered

- Extracted a read-only certified-feed loader from the Quote worker.
- On a cold HTTP cache, the endpoint uses the loader under a one-second budget
  and atomically restores the certified feed into its resident runtime.
- Main wires this loader only when public Quote collection is enabled; no CLOB
  request, order, wallet, signer, or structure mutation is introduced.

## Verification

- Red/green HTTP contract test proves a parent with an empty runtime serves a
  feed published by an isolated producer.
- Related Quote wiring and opportunity endpoint suite: 67 passed.
- Changed-file Ruff: passed.

## Remaining production gate

Deploy this handoff fix and obtain repeated public HTTP 200 evidence with the
durable candidate identities. Then complete the separate bounded cleanup-child
repair before declaring continuous-operation acceptance.
