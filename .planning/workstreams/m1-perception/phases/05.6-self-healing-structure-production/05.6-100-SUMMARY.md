# 05.6-100 Summary — Project durable Structure control-plane state

## Delivered

- Extended Postgres-only `operational_snapshot` with Structure range and
  certifier state counts, oldest retryable range age, latest certified manifest
  identity, and isolated `structure:current:shadow` identity.
- The projection uses bounded read-only queries with the same statement timeout
  as the existing Quote operator view. It never consults SQLite or legacy
  current-pointer truth.
- Existing HTTP control-plane behavior remains fail-closed: unavailable reads
  return the typed unavailable response instead of an empty healthy snapshot.

## Verification

- Real Postgres snapshot contract proves the complete empty Structure shape.
- Artifact/exporter/worker/Postgres/CLI/HTTP/migration regression: 47 passed;
  Ruff passes.

## Boundary

This exposes evidence but does not schedule workers. Controlled scheduling,
designated cloud migration, kill-after-upload takeover proof, and sustained
online soak remain required for M1 acceptance.
