# 05.6-197 Summary — Cloud Sampler Service Contract

## Delivered

- Added cloud soak start/sample/verify CLI paths backed by the Postgres ledger.
- Added direct authenticated Fly Machines HTTPS reads so the production image
  does not require `flyctl`.
- Added explicitly enabled, signal-aware `cloud-soak-serve`; failed reads or
  writes stop the service and create a verifier-visible gap.
- Declared an isolated no-volume/no-HTTP `soak_sampler` Fly process alongside
  the unchanged five formal data-plane roles.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_soak_cli.py -k cloud_soak -q`
- `uv run pytest tests/m1-perception/test_control_plane_deployment_templates.py tests/m1-perception/test_control_plane_soak_cli.py -k 'cloud_soak or deployment' -q`
- `uv run ruff check src/polyarb/cli_control_plane.py tests/m1-perception/test_control_plane_soak_cli.py tests/m1-perception/test_control_plane_deployment_templates.py`

## Next

Apply revision 017, supply the sampler-only Fly API read token, create the
formal cloud baseline, and start the independent sampler before retiring the
local LaunchAgent window.
