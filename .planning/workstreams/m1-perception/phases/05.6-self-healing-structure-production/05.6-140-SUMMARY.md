# 05.6-140 Summary — Wire bounded source lanes into the worker service

## Delivered

- Wired the control-plane service to construct eight independently identified
  `TransactionalStructureSourceWorker` lanes inside one bounded source pool.
- Each lane owns a Gamma client while sharing the existing R2 client and
  bucket; event cursor seriality remains a consequence of its single durable
  runnable input.

## Verification

- RED: CLI factory test failed because the source pool was not wired.
- GREEN: focused factory test and Ruff passed.
- `uv run pytest tests/m1-perception/test_transactional_structure_source_worker.py tests/m1-perception/test_transactional_control_plane_scheduler.py tests/m1-perception/test_control_plane_cli.py -q` — 33 passed.

## Boundary

This is local wiring only; the lane image has not yet been deployed. No
Telegram, production L1/L2, or pointer behavior changed.
