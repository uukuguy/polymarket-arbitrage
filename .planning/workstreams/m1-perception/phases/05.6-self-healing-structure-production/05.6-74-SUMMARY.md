# 05.6-74 Summary — Independent control-plane operator route

## Delivered

- Added `GET /perception/control-plane`, backed only by the durable Postgres
  control-plane projection.
- The route returns a typed `503 control-plane-read-unavailable` whenever the
  DSN is absent, the database is unavailable, or the bounded read times out;
  it never substitutes an empty healthy SQLite response.
- Daemon wiring creates the Postgres repository only from an explicit
  `POLYARB_SUPABASE_DB_DSN`; Makefile exposes `make perception-control-plane`.

## Verification

- HTTP available/unavailable contract, PostgreSQL repository contract, and
  Makefile contract: 8 passed.
- Ruff and `compileall` pass; targeted diff check passes.

## Production status

No production migration or pointer mutation occurred. Until migration 009 is
applied to the designated authority, the route truthfully returns unavailable.
