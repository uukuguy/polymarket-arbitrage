# 05.6-126 Summary — Recover scoped transactional circuits

## Delivered

- Added additive revision 014 job-scoped circuit authority.
- Made retry delay deterministic (15/30/60/120/240/300 seconds), threshold-aware,
  fenced, and atomically coupled to incident/outbox creation.
- Connected durable worker success to fenced recovery: circuit reset, matching
  incident resolution, immutable `recovered` event, and recovery alert intent.
- Added bounded open-circuit facts to the Postgres-only operator snapshot.

## Verification

- RED confirmed revision 014 was absent before implementation.
- Focused real-Postgres, worker, CLI, rollout and HTTP contracts: 78 passed.
- `make docs-m1-check` and `make planning-status` passed.

## Boundary

This is local control-plane implementation and local rollout evidence only.
It did not migrate, deploy, start, or fault any cloud workload. Named isolated
non-production identities are still required for cloud shadow, worker-loss, and
24-hour soak evidence.
