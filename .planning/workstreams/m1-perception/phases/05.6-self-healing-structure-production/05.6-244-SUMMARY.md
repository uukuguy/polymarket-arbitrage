# 05.6-244 Summary — Typed CLOB 429 recovery

## Outcome

- Added `ClobRateLimitError`, a machine-readable CLOB 429 boundary that retains
  neither SDK response body nor request/token text.
- The real PostgreSQL `quote-batch` worker records the typed failure through its
  existing durable retry/circuit/incident path; no private 429 backoff or outer
  timeout was introduced.
- Reclassified `clob-429` from an unimplemented legacy-shaped production canary
  to a truthful `disposable-exact-image` attack on the new DAG.
- Added real-worker commissioning, isolated harness, Make entry, exact CLI/help
  tests, climb gates, learning FAQ, and H-055 state.

## Verification

- Client test proves a real SDK `PolyApiException` with HTTP 429 is translated,
  preserves only the in-memory cause, and exposes no provider body/token text.
- Real migrated PostgreSQL proof shows the failed epoch has
  `ClobRateLimitError`, no R2 object and no Quote receipt; policy-due recovery
  of the same immutable input writes one 1/1 receipt and resolves the incident.
- OrbStack remained the only Docker runtime and the isolated database was
  removed after verification.

## Boundaries

- No legacy SQLite chaos evidence was imported as new-DAG proof.
- No production provider/DB, Fly/SSH/deploy, Machine, canary, Colima, global
  Docker context, or qualification mutation occurred.
- Gamma timeout and malformed-page remain the only unimplemented provider
  attack contracts for the PostgreSQL production DAG.
