# 05.6-225 Summary — Eliminate control-plane schema revision drift

## Outcome

- Control-plane schema identity now has one source of truth:
  `CONTROL_PLANE_SCHEMA_REVISION` in `schema_contract.py`.
- Database role administration, rendered rollout evidence, real PostgreSQL
  fixtures and the revision-026 adversarial authority matrix all consume that
  authority instead of copying a migration number.
- A dedicated contract requires the authority to equal Alembic's sole head, so
  adding a migration without updating the reviewed production identity fails
  immediately.

## Root cause

- H-040 cycle 44 passed eight of nine deterministic production-enablement gates.
  Its integration gate failed because `test_026` said it required the current
  repository revision but still migrated its database to stale revision `031`;
  the live role administrator correctly required revision `036`.
- This was dormant orchestration drift, not a commissioning harness failure.
  Every new database/interruption/Make test in H-040 passed during that cycle.

## Verification

- The stale test first reproduced `database-role-admin.revision-mismatch` on
  real PostgreSQL 16, then passed using the shared authority.
- Schema-head, adversarial authority, rollout and role-admin suites pass (38
  cases), and Ruff passes on all affected files.
- The deterministic climb integration gate now includes the explicit
  schema-authority/head test before the historical migration matrix.

## Next boundary

- Preserve cycle 44 as falsified evidence and rerun H-040 on the repaired exact
  HEAD. A successful successor may confirm the hypothesis; cycle 44 is not
  rewritten or discarded.
