# 05.6-154 Summary — Materialize Structure directly from sealed event evidence

## Delivered

- Terminal event pages now atomically complete a source window and enqueue its
  fenced materializer job without creating mutable exact-ID market batches.
- The materializer can expand sealed nested event markets into a v2 Structure
  bundle, deterministically inheriting the parent neg-risk group identity.
- Existing v1 two-stream source windows remain accepted for replay.

## Why

- A live Gamma window contained thousands of frozen market batches. Some
  members closed before their second fetch, so the safe quarantine path worked
  but no full window could converge online. Event responses already contain the
  Structure market fields, which removes that mutable second-read interval.

## Verification

- RED/GREEN bundle contract proves event-only materialization and v2 identity.
- Real PostgreSQL contract proves terminal event receipt releases exactly one
  materializer without market jobs.
- Focused Gamma/source/materializer/scheduler/PostgreSQL/CLI suite (139 tests),
  Ruff, and diff check passed before staging deployment.
