# Plan 05.6-168 Summary — Scoped retry-fault and alert delivery

## Delivered

- Added a default-off exact-key, finite staging retry fault for Structure and Quote.
- Added acceptance-run scoped alert claim SQL so historical outbox rows cannot be selected.
- Added CLI and Makefile entrypoints for a scoped alert worker.
- Added focused worker, CLI, real-Postgres, and alert delivery tests plus learning doc 74.
- Isolated the shared acknowledgement argument so retry-only fault runs do not invoke
  the unrelated R2 crash guard; the CLI integration regression covers this boundary.
- Made the intentional retry signal return a durable `retryable` worker outcome after
  persistence, preventing Fly restart policy from resetting the finite fault counter.

## Verification

`uv run pytest tests/m1-perception/test_control_plane_cli.py tests/m1-perception/test_transactional_structure_worker.py tests/m1-perception/test_transactional_quote_worker.py tests/m1-perception/test_transactional_alert_delivery.py tests/m1-perception/test_control_plane_postgres.py -q`

`uv run ruff check src/polyarb/cli_control_plane.py src/polyarb/control_plane/postgres.py src/polyarb/control_plane/alert_delivery.py src/polyarb/control_plane/structure_worker.py src/polyarb/control_plane/quote_worker.py`

## Next

Deploy staging image, execute three retry attempts plus recovery for one Structure and one Quote job, run scoped alert delivery, then start the 24-hour soak evidence window.
