# 05.6-172 — 事务性 soak 证据核心

## 完成内容

- 新增 `soak_evidence` 纯函数模块，用 JSONL 记录控制面快照和精确的 Fly machine
  状态；每条记录附带 canonical SHA-256，篡改或结构漂移会 fail-closed。
- 验证器以首条为历史基线：要求 API 持续可用、machine identity 不变且全部 `started`、
  采样时间严格递增且无超长间隔，并拒绝之后新增的 expired lease 或 open circuit。
- 完全是读路径的证据产物，不改动 job、receipt、queue 或生产 machine。

## 验证

- `uv run pytest tests/m1-perception/test_control_plane_soak_evidence.py`：6 passed。
- `uv run ruff check src/polyarb/control_plane/soak_evidence.py
  tests/m1-perception/test_control_plane_soak_evidence.py`：通过。

## 后续

- 将 recorder 接入 `cli_control_plane` 与 Makefile，随后在 staging 五角色拓扑上建立
  第一条 baseline，并以定时只读采样累积 24 小时验收窗口。
