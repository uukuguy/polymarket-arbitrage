# 05.6-85 Summary — Fenced Structure generation admission

## Delivered

- Added additive control-plane inputs for one immutable Structure generation
  and its bounded normalization ranges.
- `enqueue_structure_generation` authenticates a stable R2 bundle/identity,
  creates deterministic `structure-normalize` jobs, and persists each exact
  component/start/end range with its input digest.
- `structure_range_spec` restores those inputs for lease takeover entirely
  from Postgres. No replacement path needs SQLite's legacy/current pointer.

## Verification

- Real PostgreSQL contract admits events and markets ranges, then verifies the
  recovered first range has the original bundle key/digest and boundaries.
- Migration, Structure/Quote artifact, and fenced control-plane suites pass
  23 focused cases with changed-file Ruff and diff checks.

## Boundary

No Structure worker, range receipt, manifest certification, shadow pointer,
production migration, R2 write, or legacy pointer mutation is present yet.
