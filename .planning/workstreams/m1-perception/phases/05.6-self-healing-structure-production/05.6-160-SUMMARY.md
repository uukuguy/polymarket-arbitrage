# 05.6-160 Summary — Reject duplicate Quote legs across v3 shards

## Delivered

- Made v3 Quote admission reject every repeated YES token across authenticated
  market shards, including byte-for-byte identical duplicate market rows.
- Added a regression that proves the invariant at the shard aggregation
  boundary; duplicate legs can no longer be silently coalesced.

## Verification

- `uv run pytest tests/m1-perception/test_transactional_quote_admission.py -q`
- `uv run ruff check src/polyarb/control_plane/quote_admission.py tests/m1-perception/test_transactional_quote_admission.py`

## Next

- Continue staging the bounded source window through manifest admission,
  Structure ranges, certification, and Quote admission before the restart test.
