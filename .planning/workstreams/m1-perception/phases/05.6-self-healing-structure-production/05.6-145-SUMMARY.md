# 05.6-145 Summary — Fence quarantined source-window jobs at claim time

## Delivered

- `claim_job` now excludes `structure-fetch` jobs whose persisted source window
  is quarantined; it still admits generic/circuit `structure-fetch` work that
  has no source-page input.
- Real PostgreSQL contract proves an orphaned market job cannot preempt the
  next admitted source window.

## Verification

- RED: an orphaned market job from a manually quarantined window was claimed
  before the successor event job.
- GREEN: successor event job is claimed instead.
- Focused source worker, scheduler, CLI and PostgreSQL suite passed (76 total),
  with Ruff passing.

## Boundary

This removes reliance on manual staging cleanup for future quarantined source
windows. It does not alter existing receipts, publication pointers, Telegram,
or production L1/L2.
