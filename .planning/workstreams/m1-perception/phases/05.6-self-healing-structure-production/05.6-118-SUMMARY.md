# 05.6-118 Summary — Release Quote admission from Structure certification

## Delivered

- Added revision 012 immutable `quote-admit` inputs downstream of certified
  Structure generations.
- A successful fenced Structure certifier transaction now creates the manifest
  and the one matching Quote-admit job together.
- The admission fence verifies generation key, bundle key, and bundle digest
  before it can atomically create existing Quote batches and certifier work.
- Added the worker-side authenticated bundle-to-leg derivation kernel.

## Verification

- Real PostgreSQL proves complete Structure certification releases the matching
  admission lease and it can create immutable Quote batch input.
- Migration 012, source/Structure/Quote worker contracts, admission contracts,
  and Ruff pass (77 focused tests).

## Boundary

The admission bridge is not yet in the persistent scheduler. It has not been
deployed, started, shadowed, or used to change any pointer.
