# 05.6-151 Summary — Bound repeated exact-batch integrity retries

## Delivered

- A scoped exact-ID batch with `PaginationIntegrityError` is retried twice.
  If its third fenced lease still fails, the source window is quarantined as
  `StructureSourceExactBatchIntegrityError` and a later cadence can obtain a
  new scope.
- An explicit closed-member response continues to quarantine immediately;
  timeouts and event-stream errors retain normal retryable incident behavior.

## Why

- Live staging reproduced `exact-id market response identity set mismatch` on
  the same frozen batch through four attempts. Its cardinality contract could
  not become true through unbounded retry, so it otherwise prevented terminal
  materialization forever.

## Verification

- RED/GREEN worker contract proves the third exact-batch integrity failure
  quarantines the window.
- Closed-member and ordinary-timeout contracts remain covered.
- Full focused source/scheduler/CLI/PostgreSQL suite and Ruff passed.
