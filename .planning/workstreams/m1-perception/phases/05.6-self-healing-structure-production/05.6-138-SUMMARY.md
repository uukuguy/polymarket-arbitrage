# 05.6-138 Summary — Calibrate the scoped source capacity to live evidence

## Delivered

- Raised the bounded default from 1,000 to 5,000 exact-ID market batches.
- Retained the hard cap: a pathological source cannot expand into an unbounded
  fetch; the live event universe is now inside the accepted bound.

## Verification

- Staging terminal event traversal established 210 sealed event pages and
  demonstrated the former `1,000 × 25` bound is insufficient.
- `uv run pytest tests/m1-perception/test_transactional_structure_source_worker.py -q` — 8 passed.
- Focused Ruff check — passed.

## Boundary

This is a staging Structure source capacity calibration. It does not alter
Telegram, production L1/L2, or any publication pointer.
