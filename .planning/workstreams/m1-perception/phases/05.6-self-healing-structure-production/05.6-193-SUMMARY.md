# 05.6-193 Summary — Quote-to-Structure opportunity input contract repair

## Outcome

Corrected the transactional opportunity certifier's current-input lookup. A published
`quote:<structure-digest>` generation is admitted by the durable
`structure:<structure-digest>:quote-admit` job, not by a fictitious
`quote:<structure-digest>:admit` job. The reader now derives and verifies that
Structure generation identity before loading frozen Quote batch receipts.

## Verification

- Real Postgres contract tests: 2 passed, including the complete pointer → admission
  → frozen-batch chain.
- Opportunity, scheduler, and CLI regression tests: 37 passed.
- Ruff format and lint checks passed.

## Operational effect

The formal coordinator can now turn the existing certified Quote pointer into an
R2-authenticated, atomically published opportunity projection instead of silently
returning `idle`.
