# 05.6-86 Summary — Read-only legacy Structure shadow exporter

## Delivered

- Added the sole SQLite-reading boundary for transactional Structure shadow:
  `read_legacy_structure_bundle` accepts only the exact published legacy
  current generation and requires its comparison receipt digest.
- It exports all six frozen components with stable SQL ordering and produces
  the `StructureBundleIdentity` consumed by canonical bundle serialization.
- Missing current binding, non-published source, absent receipt, SQLite error
  or binary data fails closed with a typed refusal; none of these cases create
  a control-plane job or mutate a pointer.

## Verification

- A temporary SQLite contract proves exported current data canonicalizes into
  a bundle and proves non-current source refusal.
- Structure/Quote artifact, real PostgreSQL control-plane and Alembic suites
  pass 24 focused cases; changed-file Ruff/diff checks pass.

## Boundary

The exporter does not upload/admit or execute a Structure range. The next
slice consumes the already admitted R2 bundle in a fenced worker, with no
SQLite access after admission.
