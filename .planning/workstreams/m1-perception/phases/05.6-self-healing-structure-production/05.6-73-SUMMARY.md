# 05.6-73 Summary — Durable control-plane operational projection

## Delivered

- Added `PostgresControlPlane.operational_snapshot()` as a bounded read-only
  transaction over M1 jobs, attempts, incidents, and alert outbox facts.
- The projection applies a five-second Postgres statement timeout and fixed
  caller limit (`1..100`); it has no SQLite dependency.
- Added a real PostgreSQL contract proving expired leases, fenced work,
  incidents, and pending alert intent are visible in one operator snapshot.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_postgres.py -q` → 5 passed.
- `uv run ruff check src/polyarb/control_plane tests/m1-perception/test_control_plane_postgres.py` → passed.
- Targeted `git diff --check` → passed. Repository-wide diff check remains
  intentionally excluded because the pre-existing user-owned
  `.superpowers/sdd/task-7-brief.md` has a trailing blank-line diagnostic.

## Production status

This is the repository read primitive only. It is not yet exposed as HTTP,
does not mutate production Postgres, and does not change publication pointers.
