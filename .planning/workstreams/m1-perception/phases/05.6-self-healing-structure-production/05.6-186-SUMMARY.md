# 05.6-186 Summary — formal opportunity API boundary

## Delivered

- Added `/perception/opportunities` to the independent transactional API.
- The route invokes only a control-plane projection reader and validates bounded
  cursor pagination.
- A missing projection or unavailable Postgres reader returns structured 503;
  it never falls back to SQLite or represents unavailable truth as zero.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_api.py -q` — 2 passed.

## Next

Add the Postgres projection migration and certify it from the immutable current
Structure and Quote artifacts before enabling this route in the formal runtime.
