# 05.6-89 Summary — Fence Structure range receipts atomically

## Delivered

- Added `m1_structure_range_receipts` to the control-plane authority.  Each
  receipt binds the admitted bundle/range identity to its immutable range
  artifact, digest, record count, and commit time.
- Added `record_structure_range`, which advances the job checkpoint, writes
  the generic checkpoint receipt, records the typed Structure receipt, and
  closes the matching attempt in one Postgres transaction.
- The operation validates the admitted range before writing, is idempotent
  for the same immutable artifact, and rejects a stale lease epoch before it
  can overwrite a replacement worker's result.

## Verification

- Focused adversarial receipt and migration contracts pass.
- Full Structure/control-plane regression: 27 passed.

## Boundary

The durable effect is complete, but no Structure worker yet downloads,
validates, normalizes, uploads, records, and finishes a range end-to-end.
That worker is the next slice; it must call this API only after the
authenticated bundle and output artifact contracts succeed.
