# Plan 05.6-201 Summary — immediate runtime-policy replay surface

## Outcome

Plan 01 now has a documented, read-only operator entry point for replaying
immutable cloud soak observations and identifying the first live-policy break.
The Make target requires an explicit scoped Postgres DSN, passes only the
opaque `run_id` to the CLI, and contains no Fly, deployment, job, incident, or
recovery mutation wiring.

## Task commits

- Task 1 — `43e2af46`: define runtime evidence contracts.
- Task 2 — `afa55cd5`: persist fenced runtime evidence.
- Task 3 — `be123ac2`: replay runtime policy immediately.
- Task 4 — this plan-closure commit: add the Make entry, regression contract,
  and this summary.

## Verification

- `uv run pytest tests/alembic/test_022.py tests/m1-perception/test_control_plane_runtime_models.py tests/m1-perception/test_control_plane_runtime_policy.py tests/m1-perception/test_control_plane_postgres.py tests/m1-perception/test_control_plane_cli.py tests/m1-perception/test_makefile_contract.py -q`
  — exit 0; 251 selected tests reached 100% with no failures.
- `uv run ruff check tests/m1-perception/test_makefile_contract.py` — passed.
- `make -n runtime-policy-replay run_id=run-a` — resolved exactly to the
  read-only CLI invocation after the run-id and scoped-DSN checks:

  ```text
  test -n "run-a" || (echo "usage: make runtime-policy-replay run_id=<run-id>" >&2; exit 2)
  test -n "$POLYARB_SUPABASE_DB_DSN" || (echo "ERROR: explicitly export scoped DSN" >&2; exit 2)
  uv run python -m polyarb.cli_control_plane runtime-policy-replay --run-id "run-a" --json
  ```

- `make help | rg runtime-policy-replay` — target is listed with its
  read-only/no-Machines-mutation description.
- Running without a scoped DSN fails closed with exit 2 and only:

  ```text
  ERROR: explicitly export scoped DSN
  make: *** [runtime-policy-replay] Error 2
  ```

## Real failed-run replay

The worktree intentionally lacks the ignored `.env`, so the main checkout's
`.env` was sourced only to provide the scoped DSN for the prescribed read-only
command. No Fly, deploy, job, incident, or recovery mutation command was
invoked. The exact safe JSON result was:

```json
{"first_breaking_at":"2026-08-23T16:22:21.027503+00:00","max_gap_seconds":302.337234,"reason_codes":["lease.expired"],"sample_count":191,"status":"BREAKING"}
```

The replay exited 0 and identified the expected first breaker at
`2026-08-23T16:22:21.027503+00:00` with reason `lease.expired`.

## Deviations

The exact plan-wide Ruff command was run and returned three pre-existing
findings outside this Task's ownership: two E501 lines in
`tests/m1-perception/test_control_plane_watchdog.py` (lines 89–90) and one
I001 import-order finding in
`tests/m1-perception/test_transactional_structure_source_worker.py:1:1`.
They were left untouched. The owned Make contract test is Ruff-clean.

## Next-plan interfaces

Plan 02 (`05.6-202`) consumes the runtime models and transactional persistence
from Tasks 1–2, and can use `runtime-policy-replay` as a read-only historical
regression oracle. Its task-local reporters should emit heartbeat, progress,
success, and failure facts from each production job; the periodic sampler must
remain only a backstop. No deployment or production mutation is authorized by
this plan.
