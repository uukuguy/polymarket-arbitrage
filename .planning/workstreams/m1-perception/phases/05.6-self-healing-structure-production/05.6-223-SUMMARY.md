# 05.6-223 Summary — Emit cleanup-safe stale-owner attack proofs

## Outcome

- `StaleOwnerCommissioningAdapter` drives the real eight-node takeover/fence
  primitive through the cleanup-safe runner's seven ordered lifecycle stages.
- Every stage receipt names a real original/replacement attempt; recovery names
  the actual replacement `job.succeeded` event and verification names the exact
  business postcondition.
- Each node writes an exclusive intent, seven stage files and a final proof with
  release/config/node/attack identity through `run_disposable_attack`.

## Negative evidence boundary

- The stale transaction is not allowed to append a “rejected” business event,
  because that would grant the stale owner the write authority under test.
- Detection and cleanup instead query PostgreSQL independently and require zero
  old-epoch succeeded attempts and zero old-epoch success events.
- The replacement commit occurs only after cleanup and its final attempt is
  re-read to prove ownership by the replacement epoch.

## Verification

- 58 runtime and commissioning-runner tests pass, including eight complete
  real-PostgreSQL stale-owner proof directories.
- Ruff and Pyright pass; no direct lease mutation, outer timeout, production
  database write, Machine action or provider call was added.
- H-039 must be confirmed by climb after this commit.

## Next boundary

- Add an exact-image harness that creates/migrates/drops one isolated disposable
  database per proof and exposes a uniform Make entry. Only its release-bound
  artifacts may enter the final 66-proof commissioning envelope.
