# 05.6-177 — 自动 24 小时 soak verdict

## 完成内容

- 安装用户级、非 RunAtLoad 的 LaunchAgent
  `com.polyarb.m1-transactional-soak-verifier`，从安装时起 86,460 秒后运行本地
  `control-plane-soak-verify`。
- PASS 时以原子 rename 保存 `staging-transactional-soak-verdict.json`；失败时保留
  `staging-transactional-soak-verdict-failure.log`，不伪造 PASS 或吞掉 fail-closed 原因。
- 此任务不接触 Fly、数据库、R2、job、receipt 或 Telegram；它仅验证 sampler 已累积的
  immutable JSONL。它不是常驻程序。

## 运维

- 检查：`launchctl print gui/501/com.polyarb.m1-transactional-soak-verifier`
- 移除：`launchctl bootout gui/501/com.polyarb.m1-transactional-soak-verifier`
- 若睡眠或采样延迟造成 >900 秒 gap，任务将产出失败日志；必须重新建立 baseline，而不是
  手工修改证据。
