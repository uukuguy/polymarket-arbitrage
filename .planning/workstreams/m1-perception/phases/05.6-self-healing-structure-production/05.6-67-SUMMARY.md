# 05.6-67 Summary — Structure publication read deadline

## Delivered

- A Structure publication slice now derives one absolute monotonic deadline.
- Market source reads and bounded parent resolution use a deadline-aware SQLite
  connection with a bounded busy wait and progress handler.
- A deadline interruption returns the last durable publication checkpoint
  instead of escaping to the child watchdog; no cursor or count advances for
  the interrupted current chunk.

## Verification

- `uv run pytest tests/m1-perception/test_structure_generation_publication.py -k 'preserves_prior_checkpoint or expired_read_budget' -v` — 2 passed.
- `uv run ruff check src/polyarb/perception/structure_publication.py src/polyarb/storage/sqlite_store.py tests/m1-perception/test_structure_generation_publication.py` — passed.

## Remaining

- Extend the same cancellation guarantee to the remaining publication-side
  reads/certification and to every health authority read before production
  deployment.
