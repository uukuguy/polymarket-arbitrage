# 05.6-123 Summary — Add transactional alert delivery leases

## Delivered

- Added additive revision 013: fenced lease ownership for durable alert outbox
  rows, preserving all prior intent and delivery history.
- Added atomic outbox claim/finish APIs plus append-only per-attempt delivery
  receipts.
- Added an isolated alert-worker service entry and a third Fly-app rollout
  template. Dashboard alert intent now receives a durable `dashboard-visible`
  delivery receipt;
  unsupported channels retry without blocking Structure or Quote work.
- Updated preflight, migration target and rollout artifact contract to revision
  013, and added learning document 68.

## Verification

- RED: revision 013 and alert-delivery API/worker contracts failed before
  implementation.
- Real PostgreSQL proves one fenced dashboard delivery and immutable receipt.
- Focused Alembic, repository, worker and scheduler tests pass with Ruff and
  targeted diff checks.

## Boundary

No external provider sender is configured or deployed. A dashboard receipt
proves durable control-plane visibility only; it does not claim Telegram or
webhook delivery.
