# 05.6-174 — Quote R2 实机接管验收

## 完成内容

- 在 staging 的精确 Quote job `quote:13dc…9485:batch:69` 执行一次受确认的
  `R2 uploaded → process loss → lease expiry → alternate worker takeover`。
- 故障机已恢复为常规 Quote pool 命令，五角色拓扑全部 `started`；Quote backlog 回到零。
- 建立只读 transactional soak JSONL baseline 与恢复后样本；采样器独立检查 control API、
  精确 machine identity 和相对 baseline 的失败计数。
- 新增教学文档 76，说明 R2 不等于完成、epoch fencing、唯一 receipt 和 soak 判读。

## staging 证据

- fault worker 明确记录 `KeyboardInterrupt: intentional staging crash after verified R2 upload`。
- 接管 worker 记录该 job `outcome=succeeded`；数据库查询确认 `attempt_count=2`、
  `lease_epoch=2`、`m1_quote_batch_receipts` 为 1 行（487 responses）。
- 恢复后 control API：`available`、`expired_leases=6`、`open_circuit_count=74`，Quote
  `unfinished_count=0`。

## 后续

- 以最多 900 秒间隔继续采集 staging soak 证据直到覆盖 24 小时；期间不再注入故障。
- Fly secrets write 角色修复后，单独完成 scoped Telegram receipt，不重放历史 outbox。
