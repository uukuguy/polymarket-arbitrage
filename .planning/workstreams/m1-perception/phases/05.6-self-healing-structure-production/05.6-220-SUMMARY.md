# 05.6-220 Summary — Execute eight real PostgreSQL normal turns

## Outcome

- A reusable disposable commissioning fixture now completes all eight runtime
  node types through their real transactional domain APIs.
- Every normal-turn result references three independent PostgreSQL facts: its
  succeeded attempt, matching authoritative `job.succeeded` runtime event and
  causally bound business postcondition.
- Successor and pointer evidence is matched to the exact producer job or
  generation; an unrelated row in the expected table cannot satisfy the turn.

## Runtime boundary

- The fixture derives progress and attempt behavior from the central runtime
  policy and does not add an experiment-wide timeout.
- It targets disposable migrated databases only. It grants no production,
  provider, Machine, SSH or Docker authority and does not manufacture final
  commissioning stage receipts.
- Exact-image execution must isolate a database per node/attack so queued
  successor jobs cannot contaminate a later claim.

## Verification

- The runtime-coverage suite exercises all eight node types against real
  PostgreSQL and checks the three fact authorities.
- Ruff, Pyright, diff checks, planning-status and the H-037 climb gate are the
  required completion gates for this plan.

## Next boundary

- Split preparation from terminal completion so directed attacks can hold a
  real lease at the fault boundary.
- First prove stale-owner rejection and current-owner recovery across all eight
  nodes, then reuse that boundary for heartbeat, progress, worker-exit and
  retry-budget attacks.
