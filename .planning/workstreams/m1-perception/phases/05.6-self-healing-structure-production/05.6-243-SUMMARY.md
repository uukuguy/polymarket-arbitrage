# 05.6-243 Summary — Fail-closed CLOB batch coverage

## Outcome

- Found and fixed a real Quote integrity defect: a CLOB response that omitted a
  requested token previously became a successful batch containing
  `missing-book`, allowing incomplete provider coverage to reach certification.
- `TransactionalQuoteBatchWorker` now raises
  `IncompleteQuoteBatchCoverageError` before R2 upload or database receipt when
  received book count is smaller than the deduplicated requested token count.
- Reclassified `clob-missing-leg` from an unimplemented production canary to a
  truthful `disposable-exact-image` attack on the new PostgreSQL `quote-batch`
  node. The old SQLite `candidate` chaos path is not accepted as evidence for
  this DAG.
- Added the real-worker, real-migrated-PostgreSQL adapter, isolated harness,
  Make entry, exact CLI/help tests, climb gates, learning FAQ, and H-054 state.

## Verification

- Unit proof: 0/1 CLOB coverage raises the typed error, writes no object and no
  Quote receipt, and records one retryable incident.
- Commissioning proof: the failed epoch leaves no partial artifact; the shared
  retry due-at admits the same immutable batch after cleanup; 1/1 coverage
  writes exactly one receipt, closes the circuit, and resolves the incident.
- The isolated harness passed against a fully migrated PostgreSQL database on
  OrbStack and removed its owned database and roles.

## Boundaries

- No evidence was imported from the legacy SQLite perception chaos surface;
  its component identity and absent `config_id` binding do not match the new
  production DAG.
- No production provider/DB, Fly/SSH/deploy, Machine, canary, Colima, global
  Docker context, or qualification mutation occurred.
- Gamma timeout, Gamma malformed response, and CLOB 429 remain fail-closed
  production-canary contracts until equivalent new-DAG control surfaces exist.
