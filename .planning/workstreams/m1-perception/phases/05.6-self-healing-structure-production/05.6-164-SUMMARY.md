# 05.6-164 Summary — Quarantine unprovable neg-risk source children

## Delivered

- Restored the legacy fail-closed handling for an active Gamma event that
  explicitly claims standard neg-risk but omits `negRiskMarketID`.
- Its active child is excluded only when that child repeats the unprovable
  `negRisk=true` claim without a group identity; no group is inferred and the
  market-truth validator remains unchanged for every other source shape.
- Added the v3 event-page regression for the exact parent/child data shape
  observed in staging.

## Verification

- `uv run pytest tests/m1-perception/test_transactional_structure_source_bundle.py tests/m1-perception/test_transactional_structure_worker.py tests/m1-perception/test_transactional_control_plane_scheduler.py -q`
- `uv run ruff check src/polyarb/control_plane/structure_source.py tests/m1-perception/test_transactional_structure_source_bundle.py`
- `git diff --check -- src/polyarb/control_plane/structure_source.py tests/m1-perception/test_transactional_structure_source_bundle.py`

## Next

- Deploy staging-only and verify a previously retrying source materializer
  crosses page 20 and eventually yields a certified Structure generation.
