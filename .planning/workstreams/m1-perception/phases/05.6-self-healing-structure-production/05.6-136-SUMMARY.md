# 05.6-136 Summary — Exercise scoped batch materialization end to end

## Delivered

- Converted the real Postgres/R2 source-materializer contract to complete an
  event-rooted exact-ID market batch, preserving the batch identity through
  terminal materialization and normalizer admission.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_postgres.py -k
  'real_source_window_materializer_turn_admits_normalizer_work' -q` — passed.

## Boundary

Test-only reinforcement of the staging Structure control-plane path; no
Telegram or production L1/L2 configuration changed.
