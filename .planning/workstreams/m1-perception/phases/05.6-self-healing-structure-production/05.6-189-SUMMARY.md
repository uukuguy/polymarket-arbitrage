# 05.6-189 Summary — atomic transactional opportunity publication

## Delivered

- Added `publish_opportunity_projection`, which verifies the current certified
  Quote pointer and both certified manifests before writing a complete immutable
  projection and atomically advancing `opportunity:current`.
- Added real Postgres coverage proving a published page is readable and an
  invalid subsequent publication leaves the prior pointer untouched.

## Verification

- `uv run ruff check src/polyarb/control_plane/postgres.py`
- `uv run python -m compileall -q src/polyarb/control_plane/postgres.py`
- `uv run pytest tests/m1-perception/test_control_plane_postgres.py -k opportunity_projection_publish -q` — 1 passed.

## Next

Implement the R2-backed certifier that supplies complete authenticated rows to
this atomic publisher, then attach it to the formal control-plane worker.
