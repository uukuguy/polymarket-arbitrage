# 05.6-114 Summary — Fence source-window bundle admission

## Delivered

- Added revision 011 source-window bundle receipts.
- A terminal market page now releases exactly one `structure-materialize` job.
- `admit_structure_source_bundle` validates the sealed source receipt digest,
  fences the materializer lease, writes an immutable bundle receipt, and
  atomically admits the existing Structure range/certifier graph.
- Added `TransactionalStructureSourceMaterializer`, which reads only durable
  R2 page artifacts and Postgres source receipts before publishing its bundle.
- Updated the migration roundtrip and read-only preflight contract to revision
  011.

## Verification

- Real PostgreSQL proves source window → authenticated R2 pages → current
  materializer lease → immutable bundle receipt → runnable normalizer range.
- Alembic 011 forward/reverse contract, source-materializer tests, existing
  control-plane tests, Ruff, manual validation, and planning status pass.

## Boundary

This completes the local durable Structure source chain through range admission.
The persistent scheduler, named cloud deployment, shadow comparison, and
continuous fault/soak acceptance remain separate, unstarted rollout work.
