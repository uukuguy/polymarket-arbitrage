# 05.6-247 Summary — recoverable partial attacks and truthful runtime identity

## Outcome

- Fixed complete commissioning to run transactional workers under the explicit
  `control-plane` runtime identity rather than inheriting the legacy daemon
  default and its unrelated `/scan` secret contract.
- Partial attack directories are atomically preserved under deterministic
  `failed-attempts/.../attempt-NNNN` paths before a clean retry starts. Complete
  or identity-invalid proofs are never archived or overwritten.
- Nested primary+cleanup failures now expose only safe owned reason codes and
  exception class names; provider bodies and generic exception messages remain
  absent from operator output.

## Verification

- TDD covers partial-attempt archival, nested safe diagnostics and Make-level
  control-plane identity declaration; Ruff and Pyright pass.
- A real OrbStack PostgreSQL 16 diagnostic resume skipped 40 independently
  verified proofs, archived two failed Gamma attempts, completed the remaining
  matrix, and assembled 66/66 attacks, 8/8 nodes and one real E2E lineage with
  `status=ready`.
- The diagnostic bundle binds the previous `aba7a9a4` release. A fresh full
  bundle was then regenerated from zero against exact commit `e17f1359` and
  independently verified ready: 66/66 attacks, 8/8 nodes, real E2E lineage,
  `qualification_may_start=true`, evidence digest `67f9cd4f84a27c5f`.
- Exact-HEAD commissioning regression passed. Climb H-057 cycle 62 passed all
  nine gates at 100%: integration passed 183 selected tests, CLI passed 26,
  and `disaster_pattern=false`.

## Boundaries

- No empty-secret bypass was introduced; the process receives its truthful
  control-plane role.
- No production provider/DB, Fly/SSH/deploy, Machine, live canary,
  qualification mutation, Colima, or global Docker context mutation occurred.
