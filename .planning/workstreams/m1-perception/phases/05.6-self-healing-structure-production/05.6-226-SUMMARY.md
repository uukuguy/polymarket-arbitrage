# 05.6-226 Summary — Execute eight stale-owner proofs in the exact image

## Outcome

- Fresh `make test-m1` passed 4,167 tests, one skip and one expected xfail in
  1,627.14 seconds without an outer timeout.
- OrbStack built and pushed the non-deploying runtime image for exact HEAD
  `a608ee98c803c26465f4bb66b3eab822a9075357`.
- The registry image digest is
  `sha256:50f1d47284ddd19e297d59e4d91b51c6402e657e415e47702d78a4233c4294f2`;
  independent inspection proved linux/amd64, user `polyarb`, the exact revision
  label and local image ID `sha256:c5e7f8bc...`.

## Exact-image commissioning evidence

- The immutable image ran the complete eight-node stale-owner harness against
  a port-unpublished PostgreSQL 16 container through a shared loopback network
  namespace.
- It created and migrated one isolated database per node, wrote 72 append-only
  lifecycle artifacts and returned eight completed proofs.
- Independent verification required one proof per production node, exact
  release/config identity and `cleanup_verified=true` on every proof.
- The test cluster reported zero `m1_commissioning_%` databases and zero
  migration-created capability roles after the run. The evidence tree contains
  no DSN, password, token, secret or provider body.

## Runtime ownership and cleanup

- Every Docker command explicitly selected OrbStack or its already verified
  Unix endpoint. The user-global context remained `orbstack`; Colima was neither
  installed nor referenced.
- The disposable PostgreSQL container was stopped and removed after the zero-
  residue query. Its test-only data is not recoverable; all required receipts
  were already persisted in the tracked evidence directory.
- Temporary Fly registry credentials were isolated from the user's Docker
  config and moved to Trash after the pull. No Fly deployment or Machine action
  occurred.

## Next boundary

- The stale-owner obligation is complete for all eight nodes on this exact
  release/config, but the 66-proof commissioning matrix is not complete.
- Add heartbeat-outage, progress-stall, worker-exit and retry-budget exhaustion
  adapters to the same isolated harness before any production canary or new
  healthy-effective qualification epoch.
