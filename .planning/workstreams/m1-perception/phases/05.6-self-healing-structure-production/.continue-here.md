---
context: phase
phase: 05.6-self-healing-structure-production
task: 211
status: in_progress
last_updated: 2026-08-29T06:25:32+08:00
---

# Resume Plan 05.6-211

## Current truth

- Production database is revision 033.
- Coordinator `e82d1220b2d138` runs exact release `65cee5b9`; controller is
  stopped. Structure, Quote and qualification remain on `282480ec`.
- One source circuit action remains, but the old budget clock incorrectly
  blocks it until `2026-08-29T12:42:48.877848Z`.
- Plan 211 revision 034 and executable repair are local only. Do not spend the
  final probe before both are live.
- Three user-owned `.superpowers/sdd/` files are dirty and must never be staged,
  edited or committed by this work.

## Verified local repair

- reconciliation compares absolute circuit `next_probe_at` directly;
- circuit budgets count actions and reject every nonzero cooldown;
- revision 034 clears only derived circuit-budget deadlines;
- real PostgreSQL migration and runtime fault matrix pass;
- full M1: 4,030 passed, one skipped, one expected xfail in 1,574.08 seconds;
- changed-file Ruff/Pyright and diff gates pass.

## Next command

```bash
git status --short
```

Stage only Plan 211 files, commit with its SUMMARY, then build and verify an
exact amd64 revision-034 image. Stop the coordinator before migration; execute
only the exact source probe in an isolated 512MB auto-removed Machine. Fly
commands must unset `FLY_API_TOKEN` and `FLY_ACCESS_TOKEN`; never emit env
values or provider bodies.
