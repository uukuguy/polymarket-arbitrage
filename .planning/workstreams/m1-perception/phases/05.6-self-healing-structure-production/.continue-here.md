---
context: phase
phase: 05.6-self-healing-structure-production
task: 3
total_tasks: 3
status: paused
last_updated: 2026-08-31T00:25:26.490Z
---

# M1 Mainline Handoff

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Whole-window restart | Contained faults previously caused qualification/soak restarts. | blocking | Require an identity-bound Structure → Quote → Opportunity successor proof; preserve epoch unless a truth-breaking fact invalidates it. |
| Global Docker mutation | Colima/global-context risk affected unrelated projects. | blocking | Use OrbStack only; check `docker context show` before image work and never change global context. |
| Unbounded diagnostics | Interrupted pytest and `flyctl ssh` reads remained alive for hours. | advisory | Use bounded owned commands; terminate only clearly owned orphan diagnostics. |

<current_state>

M1 self-healing is integrated into `main` at `34e5d5d6`. The feature branch and
worktree are removed. The recurring Quote certifier repair is `3a70cd9f`; H-063
has two 100% local confirmations including the isolated PostgreSQL recovery
matrix. Production proved `Structure 2263… → Quote 2263… → Opportunity` and
resumed eligibility on the same epoch with zero expired leases and zero circuits.

`STATE.md` and the prior handoff describe obsolete revision-034 work. Use JOURNAL
Sessions 387–389 and this file for current context.
</current_state>

<completed_work>

- Durable Quote-generation receipt fan-in repairs skipped certifier wakeups.
- Production successor proof: `runtime-v36-rolling-resume/proof.json`.
- Scoped control-plane typing and daemon pool regression pass Pyright/tests.
- M1 merged to main; local state preserved in remote archive branches.
</completed_work>

<remaining_work>

- No active M1 implementation or merge task remains.
- Choose a new bounded objective only from fresh production evidence.
</remaining_work>

<decisions_made>

- Qualification is rolling/product-local, not an all-or-nothing 24-hour restart.
- OrbStack is the sole Docker context; Colima is excluded.
- Archives pushed: `origin/archive/main-local-preintegration` and `origin/archive/m1-worktree-residue`.
</decisions_made>

<blockers>

- None. Untracked `.wrangler/` is local material: do not delete or commit without scope.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable state.
2. `.planning/JOURNAL.md` Sessions 387–389 — production and integration facts.
3. `docs/learning/105-市场全集与可执行报价必须使用两个时钟.md` — Structure/Quote clocks.
4. `.planning/threads/market-observation-architecture.md` §2.84 — lost-wakeup handling.

## Infrastructure State

- Main: `34e5d5d6`; M1 feature worktree removed.
- Docker: `orbstack` only.
- No owned background commands are running.

<next_action>

Start with: `/gsd-resume-work --ws m1-perception`
</next_action>
