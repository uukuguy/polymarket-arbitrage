---
context: phase
phase: 05.6-self-healing-structure-production
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-09-04T19:18:49.994Z
---

# Event Research Workbench — Resume Here

<current_state>

The deployed Dashboard now links Event titles from Structure, Quote Coverage, and Analysis into `/business/events/[event_id]`. The event page and API endpoint are live and use one current Quote pointer plus its exact parent Structure generation, but this is only a partial delivery.

Task 1 (truthful candidate economics) is complete in `e8636b79`. The deployed event detail authority (`9a3ab9b7`) and workbench (`87af8d3f`) lack the planned quote-leg aggregation/legs endpoint, strict frontend decoder, source-aware focus behavior, comprehensive fixtures, and final information-dense presentation. Do not describe this workbench as complete.

</current_state>

<completed_work>

Completed Tasks:

- Task 1: truthful candidate economics — Done (`e8636b79`). Analysis now presents theoretical gross profit, capital required, and gross ROI, never the former ranking proxy as dollars.
- Task 2: fenced Event research detail — In progress. Current-lineage detail route and repository reader are live (`9a3ab9b7`), but group-leg evidence and full contract coverage are absent.
- Task 3: workbench and main-table links — In progress. The page is live (`87af8d3f`) and links are live (`b883c8d6`, `991e6c13`, `88bacc6a`, `f443a785`), but the remaining UX/data-contract work is not done.
- Task 4: production validation — Partially done. `make smoke-control-plane-prod`, live event API reads, and authenticated browser navigation passed before pausing.

</completed_work>

<remaining_work>

- Task 2: Write red tests, aggregate distinct observed/executable/non-executable/missing legs, and add `GET /perception/business/events/{event_id}/groups/{group_id}/legs` with current-lineage/ownership fencing and bounded cursor.
- Task 2: Sort candidates by actual `gross_profit_usd`, then ROI; show coverage gaps after positive candidates according to the implementation plan.
- Task 3: Replace the permissive/minimal Event detail TypeScript decoder with the strict `m1.event-research-detail.v1` contract; reject malformed lineage, non-finite economics, invalid counts, and unavailable payloads that carry facts.
- Task 3: Implement `from`-aware initial emphasis and focused-group behavior, then restore an information-dense yet readable research layout. Do not let the recent compact main-page rewrites become the final dashboard design.
- Task 3: Add Dashboard link/decoder/workbench contract tests.
- Task 4: Run focused and full verification, deploy the API via the current Fly rollout config, and validate the same authenticated browser session with Playwright.
- Documentation: add the required plan SUMMARY and an incremental learning document before claiming this plan finished.

</remaining_work>

<decisions_made>

- The three business pages have distinct jobs: Structure identifies operationally active market context; Quote Coverage reports input health rather than opportunities; Analysis displays combined-price research candidates. Event detail gives their shared investment-research context.
- The Event workbench uses a single backend envelope instead of browser-side joins of paginated endpoints, so its facts share one lineage.
- `gross_profit_usd = (1 - bundle_cost) * max_bundle_size`; `capital_required_usd = bundle_cost * max_bundle_size`; `gross_roi_bps = gross_profit / capital * 10,000`. These are theoretical, research-only values: fees, slippage, simultaneous execution, resolution, and settlement are not assessed.
- Do not add raw market/order-book storage or a persistent detail projection. Reads must remain bounded (200 rows) from existing projections; Supabase capacity was a real production constraint.
- The stopped coordinator must not be restarted as part of this work.

</decisions_made>

<blockers>

- No human blocker is pending. The existing authenticated browser session should be reused; do not open a fresh browser instance that forces the user to log in again.
- Preserve unrelated dirty files: `.planning/JOURNAL.md`, `.wrangler/`, and `output/` are not this plan's changes.

</blockers>

## Required Reading (in order)

1. `docs/superpowers/specs/2026-09-04-event-research-workbench-design.md` — approved product/data contract and why the old tables were not useful.
1. `docs/superpowers/plans/2026-09-04-event-research-workbench.md` — exact remaining task and verification sequence.
1. `src/polyarb/control_plane/postgres.py` (`business_event_detail`, `business_analysis_page`, `business_quote_coverage_page`) — current partial authority and remaining repository seam.
1. `src/polyarb/control_plane/api.py` — route ordering/parameter-validation pattern.
1. `dashboard/lib/business-research.ts` and `dashboard/app/business/events/[event_id]/page.tsx` — deliberately minimal current frontend contract.
1. `.planning/workstreams/m1-perception/STATE.md` — production invariants and workstream status.
1. `.planning/threads/market-observation-architecture.md` — chain-truth discipline before adding any fail-soft path.

## Critical Anti-Patterns (do NOT repeat these)

- Presenting `executable_economic_value` as money: it was a rank proxy, not profit. → Derive the documented bundle economics at read time and retain the research-only warning.
- Calling price extremity or quote coverage an opportunity: it creates noisy false investment signals. → Quote Coverage stays a health view; only candidate/group economics can be a research lead, and only the existing opportunity projection can say Certified.
- Client-side joining separately paginated Structure, Quote, and Analysis reads: it breaks lineage truth. → Extend the fenced backend authority; never compose those feeds in the browser.
- Materializing new raw/detail copies in Supabase: it risks another quota incident. → Use bounded current-generation reads only and retain the 200-row cap.

## Infrastructure State

- Control-plane API: `https://polyarb-control-api.fly.dev`; last smoke test passed with `make smoke-control-plane-prod`.
- Dashboard: Vercel auto-deploys pushed `main`; current authenticated route is `https://polymarket-arbitrage-jiangwen-su-s-projects.vercel.app`.
- Browser: reuse `/Users/sujiangwen/.codex/skills/playwright/scripts/playwright_cli.sh` and its existing session. Do not create a new profile/window.
- Fly deploy: `flyctl deploy --config output/control-plane-rollout-current/fly-control-api.toml --app polyarb-control-api --ha=false`.
- Database: capacity is constrained; do not add persistent raw/full projections. The coordinator is intentionally stopped and must remain stopped.

<context>

The user correctly rejected a bare Structure table and price-extreme Quote list as business intelligence. The current direction is a research workbench: show whether a live event has operational structure, usable quote evidence, and a group-level combined-price lead, together with the exact unknowns that prevent it from becoming a Certified opportunity. The page must have enough information density to investigate, but it must never imply executable profit from shallow top-of-book data.

</context>

<next_action>

Start with:

```bash
make planning-status
uv run pytest tests/m1-perception/test_control_plane_api.py tests/m1-perception/test_control_plane_postgres.py -k 'event_detail or event_group_legs' -q
```

Then add the failing lineage/leg-count tests before implementation. After backend work, run `make dashboard-typecheck dashboard-build`, deploy only through the stated Fly config, run `make smoke-control-plane-prod`, and inspect the same authenticated browser session.

</next_action>
