# 05.6-233 Summary — Prove source receipt gap recovery

## Outcome

- Added a disposable `source-receipt-gap` commissioning adapter for the
  `structure-materialize` node. It admits one real Structure source window,
  completes two of three required source-page receipts and deliberately holds
  the final fenced producer attempt open.
- The detector proves the public digest API returns the typed incomplete-input
  condition and names the exact missing job. While the gap exists, there is no
  materializer job, partial bundle, normalizer range, incident or recovery
  action. This is ordinary fan-in waiting, not a synthetic timeout or outage.
- Completing the withheld receipt through the normal fenced producer API
  releases exactly one materializer turn. Its real terminal transaction commits
  one complete source bundle, enqueues one normalizer range and records one
  succeeded materializer attempt.

## Harness and entry point

- Added an isolated, migrated PostgreSQL harness command fixed to
  `source-receipt-gap / structure-materialize`, plus
  `make m1-production-commissioning-source-receipt-gap`.
- The Make entry fails closed without an explicit loopback
  `POLYARB_CONTROL_PLANE_TEST_DSN`, evidence root, exact release and exact config.
  It has no production fallback, target-skipping flag or outer timeout.
- The climb deterministic-runtime profile now includes the adapter, isolated
  harness and exact Make argv contracts.

## Verification and boundary

- The adapter, harness cleanup, Make contract and climb-profile tests pass.
  Ruff passes for every changed Python file and Pyright reports zero errors for
  the two changed production modules.
- The broader grouped regression passed every test except the intentional
  exact-HEAD image guard while runtime inputs were still uncommitted. The guard
  correctly rejected a build from dirty source; it will be rerun after this
  commit.
- Injection and recovery use public control-plane writes only. Read-only SQL is
  used for negative and cardinality assertions; no lease timestamp, receipt,
  job, incident or action row is mutated privately.
- No production database, provider, Fly Machine, SSH, deployment, Docker build,
  canary or qualification window was touched. Docker context remained
  `orbstack`; no Colima operation or global context mutation occurred.
