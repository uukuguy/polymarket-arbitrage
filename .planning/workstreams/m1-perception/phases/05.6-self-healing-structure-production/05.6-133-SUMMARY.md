# 05.6-133 Summary — Terminally materialize scoped Structure batches

## Delivered

- On a terminal events page, the source worker reconstructs prior
  receipt-authenticated R2 event artifacts plus the current artifact, derives
  deterministic open-member batches, and supplies them to the same fenced
  terminal receipt transaction.
- The repository atomically records the event receipt, changes the window to
  `events-complete`, and admits all immutable exact-ID market jobs.
- Scoped market jobs require terminal/no-cursor receipts. Only the receipt that
  observes zero outstanding market inputs advances the window to `complete`
  and releases one materializer job. Legacy cursor inputs remain readable.
- A batch-count overflow quarantines the current source lease instead of
  retrying or falling back to global market-keyset pagination.

## Verification

- RED: terminal worker could not accept a batch size, and the first of two
  scoped market batches incorrectly released a materializer.
- GREEN: `uv run pytest tests/alembic/test_015.py tests/m1-perception/test_control_plane_postgres.py tests/m1-perception/test_gamma_client.py tests/m1-perception/test_transactional_structure_source_worker.py -q` — 99 passed.
- Focused Ruff on migration, Gamma client, control-plane source/repository,
  and all touched tests — passed.

## Boundary

The isolated staging worker remains stopped. This commit has not applied
revision `015` to staging or changed a publication pointer, Telegram, L1, or
L2 configuration. Cloud shadow/fault evidence is the next gate.
