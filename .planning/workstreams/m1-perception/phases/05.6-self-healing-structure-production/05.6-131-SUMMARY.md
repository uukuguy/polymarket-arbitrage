# 05.6-131 Summary — Persist event-rooted market batch inputs

## Delivered

- Added additive Alembic revision `015`; it stores nullable canonical
  `market_ids_json` and authenticated `market_ids_digest` alongside existing
  cursor inputs, preserving legacy staging facts without reinterpretation.
- Extended `StructureSourcePageSpec` with sorted, unique exact market-ID batch
  identity and SHA-256 digest.
- A terminal event receipt can now atomically persist its immutable scoped
  market batches in the same fenced PostgreSQL transaction. Replays authenticate
  existing batch inputs rather than replacing them.

## Verification

- RED: `tests/alembic/test_015.py` failed because revision `015` and the
  `market_ids` model input did not exist.
- GREEN: `uv run pytest tests/alembic/test_015.py tests/m1-perception/test_control_plane_postgres.py tests/m1-perception/test_transactional_structure_source_worker.py -q` — 44 passed.
- `uv run ruff check alembic/versions/015_m1_event_rooted_structure_source.py src/polyarb/control_plane/models.py src/polyarb/control_plane/postgres.py tests/alembic/test_015.py tests/m1-perception/test_control_plane_postgres.py` — passed.

## Boundary

This slice does not yet derive batches from R2 event artifacts or issue exact-ID
Gamma requests. The worker remains stopped in staging; no pointer, Telegram,
L1, or L2 state changed.
