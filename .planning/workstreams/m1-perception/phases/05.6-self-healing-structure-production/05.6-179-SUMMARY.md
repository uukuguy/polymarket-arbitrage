# 05.6-179 — Transactional soak forward-progress gate

## Completed

- Versioned newly created soak observations as `m1-transactional-soak-v2`.
- Added a self-authenticated cumulative succeeded-job count from the existing
  read-only control API snapshot.
- Kept historic v1 evidence valid, while requiring v2 counts to be
  non-negative, monotonic, and strictly advanced over a completed window.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_soak_evidence.py tests/m1-perception/test_control_plane_soak_cli.py -q` — 10 passed.
- `uv run ruff check src/polyarb/control_plane/soak_evidence.py tests/m1-perception/test_control_plane_soak_evidence.py tests/m1-perception/test_control_plane_soak_cli.py` — passed.

## Consequence

The next qualification window starts in a separate v2 JSONL file.  It will
prove durable completed work, not merely API/Machine liveness; the historical
v1 stream remains intact and is not relabeled or edited.
