# 05.6-161 Summary — Admit authenticated v3 source manifests

## Delivered

- Extended the fenced source-bundle admission contract to recognize
  `gamma-source-window-events-v3-sharded` alongside the established v1 and v2
  Gamma source identities.
- Converted the real-Postgres atomic materializer admission contract to v3,
  proving a sharded identity records the immutable bundle and enqueues its
  Structure work under the materializer lease.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_postgres.py::test_materializer_lease_atomically_records_bundle_and_admits_ranges -q`
- `uv run ruff check src/polyarb/control_plane/postgres.py tests/m1-perception/test_control_plane_postgres.py`

## Next

- Deploy only to staging and reclaim the preserved final materializer job;
  verify manifest admission, named ranges, certification, and Quote admission.
