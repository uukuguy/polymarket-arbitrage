# 05.6-71 Summary — Shadow source identity foundation

## Delivered

- Added the first read-only SQLite shadow boundary: deterministic identities
  for Structure publication checkpoints, Quote attempts, and incident events.
- Added a bounded SQLite URI read that extracts these source facts without
  acquiring a writer lock or issuing DML.
- Added idempotent projection into the fenced Postgres job authority. Source
  identities become `shadow:<kind>` jobs only; no manifest or pointer method
  is reachable from the shadow function.
- The identity namespace is explicitly `sqlite:` and contains no publication
  pointer operation; the shadow path therefore cannot switch production truth.

## Verification

- RED: `uv run pytest tests/m1-perception/test_control_plane_shadow.py -q --tb=short` failed because the shadow module did not exist.
- `uv run pytest tests/m1-perception/test_control_plane_shadow.py -q --tb=short` — 3 passed, including real PostgreSQL 16 double projection (`m1_jobs=3`, `m1_publication_pointers=0`).
- `uv run ruff check src/polyarb/control_plane/shadow.py tests/m1-perception/test_control_plane_shadow.py` — passed.

## Next

Extend this tested primitive into the operator CLI and status projection, then
run it against the existing authority database twice before any cutover.
