# 05.6-79 Summary — Recoverable Quote identity inputs

## Delivered

- Extended admitted Quote batch inputs with the exact frozen leg identity used
  to turn a CLOB book into a market-facing Quote: neg-risk market, market,
  condition, event, slug, YES token, and membership hash.
- A replacement worker can reload the persisted `QuoteBatchSpec` with the same
  leg mapping. It must not consult SQLite or a newer Structure generation to
  rebuild contextual fields after it acquires a replacement lease.
- Retained the token-only admission form for existing callers, but made the
  transactional worker path explicit: exactly one input form is accepted and
  duplicate YES-token mappings are rejected as ambiguous.

## Verification

- PostgreSQL control-plane regression includes an admission/reload takeover
  proof for contextual legs.
- Focused control-plane, migration, and artifact suites plus changed-file Ruff
  are run before this commit.

## Boundary

This is an admitted-input contract only. The next slice still has to claim a
Quote batch, fetch the frozen tokens, transform its frozen legs, upload the
artifact, and commit the receipt under the lease fence.
