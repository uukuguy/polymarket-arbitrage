---
phase: 05.6-self-healing-structure-production
plan: 30
subsystem: dashboard-producer-progress
status: completed
updated: 2026-08-10
---

# Plan 05.6-30 Summary: Expose live producer checkpoints in the incident console

- Added the read-only `/perception/producer-progress` endpoint. It returns the
  newest durable Quote collection and Structure snapshot checkpoints within
  the dedicated incident read lane.
- The M1 incident console now presents those checkpoints next to the producer
  lease and active incidents, so an operator can identify the active/stalled
  stage, timing evidence, and automatic retry path without SSH access.
- Added HTTP/console contract tests. Focused tests and Ruff pass; the full
  perception HTTP file was also started but the runner did not return a final
  completion status after its progress output, so it is not recorded as a
  passing verification claim.
