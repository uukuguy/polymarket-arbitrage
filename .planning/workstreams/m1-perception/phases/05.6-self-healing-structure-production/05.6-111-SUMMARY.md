# 05.6-111 Summary — Establish transactional Structure source-window authority

## Delivered

- Added additive Alembic revision 010: source windows, immutable page inputs,
  and authenticated source-page receipts.
- Added `StructureSourcePageSpec`; stable work identity is window/stream/
  ordinal while the opaque Gamma cursor remains authenticated input identity.
- Added fenced Postgres admission and page-recording operations. A successful
  event page transaction creates only the legal next event page; only the
  terminal event page releases the first market page.
- Updated deployment preflight to require all revision-010 tables.

## Verification

- New real-Postgres contracts prove cursor successor creation, events-to-
  markets handoff, and rejection of a stale lease attempting to advance a
  cursor.
- `uv run pytest tests/alembic/test_010.py tests/m1-perception/test_transactional_structure_source.py tests/m1-perception/test_control_plane_postgres.py tests/m1-perception/test_control_plane_cli.py -q` → 49 passed.
- Targeted Ruff and diff checks pass.

## Boundary

This creates durable source authority but does not yet call Gamma or upload a
source page to R2. The next plan slice must add the single-page worker and its
crash-after-upload-before-receipt takeover proof before this can replace the
legacy SQLite collector.
