# 05.6-75 Summary — Operator-safe shadow CLI

## Delivered

- Added `python -m polyarb.cli_control_plane shadow-sync` and `status`.
- `shadow-sync` reads bounded SQLite evidence, projects deterministic Postgres
  jobs, reports `pointer_mutations=0`, and has no pointer-switch code path.
- Added Makefile entries `control-plane-shadow-sync` and
  `control-plane-status`; both load `.env`, require the DSN, and never echo it.

## Verification

- CLI RED/GREEN: 2 passed.
- Existing shadow/repository suite was invoked; the direct CLI suite and Ruff
  passed after import normalization.
- Targeted diff check passes.

## Production status

The commands remain inert until an explicitly configured DSN is provided.
They are the required evidence path for additive migration and double shadow
projection; no production database or publication pointer was changed.
