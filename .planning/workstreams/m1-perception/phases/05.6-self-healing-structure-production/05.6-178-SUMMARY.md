# 05.6-178 — Quote retry circuit 实机恢复

## 完成内容

- 对自然出现的 Quote batch `…batch:25` 注入恰好三次 staging-only retry fault；没有重放
  历史 job 或人为插入数据。
- circuit 在第三次失败后打开；故障 worker 被停下并恢复为正常命令，另一 Quote pool 在
  half-open probe 后完成同一 job。两台 Quote pool 已恢复标准运行命令。

## staging 证据

- job 最终 `succeeded`，`attempt_count=4`、`lease_epoch=4`：前三次是
  `fly-control-plane-quote-batch-retry-fault` 的 retryable attempts，第四次由
  `fly-control-plane-quote-batch-2` 成功。
- incident 已 resolved，事件顺序为两次 `attempt-failed`、`circuit-opened`、`recovered`。
  circuit count 从 74→75→74。
- 此 scoped recovery 的 dashboard/Telegram outbox 各一条、均 pending、attempt_count 为 0；
  它们尚未被 alert worker 认领，历史 outbox 从未被选中。

## 后续

- 只剩 Fly secrets-write 恢复后的 scoped Telegram delivery receipt，以及当前自动化
  24-hour soak 的 PASS verdict。二者完成后将以事实文件运行最终 fault/soak verifier。
