# 05.6-232 Summary — Record one-release shared commissioning matrix

## Outcome

- Built and pushed the non-deploying exact-HEAD image
  `m1-runtime-v27-1b432ad5` through the command-scoped OrbStack build entry.
  No Machine or application deployment was requested.
- An independent pull through an isolated Docker authentication directory
  proved linux/amd64, non-root user `polyarb`, exact revision
  `1b432ad52279afff72ce10b4bec50daad2f04ea3`, image ID
  `sha256:8fa52d77fb59e84af63e1ffc8ddb91eff1e167406e7f106d9461694b2f25a73b`
  and registry digest
  `sha256:43f08545cd9dae2c6f9a9f41ea50444a9b11b07bcf7e4c928b11c273174a4897`.
- The image itself recomputed the observe-only, empty-allowlist qualification
  config as
  `sha256:f79dc5e1268d5b75f245e6c7d59f5483609ab6e32aff99fe25cb1cc12df70a90`.

## Shared attack evidence

- The same immutable image ran stale-owner terminal write, progress stall,
  retry-budget exhaustion, heartbeat outage and worker exit against all eight
  production transaction nodes: 40/40 proofs passed under one release/config.
- Every node lifecycle used an independently created and migrated PostgreSQL
  database, while all executions shared one port-unpublished PostgreSQL 16
  container. Attack containers shared its network namespace so the existing
  loopback-only test-DSN guard remained intact.
- Independent proof inspection found exactly eight nodes, five attacks, one
  release, one config, non-empty detector/action/recovery/business identities
  and `cleanup_verified=true` throughout. Qualification impact remained exact:
  heartbeat/stale-owner pause; progress/retry-budget/worker-exit block.
- The evidence tree contains 40 nine-file lifecycle directories (360 files)
  plus one execution manifest. A sensitive-value scan returned zero matches,
  and the preserved bytes matched the temporary source tree by SHA-256.

## Cleanup and boundary

- Before teardown, independent SQL queries returned zero
  `m1_commissioning_%` databases and zero migration-created runtime or
  qualification roles. A later cleanup command attempted to print those
  counts again but zsh interpreted the unquoted `count(*)` inside nested command
  substitution as a glob; those duplicate fields were blank and are not used
  as evidence.
- The explicitly named PostgreSQL container and internal Docker network were
  removed only after evidence preservation. Their disposable test data is not
  recoverable. The temporary registry authentication and source-evidence
  directories were moved to Trash; global Docker context remained `orbstack`.
- No production database, provider call, Fly Machine, SSH, deployment, canary
  or qualification window was touched. These 40 proofs are the shared subset,
  not the final 66-proof envelope: 22 node-specific disposable proofs and four
  production-canary proofs remain.
