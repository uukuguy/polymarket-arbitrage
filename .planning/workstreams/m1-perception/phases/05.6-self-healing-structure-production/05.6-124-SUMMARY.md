# 05.6-124 Summary — Deliver Telegram alert intents transactionally

## Delivered

- Added Telegram delivery to the isolated transactional alert worker, reusing
  the existing configured bot-token/chat-ID contract.
- Successful delivery records only `telegram:<message_id>`; the durable
  receipt contains no token, chat ID, or provider response body.
- HTTP, network, malformed response, and missing-credential cases preserve the
  intent as retryable delivery evidence without affecting data-plane work.
- When Telegram configuration is present, transactional worker failures create
  both dashboard and Telegram outbox intent; otherwise only dashboard intent
  is emitted.

## Verification

- RED: Telegram delivery worker constructor and sender path were absent.
- Focused worker contracts prove success message-ID receipt, HTTP retry,
  missing-credential retry, and configuration-gated channel selection.
- Ruff and targeted diff checks pass.

## Boundary

No Telegram request or cloud deployment has occurred. This is a local sender
contract; provider delivery and operational secret placement require the named
non-production alert app during rollout evidence collection.
