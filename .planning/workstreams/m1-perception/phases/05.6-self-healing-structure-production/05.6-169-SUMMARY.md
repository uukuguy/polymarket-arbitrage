# 05.6-169 — 事务型云端采集工作池

## 完成内容

- 新增 lease-fenced 的 role-local worker loop，并把服务拆为 `coordinator`、
  `structure-range`、`quote-batch`；旧 `all` 模式保留兼容。
- source admission 在同一 Postgres advisory-lock 事务中以 Structure=2000、Quote=512
  未完成任务高水位进行背压，返回显式 admission decision。
- 独立 Postgres-only operator API 增加 `queue_health`：每个池的 unfinished count、oldest age
  和 next runnable hint；提示不替代 fenced claim。
- Fly worker 模板和 Makefile 已声明对应角色入口。

## 验证

- `make planning-status`：无 drift。
- 目标回归：scheduler、CLI、Postgres、control API、deployment template、source worker 测试通过；Ruff 通过。
- staging API `/healthz` 在旧正常 worker 保持 200；未改生产 L1/L2、公共 pointer 或 Telegram 配置。

## staging 上线证据

- 本地 `linux/amd64` 镜像 `m1-worker-topology-amd64-522eb389@sha256:1de608e9…dcff`
  已部署到 worker 与 API staging apps。初次 ARM tag 被 Fly 正确拒绝，未影响运行机器。
- Worker topology：coordinator `48e3104c979578`；Structure pools `2879110cd63738`、
  `890e30b6d627e8`；Quote pools `080e9255b1ddd8`、`81597df969de18`。每个 pool 使用独立
  worker ID，API health check 200。
- 真实 API 样本显示 runnable `9327 → 8278`；Structure unfinished `3243 → 3043`；Quote
  unfinished `623 → 475`。协调器记录了 `backpressured:structure`，证实高水位在阻止继续灌入。

## 未完成的线上证据

- 仍需连续采集至少 15 分钟 start/mid/end 样本，确认队列深度和最老年龄不再反向增长；然后才
  开始 24 小时 soak。Quote retry circuit、R2 process-loss takeover 和 scoped Telegram delivery
  也仍是独立未完成项。
