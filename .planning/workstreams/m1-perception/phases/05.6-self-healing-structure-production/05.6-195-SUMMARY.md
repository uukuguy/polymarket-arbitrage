# 05.6-195 Summary — fenced opportunity certification job

Quote certification now atomically enqueues a deterministic
`<quote-generation>:opportunity-certify` job. The opportunity certifier claims that
job through `m1_jobs`, finishes it on publication/current state, and records a bounded
retryable wait when its immutable Quote input is not yet usable.

Verification: real Postgres Quote certification tests passed; 40 focused opportunity,
scheduler, and CLI tests passed with Ruff clean.
