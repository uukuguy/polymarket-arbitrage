# 05.6-191 Summary — R2-authenticated opportunity certifier

## Delivered

- Added `TransactionalOpportunityCertifier`, which reads only the current
  certified Quote batch snapshot, digest-authenticates each R2 batch object,
  computes complete positive opportunity rows, and invokes atomic publication.

## Verification

- `uv run ruff check src/polyarb/control_plane/opportunity_worker.py tests/m1-perception/test_transactional_opportunity_projection.py`
- `uv run pytest tests/m1-perception/test_transactional_opportunity_projection.py -q` — 2 passed.

## Next

Wire the certifier into the formal coordinator loop, migrate revision 016, and
verify a live formal opportunity page before the independent soak concludes.
