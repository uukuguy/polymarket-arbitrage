---
phase: 05.6-self-healing-structure-production
plan: 02
subsystem: production
tags: [l1, l2, l3, quote, self-healing, fly-io, polywatch]
status: in_progress
requires:
  - phase: 05.6-self-healing-structure-production/01
    provides: resumable Structure implementation and local recovery contracts
provides:
  - exact L1 and L2 production releases with restored strict health
  - bounded Quote retention and 60-second production trigger cadence
  - runtime-database L2 control reads and restored 5-market/10-token L3 evidence
affects: [m1-production-closure, m1-operator-runbook]
key-files:
  modified:
    - src/polyarb/daemon/quote_worker.py
    - src/polyarb/observation/l3_promote.py
    - src/polyarb/daemon/l2_main.py
    - src/polyarb/storage/l3_evidence_store.py
    - fly.toml
    - tests/perception/test_candidate_watcher.py
key-decisions:
  - "Certified Quote publication is the user-facing success boundary; bounded retention follows it."
  - "L2 candidate and promoter reads prefer bounded runtime-database queries over global REST history."
  - "Production Quote attempts are triggered every 60 seconds without relaxing the 300-second hard SLA."
  - "One prior certified feed may serve only while both Quote age and handoff age remain within 300 seconds; the response exposes both revisions."
  - "Plan remains open until written-spec review, implementation, and a natural production handoff proof are complete."
requirements-completed: []
updated: 2026-08-05
---

# Plan 05.6-02 Summary: Exact deployment and continuous production proof

**Status:** In progress. L1/L2 production is serving strict-health-passing data;
one explicitly surfaced availability-policy gate remains open.

## Delivered and deployed

- L1 release `f662b2cc9aff2e4702a365c8385c746639cc7338`, machine
  `6830939c0070d8`, image digest
  `sha256:1bb7117f562c3d886a5f112d4dac7e6aa2a6a5d4ea586ff591a9aeea8274b912`.
- L2 release `a9a282586c3f1e1a452aa96dee99fc9af1f47be0`, machine
  `85e647c4eed598`, image digest
  `sha256:60dd2631c274fc5efc581aa4199f2d0bf28b2eb53d41552035e651edbb2bc3c2`.
- Quote publishes its certified feed before retention/reconciliation work,
  deletes at most one historical run per pass, and uses a 60-second production
  trigger while retaining the unchanged 240-second warning and 300-second
  failure boundary.
- L2 coalesces pending top-of-book mirror writes, preserves real book depth
  through later price-only frames, reads candidate/promoter inputs through the
  least-privilege runtime database, converts PostgreSQL numeric/time values for
  SQLite selection, and waits for the real candidate set before promoter run 0.

## Production evidence

- L1 strict health returned HTTP 200; snapshot 781 published complete market
  truth (`117453` markets, `15975` events).
- The real operator path `make scan-arb-live min_edge_bps=0` returned run 1442,
  14 verified standard-neg-risk opportunities, and a first gross edge of
  550 bps.
- A 32-sample production observation covered runs 1434 through 1439. Every L1
  strict-health response was HTTP 200; steady Quote age peaked at 270 seconds
  immediately after restart and then at 200 seconds across normal cycles.
- L2 strict health returned HTTP 200 with 5/5 markets, mapping/desired/
  committed/evidenced all 10/10, and two independent 30-second durable samples.
- Resident Polywatch detected the exact deployment interruption at 13:34 UTC,
  sent the alert, observed recovery at 13:36, sent one recovery, and cleared
  its durable incident set.

## Verification

- Full repository pytest passed from final test tree with one expected xfail,
  one skip, and no failures.
- The scheduler lane test's redundant 0.3-second whole-cycle wall-clock
  assertion was independently reproduced as flaky. Its actual 50-millisecond
  reserved-lane bound, admission order, durable timeout facts, and 0.5-second
  hang guard remain; the corrected test passed 20/20 repeated runs.
- Changed-file Ruff and `git diff --check` passed.

## Open gate

A newly certified Structure currently invalidates the previous opportunity
feed until the matching Quote commits. Production measured one 93-second
refresh window in which L1 health remained HTTP 200/warn but the opportunity
endpoint returned 503. The recommended repair is an atomic double buffer:
continue serving exactly one prior complete revision while its Quote remains
within the existing 300-second SLA, mark it refreshing, and atomically replace
it with the new complete revision. Partial projections and data older than the
hard SLA remain forbidden.

The user approved the recommended direction. The self-reviewed written contract
is committed at `7fa4004` in
`docs/superpowers/specs/2026-08-01-m1-opportunity-feed-double-buffer-design.md`.
It is not implemented: the mandatory written-spec review, implementation plan,
TDD changes, and natural production handoff proof remain open. Plan 05.6-02 and
the phase therefore remain in progress.

The user subsequently approved the written spec. Self-reviewed implementation
plan commit `4340b41` decomposes the work into one shared feed policy, HTTP
serving semantics, health/Polywatch chain truth, and exact production crossing
proof. TDD execution and production proof remain open.

## Local double-buffer implementation

- `83fb405` adds the pure shared availability policy and exact 300-second
  Quote/handoff plus 50,400-second Universe boundaries.
- `05decdf` serves one previous certified feed with explicit `refreshing`,
  latest Structure ID, and served source/run IDs; durable opportunity lookup
  remains bound to the served revision. Structure completion age is a separate
  fact, preserving the existing taken-at market-truth metric.
- `cf8698b` makes strict health consume the same policy. Polywatch recognizes
  the new bounded warning, rejects incoherent response versions, and alerts if
  the opportunity endpoint is actually unreachable during refresh.
- Final-tree focused tests passed, then all 3,783 repository tests passed with
  one expected xfail and one skip. Changed-file Ruff, `git diff --check`,
  `docs-m1-check`, and `planning-status` passed.

The local implementation is complete. Exact deployment identity and one
timestamped natural Structure `N → N+1` crossing with no opportunity 503 remain
the only closure gate.

## Release 234 maintenance deployment and slice-budget remediation

- User-approved SHA `23ece7048e5ddd5e6b4d42a8d34492785ebda2c9`
  deployed as Fly release 234 and image
  `sha256:405f85e373e52f6f0258eb9bbe10a8b29b1c2de77795e43e6d3de7cfd6e3ea3c`.
  Both app and cron machines reached `started`; the app service check passed,
  `/healthz.releaseId` matched the exact approval, Quote stayed disabled, and
  generation reads stayed on `legacy`.
- Natural production evidence proved the failure-streak repair: a durable
  generation checkpoint reset `snapshot:failure_counter` from 5 to 0 without
  a manual restart, pointer write, or cleanup command. The resident cleanup
  runtime also committed repeated 500-row checkpoints with zero consecutive
  failures.
- The same observation exposed a separate non-acceptable loop. For ten
  minutes generation 868 repeatedly returned zero-row publication checkpoints
  while its durable cursor remained `issues|1835728`; cleanup advanced only
  about one 500-row transaction per minute and retained/reclaimable stayed
  9/7. Production therefore remained protected and unqualified.
- Root cause: every isolated scheduler child repeated Structure schema
  reconciliation on the multi-gigabyte database before entering its 45-second
  publication slice. The spent budget then produced a zero-chunk checkpoint
  that the parent treated as progress.
- Local fix `e436187` gives only resident scheduler children the daemon-startup
  schema-ready contract; standalone `structure-sync` retains its safe default
  initialization. A slice that executes no chunk now fails into the existing
  retry/alert path instead of refreshing false progress.
- Verification: four complete related test files passed; the final M1 JUnit
  contains 2,780 tests, zero failures/errors, two skips, and 1298.338 seconds.
  Canonical `ruff check src tests`, `docs-m1-check`, and diff checks passed.

Release 234 remains deployed but does not satisfy maintenance acceptance. The
schema-ready fix requires a new exact-SHA protected deployment and natural
proof that generation 868 advances beyond the frozen cursor while cleanup
continues fairly to retained <=2/reclaimable=0. Generation-read and Quote
cutovers remain forbidden until those gates pass.

## Classifier-v3 exact candidate conservation — local deploy candidate

- Design-to-implementation range `e8ee0fe..4657529` contains 27 commits. It
  replaces classifier-v2's complete-source diagnostic drift with a closed v3
  eligible-domain partition: 41,768 eligible candidates, 125,158 authenticated
  expected exclusions across seven exact reasons, and zero unresolved
  diagnostics out of 166,926 production candidates.
- Every projection checkpoint persists candidate/member/exclusion/diagnostic
  conservation, seven domain-separated exclusion chains, and its cursor in one
  CAS. Exact candidate identity is unique inside a chunk and across restart
  boundaries with O(page<=500) memory. A terminal v2 receipt is never mutated;
  the same frozen identity starts or resumes a deterministic v3 comparison.
- Sealed status and health expose v3 exclusion evidence only after receipt,
  identity, reconstruction, row-chain, independent frozen-source-count, and
  semantic validation. Stale v3 exposes only a fixed generic stale reason and
  aggregate diagnostic total/root; keyed labels, samples, class partitions,
  and projection/exclusion evidence remain suppressed when independent
  commitments do not exist.
- Scheduler defer receipts now bind the current authenticated classifier
  contract, or expected v3 when authority is unavailable. Release-238 durable
  immediate follow-up remains active only after bounded progress; terminal,
  no-work, and zero-progress paths do not busy-loop.
- The exact production-shaped proof locks
  `166,926 = 41,768 + 125,158 + 0` for limits 1/17/500 with an independent
  primitive byte-level oracle. The real traced SQL plans use bounded indexes,
  process at most 500 candidates per page, reject market/member scans and temp
  sorts, and preserve the unchanged performance thresholds. Learning document
  `docs/learning/50-Classifier-v3候选守恒.md` records the operator mental model.

### Final verification and review

- Independent complete-range review of `e8ee0fe..4657529` ended READY TO MERGE
  with no Critical, Important, or Minor findings after three production fixes
  and two mandatory-gate fixture corrections.
- Final focused gate collected 534 tests across projection, classification,
  end-to-end, performance, health, and scheduler files; exit 0. Its 120k
  classifier gate completed in 814.46 seconds, and all exact 166,926 limits
  passed.
- Final full M1 gate: `2887 passed, 1 skipped, 1 xfailed` in 1282.46 seconds,
  with zero failures/errors. `ruff check src tests`, `compileall`,
  `docs-m1-check`, hooks-path, and the 84-plan planning audit passed from the
  final implementation tree.
- Protected boundary: no deploy, Fly/config change, Structure read-mode or
  pointer change, Quote enablement, restart, manual cleanup, or production data
  mutation occurred. The live release remains on legacy reads with Quote off.

Classifier-v3 is locally qualified for an exact-SHA protected deployment. The
plan remains in progress until that SHA is explicitly approved and production
proves v3 sealing, authenticated health, immediate checkpoint continuation,
terminal no-busy-loop behavior, and the downstream protected read/Quote/
opportunity acceptance sequence.
