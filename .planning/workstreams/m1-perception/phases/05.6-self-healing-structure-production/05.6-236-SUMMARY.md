# 05.6-236 Summary — Make normalization corruption visibly terminal

## Outcome

- Added `StructureNormalizationInputInvalid`, carrying only the exact
  manifest-authorized artifact key and frozen range identity; provider bodies
  and arbitrary exception text never enter operator evidence.
- Added `finish_quarantined_with_incident`, an exact-lease PostgreSQL
  transaction that appends `job.terminal-failed`, quarantines job and attempt,
  opens one critical incident, and writes its Dashboard alert intent together.
- The terminal fact uses `validation.failed`, `failure.schema`, and
  `qualification_impact=blocked`. Corrupt immutable input does not enter the
  transient retry circuit or consume a retry budget.
- Added the `normalization-payload-corrupt` disposable adapter. It first
  certifies a good Structure generation and publishes its isolated shadow
  pointer, then supplies a digest-authenticated but schema-invalid v3 shard to
  the real transactional normalizer.
- The attack proves zero range receipt and zero manifest for the bad
  generation, a waiting certifier, an open critical operator incident, no job
  circuit, and the unchanged prior certified shadow pointer.
- Added isolated-database harness, explicit-DSN Make target, exact argv
  contracts, and climb production-enablement gates.

## Verification

- Worker typed-quarantine contract: passed.
- Real PostgreSQL atomic quarantine/terminal/incident/outbox contract: passed.
- Disposable corruption adapter and zero-residue harness: passed.
- Make help, DSN fail-closed, exact command contracts: passed.
- Ruff passed all touched Python; production-module Pyright reported zero
  errors.

## Boundaries

- Disposable loopback PostgreSQL and in-memory exact-byte object boundary only.
- No production provider or database request, Fly action, deployment, Docker
  build/context mutation, Colima, canary, or qualification operation occurred.
- Exact-image rebuild remains deferred until the remaining disposable attack
  matrix can share one release/config run.
