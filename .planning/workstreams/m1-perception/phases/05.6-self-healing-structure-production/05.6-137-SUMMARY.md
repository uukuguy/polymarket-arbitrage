# 05.6-137 Summary — Read event receipts while sealing a source window

## Delivered

- Added a receipt-authenticated, events-only control-plane reader for terminal
  source sealing before the window transitions to `events-complete`.
- Kept the materializer-facing full page reader terminal-gated, so scoped
  batch derivation no longer self-blocks while final publication remains fenced.
- Added Postgres and transactional-worker regression coverage.

## Verification

- `uv run pytest tests/m1-perception/test_transactional_structure_source_worker.py -q` — 7 passed.
- `uv run pytest tests/m1-perception/test_control_plane_postgres.py -k
  'terminal_event_page_atomically_admits_immutable_market_batches or
  only_last_scoped_market_batch_releases_materializer or
  real_source_window_materializer_turn_admits_normalizer_work or
  source_window_page_receipt_fences_cursor_and_advances_event_stream' -q` — 4 passed.
- Focused Ruff check — passed.

## Boundary

This corrects only the staging Structure source transaction sequence. Telegram
and production L1/L2 are unchanged.
