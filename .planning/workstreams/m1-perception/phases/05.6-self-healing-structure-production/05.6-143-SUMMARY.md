# 05.6-143 Summary — Bound terminal event batch planning

## Delivered

- Found in staging that terminal event page 210 held an expired `structure-fetch`
  lease while synchronously reading all prior R2 event artifacts.
- Bounded that read-only terminal batch-planning step to 90 seconds.  A timeout
  records the existing retryable incident path, allowing a later scheduler tick
  to reclaim the expired lease with a new epoch.

## Verification

- RED: terminal planner timeout contract failed before the option existed.
- GREEN: `uv run pytest tests/m1-perception/test_transactional_structure_source_worker.py -q`
  — 11 passed.
- Regression: source worker, scheduler, CLI and PostgreSQL control-plane tests
  — 74 passed.
- `uv run ruff check src/polyarb/control_plane/structure_source.py
  tests/m1-perception/test_transactional_structure_source_worker.py` — passed.

## Boundary

The timed operation only reads source artifacts.  It never writes a durable
receipt; delayed work therefore cannot commit after the fenced lease expires.
Telegram, production L1/L2, and their credentials remain untouched.
