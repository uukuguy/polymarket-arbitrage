# 05.6-237 Summary — Invalidate Structure parity conflicts

## Outcome

- Split `StructureParityMismatchError` from ordinary incomplete-generation
  waiting. Missing receipts remain a legal non-terminal barrier; complete
  generations that contradict frozen component counts do not.
- The real Structure certifier now atomically quarantines a parity-conflicting
  job/attempt, appends `job.terminal-failed`, opens one critical incident and
  Dashboard alert, and emits `integrity.conflict` with
  `qualification_impact=invalidated`.
- Generalized the existing lease-fenced quarantine transaction only across the
  two explicitly admitted semantic pairs: schema/blocked and
  integrity-conflict/invalidated. Neither path creates a retry circuit.
- Added the `structure-parity-mismatch` disposable adapter. It publishes a good
  prior shadow generation, completes a valid candidate range, injects a frozen
  count conflict in the isolated database, and runs the real certifier.
- The attack proves the candidate creates no authoritative manifest or Quote
  admission, the critical alert is durable, the retry circuit remains absent,
  and the prior shadow pointer remains authoritative.
- Added the isolated-database harness, explicit-DSN Make target, exact argv
  contracts, climb gates, and learning FAQ.

## Verification

- Typed certifier distinction and runtime detail registry: passed.
- Real PostgreSQL atomic quarantine variants: passed.
- Disposable parity adapter and zero-residue harness: passed.
- Make help/exact command and climb adapter contracts: passed.
- Ruff passed all touched Python; production-module Pyright reported zero
  errors.

## Boundaries

- Disposable loopback PostgreSQL and in-memory exact-byte object boundary only.
- No production provider/database request, Fly action, deployment, Docker
  build/context mutation, Colima, canary or qualification operation occurred.
- Exact-image rebuild remains deferred until the remaining disposable attack
  matrix can share one release/config run.
