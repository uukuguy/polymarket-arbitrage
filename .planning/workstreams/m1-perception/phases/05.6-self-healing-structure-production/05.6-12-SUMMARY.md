---
phase: 05.6-self-healing-structure-production
plan: 12
subsystem: m2-opportunity-read-health
status: in_progress
updated: 2026-08-09
---

# Plan 05.6-12 Summary: Certified opportunity-read fallback availability

- Production showed `/arbitrage/opportunities` correctly returned an
  authenticated, in-SLA fallback when the large SQLite live source-health read
  timed out, but three diagnostic failures made strict `/health` return 503.
- Strict health now keeps `last-known-authenticated` as `warn` at any failure
  count. The diagnostic remains visible with its failure count and error kind;
  it is not silently treated as live.
- Authentication-invalid source binding and the independent Quote/universe
  freshness gates remain fail-closed. Targeted fallback, unavailable-source and
  invalid-binding tests pass with changed-file Ruff and diff gates.
- Remaining production gate: exact Fly deployment and a live fallback response
  that is available with warning rather than a synthetic 503.
