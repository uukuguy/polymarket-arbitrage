# 05.6-162 Summary — Treat incomplete Structure generations as waiting

## Delivered

- Aligned Structure certification with the existing Quote-certifier contract:
  missing range receipts now transition the fenced certifier lease to a bounded
  retryable `waiting` state without opening an incident circuit.
- Preserved the incident path for every other Structure certification error.
- Added a regression proving an incomplete generation cannot call the incident
  writer and is rescheduled after the configured five-second delay.

## Verification

- `uv run pytest tests/m1-perception/test_transactional_structure_worker.py -q`
- `uv run ruff check src/polyarb/control_plane/structure_worker.py tests/m1-perception/test_transactional_structure_worker.py`

## Next

- Deploy staging-only, verify the existing v3 generation stops creating
  incomplete-generation circuit incidents, then complete its range/certify/
  Quote/restart acceptance chain.
