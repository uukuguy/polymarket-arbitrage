# 05.6-64 Summary — full health projection budget

Under active Structure recovery, a complete `/healthz` projection measured
4.93 seconds. Its four-second internal deadline incorrectly emitted a
health-read P1 and left the single read lane occupied while the timed-out
worker completed its remaining bounded authority reads.

The health projection now has an eight-second internal budget, strictly below
the ten-second Fly and Polywatch deadlines. Real saturation and timeout remain
explicit P1 envelopes. Focused tests cover the new budget and retain the
existing saturation, timeout, and connection-interrupt semantics.
