# 05.6-239 Summary — Stage-exact R2 read timeout recovery

## Outcome

- Added a deterministic `r2-read-timeout` disposable adapter for all six
  R2-reading production nodes: Structure materialize/normalize/certify, Quote
  admit/certify, and Opportunity certify.
- The injected fault is botocore's provider-shaped `ReadTimeoutError` on one
  exact immutable GET. No sleep, whole-run timeout, private retry clock, or
  production provider is involved.
- The first lease commits one retryable failure and warning incident through
  the central retry policy, while its node-specific business postcondition is
  proven absent.
- Recovery claims only at the persisted policy due-at, retains the exact job
  `input_identity`, reads and verifies the same artifact SHA-256, then commits
  exactly one real terminal success and business fact.
- The normal-turn fixture can now stop progress at an exact attack stage. This
  prevents contradictory evidence where an injected read timeout is reported
  against an already-advanced commit stage; replacement epochs still progress
  through the complete normal transaction.
- Added a six-database cleanup-safe harness, Make entry, exact argv/help
  contracts, climb gates, H-051 state, and the learning FAQ.

## Verification

- Six-node real-PostgreSQL adapter contract: passed.
- Six isolated migrated database harness runs with zero residual disposable
  databases: passed.
- Existing eight-node normal-turn fixture contract: passed after the staged
  progress extension.
- Make help/exact command and climb evaluator contract: passed.
- Ruff passed all touched Python; production-module Pyright reported zero
  errors.

## Boundaries

- Local disposable PostgreSQL and an in-memory provider-shaped object boundary
  only.
- No production provider/database request, Fly action, deployment, Docker
  build/context mutation, Colima, canary, or qualification operation occurred.
- R2 write timeout commissioning and the combined exact-image rebuild remain
  deferred to the next hypothesis.
