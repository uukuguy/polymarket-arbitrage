# 05.6-41 — Certification Cursor Writer Bound Summary

## Delivered

- Used the live `event_tags` certification cursor and 75-second child timeout
  to isolate a second default SQLite writer wait in
  `advance_structure_certification_chunk`.
- Bounded certification cursor commits with the same caller-provided writer
  budget used by Structure normalization, contract reconciliation, and sealing.
- Propagated the publication-step budget into certification so a contended
  cursor checkpoint fails visibly and retries instead of consuming the entire
  Structure child hard limit.
- Added a real locked-writer regression test for certification checkpoints.

## Verification

- `uv run pytest tests/m1-perception/test_structure_generation_publication.py::test_certification_chunk_writer_wait_is_explicitly_bounded tests/m1-perception/test_structure_generation_publication.py::test_publication_step_bounds_contract_reconciliation_writer_wait tests/m1-perception/test_structure_generation_publication.py::test_normalization_chunk_writer_wait_is_explicitly_bounded -q`
- `uv run ruff check src/polyarb/perception/structure_publication.py src/polyarb/storage/sqlite_store.py tests/m1-perception/test_structure_generation_publication.py`

## Production acceptance still required

Deploy to L1 authority and Polywatch cron, then verify certification proceeds
past the prior `event_tags` cursor without a 75-second timeout. Keep the P1
incident open until a new Structure pointer, a certified Quote run for that
pointer, and healthy read-lane evidence all coexist.
