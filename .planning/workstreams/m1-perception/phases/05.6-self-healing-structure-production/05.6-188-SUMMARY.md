# 05.6-188 Summary — transactional opportunity projection reader

## Delivered

- Added a bounded read-only `PostgresControlPlane.current_opportunities` query.
- Reads only the atomically published `opportunity:current` pointer and its
  immutable generation rows; it never reconstructs truth from SQLite, jobs, or R2.
- Updated integration setup to apply revision 016 and truncate the new projection
  authority tables between tests.

## Verification

- `uv run python -m compileall -q src/polyarb/control_plane/postgres.py`
- `uv run pytest tests/m1-perception/test_control_plane_api.py tests/alembic/test_016.py -q` — 3 passed.

## Next

Implement the authenticated R2 projection certifier and atomically publish its
complete result into these tables.
