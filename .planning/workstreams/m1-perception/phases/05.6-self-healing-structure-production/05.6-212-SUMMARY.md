---
phase: 05.6-self-healing-structure-production
plan: "212"
subsystem: runtime
tags: [attempt-lifecycle, transport-generation, recovery-episode, interruption]

requires:
  - phase: 05.6-self-healing-structure-production
    provides: revision 034 single-authority circuit timing and failure-aware circuits
provides:
  - in-progress audit of M1 timeout, sequencing and interruption boundaries
  - implementation contract for transport replacement and episode-scoped recovery
  - failure-triggered Gamma HTTP transport generation replacement
  - durable pre-I/O stage identity for Structure source attempts
  - revision 035 episode-scoped recovery action budgets
  - complete M1 timeout, sequencing, cancellation and recovery boundary inventory
  - deterministic 16-case runtime fault matrix v3
  - single-pass planning history index for all workstreams
  - non-blocking database claims for every asynchronous transactional worker
  - non-blocking terminal fan-in with durable lost-wakeup repair
  - pooled Structure source shutdown policy propagation
  - bounded concurrent Structure range capacity with single-generation backpressure
  - explicit high-frequency SQLite drift read connection ownership
  - cross-shape Structure generation backpressure and ordered materialization
  - explicit, full-shape and reversible Machine capacity canaries
  - revision 036 index-backed bounded operator snapshots
  - executable exact-recovery Make guard and index-backed candidate selection
  - pre-limit exact recovery selection and bounded normal transport cleanup
  - lease-independent Structure certification attempt ceiling
affects: [m1-perception, runtime-recovery, production-source, qualification]

key-files:
  created:
    - alembic/versions/035_m1_recovery_budget_episodes.py
    - alembic/versions/036_m1_operator_snapshot_indexes.py
    - docs/superpowers/specs/2026-08-29-m1-attempt-lifecycle-and-recovery-episodes-design.md
    - docs/superpowers/plans/2026-08-29-m1-attempt-lifecycle-and-recovery-episodes.md
  modified:
    - src/polyarb/clients/gamma_client.py
    - src/polyarb/control_plane/structure_source.py
    - src/polyarb/control_plane/runtime_contract.py
    - src/polyarb/control_plane/recovery_models.py
    - src/polyarb/control_plane/recovery_store.py
    - src/polyarb/control_plane/runtime_observe.py
    - src/polyarb/control_plane/runtime_fault_matrix.py
    - src/polyarb/control_plane/scheduler.py
    - src/polyarb/control_plane/db_role_admin.py
    - src/polyarb/control_plane/rollout.py
    - src/polyarb/control_plane/service_lifecycle.py
    - src/polyarb/control_plane/quote_admission.py
    - src/polyarb/control_plane/quote_worker.py
    - src/polyarb/control_plane/structure_worker.py
    - src/polyarb/control_plane/postgres.py
    - src/polyarb/control_plane/fly_machine_update.py
    - src/polyarb/config.py
    - src/polyarb/storage/sqlite_store.py

key-decisions:
  - "A durable attempt owns work lifetime; provider bounds, cadence and shutdown grace are distinct authorities."
  - "A failed source attempt replaces its HTTP transport generation while retaining the service-owned limiter."
  - "Recovery budgets are scoped to a failure episode and never silently refill or erase legacy history."
  - "Database claims are subordinate blocking I/O and may never execute on the shared asyncio event loop."
  - "Capacity, scheduler waves, attempt lifetime and source admission are separate authorities."
  - "SQLite transaction context and connection close are separate lifecycle authorities."
  - "A successor wakeup is an optimization; committed producer facts plus bounded repair are the convergence authority."
  - "A generation remains admission-visible while it is a materializer, ranges or a certifier."
  - "Capacity changes are explicit guest-shape experiments; they never redefine timeout or freshness policy."
  - "Bounded operator samples require access-path bounds, not only LIMIT syntax and a larger statement timeout."
  - "An exact recovery path is incomplete until both its public entrypoint and candidate read execute inside their existing boundaries."
  - "Exact mutation targets are database predicates before sampling; normal cleanup is part of the shutdown budget."
  - "An absolute attempt ceiling is a distinct authority, never a lease multiplier."

requirements-completed:
  - Task 1 transport generation replacement
  - Task 2 pre-I/O stage truth and stage-specific failure identity
  - Task 3 episode-scoped recovery budgets and revision 035
  - Task 4 timeout, sequencing and interruption inventory
  - Task 5 single-pass and interruptible planning-status
  - Task 7 asynchronous claim isolation and event-loop liveness
  - Task 8 bounded Quote concurrency with lane-local failure containment
  - Task 9 durable recovery incident closure and reopening
  - Task 10 truthful terminal fan-in and lost-wakeup self-repair
  - Task 11 pooled Structure source shutdown authority propagation
  - Task 12 cumulative lease renewal across fast sub-calls
  - Task 13 minimal platform readiness boundary
  - Task 14 deterministic health resource evidence
  - Task 15 bounded Structure range capacity and generational backpressure
  - Task 16 explicit SQLite drift reader ownership
  - Task 17 single-live-lease worker identity fencing
  - Task 18 producer-commit independence from shared certifier locks
  - Task 19 cross-shape generation admission and materializer ordering
  - Task 20 fail-closed Machine guest-shape update contract
  - Task 21 revision 036 bounded operator-snapshot access paths
  - Task 22 executable exact-recovery Make entrypoint
  - Task 23 index-backed recovery candidate selection
  - Task 24 exact target selection before sample limit
  - Task 25 bounded normal Gamma transport cleanup
  - Task 26 lease-independent Structure certification ceiling

duration: "in progress"
completed: null
---

# Plan 05.6-212 Summary: Attempt Lifecycle and Recovery Episodes

Plan 212 is in progress. Production evidence isolated the remaining Structure
source failure to the long-lived coordinator transport: the exact page and
cursor completed quickly in a fresh local client and a fresh Fly Machine using
the same image, while retries in the resident coordinator timed out before the
first durable stage checkpoint. The existing client survives failed attempts,
so later attempts reuse the same transport generation.

The audit also found that recovery budgets are keyed to a target forever. A
new failure fingerprint therefore inherits an exhausted budget from an older
incident. The selected design replaces transport after a failed durable
attempt, persists stage-start facts before blocking I/O, and keys action budgets
by immutable failure episode. This summary is the required plan anchor and will
be updated with implementation, verification and production evidence after
each atomic task.

Task 1 is complete. `GammaClient.reset_transport()` retires the failed HTTP
pool within the existing cleanup bound and installs a new pool even when close
times out or fails; the service-owned `AsyncLimiter` is unchanged. Every
retryable Structure source failure invokes replacement before its durable
failure receipt. A secondary replacement error is logged by exception class
only and cannot overwrite the original failure identity. Both complete focused
test files pass, and Ruff/Pyright report zero findings.

Task 2 is complete. Runtime `current_stage` advances only after the matching
progress fact is durable. Structure source now writes start and completion
facts around fetch, validation and upload, so a timeout cannot collapse back
to the generic `started` state. Retry fingerprints include that durable stage.
The prior false `commit-page=1/1` written before the fenced receipt was also
removed: `commit-page=0/1` now precedes commit-side DB work, and the terminal
job receipt is the only completion authority. Runtime, source-worker and real
PostgreSQL runtime-progress tests pass; Ruff/Pyright remain clean.

Task 3 is complete. Revision 035 extends the budget primary key with an
immutable `episode_key`: job actions use the exact attempt ID and circuit
actions use the exact failure fingerprint. Existing target-only rows remain
under `legacy` and are never rewritten or refilled. Live reconciliation,
observe-only snapshots, historical replay, action idempotency, action detail,
budget locking and consumption now carry the same episode identity. A real
PostgreSQL proof retained a legacy exhausted row while two fingerprints for
the same circuit independently consumed their own budgets. The
034→035→034→035 round trip conservatively preserved exhaustion and scoped-role
privileges. Recovery/CLI/observe suites and Ruff/Pyright pass.

Task 4 is complete. `docs/dev/m1-runtime-boundary-inventory.md` classifies
every production provider, worker-I/O, DB, attempt, cadence, retry, circuit,
terminal and qualification clock, including cancellation and durable resume
evidence. The audit found two additional sequencing defects: rollout and
role-admin gates still expected revision 034 after revision 035, and the
scheduler reused a cadence remainder measured before a slow observer callback.
Both now have one checked authority. Fault matrix v3 runs 16 deterministic
real-PostgreSQL cases and selects each case by exact job identity instead of a
global `LIMIT 1`; transport replacement, pre-I/O timeout, service interruption
and recovery-episode isolation are explicit cases. Scheduler, role, rollout,
policy and full fault-matrix suites pass.

Task 5 is complete. `planning-status` now reads the reachable Git history once,
builds a path-creation/rename/commit-graph index in memory, and reuses it across
all plan rows while preserving each plan's exact creation-to-summary lifetime.
A regression test proves two plans require one Git subprocess instead of six.
The full planning-status suite passes, and the real 90-plan repository check
reports zero drift and valid evidence hashes in 0.86 seconds, down from the
observed approximately 53-second per-plan scan.

The first complete focused gate exposed one stale direct PostgreSQL test caller
that scheduled a circuit action without forwarding the newly mandatory failure
episode. Production callers and the fault matrix already carried the value.
The test now exercises the same explicit episode contract instead of relying on
the removed target-only fallback.

The first revision-035 production probe replaced the poisoned Gamma transport
but exposed a second, independent defect: the coordinator still timed out the
same page three times at `fetch-page=0/1`. The exact release, cursor, provider
policy, app and region returned 100 records in 0.164 seconds in an isolated
Machine. A second read-only probe measured eight sequential claim-shaped
database reads in 2.292 seconds. The page and individual query were healthy;
the coordinator alone combined eight source lanes and eight extra
materializer turns whose asynchronous `run_once()` methods called synchronous
`claim_job()` on the event-loop thread.

Task 7 is locally complete. `claim_worker_job()` routes all five asynchronous
claim sites through the existing cancellable daemon bridge without adding a
timeout; PostgreSQL connect, statement and lock policies remain the only DB
authorities. The source failure-path spec lookup is bridged as well. A
behavioral regression blocks a fake claim while proving an unrelated loop task
still advances, and an AST coverage test rejects any direct `.claim_job()` in
the audited async worker methods. Seven affected worker/policy/scheduler files
pass, Ruff is clean, and modified production files report zero Pyright errors.
Production coordinator validation is complete on exact release `33ad6826` and
child manifest `sha256:aa723729…e93b6`. The source circuit one-shot executed in
the runtime-controller app with its scoped DSN, consumed only the exact current
episode, changed zero pointers, and auto-removed. The formerly failing page
then completed as `succeeded:1/8`; subsequent pages continued succeeding while
the other coordinator lanes remained live. This is the production proof that
event-loop claim starvation, rather than provider or single-query latency, was
the second root cause.

The resident controller, coordinator, Structure worker, Quote worker and
qualification worker now run the same exact v7 child digest. All preserve
`SIGTERM/40s`; the resident controller remains observe-only with an empty
allowlist. Qualification is bound to full release
`33ad682654f2ccd5766e5c78265d79b8142b3005`, so predecessor evidence cannot be
transferred into the new certificate window.

The older Quote-admit circuit was recovered separately with an exact
`probe-circuit` one-shot in the controller app. Action
`9edb8dbd-9a97-4b3b-9a62-66a3ac081588` succeeded, consumed one action from its
fingerprint-scoped episode, and changed zero pointers. Its resumed lease epoch
11 was observed at `upload-batches=65/148` with a 1.8-second progress age,
fresh heartbeat, and the job's own 20-minute attempt boundary. Other lanes
continued to complete source pages concurrently; the old 29-second outer
timeout no longer kills or stalls this legitimate long task.

M1 remains incomplete. A new 86,400-second release-bound qualification
certificate is still required after the revision-035 production recovery.
The fresh full M1 regression is running without an outer timeout; this summary
will record its terminal count and the resumed Quote pipeline terminal evidence
before Plan 212 is closed.

That resumed pipeline exposed a third independent sequencing defect before the
qualification gate could be accepted. The dedicated Quote process advertised a
`pool-turns` bound, but `TransactionalWorkerLoop` executed those turns as a
serial list. Production measured roughly 11–12 seconds per 500-token batch; 148
batches therefore required about 28 minutes on one lane and contradicted the
900-second freshness gate. Increasing the freshness or attempt timeout would
have hidden a capacity defect rather than made the task recoverable.

Task 8 is locally complete. `TransactionalQuoteBatchPool` now gives every lane
an independent worker identity and durable lease while sharing the existing
bounded CLOB reader and R2 client. The lane count is derived from the single
`clob_batch_max_concurrency` authority (default 12), not a second scheduler
constant. A wave waits for healthy siblings before propagating one failed lane;
the role loop records that failure and continues instead of terminating the
Machine and stranding the queue. SIGTERM cancellation propagates to every
active lane, whose existing worker path owns its fenced `finish_interrupted`
transition, and the pool exposes the common lease-derived terminal grace.
Focused pool, lifecycle, CLI and deployment suites pass; Ruff and modified-file
Pyright report zero findings. Production rollout and final freshness evidence
remain pending.

The v8 production wave then exposed a recovery chain-truth gap. Both exact
circuit actions had succeeded, their jobs were terminal and their circuits were
closed, yet the reconciler-created `recovery-started` incidents remained open.
`record_job_recovery()` resolved only the worker's `job-retry:*` incident while
the reconciler wrote a separate `recovery:{job|circuit}:*` dedupe family. A new
episode also updated a resolved incident without reopening it, so the status
projection could be wrong in both directions.

Task 9 is locally complete. A terminal success or durable checkpoint under the
current lease now atomically resolves the job-retry incident and both bounded
runtime-recovery target forms, emits one idempotent `recovered` event/alert per
incident, and closes any positive circuit streak. A later recovery episode
explicitly reopens its deterministic incident and clears `resolved_at`. A real
PostgreSQL test proves open → recovered → reopened ordering; the existing
lock/statement rollback, terminal and checkpoint recovery tests remain green.
Production rollout and repair of the two historical open projections remain
pending.

The v8 Quote pool drained all 148 admitted batches in roughly 95 seconds. Its
first wave completed 12/12 lanes concurrently; a later PostgreSQL
`OperationalError` remained lane-local, the role stayed alive, failed leases
were reclaimed, and the queue reached zero unfinished jobs. Available memory
remained above 580 MiB. This proves the 900-second freshness contradiction was
capacity/serialization, not a reason to enlarge a timeout. Qualification was
not restarted because the now-complete generation exposed one more ordering
defect: its Quote certifier remained `waiting` after every batch was terminal.

Task 10 is locally complete. The fan-in wake query used receipt count as a
proxy for producer completion, ran before nonterminal `record → finish`
callers reached `succeeded`, and let concurrent final transactions observe
disjoint committed sets. Both could therefore decline the wake permanently.
Structure had the same latent defect. Terminal producers now transition first,
then serialize on the certifier row and require every sibling to have both an
immutable receipt and `succeeded` job state. The legacy two-call API wakes from
`finish(SUCCEEDED)`. Each certifier also performs a one-row,
`FOR UPDATE SKIP LOCKED` repair from durable facts before claim, so historical
lost wakeups resume without operator SQL while incomplete generations remain
closed. Deterministic concurrent PostgreSQL regressions pass for Structure and
Quote; the complete affected control-plane/worker group, Ruff and Pyright are
green. Exact-release production rollout remains pending.

Production proved Task 10 immediately after the v9 coordinator started: the
historically stuck Quote certifier repaired itself without SQL, certified the
complete generation, and published `quote:18daa4bd…dec5` at
`2026-08-29T01:28:58Z`. The opportunity successor then claimed the same
generation and began its 148-shard computation. This closes the lost-wakeup
production proof.

That rollout also exposed Task 11. The old coordinator received SIGTERM while
the Structure source pool was active, but shutdown printed `ValueError` and
exited 1 after ten seconds. The central grace resolver correctly requires a
lease for `structure-source`; unlike the Quote pool, the Structure pool did not
propagate the common lease of its eight lanes. It now validates one shared
positive lane policy and exposes that derived lease, so the existing
`structure-fetch` runtime policy remains the sole terminal-grace authority.
The RED lifecycle reproduction and complete source/scheduler suites pass;
Ruff and Pyright are clean. A v10 rollout is required before the final
qualification epoch.

Production v10 then proved Task 11 under a live Structure source job. SIGTERM
reached all active services, including `structure-source`; the coordinator
reported `stopped`, exited normally with code 0 in about three seconds, and
restarted in about one second. The two historical runtime-recovery incidents
were closed through `record_job_recovery()` using their exact successful lease
facts; the resulting status had zero open runtime incidents, circuits, and
pending/running actions.

The resumed Opportunity successor exposed Task 12. Its 148 R2 reads were each
shorter than the heartbeat interval. Both synchronous bridges checked heartbeat
only when one call timed out waiting, so the healthy fast sequence never renewed
its cumulative 120-second lease and eventually logged `stale-lease`. The fix
checks `heartbeat_if_due()` after every successful nonterminal sub-call as well
as during slow waits; the runtime remains the only elapsed-time authority and
the absolute attempt deadline is unchanged. Terminal calls retain their
post-commit no-renewal rule. RED/GREEN tests cover both Quote/Opportunity and
Structure bridges; all affected suites, Ruff and Pyright pass. Exact-release
production rollout and fresh Opportunity/qualification evidence remain pending.

Exact v11 release `c0fa2c2be4df970a89664634ff46fb803715b2fe` was built
as OCI index `sha256:ff14383b…fd5d2` with linux/amd64 child
`sha256:ad92667d…d206c`, nonroot UID 10001 and exact revision label. A first
tag whose full label did not equal Git was superseded before deployment. All
five formal Machines now run the exact child digest, retain `SIGTERM/40s`, and
the controller remains observe-only with an empty allowlist.

Production directly proves Task 12. Opportunity lease epoch 19 started at
`02:02:19Z`, renewed at `02:05:21Z`—after its nominal 120-second lease—and
published the current projection at `02:05:51Z`. The roughly 212-second attempt
ended with both job and attempt `succeeded`; its open retry incident closed and
the open circuit count stayed zero. Structure generation `71a897fa…1432` then
resumed from its durable checkpoint, completed 1,115-range parity, and published
a fresh manifest at `02:13:42Z`. The older expired certifier reclaimed itself
at lease epoch 3 and resumed from checkpoint 375 without manual SQL. The
downstream Quote/Opportunity chain is complete. The new qualification release
still has no certificate.

The same production observation found Task 13: `polyarb-control-api` repeatedly
failed its Fly health check for 16–56 seconds because the five-second
`/healthz` boundary called the full operator snapshot through a 10.5-second
application envelope. That envelope also omitted the mandatory scoped-session
bootstrap, so the timeout model did not describe the real
`connect -> bootstrap -> data statement` sequence. Readiness is now a separate
one-statement durable-authority probe with a 3.5-second complete policy below
Fly's five seconds; full snapshots remain on the operator route. API,
deployment-contract, DB-role, alert-clock, Ruff, Pyright and real PostgreSQL
focused gates pass. Exact API rollout and a sustained platform-health proof
remain pending.

The pre-Task-13 full suite then ended with four legacy health `pass -> warn`
failures. Each target Quote/Structure check was pass; the hidden warning was
the real host volume at 20.18% free, which crossed the production 20% threshold
under long-suite and Docker-layer pressure. General health fixtures now own a
stable 50% disk input, while the dedicated 25/19/9-percent threshold test keeps
the production behavior covered. Both complete affected files and Ruff pass;
the fresh exact-head full suite is recorded below.

The exact control API image was then rolled only to Machine
`d8d0e27a734158`. It runs linux/amd64 manifest `sha256:ac24c774…347bf4`,
retains `SIGTERM/40s`, and returned the minimal readiness body continuously
from `02:47:56Z` through the `03:00:18Z` observation. The previous image had
failed as often as every minute through `02:45:33Z`; no post-start platform
failure appeared during the first sustained 12-minute window. Task 13's
production health contradiction is therefore closed without changing any
data-plane Machine.

That repaired operator path exposed Task 15 rather than another timeout:
Structure generation `0f46c624…` held 666 runnable ranges behind one active
lane while an older certifier was also resuming. Durable completions measured
41/5 minutes and 123/15 minutes (8.2 ranges/minute), projecting roughly 136
minutes for a 1,115-range generation against the 15-minute freshness gate.
The default admission high-water of 2,000 also admitted a second generation
while the first still had 1,115 unfinished ranges.

Task 15 implementation separates capacity, wave count and admission.
`structure_range_max_concurrency` bounds a 12-lane pool; every lane has a
distinct fenced worker identity and the pool drains siblings before exposing
an error. The default Structure high-water is now one unfinished range at the
PostgreSQL, admitter, scheduler and CLI boundaries. RED tests proved the old
missing pool and 2,000 default; focused pool/CLI/default-PostgreSQL tests,
Ruff and modified-file Pyright are green. The complete no-outer-timeout M1
suite is green. A Structure-only production canary remains required before
this repair is accepted.

The no-outer-timeout baseline suite completed with `4060 passed, 1 skipped,
1 xfailed` in 1,649.78 seconds. During its 120k production-shaped drift gate,
the process held roughly 600 descriptors to `classifier-v2-1.db` before GC
reclaimed them. `with sqlite3.connect(...)` had been mistaken for connection
ownership even though its exit only handles transaction outcome. Task 16 adds
one explicit high-frequency Structure drift read owner and routes source,
projection, member, truth and evidence pages through it. A RED/GREEN real
SQLite wrapper test proves ten opened page connections receive ten explicit
closes; the complete drift classification, projection and end-to-end suites
pass.
This repair changes no classifier result, page size, checkpoint, timeout or
writer transaction.

The exact source/runtime suite after Tasks 15 and 16 collected 4,068 tests and
completed without an outer timeout: `4066 passed, 1 skipped, 1 xfailed` in
1,664.63 seconds. The earlier pre-capacity baseline was `4060 passed, 1
skipped, 1 xfailed` in 1,649.78 seconds. The only later edit is benchmark
instrumentation: its direct phase and terminal probes now use explicit
`closing()` as well, because a test observer may not reintroduce the resource
lifecycle defect that production code removed. Its standalone 120k terminal
proof passed in 902.94 seconds under the gate's own 45-second child-slice
policy, without a competing parent timeout. Repeated sampling across the
calibration run and all three measured samples peaked at four `lsof` entries
over three unique DB/WAL/SHM paths; the count did not grow across the 2,000
phase observations.

Before the exact image canary, production required a configuration-only
backpressure stopgap. Coordinator Machine `e82d1220b2d138` retained the exact
v11 image, resources, region and `SIGTERM/40s` contract while its command gained
`--structure-high-water 1`. The old process received SIGTERM at `03:22:00Z`,
persisted stopped source/materializer turns, exited code 0 in six seconds and
restarted at `03:22:07Z`. Its first durable source result was
`backpressured:structure` at `03:22:22Z`. The already-admitted second
generation remains recoverable backlog, but no third generation was admitted;
this stopgap prevents further queue growth without pretending that it supplies
the missing twelve-lane capacity proof.

The first exact v12 Structure-only canary was deliberately rejected. It proved
all twelve lane identities and initially reduced unfinished ranges from 1,150
to 936, but terminal `OperationalError` paths accumulated retryable attempts,
the operator snapshot intermittently returned 503 under load, and projected
drain remained above 900 seconds. More importantly, production SQL showed
lanes 6 and 10 each owning two jobs whose earlier lease and attempt were still
live. The pool was bounded in memory, but its durable ownership was not.

Only Machine `683e46ea500dd8` had received v12. It was restored to the immutable
v11 child digest with the preserved-config renderer/verifier. The v12 process
handled SIGTERM, reported stopped, exited code 0 within twelve seconds and the
v11 instance reclaimed every interrupted/expired range without manual SQL;
the range projection returned to one active lease, zero expired leases and
zero open circuits.

Task 17 makes worker identity a database capacity fence. `claim_job()` uses one
fixed SQL round to acquire a transaction advisory try-lock for `worker_id` and
inspect any unexpired `leased` job held by that identity. A terminal
`checkpointed` attempt remains immediately resumable. Lock contention or a
live lease returns no job. A failed terminal write therefore idles only its lane
until durable expiry/reclaim instead of opening another attempt and multiplying
database pressure. A real-PostgreSQL RED test reproduced the old second claim;
the new guard passes that test plus reclaim/checkpoint compatibility. The
complete PostgreSQL, Structure, Quote and scheduler regression group passes;
Ruff and modified-file Pyright report zero findings. A new exact-image canary
is still required.

Exact v13 then isolated the next contradiction instead of passing the canary.
The five-minute window from `04:32:55Z` contained 229 successful Structure
attempts, 34 retryable `OperationalError` attempts, ten `LeaseExpired`
attempts and one still running attempt: 45.8 successes/minute versus the
unchanged 74.3/minute acceptance floor. All twelve worker identities were
distinct and live ownership never exceeded twelve, so Task 17 was effective.
Only Structure Machine `683e46ea500dd8` received v13; it was restored through
the preserved-config renderer/verifier to exact v11 child
`sha256:ad92667d…d206c`, unchanged shared-1x/1GiB/ams command and
`SIGTERM/40s`. No manual database mutation was performed.

Task 18 removes the remaining fan-in single point. Every terminal range or
batch transaction previously waited on the same certifier row before it could
commit its own success. Under twelve lanes, the one-second lock policy turned
normal serialization into producer failure; increasing that timeout would
only enlarge the synchronized stall. Direct wake now proves the successor row
exists, then uses `FOR UPDATE SKIP LOCKED`. A busy row skips only the wake
optimization; the producer receipt, job state and attempt state still commit.
Each certifier turn already runs the bounded durable repair before claim, so a
missed direct wake converges from committed inputs, receipts and terminal
producer facts. Real-PostgreSQL RED tests held the shared successor lock and
reproduced both Structure and Quote `LockNotAvailable`; GREEN proves producer
commit, waiting successor, one-row repair and subsequent claim. Missing
successor rows remain fail-closed.

The exact Task 18 source then passed the complete no-outer-timeout M1 gate:
`4069 passed, 1 skipped, 1 xfailed` in 1,694.91 seconds. The 120k Structure
drift test completed naturally inside that run; no evaluator or operator
wall-clock restarted it.

Production observation then exposed Task 19. The admission-only
`--structure-high-water 1` stopgap was present on the coordinator, but source
windows admitted at `01:17Z` and `02:04Z` did not materialize until `04:37Z`
and `04:51Z`. By then their pre-range admission fact was hours old, so they
created two additional 1,117-range generations behind the active generation.
A fourth source window still had one retryable materializer. The coordinator
was reversibly stopped before that materializer could run; dedicated Structure
and Quote workers and every durable job/receipt remained intact. Production
held at three unfinished generations while the v11 Structure worker continued
draining one fenced lease.

Task 19 makes generation backpressure invariant across durable shape changes.
Admission probes at most `structure_high_water` unfinished jobs across
`structure-materialize`, `structure-normalize` and `structure-certify`, rather
than counting the entire growing queue of ranges. Materializer claim selects
only the oldest unfinished materializer and returns idle while any prior range
or certifier is unfinished; the existing `FOR UPDATE SKIP LOCKED` claim then
serializes replacement coordinators on that exact job. Four real-PostgreSQL
RED tests reproduced the pre-range gap, post-range/pre-certifier gap, two
materializer contenders and prior-generation overtake. GREEN plus all thirteen
materializer/admission tests, Ruff and Pyright pass. Read-only production-scale
probes measured the two fixed guard rounds at roughly 224–231ms, below the
unchanged database boundary. No timeout, retry, lease or freshness value
changed.

The exact Task 19 commit `cd5c4f04` passed the complete M1 gate without an
outer timeout: `4073 passed, 1 skipped, 1 xfailed` in 1,702.30 seconds. The
complete real-PostgreSQL file, 195 source/materializer/scheduler tests, Ruff,
modified-file Pyright, climb 50/50 and planning-status also pass. The next
artifact must be built from the documentation/evidence commit that records
this result, then verified and canaried before the stopped coordinator is
restored.

Exact v14 commit `06b05af3` was built as OCI index
`sha256:35304e8d…6c399`; its linux/amd64 child is
`sha256:7d3bc690…e071`. The image label, nonroot UID 10001, Alembic 035,
Supercronic checksum and eight-job runtime order passed locally, and the Fly
registry returned the exact index digest. Only Structure Machine
`683e46ea500dd8` received v14 through a fresh-version full-config update; its
ID, ams region, shared-1x/1GiB shape, 12-lane command and `SIGTERM/40s`
contract were verified unchanged.

The v14 canary closes the Task 18 correctness failure but not the capacity
gate. A fresh sliding five-minute window completed 288 Structure ranges
(57.6/minute), with twelve distinct live owners, zero window
`OperationalError`, zero expired leases and zero open circuits. The sole
post-deploy `OperationalError` occurred during startup, and that same job later
succeeded without opening a circuit. This is materially better than v13's
45.8/minute retry storm, but remains below the unchanged 74.3/minute floor.

Durable stage events show the remaining cost is real work, not an undersized
timeout: across 168 complete attempts, mean spans were 2.520 seconds from
claim to read, 6.376 seconds through read/normalize, 2.747 seconds through R2
upload, 1.430 seconds to commit stage and 1.589 seconds for terminal commit.
Twelve CPU-and-I/O lanes on one shared vCPU therefore cannot be accepted by
altering the 900-second objective.

Task 20 turns the next A/B experiment into a formal rollout contract. The
Machine update renderer now accepts a resource change only as the complete
`cpu_kind/cpus/memory_mb` triple, rejects partial or non-positive shapes,
copies no arbitrary config patch, and includes redacted old/new guest shapes
in its proof. Image, lifecycle, optimistic version and every unrequested
config path retain exact verification. Focused renderer and Makefile tests,
Ruff and Pyright pass. A shared-2x Structure-only production canary remains
required before this shape or any sibling rollout is accepted.

The formal A/B then accepted shared-2x without changing memory, lanes or any
clock. In the exact five-minute window ending `06:23:00Z`, 396 attempts that
started in the window succeeded (79.2/minute); the completion-time projection
was 408 (81.6/minute). Both exceed the 74.3/minute floor. Twelve distinct
owners held at most twelve live leases, and the window contained zero
`OperationalError`, expired leases or open circuits. Mean complete-attempt
duration fell from 14.662 seconds on shared-1x to 5.909 seconds on shared-2x.

Coordinator v14 was then restored on its original stopped Machine, explicitly
started and verified with the preserved `--structure-high-water 1` command,
shared-1x/1GiB/ams resources and `SIGTERM/40s`. During the first 68 seconds its
pre-existing retryable materializer retained attempt count 2 and its original
`04:53:24Z` update time; it created zero materializer attempts, zero Structure
fetch jobs and zero new range generations. This is direct production evidence
that Task 19 waits on prior range/certifier facts instead of overtaking them.

The same canary load reproduced a separate observation-path failure:
`control-plane status` raised `QueryCanceled` under the unchanged five-second
database statement boundary. A read-only production `EXPLAIN ANALYZE` later
completed warm in 2.337 seconds, but showed the bounded samples were not
bounded access paths: pending outbox sampling sequentially scanned 9,848 rows
(630.683ms), while latest-attempt sampling parallel-scanned roughly 111k
history rows. `LIMIT` bounded response size, not database work.

Task 21 adds revision 036. It builds a global latest-attempt index and a
pending-only latest-outbox index with `CREATE INDEX CONCURRENTLY` inside an
Alembic autocommit block, preserving the delivery scheduler's existing index
and all five-second/one-second database policies. Real PostgreSQL RED/GREEN
tests prove upgrade, planner selection and downgrade. Rollout/database role
contracts now require revision 036. The production concurrent migration
completed online. The warm snapshot plan fell from 2.337 seconds to 129.318ms;
the new latest-attempt and pending-outbox index nodes returned five rows in
0.023ms and 0.028ms. The complete CLI, including scoped bootstrap and
connection setup, returned `status=ok` in 4.26 seconds under the unchanged
five-second statement boundary.

The first exact circuit-recovery probe then exposed two defects in the
recovery path itself. Task 22 fixes the public Make entrypoint: its all-or-none
selector guard used unescaped parentheses that `/bin/sh` rejected before the
CLI started. A dry-run contract had missed the defect because `make -n` never
executes or parses the recipe. The regression now runs the target through a
no-I/O fake `uv`, and the guard uses a valid grouped POSIX test without
weakening `enable=1` or the exact three-field selector.

Task 23 keeps candidate discovery inside the controller's existing two-second
statement boundary. The isolated execute-only Machine proved the scoped role
and exact circuit selector were correct, but stopped before any recovery
action with `QueryCanceled` / SQLSTATE 57014. The old negative predicate read
took 4.872 seconds from the local controller; its warm production plan took
739.698ms and examined 102,828 terminal jobs plus 7,010 runtime rows. Naming
the five legal nonterminal states positively lets the existing
`m1_jobs_state` index bound the selective side of the join. The production
plan now completes in 19.528ms and the real candidate read succeeds without
changing statement, lock, circuit, retry or action-budget clocks. An exact
image execute-only circuit probe remains the production gate before the
resident controller is rolled.

Coordinator observation continued for 1,101 seconds after Task 19 rollout.
The blocked materializer retained the same attempt count and timestamp, while
fetch count and total range generations remained unchanged. This confirms the
generation fence is durable over scheduler cycles rather than a short startup
coincidence.

The subsequent complete M1 gate passed `5095 passed, 1 skipped, 1 xfailed` in
1,995.03 seconds without an outer timeout. That clean baseline let the wider
boundary audit add three independent RED/GREEN contracts. An execute-only
exact target had previously been filtered in Python after the generic
100-candidate SQL sample, so unrelated rows could hide the requested job.
Task 24 makes the exact job key a PostgreSQL predicate before ordering and
`LIMIT`, while still verifying target type and expected action from returned
durable facts.

Task 25 closes the normal-shutdown asymmetry. Transport replacement and
cancellation cleanup were bounded, but explicit `GammaClient.aclose()` after
the durable worker drain was not. Explicit close now uses the same two-second
fail-soft cleanup authority, so `scheduler.aclose()` cannot hold a completed
SIGTERM sequence until Fly's 40-second platform kill.

Task 26 separates the Structure certifier's absolute one-hour attempt ceiling
from lease-derived fencing. The former `lease * 120` formula happened to equal
3,600 seconds only at the default 30-second lease and became 14,400 seconds at
lease 120. The policy spec now chooses either a relative multiplier or one
absolute ceiling; both 30- and 120-second certifier leases resolve to exactly
3,600 seconds, and an incompatible longer lease fails closed. The complete
Gamma/runtime/CLI/Make focused group, the complete real-PostgreSQL file, Ruff
and modified-production Pyright all pass.

The H-025 `fenced-deadline-reconciler` climb cycle then scored 100/100 on all
five local dimensions: unit, real-PostgreSQL integration, CLI/Make contracts,
restart/reclaim behavior and planning integrity. Climb confirmed the
hypothesis from those local gates; its optional external target evaluation was
not configured, so the later exact-image production probe remains a separate
rollout gate and is not implied by this confirmation.
