---
phase: 05.6-self-healing-structure-production
plan: 29
subsystem: dashboard-incident-recovery
status: completed
updated: 2026-08-10
---

# Plan 05.6-29 Summary: Close recovered Structure P1s from durable proof

- Production exposed a callback-contract defect: SnapshotScheduler calls the
  Structure success callback positionally, while the incident lifecycle only
  accepted a keyword-only snapshot id, leaving an already recovered P1 open.
- The lifecycle now accepts the scheduler callback contract, with regression
  coverage that a durable successful attempt verifies the incident.
- Daemon startup also reconciles an already-published current generation; the
  incident manager still requires a post-recovery successful attempt before it
  permits verification, closing deploy-window gaps without false green status.
