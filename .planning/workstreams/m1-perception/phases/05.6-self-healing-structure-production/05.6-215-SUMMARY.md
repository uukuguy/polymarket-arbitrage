# 05.6-215 Summary — Close the open-circuit/expired-lease recovery deadlock

## Outcome

- Circuit-probe admission now defers a live lease on its own target before any
  action, idempotency, incident, or recovery-budget row is written.
- A due probe may absorb its own expired lease only under the exact persisted
  attempt and lease fence. The expired attempt transition and probe release
  commit atomically in one PostgreSQL transaction.
- Sibling active actions, leased jobs, and released probe windows continue to
  serialize the singleton worker lane. No timeout was added or enlarged.

## Root cause

- `RuntimeReconciler` correctly gives an open circuit authority over its job
  lifecycle and therefore selects `probe-circuit` before expired-lease reclaim.
- The probe executor previously accepted only `retryable` or `runnable` jobs.
  When the attempt that opened the circuit later lost its lease, the job stayed
  `leased`; every probe became `stale-noop`, while the circuit priority made a
  separate `reclaim-job` decision unreachable.
- A scheduled stale-noop had already consumed one episode-scoped recovery
  action. Repetition could exhaust the budget without creating a new business
  attempt: a deterministic state-machine deadlock, not a slow-operation
  timeout.

## Safety boundary

- Admission rejects the same target while its lease is missing an expiry or
  has not expired. This rejection happens before all durable side effects.
- Execution locks the circuit, job, runtime projection, and exact attempt and
  validates `attempt_id`, `lease_epoch`, `lease_owner`, and expiry.
- Only an expired `running` or `checkpointed` attempt can be absorbed. A
  running attempt becomes `retryable` with `RecoveryLeaseExpired`; the job
  lease is cleared, runtime recovery is recorded, and the probe window is
  released within the same transaction.
- Any fence change remains a stale failure. There is no manual database repair,
  resident Machine SSH, broader worker concurrency, or timeout inflation.

## Verification

- RED: the real-PostgreSQL regression returned `stale-noop` for a due circuit
  whose exact attempt lease had expired.
- GREEN: the regression proves a live exact-target lease creates zero action
  and budget rows, then proves the expired lease is reclaimed and exactly one
  budget unit releases the probe.
- The existing three-stage singleton-lane test still passes for pending action,
  released-unclaimed probe, and leased sibling blockers.
- The recovery/circuit/probe/reclaim PostgreSQL group passes in full; Ruff,
  Pyright for both changed production modules, and `git diff --check` pass.
- Exact commit `5c3ee3cdf28f7b667d33e6b902d7592b07e2b052` passed the
  complete M1 gate: **5,118 passed, 1 skipped, 1 expected xfail in 2,298.69
  seconds (38:18)**, without an outer timeout.
- Exact image `m1-runtime-v22-5c3ee3cd` was independently pulled through the
  explicit OrbStack socket and verified as linux/amd64, UID 10001 `polyarb`,
  Python 3.12.14, migration 036, the pinned Supercronic hash, and the exact OCI
  revision. Its manifest digest is
  `sha256:fc9fb14bd6eb5973597d0cdc1df918004277f0c4cd27e8be807f3148efc8b4a3`.

## Production recovery evidence

- All eight formal Machines were rolled once and independently verified on
  exact v22 with `SIGTERM/40`. The resident controller stayed stopped during
  recovery and retained observe-only mode with an empty allowlist.
- Structure action `a2d171b2-20f3-452b-a89e-8c921d577538` atomically absorbed
  stale attempt `502de05d-7c55-4c7f-a430-5147d709e093` at epoch 4 and released
  probe attempt `aaeda1ef-d6c1-4cc0-8ad2-1f8b13194d46` at epoch 5. The new
  attempt crossed the old production stop at `756/1117`, reached `1117/1117`,
  committed certification, increased Structure certifier successes from 70 to
  71, and closed the circuit with all four overdue authorities at zero.
- Quote action `b4a03fbd-681b-40ed-a4e7-78fce87d80dc` released the exactly
  named batch. Its new attempt succeeded, batch successes increased from 9,948
  to 9,949, Quote certifier successes increased from 69 to 70, and the final
  circuit closed. The downstream opportunity attempt completed `147/147` and
  published its terminal pointer.
- The controller was then started and formally verified against its exact v22
  update payload. Final topology was eight of eight started on v22; the
  independent watchdog passed healthy with zero failures.
- Two typed control-status 503s occurred only inside the rollout/recovery write
  window while process liveness and strict readiness remained 200. A subsequent
  20-sample stable read audit passed 20/20 with 1.128-second mean and
  1.562-second maximum latency, below the existing five-second statement
  boundary. No timeout was enlarged from this transitional evidence.
- Fresh v22 qualification entered `accumulating` and advanced from 0 to 31
  eligible seconds with no breaker, no open circuit, no active overdue task,
  and a healthy resident controller lease. The final continuous 86,400-second
  certificate and independent reverification remain pending; M1 is not yet
  complete.
