# 05.6-98 Summary — Guard Structure shadow-pointer publication

## Delivered

- Added `control-plane structure-shadow-publish --enable --generation-key
  structure:<sha256>` and the equivalent Make entry point.
- The command refuses before any database connection without `--enable`,
  requires an exact Structure generation key, reads the prior isolated shadow
  identity, invokes the certified-manifest repository gate, and emits old/new
  identities with `legacy_pointer_mutations=0`.
- Added a read-only Structure shadow-pointer projection to the Postgres
  repository; it never queries the legacy pointer namespace.

## Verification

- CLI contracts prove acknowledgement-before-connect and auditable previous /
  current output.
- Full focused Structure/control-plane suite: 42 passed; Ruff passes and the
  Make target dry-runs only with acknowledgement and generation key.

## Boundary

This command is intentionally unscheduled and has not been invoked against a
cloud control plane. Stable-key range slicing and status/health projections
still precede any authorized production migration or continuous operation.
