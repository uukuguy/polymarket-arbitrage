# 05.6-176 — soak gate 可判读性

## 完成内容

- `soak-verify` 对由 verifier 产生的 `SoakEvidenceError` 输出其安全、具体的 gate 原因，
  而不是只有类型名；OS/CLI 错误仍仅输出类型，避免泄露外部响应或路径细节。
- 新回归覆盖未满 24 小时窗口，确保 operator 能区分“继续采样”与真正的运行失败。

## 验证

- soak evidence/CLI 测试：9 passed；Ruff 通过。
- 当前 live evidence 的 fail-closed 输出为 `soak must cover at least 24 hours`，这是预期
  的未完成窗口状态，而非采集故障。
