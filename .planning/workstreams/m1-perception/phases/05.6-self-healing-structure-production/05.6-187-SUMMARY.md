# 05.6-187 Summary — certified opportunity projection schema

## Delivered

- Added revision 016 with immutable per-Quote-generation opportunity
  projections, generation-scoped rows, and a separate current pointer.
- Enforced positive executable economics and ordered quote timestamps at the
  database boundary.

## Verification

- `uv run pytest tests/alembic/test_016.py tests/m1-perception/test_control_plane_api.py -q` — 3 passed.

## Next

Implement the fenced projection certifier and Postgres pagination reader, then
wire its worker into the formal coordinator before applying revision 016.
