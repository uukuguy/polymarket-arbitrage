# 05.6-105 Summary — Gate cloud shadow work with an identity preflight

## Delivered

- Added `control-plane preflight --expected-database NAME` and
  `make control-plane-preflight expected_database=NAME`.
- The read-only gate proves the connected Postgres authority is the explicitly
  named database, all fourteen revision-009 control-plane tables exist, and
  the configured R2 bucket accepts a read-only `HeadBucket` request.
- A successful result is deliberately labeled `ready-for-shadow-only`; it
  grants no migration, pointer, scheduler-loop, or worker-start action.

## Verification

- CLI contract covers named database and R2 readiness output.
- Real Testcontainers Postgres contract covers database identity, full schema,
  and mismatch refusal.
- Full transactional control-plane regression, Ruff and planning-status run
  before commit.

## Boundary

- This adds an auditable deployment prerequisite, not a cloud change. A
  designated environment name and explicit rollout authorization are still
  required to invoke it against cloud infrastructure.
