# Plan 05.6-02: Exact deployment and continuous production proof

**Goal:** Deploy the self-healing producer and prove the live
Structure → Quote → opportunity chain returns to and remains in service.

## Requirements

- [ ] All repository and image gates pass on one committed source revision.
- [ ] Deploy that exact revision to `polyarb-l1` and record release/machine/image.
- [ ] Structure window visibly advances and publishes a fresh certified snapshot.
- [ ] Scheduler failure counter returns to zero and state returns to `RUNNING`.
- [ ] Quote publishes against the new Structure within its unchanged SLA.
- [ ] Opportunity diagnosis returns HTTP 200 with verified coverage/provenance.
- [ ] A subsequent scheduled Structure cycle continues without terminal pause.
- [ ] Timeout/restart recovery is evidenced by durable cursor progress or an
  equivalent controlled production-safe interruption.
- [ ] Polywatch/alert state is reconciled and no unresolved silent incident remains.
- [ ] JOURNAL, STATE, roadmap, production manual and final summary record exact evidence.

## Makefile surfaces

- `make smoke-health-prod`
- `make smoke-market-truth-prod`
- `make diagnose-arb-feed-prod min_edge_bps=0`

## Acceptance boundary

No “M1 production recovered” claim is allowed until all live gates above pass.
