# 05.6-94 Summary — Upload canonical Structure manifests before certification

## Delivered

- Added a canonical `StructureManifestArtifact`, content-addressed at
  `structure-manifests/<sha256>/manifest.ndjson`, with R2 PUT/HEAD
  authentication equivalent to the source and range artifacts.
- Added `structure_manifest_payload`, which derives one canonical manifest
  solely from admitted Postgres inputs and durable typed range receipts.
- Added `TransactionalStructureCertifier`: claim → derive manifest → R2
  PUT/HEAD → fenced repository certification.  Certification itself atomically
  terminalizes the job/attempt; the worker does not issue a second stale-prone
  finish operation.

## Verification

- Worker contract proves the R2 object identity passed to certification is the
  object actually PUT/HEAD verified.
- Structure artifact, worker, real Postgres, CLI, and migration regression:
  37 passed; Ruff passes.

## Boundary

The manifest proves transactional range completeness but is not yet compared
to legacy publication truth and does not publish a pointer.  Parity and the
reversible, separately authorized shadow-pointer gate are next.
