# 05.6-249 Summary — Keep qualification release/config rollout atomic

## Outcome

- The Machine update guard now permits exactly the two qualification identity
  fields: `POLYARB_QUALIFICATION_RELEASE_ID` and
  `POLYARB_QUALIFICATION_CONFIG_ID`.
- All other environment overlays remain rejected. The redacted update proof
  reports only changed key names and never identity values or Machine env.
- This closes the v1-to-v2 rollout contradiction where the renderer produced a
  new canonical config digest but the update guard allowed only the release
  half of the identity to advance.

## Verification

- The regression was red on `Machine env update key is outside rollout policy`
  before the implementation change and green afterward.
- All 14 Machine-update contract tests pass.
- Ruff and Pyright pass for the implementation and regression; `git diff
  --check` is clean.

## Production boundary

- This is operator-side rollout tooling only. The commissioned runtime image
  remains exact release `e17f1359957bd8a585df62d68aa7b4bdbf2c7820` and was
  not rebuilt.
- No production Machine, database row, role, credential or recovery action was
  changed while discovering and repairing this guard.
