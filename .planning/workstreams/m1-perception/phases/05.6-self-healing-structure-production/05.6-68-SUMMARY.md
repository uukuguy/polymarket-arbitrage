# 05.6-68 Summary — Structure certification read deadline

## Delivered

- `advance_structure_certification_chunk` now receives the publication slice's
  absolute deadline and uses the deadline-aware SQLite read connection.
- An expired certification read becomes `StructurePublicationDeadlineReached`,
  preserving the prior durable certification cursor for the next slice.

## Verification

- `uv run pytest tests/m1-perception/test_structure_generation_publication.py -k 'certification_rejects_an_expired or expired_read_budget or preserves_prior_checkpoint' -v` — 4 passed.
- `uv run ruff check src/polyarb/perception/structure_publication.py src/polyarb/storage/sqlite_store.py tests/m1-perception/test_structure_generation_publication.py` — passed.

## Production target

Generation 892 reached `issues|done` under the prior deployment and then
repeatedly timed out in certification. This change specifically bounds that
remaining read path; live acceptance still requires published snapshot, Quote
receipt, incident closure, and soak evidence.
