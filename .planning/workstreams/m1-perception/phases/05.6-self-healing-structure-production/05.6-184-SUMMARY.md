# 05.6-184 Summary — one-way transactional formal topology

## Delivered

- Replaced the generic three-role, 512MB worker deployment artifact with the
  staging-proven five-role topology: one coordinator, two uniquely identified
  structure-range workers, and two uniquely identified quote-batch workers.
- Promoted the artifact checklist from an authorized reversible switch to a
  one-way formal transactional promotion, matching the project decision that
  legacy L1 is neither a formal runtime nor a fallback.
- Kept rendering local-only; it neither deploys an app nor reads cloud secrets.

## Verification

- `uv run pytest tests/m1-perception/test_control_plane_deployment_templates.py tests/m1-perception/test_control_plane_rollout.py tests/m1-perception/test_control_plane_cli.py -k 'deployment_templates or rollout or render_rollout' -q` — 7 passed.
- `make planning-status` — no drift.
- `make climb-check && uv run pytest tests/climb -q` — 37 passed in each suite invocation.

## Next

Render the formal application artifacts, create the three isolated Fly apps,
transfer only the required existing transactional secrets without printing
values, apply the existing revision, then run formal live acceptance and start
a new 24-hour formal soak. No Supabase project creation or subscription change
is part of this work.
