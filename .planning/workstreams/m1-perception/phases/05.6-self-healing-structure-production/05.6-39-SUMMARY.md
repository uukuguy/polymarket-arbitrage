# 05.6-39 — Structure P1 Dashboard and Bounded Writer Summary

## Delivered

- Extended the typed public incident contract and Vercel Perception Dashboard
  with a prominent `P1 Structure publication incident` panel.
- The panel distinguishes the 45-second cooperative checkpoint target from the
  75-second hard child limit, shows stage/elapsed/failure, and states that
  gross candidates are not executable while the market map is stale.
- Kept an existing open Structure P1 readable at deploy time: old durable
  evidence receives only the static runtime-budget context at read time;
  newly written incident evidence persists both budgets.
- Bounded `append_structure_publication_chunk` SQLite writer acquisition with
  a per-step timeout capped at five seconds and derived from the remaining
  cooperative slice, rather than inheriting the 120-second default wait.
- Added the operator procedure to the M1 living manual.

## Verification

- `uv run pytest tests/m1-perception/test_dashboard_perception_contract.py tests/m1-perception/test_perception_http.py tests/m1-perception/test_structure_generation_publication.py tests/m1-perception/test_scheduler.py -q`
- `cd dashboard && npx tsc --noEmit`
- `make docs-m1-check`
- Focused `uv run ruff check` over changed Python and test files.
- `make planning-status` reported no planning drift before this commit.

## Production acceptance still required

This commit must be deployed to L1 and Dashboard. Acceptance is not a test
pass: it requires a post-deploy fresh Structure pointer, a later certified
Quote run for that pointer, no open P1/P2 incident, a reachable Dashboard
panel, and Polywatch fault/recovery evidence.
