# 05.6-132 Summary — Fetch Structure markets by sealed event members

## Delivered

- Added `GammaClient.fetch_markets_by_ids`, limited to one sorted, unique
  1–25-member exact-ID request. Its response must contain exactly the admitted
  IDs once each and every row must still be open and non-archived.
- Added `market_batches_from_event_records`, which reuses the existing event
  normalizer semantics to produce sorted, bounded open-member market batches.
- Scoped market artifact headers now bind both admitted IDs and their digest;
  legacy event/uncoped artifact bytes remain unchanged.
- A worker holding a scoped market input calls only the exact-ID API and emits
  a terminal page receipt. It never calls `/markets/keyset` for that input.

## Verification

- RED: exact-ID and event batch imports were absent; the scoped worker test
  demonstrated the old global market path was still used; artifact parsing lost
  the batch identity.
- GREEN: targeted Gamma, artifact, and worker contracts pass.
- `uv run ruff check src/polyarb/clients/gamma_client.py src/polyarb/control_plane/structure_source.py tests/m1-perception/test_gamma_client.py tests/m1-perception/test_transactional_structure_source_worker.py` — passed.

## Boundary

The terminal-event worker has not yet reconstructed its prior authenticated R2
event pages and supplied batches to the repository. Staging remains stopped;
no pointer or notification configuration changed.
