# 05.6-115 Summary — Schedule transactional Structure source collection

## Delivered

- Extended the bounded control-plane scheduler with `structure-source` and
  `structure-source-materialize` turns before existing Structure and Quote
  turns.
- Added an explicit `structure-source-once` operator command and Make target.
  It idempotently admits one named source window and processes no more than one
  Gamma page; the long-running scheduler never invents source windows.
- Kept Gamma/R2 credentials in the worker construction path. The API remains
  Postgres-only, and the source worker closes its Gamma transport on shutdown.
- Updated the learning note and M1 operator manual to distinguish the new
  source-window authority from the legacy SQLite comparison exporter.

## Verification

- Scheduler rotation and source CLI enable/one-page contracts pass.
- Source worker/materializer regressions, Ruff, `make docs-m1-check`, and
  `make planning-status` pass.

## Boundary

The worker has not been deployed or started against a cloud target. Continuous
collection still requires a designated non-production database/R2 bucket/Fly
apps, named migration and preflight, then fresh shadow, worker-loss, and
24-hour soak evidence before any reversible pointer-switch decision.
