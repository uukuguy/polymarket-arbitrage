# Phase 05.4 Continuous L3 Soak Log

**Status:** `TASK 3 PRE-DEPLOY — migration and isolated credentials verified`
**Release-candidate gate completed:** `2026-07-24T02:04:32Z`  
**Verified source SHA:** `95bf1bd8714b92056c1ca6cca2d13ac9bd3d06d5`  
**AcceptanceConfig digest:** `c9269392e044c633e4ecfa4879ba6a66da2811bfb0cf444df275c57fadd35d60`

The verified SHA is the source candidate tested before this log-only Task 1
commit. This log does not authorize or change production. No migration,
credential creation/distribution, secret/config mutation, deployment, restart,
manifest bind, retention cleanup, production chaos, soak checkpoint, trade,
external submission, or push occurred during Task 1.

## Diagnostic-only history

release-37 and both Phase 05 windows remain **diagnostic-only / permanently
ineligible for strict PASS**:

- release: `37`
- machine: `85e647c4eed598`
- instance: `01KXZJKY9SKEJAY2DD8MMPNB2E`
- image: `deployment-01KXZJHS9QT8T6X0J33KVPTB5V`
- digest:
  `sha256:5da8e954897f60cf05f9d6664e99a15247d46a2bd4fd0edbb433c200af8b412c`
- later diagnostic T0: `2026-07-21T14:29:47.941Z`

No later health probe, SQL query, log search, or continuous-evidence deployment
may backfill or upgrade those windows. The detailed historical observations
remain in `../05-ws-book-prices/05-SOAK-LOG.md`.

## Local release-candidate evidence

### Bound AcceptanceConfig

| Field | Verified value |
|---|---:|
| `recipe_sha256` | `9708aff98bc8ff36895dc9149dc315497ef405e6956f50b0e0a9d2543eb59057` |
| `sample_interval_s` | `30` |
| `max_sample_gap_s` | `75` |
| `promote_interval_s` | `300` |
| `promote_max_start_gap_s` | `360` |
| `market_book_fresh_s` | `120` |
| `market_ohlc_fresh_s` | `120` |
| `expected_market_count` | `5` |
| `expected_token_count` | `10` |
| `retention_days` | `30` |
| `schema_revision` | `007` |
| `code_version` | `0.1.0` |

### Gate results

| Gate | Result |
|---|---|
| Focused 007/evidence/promoter/sampler/health/verdict/CLI/chaos matrix | **PASS** — 350 passed; `-rs` reported no skipped integration test |
| Full `uv run pytest -q` | **PASS** — reached 100%; 1 established xfail and 1 established skip; only existing warnings |
| Repository-wide `uv run ruff check src tests scripts` | **KNOWN BASELINE RED** — exactly 250 findings |
| Plan 04 Ruff amendment | **PASS** — base `4c9c5e6` and verified SHA each have 250 findings in the same 82 files; normalized outputs are byte-identical with SHA-256 `2bf1014e3795d20a1559c20934e26c622c1c0244bc90f78db0523706bc6fc3ab`; plan-scope Ruff reports `All checks passed!` |
| `uv run python -m compileall -q src/polyarb scripts` | **PASS** |
| `make chaos-l2-fly-image-check` | **PASS** — required `python` present; `kill`, `which`, `curl` present; optional `pkill`, `ps`, `dig`, `ping` absent |
| `make docs-m1-check` | **PASS** — `M1 manual contract: OK` |
| `make planning-status` | **PASS** — no drift |

Security disposition: **0 unresolved High threats**. Plan 04's final independent
review reported zero Critical/Important/Minor findings, and the diff from its
summary through the verified SHA contains planning/handoff files only. Task 1's
complete regression and integrity/privilege replay found no new release-candidate
failure.

## Approval 1 — production migration to 007

**Status:** `COMPLETE — AUTHORIZED 2026-07-24T03:41:10Z / MIGRATED 2026-07-24T04:07:37Z`

The user explicitly authorized autonomous completion of Plan 05 after the
production ref and exact action scope were restated. This overrides the
separate-message ceremony, not the separate target/revision proof or fail-closed
execution boundary. Effective approval:

```text
APPROVE 05.4 PRODUCTION MIGRATION TO 007 ref=zoqsmjeejfkrokwttjbx
```

Exact approval required:

```text
APPROVE 05.4 PRODUCTION MIGRATION TO 007 ref=zoqsmjeejfkrokwttjbx
```

Production target/schema evidence:

| Evidence | Status |
|---|---|
| Approval timestamp and exact production ref | `PASS — 2026-07-24T03:41:10Z / zoqsmjeejfkrokwttjbx` |
| Redacted allowlisted target/database/user proof | `PASS — direct db.<ref>.supabase.co / postgres / postgres / TLS` |
| Pre-mutation Alembic revision | `PASS — 006` |
| Post-mutation Alembic revision | `PASS — 007 (head)` |
| Five tables, timestamp constraints, append-only triggers, grants, retention function | `PASS — 5 tables; 5 recorded_at defaults; 5 append-only triggers; 10 daemon SELECT/INSERT table pairs; daemon retention EXECUTE=false; operator EXECUTE=true; SECURITY DEFINER=true` |

Target proof must immediately precede the migration. Approval for migration
does not authorize either credential or deployment.

## Approval 2 — isolated L2 runtime credential

**Status:** `COMPLETE — AUTHORIZED 2026-07-24T03:41:10Z / PROVISIONED 2026-07-24T04:10:03Z`

Exact approval required:

```text
APPROVE 05.4 L2 RUNTIME CREDENTIAL ref=zoqsmjeejfkrokwttjbx
```

Evidence to be recorded only after the exact approval and fresh target proof:

| Evidence | Status |
|---|---|
| Approval timestamp and matching production ref | `PASS — 2026-07-24T03:41:10Z / zoqsmjeejfkrokwttjbx` |
| Dedicated LOGIN and exclusive `l3_evidence_daemon` capability proof | `PASS — polyarb_l2_runtime_054; non-owner/non-superuser; only l3_evidence_daemon inheritance` |
| Masked `POLYARB_L2_RUNTIME_DB_DSN` Fly secret proof | `PASS — staged for the exact-SHA deployment; value never printed` |
| `POLYARB_SUPABASE_DB_DSN` absent from the deployed L2 secret/config surface | `PASS — absent from the Fly secret inventory before deployment` |
| Read-only runtime credential check | `PASS — exact ref/user/TLS/grants/RLS/sequence/function-denial proof` |

No credential value may appear in this log, repository, command output, or
evidence JSON.

## Approval 3 — isolated retention operator credential

**Status:** `COMPLETE — AUTHORIZED 2026-07-24T03:41:10Z / PROVISIONED 2026-07-24T04:12:23Z`

Exact approval required:

```text
APPROVE 05.4 RETENTION OPERATOR CREDENTIAL ref=zoqsmjeejfkrokwttjbx
```

Evidence to be recorded only after the exact approval and fresh target proof:

| Evidence | Status |
|---|---|
| Approval timestamp and matching production ref | `PASS — 2026-07-24T03:41:10Z / zoqsmjeejfkrokwttjbx` |
| Dedicated LOGIN with only `l3_retention_operator` inheritance | `PASS — polyarb_l3_retention_054; non-owner/non-superuser; zero table/sequence grants` |
| Masked out-of-band `L3_RETENTION_DSN` operator-store proof | `PASS — macOS Keychain only; value never printed` |
| Retention credential absent from Fly daemon secrets/repository/evidence | `PASS — absent from Fly and local .env; no tracked secret` |
| Read-only operator capability check and cleared local session | `PASS — exact ref/user/TLS; only retention function EXECUTE; environment cleared` |

Cleanup never falls back to `service_role` and remains a separately named,
separately approved action.

## Approval 4 — exact-SHA L2 deployment

**Status:** `AUTHORIZED BY USER OVERRIDE 2026-07-24T03:41:10Z / READY FOR EXACT-SHA DEPLOYMENT`

Exact approval required:

```text
APPROVE 05.4 L2 DEPLOYMENT sha=95bf1bd8714b92056c1ca6cca2d13ac9bd3d06d5
```

Fresh schema/runtime-credential/owner-DSN-absence proof must precede deployment.
Deployment identity evidence is currently unpopulated:

| Evidence | Status |
|---|---|
| Approval timestamp and exact SHA equality | `PENDING` |
| Deployed SHA | `PENDING` |
| Release, machine ID/version, and instance | `PENDING` |
| Image ref and external digest | `PENDING` |
| Boot/start anchor and cross-check | `PENDING` |
| Deployed AcceptanceConfig/recipe/mapping/code hashes | `PENDING` |

## Readiness evidence

**Status:** `NOT STARTED`

No readiness sample or T0 is declared. After an authorized exact-SHA deployment,
readiness requires two consecutive successful `5/10/10` promoter rows plus at
least 12 complete samples over at least 330 seconds, maximum gap at most 75
seconds, per-market freshness below 120 seconds, matching AcceptanceConfig,
zero disallowed event kinds, and complete 5-market/10-book-token/5-Yes-OHLC
coverage.

## Manifest attempts

**Status:** `NONE`

Every future attempt must use a unique `05.4-SOAK-MANIFEST-<T0compact>.json`
created with `O_EXCL`. T0 must be a future eligible boot-grid boundary with the
required lead, its single manifest binding must have server `recorded_at < T0`,
and a rejected attempt remains immutable `NOT-CLOSED`. No manifest may be
edited, overwritten, rebound, or reused.

| Attempt | Manifest | T0 | Pre-T0 DB binding | Scheduled-T0 sample | Status |
|---|---|---|---|---|---|
| — | — | — | — | — | `NONE` |

## Selected checkpoint reports

**Status:** `NONE`

| Checkpoint | Exact interval | Manifest-declared artifact | Not-before | Report hash / raw-row digest | Status |
|---|---|---|---|---|---|
| T0 | `[T0,T0+30s)` | `PENDING` | `T0+30s` | `PENDING` | `NOT STARTED` |
| T+6 | `[T0,T0+6h)` | `PENDING` | `T0+6h` | `PENDING` | `NOT STARTED` |
| T+12 | `[T0,T0+12h)` | `PENDING` | `T0+12h` | `PENDING` | `NOT STARTED` |
| T+18 | `[T0,T0+18h)` | `PENDING` | `T0+18h` | `PENDING` | `NOT STARTED` |
| T+24 | `[T0,T0+24h)` | `PENDING` | `T0+24h` | `PENDING` | `NOT STARTED` |

The not-before gates are immutable. A later query never substitutes for a
missing scheduled report.

## Verdict

**Current verdict:** `NOT STARTED — NO FORMAL MANIFEST OR SOAK WINDOW`

Final PASS is unavailable until one manifest-bound exact window has five
canonical reports, one common soak hash, re-queried raw-row digests, unchanged
identity/AcceptanceConfig, exactly one boot, every required strict invariant,
and zero unapproved disallowed event kinds. Any violation remains permanently
`NOT-CLOSED`.

## Next action

Re-prove revision 007, the dedicated runtime capability, and Fly owner-DSN
absence; then dispatch the workflow at the exact verified source:

```text
APPROVE 05.4 L2 DEPLOYMENT sha=95bf1bd8714b92056c1ca6cca2d13ac9bd3d06d5
```

Any workflow/deployed-SHA mismatch remains `DEPLOY FAILED`; readiness and T0 do
not begin.

## Security incident before migration

At `2026-07-24T03:41Z`, the first local direct-DSN derivation attempt passed a
quoted connection string to Node's URL parser. Its exception included the raw
input in tool output, so the production owner password must be treated as
compromised.

Containment completed:

- Migration did not run; production remained revision `006`.
- A read-only direct TLS query confirmed the intended project/database/user and
  revision before containment.
- SQL password rotation was attempted and rejected by Supabase with
  `permission denied to alter role`; no password changed.
- The compromised owner DSN was removed from local `.env`.
- The unused generated replacement was removed from Keychain.
- Browser automation, Supabase CLI login state, and a Management API token were
  unavailable, so Dashboard rotation could not be completed automatically.

Required recovery before any migration:

1. Reset the project database password in Supabase Dashboard → Project Settings
   → Database.
2. Put the new **direct** migration DSN in local `.env` as
   `POLYARB_SUPABASE_DB_DSN`, using host
   `db.zoqsmjeejfkrokwttjbx.supabase.co`, database/user `postgres`, and
   `sslmode=require`.
3. Do not paste the password or DSN into chat.

After recovery, the first command remains the read-only revision-006 proof.

### Recovery completed

At `2026-07-24T04:04Z`, the existing Edge/GitHub session reached the exact
Supabase project and reset the database password. Supabase's generated
authoritative direct connection value was transferred directly into macOS
Keychain without printing it; the clipboard was cleared immediately. A direct
TLS login as `postgres` succeeded before any migration.

The compromised credential is no longer active. No owner DSN was restored to
`.env`, and the owner credential is not present in the Fly secret inventory.
The migration then followed the recorded 006 proof and completed exactly once
to 007.
