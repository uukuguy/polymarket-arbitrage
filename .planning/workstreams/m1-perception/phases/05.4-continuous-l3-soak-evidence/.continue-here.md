---
context: phase
phase: 05.4-continuous-l3-soak-evidence
task: 2
total_tasks: 9
status: paused_for_production_migration_approval
last_updated: 2026-07-24T02:07:45Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through
> failure. Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: Approval separation — migration, runtime credential,
  retention credential, and deployment require four distinct exact approval
  strings; one approval never implies another.
- [x] CONSTRAINT: No evidence backfill — release 37 and both earlier soak
  windows are diagnostic-only; no late probe, raw SQL query, or log search can
  heal their missing continuous observations.
- [x] CONSTRAINT: Review cadence is not observation cadence — six-hour
  checkpoints summarize a continuously persisted 30-second/per-tick ledger;
  they are not six-hour spot samples.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| General approval inference | Earlier work exposed that making deploy reachable from a main push would execute production before schema/credential approval. | blocking | Match the exact Plan 05 approval string, production ref, or verified SHA immediately before its one authorized action. |
| Checkpoint backfill | The first 24-hour run missed T+6/T+12/T+18 and a later read could not reconstruct minimum-throughout-window truth. | blocking | Use manifest-derived not-before gates and O_EXCL artifacts; retain missing/failed checkpoints as immutable NOT-CLOSED evidence. |
| Attempt counter as sample identity | After a skipped boundary, attempt count diverged from the boot grid and poisoned later valid T0 windows. | advisory | Preserve `sample_seq = boundary_index`; verdict compares the complete boot-derived `(sample_seq, scheduled_at)` schedule. |
| Late promoter healing | A successful row written hours after its terminal time could previously make a missing tick appear present. | advisory | Keep the PostgreSQL and verdict invariant `finished_at <= recorded_at < finished_at+30s`. |

<current_state>
Phase 05.4 Plans 01–04 are complete on local `main`. Plan 04 added exact-window
manifest/five-report/raw-row verification, credential/target proofs, local
full-chain chaos, exact 30-second boot scheduling, sub-30-second promoter
recording, teaching docs, and a manual-event-only deploy job. Plan 05 is
`autonomous: false`; Task 1 is complete and committed at `03313a9`. Execution is
paused before Task 2's first production mutation.
</current_state>

<completed_work>

Completed before this handoff:

- Plans 01–03: append-only schema/store, truthful membership/promoter, atomic
  sampler/events/strict health.
- Plan 04 Tasks 1–4 and whole-branch remediation: done and summarized in
  `05.4-04-SUMMARY.md`.
- Final branch review: `APPROVE — 0 remaining findings`.
- Local merge: `main` now contains Plan 04 at `37e1dfb`; the owned Plan 04
  worktree and merged branch were removed.
- Post-merge full pytest: 1,811 passed, one expected xfail, one skip.
- Planning/JOURNAL/CURRENT/STATE/thread updates: committed; planning-status has
  no drift.
- Plan 05 Task 1: complete local release-candidate gate, 350 focused tests and
  the full 1,813-test repository run, plan-scope Ruff plus byte-identical legacy
  baseline, compile, image, docs, and planning gates. The immutable local
  evidence and four approval sections are recorded in `05.4-SOAK-LOG.md`.
- Bound release candidate:
  `95bf1bd8714b92056c1ca6cca2d13ac9bd3d06d5`; AcceptanceConfig digest:
  `c9269392e044c633e4ecfa4879ba6a66da2811bfb0cf444df275c57fadd35d60`.
</completed_work>

<remaining_work>

- Task 2: stop for migration, runtime credential, and retention credential
  approvals, each separately.
- Task 3: stop for exact verified-SHA deployment approval.
- Task 4: after deployed readiness, create and bind one future-grid manifest and
  retain its T0 report.
- Tasks 5–8: wait for distinct T+6/T+12/T+18/T+24 not-before boundaries.
- Task 9: re-query all raw rows, verify all five artifacts, and record PASS or
  permanent NOT-CLOSED.
</remaining_work>

<decisions_made>

- The four production approvals are non-delegable and non-combinable.
- `scheduled_at`/`sample_seq` identify the complete boot grid; actual
  `sampled_at` independently drives gap and freshness.
- Promoter terminal evidence must reach the database in less than 30 seconds.
- Event rejection is by immutable kind, not mutable severity.
- A main push cannot execute Fly deployment; only explicitly authorized manual
  dispatch can.
- Local PASS never authorizes production.
</decisions_made>

<blockers>

- Human action: exact migration approval naming `<PROD_REF>`.
- Human action: separate runtime credential approval naming the same ref.
- Human action: separate retention credential approval naming the same ref.
- Human action: later exact deployment approval naming Task 1's verified SHA.
- External time: each selected manifest checkpoint has a hard not-before time.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — role contract, session startup, SUMMARY, teaching, and safety
   discipline.
2. `.planning/JOURNAL.md` — read SESSION 83 and its final `[NEXT — CURRENT]`.
3. `.planning/workstreams/m1-perception/STATE.md` — current phase position and
   immutable production facts.
4. `.planning/workstreams/m1-perception/phases/05.4-continuous-l3-soak-evidence/05.4-04-SUMMARY.md`
   — delivered local evidence contract and verification results.
5. `.planning/workstreams/m1-perception/phases/05.4-continuous-l3-soak-evidence/05.4-05-PLAN.md`
   — exact approvals, tasks, and not-before gates.
6. `docs/superpowers/specs/2026-07-22-m1-continuous-l3-soak-evidence-design.md`
   — approved evidence semantics.
7. `.planning/threads/market-observation-architecture.md` §1.6, §2.9, and
   §2.10 — chain-truth, cadence, and implementation closure.

## Critical Anti-Patterns (do NOT repeat these)

- Treating a six-hour checkpoint as six hours of observation → accept only the
  continuous database ledger and exact-window verifier.
- Reusing release-37 or a rejected manifest/T0 → create a unique future-grid
  manifest and retain every rejected attempt unchanged.
- Using capped REST rows for coverage → aggregate distinct identities in SQL
  over the exact interval.
- Running deploy because files match workflow paths → require the exact manual
  deployment approval and verified SHA.
- Printing/storing DSNs or credentials → record only redacted target/role/TLS/
  capability proof.

## Infrastructure State

- Git: clean local `main` at `03313a9`, 34 commits ahead of `origin/main`; no
  push performed.
- Production: Alembic 006, L1 release 127, L2 release 37; Plan 04 code is not
  deployed.
- Credentials: no Plan 05 runtime or retention LOGIN has been provisioned.
- Deploy workflow: the deploy job is gated to `workflow_dispatch`.
- Processes: no project pytest, daemon, uvicorn, soak monitor, or local chaos
  process is running.
- Current image observation: Python/kill/which/curl present; pkill/ps/dig/ping
  absent. This is capability evidence, not production authorization.

## Current Approval Gate

Task 1's local evidence is complete. Do not run target proof or migration until
the user supplies this exact approval with the real production ref:

`APPROVE 05.4 PRODUCTION MIGRATION TO 007 ref=<PROD_REF>`

<context>
The local release candidate is green and recorded, but production still runs
Alembic 006 / release 37. Local PASS never authorizes migration, credentials,
deployment, or a fresh soak.
</context>

<next_action>
Resume with `/gsd-resume-work --ws m1-perception`, then require exactly
`APPROVE 05.4 PRODUCTION MIGRATION TO 007 ref=<PROD_REF>` before Task 2 runs any
production target proof or migration.
</next_action>
