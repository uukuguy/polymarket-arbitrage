---
phase: 02-l1-production-grade
plan: 01
workstream: m1-perception
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polyarb/storage/schemas.py
  - src/polyarb/snapshot/normalizer.py
  - src/polyarb/clients/gamma_client.py
  - src/polyarb/storage/sqlite_store.py
  - tests/m1-perception/test_normalizer.py
  - tests/m1-perception/test_schema_lockstep.py
  - tests/m1-perception/test_orchestrator.py
  - tests/m1-perception/test_parquet_sqlite_consistency.py
  - tests/m1-perception/test_makefile_triple_check.sh
  - Makefile
autonomous: true
decisions_addressed: [D-12]
requirements:
  - "framework abstraction A — minimal page_fetched_at_ms add-only column (RESEARCH §5)"
  - "L11 silent failure prevention — make snapshot-markets triple check (LEARNINGS L11, S5)"
  - "D-12 parquet/SQLite dual validation"

must_haves:
  truths:
    - "Each Gamma page records its own page_fetched_at_ms; markets within one snapshot have DIFFERENT page_fetched_at_ms values."
    - "fetched_at_ms column keeps its name and value (stage 5 stamp), but schema docstring documents it as a stage stamp, not a per-row fetch time."
    - "Old snapshot Parquet files (without page_fetched_at_ms) read back via DuckDB union_by_name=true with NULL fill — backward compatible."
    - "make snapshot-markets exits non-zero whenever SQLite row count does not increment OR Parquet file does not land OR row counts mismatch (triple-check gate)."
    - "Phase 01.1 lockstep test passes — schemas.py DDL, MARKETS_COLUMN_ORDER, MARKETS_INSERT_SQL, SNAPSHOT_SCHEMA all four sync points include page_fetched_at_ms."
  artifacts:
    - path: "src/polyarb/storage/schemas.py"
      provides: "page_fetched_at_ms nullable column in markets + events DDL + MARKETS_COLUMN_ORDER + MARKETS_INSERT_SQL + SNAPSHOT_SCHEMA (4 sync points)"
      contains: "page_fetched_at_ms"
    - path: "src/polyarb/clients/gamma_client.py"
      provides: "per-page _page_fetched_at_ms stamp emitted into raw dicts"
      contains: "_page_fetched_at_ms"
    - path: "src/polyarb/snapshot/normalizer.py"
      provides: "carry _page_fetched_at_ms through to normalized row"
      contains: "page_fetched_at_ms"
    - path: "tests/m1-perception/test_makefile_triple_check.sh"
      provides: "exit-code/parquet-file/SQLite-row triple-check contract"
      contains: "snapshot-markets"
    - path: "tests/m1-perception/test_parquet_sqlite_consistency.py"
      provides: "parquet rows == SQLite snapshots.market_count"
      contains: "market_count"
  key_links:
    - from: "src/polyarb/clients/gamma_client.py"
      to: "src/polyarb/snapshot/normalizer.py"
      via: "_page_fetched_at_ms private key on raw dict"
      pattern: "_page_fetched_at_ms"
    - from: "src/polyarb/snapshot/normalizer.py"
      to: "src/polyarb/storage/schemas.py"
      via: "page_fetched_at_ms column in MARKETS_COLUMN_ORDER"
      pattern: "page_fetched_at_ms"
---

<objective>
Phase 02 Wave 1 first plan — repair Phase 01.1 L2 schema bug (fetched_at_ms is a stage stamp, NOT a per-row fetch time) by adding a nullable `page_fetched_at_ms` column that captures the real Gamma per-page fetch time. This is the minimum-viable framework-abstraction-A landing (RESEARCH §5 方案 A) — the full Market State dataclass is deferred to Phase 02.1.

Also closes LEARNINGS L11/S5 silent failure root cause by upgrading `tests/test_makefile_triple_check.sh` to the **triple-check contract**: exit 0 ↔ SQLite row +1 ↔ Parquet file exists with row count matching SQLite.

Pure local Python work — zero deploy/cloud dependency. Implements the foundation that later plans (Supabase mirror, R2 sync) build on.

Purpose: Fix L2 schema-level误导 + lock the make-snapshot CLI entry's "really worked" contract before we put it on the cloud.
Output: 4-point schema lockstep landing + per-page stamp end-to-end + Makefile triple-check gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-CONTEXT.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-RESEARCH.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-PATTERNS.md
@.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-VALIDATION.md
@.planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-LEARNINGS.md
@CLAUDE.md

<interfaces>
<!-- Critical existing contracts the executor MUST honor. Extracted from codebase 2026-05-12. -->

From src/polyarb/storage/schemas.py (Phase 01.1, the 4 sync points):
- DDL `CREATE TABLE markets(...)` — around lines 90-130
- `MARKETS_COLUMN_ORDER: tuple[str, ...]` — around line 157
- `MARKETS_INSERT_SQL: str` — around line 180
- `SNAPSHOT_SCHEMA: pa.Schema` — around lines 237-265
- Already-present similar quadruple for events with EVENTS_COLUMN_ORDER + EVENTS_INSERT_SQL (3 sync points, no parquet schema)
- The lockstep test `tests/m1-perception/test_schema_lockstep.py` reads all four sources and asserts equality of column ordering — DO NOT remove or weaken this test.

From src/polyarb/snapshot/normalizer.py:
```python
def normalize_market(raw: dict, market_to_event_map: dict[str, str] | None = None) -> dict | None:
    # returns dict with keys matching MARKETS_COLUMN_ORDER (minus snapshot_id)
```

From src/polyarb/clients/gamma_client.py:
```python
async def _paginate(self, *, path: str, params: dict, label: str) -> list[dict]:
    # loops `while True`, fetches each page, calls out.extend(page) — single place to inject per-page stamp
```

From src/polyarb/snapshot/orchestrator.py (stage 5 stamp, the L2 bug site, lines ~340-343):
```python
clob_done_ms = int(time.time() * 1000)
for m in target_markets:
    m["fetched_at_ms"] = clob_done_ms  # stage stamp — DO NOT REMOVE, just document semantically
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — add page_fetched_at_ms tests (RED state)</name>
  <files>
    tests/m1-perception/test_normalizer.py,
    tests/m1-perception/test_schema_lockstep.py,
    tests/m1-perception/test_parquet_sqlite_consistency.py,
    tests/m1-perception/test_makefile_triple_check.sh
  </files>
  <read_first>
    - tests/m1-perception/test_normalizer.py (entire file — understand existing normalize_market fixture pattern)
    - tests/m1-perception/test_schema_lockstep.py (entire file — understand 4-point lockstep assertion structure)
    - tests/m1-perception/test_orchestrator.py (T6.1 e2e SQLite + Parquet assertion — analog for new test_parquet_sqlite_consistency.py)
    - tests/m1-perception/test_makefile_contract.py (lines 37-72 — analog for triple-check shell test)
    - tests/m1-perception/conftest.py (fixtures: tmp_db_path, tmp_parquet_root, settings_for_test)
    - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-VALIDATION.md (Wave 0 Requirements section)
    - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-PATTERNS.md §2.12 (4-point lockstep mandate)
  </read_first>
  <behavior>
    Each test should be RED (fail) when run now because the production code has not yet been changed. The executor of Task 2 will make them GREEN.

    Test 1.1 — `tests/m1-perception/test_normalizer.py::test_page_fetched_at_ms_carried_from_raw`:
    - Build raw market dict with `_page_fetched_at_ms = 1715500000000`
    - Call `normalize_market(raw)` → assert returned dict has key `"page_fetched_at_ms"` == 1715500000000
    - Build raw without `_page_fetched_at_ms` → assert returned dict has `"page_fetched_at_ms"` is None (nullable)

    Test 1.2 — extend `tests/m1-perception/test_schema_lockstep.py` adding `test_page_fetched_at_ms_in_all_four_sync_points`:
    - Read schemas.py DDL via parsing `CREATE TABLE markets(...)` regex/AST → assert `"page_fetched_at_ms"` present (INTEGER nullable)
    - Read `MARKETS_COLUMN_ORDER` → assert `"page_fetched_at_ms"` ∈ tuple
    - Read `MARKETS_INSERT_SQL` string → assert `"page_fetched_at_ms"` substring + matching number of `?` placeholders
    - Read `SNAPSHOT_SCHEMA` (pyarrow.Schema) → assert `schema.field("page_fetched_at_ms").type == pa.int64()` and `schema.field("page_fetched_at_ms").nullable is True`
    - For events: assert `events` DDL also contains `page_fetched_at_ms` and `EVENTS_COLUMN_ORDER` includes it

    Test 1.3 — new file `tests/m1-perception/test_parquet_sqlite_consistency.py`:
    - Fixture: minimal in-memory orchestrator output, 3 markets
    - Run snapshot orchestrator end-to-end (use respx mocks like test_orchestrator.py T6.1)
    - Assert: `SELECT market_count FROM snapshots WHERE id = ?` from SQLite == `pyarrow.parquet.read_table(parquet_path).num_rows`
    - Assert: `COUNT(*) FROM markets WHERE snapshot_id = ?` == same value
    - This is dual-source consistency (D-12 amendment validation extension)

    Test 1.4 — new file `tests/m1-perception/test_makefile_triple_check.sh` (executable bash):
    Header:
    ```bash
    #!/usr/bin/env bash
    set -euo pipefail
    # Triple check: make snapshot-markets exit 0 ↔ SQLite row +1 ↔ Parquet file exists ↔ row counts match
    # Closes LEARNINGS L11/S5 silent failure root cause.
    ```
    Body (use POLYARB_DATA_DIR override + respx-style fixture servers won't work in shell; instead use the orchestrator's TEST_FIXTURES_DIR pattern, or run against the existing CI-friendly fast subset):
    - Record BEFORE: row count `sqlite3 $DB "SELECT COUNT(*) FROM snapshots"` and Parquet file count `find $PARQUET_ROOT -name '*.parquet' | wc -l`
    - Run `make snapshot-markets POLYARB_GAMMA_FIXTURE_DIR=tests/m1-perception/fixtures/gamma_subset_small POLYARB_CLOB_FIXTURE_DIR=tests/m1-perception/fixtures/clob_subset_small` — capture exit code
    - Assert: exit code == 0
    - Assert: AFTER snapshots count == BEFORE + 1
    - Assert: AFTER parquet count == BEFORE + 1
    - Assert: row count in newest parquet == `SELECT market_count FROM snapshots ORDER BY id DESC LIMIT 1`
    - Exit 1 with explicit diagnostic if any check fails (echo "FAIL: <which gate>" >&2)

    NOTE: if fixture dirs don't already exist, gracefully fall back to using the existing test_orchestrator fixtures. If even those aren't usable in a shell, this test should skip (exit 77 = skip per autotools convention) with a clear message, but explicitly document that this is a contract test, NOT optional. The Phase 02 Plan 04 will harden the fixture path for prod-like environments.

    Make file executable: `chmod +x tests/m1-perception/test_makefile_triple_check.sh`
  </behavior>
  <action>
    Write tests 1.1, 1.2, 1.3 as pytest tests in the indicated files. Test 1.4 is a bash script in tests/m1-perception/. Run each test once and confirm RED (fails because schemas.py / normalizer.py / gamma_client.py have not yet been updated). Commit RED state:
    ```
    test(02-01): add Wave 0 tests for page_fetched_at_ms + triple-check Makefile contract
    ```
    Use loguru style only if anything is logged. Use Python 3.12 type hints (`dict | None`, `list[str]`).
  </action>
  <verify>
    <automated>uv run pytest -x -q tests/m1-perception/test_normalizer.py::test_page_fetched_at_ms_carried_from_raw tests/m1-perception/test_schema_lockstep.py::test_page_fetched_at_ms_in_all_four_sync_points tests/m1-perception/test_parquet_sqlite_consistency.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/m1-perception/test_normalizer.py` contains the literal string `test_page_fetched_at_ms_carried_from_raw`
    - File `tests/m1-perception/test_schema_lockstep.py` contains the literal string `test_page_fetched_at_ms_in_all_four_sync_points`
    - File `tests/m1-perception/test_parquet_sqlite_consistency.py` exists and contains `parquet_path` and `market_count`
    - File `tests/m1-perception/test_makefile_triple_check.sh` exists, is executable (`test -x` returns 0), and contains the strings `make snapshot-markets`, `SELECT COUNT(*) FROM snapshots`, `find` and `*.parquet`
    - The pytest command in `<verify>` reports `FAILED` for at least 3 of these tests (RED state confirmed)
    - Commit message starts with `test(02-01):`
  </acceptance_criteria>
  <done>RED Wave 0 tests committed. Subsequent tasks must drive them to GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page_fetched_at_ms — implement (drive Wave 0 to GREEN)</name>
  <files>
    src/polyarb/storage/schemas.py,
    src/polyarb/snapshot/normalizer.py,
    src/polyarb/clients/gamma_client.py,
    src/polyarb/storage/sqlite_store.py
  </files>
  <read_first>
    - src/polyarb/storage/schemas.py (entire file — must understand all 4 sync points before editing)
    - src/polyarb/snapshot/normalizer.py (entire file — understand normalize_market return shape)
    - src/polyarb/clients/gamma_client.py (lines 150-230 — focus on `_paginate` method)
    - src/polyarb/storage/sqlite_store.py (lines 90-220 — `_row_to_tuple` follows MARKETS_COLUMN_ORDER automatically)
    - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-PATTERNS.md §2.10, §2.11, §2.12 (per-page stamp + 4-point lockstep mandate)
    - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-RESEARCH.md §5 (full implementation snippets)
    - .planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-LEARNINGS.md L2 (root cause) + P7 (schema evolution add-only constraint)
  </read_first>
  <action>
    Apply concrete edits:

    1. `src/polyarb/clients/gamma_client.py` — inside `_paginate`, after page successfully fetched and BEFORE `out.extend(page)`:
       ```python
       page_fetched_at_ms = int(time.time() * 1000)
       for raw in page:
           if isinstance(raw, dict):
               raw["_page_fetched_at_ms"] = page_fetched_at_ms
       ```
       (Add `import time` at top if not already present.)

    2. `src/polyarb/snapshot/normalizer.py` — inside `normalize_market`, in the returned dict, add the line `"page_fetched_at_ms": raw.get("_page_fetched_at_ms"),` adjacent to the existing `fetched_at_ms` placeholder. Do NOT rename `fetched_at_ms`. Add an inline comment: `# Phase 02: stage stamp filled by orchestrator stage 5; see schemas.py for semantic note`.
       If there is an `normalize_event` (or equivalent for events), apply the same change: `"page_fetched_at_ms": raw.get("_page_fetched_at_ms"),`.

    3. `src/polyarb/storage/schemas.py` — apply 4-point lockstep for markets, 3-point for events:

       Sync point 1 (DDL): In the `CREATE TABLE markets(...)` raw SQL string, add line `"page_fetched_at_ms" INTEGER,` (nullable, no NOT NULL) immediately after the existing `"fetched_at_ms"` column. Add the SAME line `"page_fetched_at_ms" INTEGER,` in `CREATE TABLE events(...)` after `fetched_at_ms`.

       Sync point 2 (column order tuples): Add `"page_fetched_at_ms",` to `MARKETS_COLUMN_ORDER` immediately after `"fetched_at_ms"`. Same for `EVENTS_COLUMN_ORDER`.

       Sync point 3 (insert SQL strings): Update `MARKETS_INSERT_SQL` to add `page_fetched_at_ms` in the column list and an additional `?` in the values placeholder, in matching position. Same for `EVENTS_INSERT_SQL`.

       Sync point 4 (pyarrow schema, markets only): Update `SNAPSHOT_SCHEMA` to add `pa.field("page_fetched_at_ms", pa.int64(), nullable=True)` immediately after the existing `fetched_at_ms` field.

       Add a module-level comment near `fetched_at_ms` definitions: `# Semantic note (Phase 02): fetched_at_ms is the STAGE 5 completion stamp, same value for all rows within a snapshot. Use page_fetched_at_ms for per-page real fetch time (nullable for pre-02 snapshots).`

    4. `src/polyarb/storage/sqlite_store.py` — verify `_row_to_tuple` reads MARKETS_COLUMN_ORDER (Phase 01.1 P7); no explicit changes needed. Verify by reading the method — if it uses `.get(col)` style iteration, it adapts automatically. If it has hard-coded column references, add explicit `row.get("page_fetched_at_ms")` handling.

    No file rename. No column rename. Per LEARNINGS P7 schema evolution = add-only.

    Run the Wave 0 tests from Task 1. They MUST now turn GREEN. Run the full m1-perception lockstep test to confirm Phase 01.1 invariants still hold. Commit:
    ```
    feat(02-01): add page_fetched_at_ms nullable column — fixes L2 fetched_at_ms误导
    ```
  </action>
  <verify>
    <automated>uv run pytest -x -q tests/m1-perception/test_normalizer.py::test_page_fetched_at_ms_carried_from_raw tests/m1-perception/test_schema_lockstep.py tests/m1-perception/test_parquet_sqlite_consistency.py tests/m1-perception/test_orchestrator.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'page_fetched_at_ms' src/polyarb/storage/schemas.py` returns a value `>= 6` (DDL markets + DDL events + 2 column orders + 2 insert SQLs + pyarrow field = at least 6 mentions, with some lines having 2 occurrences)
    - `grep 'page_fetched_at_ms' src/polyarb/snapshot/normalizer.py` returns at least one line
    - `grep '_page_fetched_at_ms' src/polyarb/clients/gamma_client.py` returns at least one line, and `grep -c 'time.time()' src/polyarb/clients/gamma_client.py` returns `>= 1`
    - The full test command above shows 0 failures (`FAILED` count is 0)
    - Test `test_page_fetched_at_ms_in_all_four_sync_points` passes
    - Phase 01.1 lockstep test (`test_schema_lockstep.py`) still passes — Phase 01.1 regression check
    - `grep "Phase 02" src/polyarb/storage/schemas.py` returns at least one hit (the semantic note comment)
    - Commit message starts with `feat(02-01):`
  </acceptance_criteria>
  <done>page_fetched_at_ms reaches Parquet + SQLite. 4-point lockstep satisfied. L2 schema bug fixed.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile triple-check gate + commit Wave 0 GREEN proof</name>
  <files>
    Makefile,
    tests/m1-perception/test_makefile_triple_check.sh,
    tests/m1-perception/test_makefile_contract.py
  </files>
  <read_first>
    - Makefile (entire — pay attention to lines 14-220 — understand .PHONY + echo-header pattern + uv run convention)
    - tests/m1-perception/test_makefile_contract.py (lines 37-72 — existing `make -n` dry-run contract pattern; extend it)
    - tests/m1-perception/test_makefile_triple_check.sh (Task 1 output)
    - .planning/workstreams/m1-perception/phases/02-l1-production-grade/02-PATTERNS.md §2.14 (Makefile 4-rule pattern)
    - .planning/workstreams/m1-perception/phases/01.1-observation-toolkit/01.1-LEARNINGS.md L11 (silent failure root cause) + S5 (SUMMARY drift incident)
  </read_first>
  <action>
    Add a new `make triple-check` target invoking `bash tests/m1-perception/test_makefile_triple_check.sh`. Recipe template (follow Phase 01.1 Makefile echo-header convention):

    ```makefile
    ## triple-check: 跑 make snapshot-markets 全链路三重契约 (exit 0 ↔ SQLite row +1 ↔ parquet file landed)
    .PHONY: triple-check
    triple-check:
    	@echo ">> triple-check — verifying make snapshot-markets does what it claims"
    	@echo ">> closes LEARNINGS L11/S5 silent failure root cause"
    	@echo ""
    	bash tests/m1-perception/test_makefile_triple_check.sh
    ```

    Also add to `test_makefile_contract.py` a new function:
    ```python
    def test_make_triple_check_dry_run_recipe() -> None:
        result = subprocess.run(["make", "-n", "triple-check"], cwd=PROJECT_ROOT, capture_output=True, text=True, check=False)
        assert result.returncode == 0
        assert "tests/m1-perception/test_makefile_triple_check.sh" in result.stdout
    ```

    Ensure `make help` continues to list `triple-check` (the `##` comment line above the target must follow the existing Makefile help-extraction grep pattern; inspect the existing `help` target to confirm).

    Verify the bash test script actually runs end-to-end. If fixture limitations force exit 77 (skip), explicitly note this in the SUMMARY and create a follow-up TODO to harden in Plan 04 (when Dockerfile + fixtures lock down).

    Commit:
    ```
    feat(02-01): add make triple-check gate — closes L11/S5 silent failure
    ```

    Then create the plan SUMMARY (`02-01-SUMMARY.md`) per CLAUDE.md plan-末 三件套 discipline. Use template `$HOME/.claude/get-shit-done/templates/summary.md`.
  </action>
  <verify>
    <automated>make -n triple-check 2>&1 | grep -q 'test_makefile_triple_check.sh' && uv run pytest -x -q tests/m1-perception/test_makefile_contract.py::test_make_triple_check_dry_run_recipe 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^triple-check:' Makefile` returns 1
    - `grep '## triple-check:' Makefile` returns at least 1 line (so `make help` will list it)
    - `make -n triple-check` exits 0 and stdout contains `tests/m1-perception/test_makefile_triple_check.sh`
    - `bash tests/m1-perception/test_makefile_triple_check.sh` exits with status 0 (full triple-check passes) OR exit 77 (skip with documented fallback) — exit 1/2 is a fail
    - `test_make_triple_check_dry_run_recipe` passes
    - File `.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-01-SUMMARY.md` exists (P8 discipline)
    - `make planning-status` reports no DRIFT for Phase 02 Plan 01 (pre-commit hook will block otherwise)
    - Commit message starts with `feat(02-01):`
  </acceptance_criteria>
  <done>Triple-check gate installed; L11 silent-failure regression closed; SUMMARY landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Polymarket Gamma API → daemon | untrusted upstream JSON; existing F-1 _safe_float, F-2 follow_redirects=False, F-5 truncation caps already in place |
| local SQLite/Parquet writes | trusted; sole writer = orchestrator process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information disclosure | loguru log of `_page_fetched_at_ms` injected into raw dict | accept | private `_` prefix is internal convention only; field is non-secret integer (ms timestamp); not a leak vector. |
| T-02-08 | Tampering (supply chain) | new pyarrow.int64 schema field; verify pinned versions unchanged | accept | no new deps added in this plan; uv.lock unchanged. |
| Phase-01 carry-over | Tampering | gamma_client `_paginate` injection point now writes to dict | accept | input is from Polymarket API; we ADD a key that downstream owns. No external code reads `_page_fetched_at_ms` from the raw dict, only our normalizer pulls it. |

No high-severity threats introduced in this plan.
</threat_model>

<verification>
- All Phase 01.1 m1-perception tests still pass (`uv run pytest -q tests/m1-perception/` = 0 failures)
- New Wave 0 tests are GREEN (4-point lockstep + per-page stamp + parquet/SQLite consistency)
- `make triple-check` exit 0 (or 77 documented skip — but explicit, not silent)
- `make planning-status` clean
</verification>

<success_criteria>
- `page_fetched_at_ms` exists in SQLite `markets` and `events` tables, in Parquet schema, and in normalized rows
- Old (pre-02) Parquet still readable via DuckDB `union_by_name=true` (regression-safe — no Phase 01.1 test breaks)
- `make triple-check` is the documented gate that closes L11 silent failure
- Phase 02 Plan 02 can build on the new schema and triple-check contract
</success_criteria>

<output>
After completion, create `.planning/workstreams/m1-perception/phases/02-l1-production-grade/02-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
