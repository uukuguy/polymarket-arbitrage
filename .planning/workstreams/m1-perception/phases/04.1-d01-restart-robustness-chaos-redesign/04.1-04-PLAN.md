---
phase: 04.1-d01-restart-robustness-chaos-redesign
plan: 04
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02", "04.1-03"]
files_modified:
  - .planning/workstreams/m1-perception/phases/04.1-d01-restart-robustness-chaos-redesign/04.1-SOAK-LOG.md
autonomous: false   # contains a checkpoint:human-verify task
requirements: []   # ROADMAP-scoped — D-06 verdict re-run + Pitfall 4 observation
must_haves:
  truths:
    - "The deployed polyarb-l2 image == latest main (G-02/G-03/G-04 all shipped) before chaos runs"
    - "After deploy, ws:subscribed_count > 3 (G-02 startup-prime populated the candidate set on restart)"
    - "The WS kill is triggered in-flight via the HMAC endpoint on a RUNNING process (G-03 — no machine restart)"
    - "RSS at T1/T2/T3 is read from /health process:rss_kb (G-04 — current process, not PID 1)"
    - "A real D-06 verdict (3 indicators) is recorded in 04.1-SOAK-LOG.md"
    - "Pitfall 4 watchdog false-trip is observed/recorded (now possible since the process survives the storm)"
  artifacts:
    - path: ".planning/workstreams/m1-perception/phases/04.1-d01-restart-robustness-chaos-redesign/04.1-SOAK-LOG.md"
      provides: "D-06 verdict + Pitfall 4 observation + raw T1/T2/T3 numbers"
      contains: "D-06"
  key_links:
    - from: "make chaos-ws-kill ON=1"
      to: "running L2 WS consumer in-flight close"
      via: "HMAC endpoint flips process-local flag (no restart)"
      pattern: "chaos-ws-kill"
    - from: "/tmp/inj4t-t{1,2,3}.json process:rss_kb"
      to: "04.1-SOAK-LOG.md RSS recovery ratio"
      via: "operator computes rss_recovery / rss_baseline"
      pattern: "process:rss_kb"
---

<objective>
Close D-06: deploy the three fixes to prod and RE-RUN the throughput chaos to get a
REAL verdict (Phase 04 Task 3 verdict was DEFERRED — 04-04-SUMMARY). With G-02 the
candidate set survives restart (subs > 3); with G-03 the kill is in-flight on a
running process (no restart) so Pitfall 4 watchdog false-trip is finally observable;
with G-04 RSS is measured against the real Python process.

Purpose: produce the real D-06 pass/fail verdict + the Pitfall 4 observation that
was blocked in Phase 04, recorded in a new 04.1-SOAK-LOG.md.

Output: 04.1-SOAK-LOG.md with the verdict + raw numbers + watchdog observation.

VALIDATION STRATEGY NOTE (for the plan-checker, Dimension 8 / Nyquist): D-05/D-06
validation is INHERENTLY MANUAL — it is a live prod chaos measurement against a
real-time candidate set, not a CI-reproducible assertion. The deterministic behavior
of each fix (startup-prime fires, endpoint flips flag, RSS reads current process) is
unit-tested in plans 04.1-01/02/03. This plan's "test" is the human-verified prod
run. This is intentional and matches the Phase 04-04 Task 3 pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/phases/04.1-d01-restart-robustness-chaos-redesign/04.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/04-candidate-set-l2-throughput/04-SOAK-LOG.md
@.planning/workstreams/m1-perception/phases/04-candidate-set-l2-throughput/04-04-SUMMARY.md

D-06 PASS criteria (from Makefile chaos-l2-inj4-throughput header, RESEARCH Q4):
  (1) frame_rate_recovery >= frame_rate_baseline * 0.90   (zero dropped frames)
  (2) watchdog state == WAITING_FOR_EVENT within 60s of cleanup
  (3) rss_recovery <= rss_baseline * 1.30                 (memory within 30%)

Pre-flight discipline (canonical refs):
- feedback_parallel-worktree-rebase-discipline-2026-05 — deployed image MUST == latest main
- feedback_fly-api-token-shadowing-2026-05 — FLY_API_TOKEN= prefix on all flyctl lines
- container-image-aware (CLAUDE.md) — G-03 chaos is now an HTTP curl, no container tooling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy fixes to prod L2 + pre-flight verify deployed image == latest main</name>
  <read_first>
    - feedback_parallel-worktree-rebase-discipline-2026-05 (deployed image == latest main)
    - feedback_fly-api-token-shadowing-2026-05 (FLY_API_TOKEN= prefix)
    - Makefile (the L2 deploy target — search for deploy-l2 / fly-l2)
  </read_first>
  <action>
    1. Confirm working tree is on latest main with 04.1-01/02/03 committed
       (`git log --oneline -6`, `git status -sb`). All three fix plans' code +
       SUMMARYs present, planning-status zero-drift.
    2. Deploy polyarb-l2 to Fly. Use the existing L2 deploy path (FLY_API_TOKEN=
       prefix per shadowing discipline — the repo secret is L1-only, so local
       `FLY_API_TOKEN= flyctl deploy -a polyarb-l2 ...` per the carry-over note).
       If the deploy path is `make`-wrapped, use that; otherwise the flyctl deploy
       line with the FLY_API_TOKEN= prefix.
    3. Pre-flight verify the DEPLOYED image is the just-built one (not a stale
       machine — the Fly machines-API EOF deferred issue): check the running image
       ref / release id matches the local build, and curl
       `https://polyarb-l2.fly.dev/health | jq '.checks | keys'` shows BOTH
       `process:rss_kb` (G-04) present AND the chaos endpoint reachable (a signed
       `make chaos-ws-kill ON=0` returns 200, then leave it cleared). This proves
       G-03 + G-04 are LIVE in the deployed image before the chaos run.
  </action>
  <verify>
    <automated>curl -sS https://polyarb-l2.fly.dev/health | jq -e '.checks["process:rss_kb"][0].observedValue != null' >/dev/null && echo "G-04 live"</automated>
  </verify>
  <acceptance_criteria>
    - Deployed image == latest main (G-02/G-03/G-04 all in the running image).
    - /health shows process:rss_kb with a numeric observedValue (G-04 live).
    - make chaos-ws-kill ON=0 returns 200 (G-03 endpoint reachable + HMAC works), flag left cleared.
    - FLY_API_TOKEN= prefix used on every flyctl line.
  </acceptance_criteria>
  <done>Fixes deployed + verified live on prod L2; image == latest main.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run prod chaos, record D-06 verdict + Pitfall 4 observation</name>
  <what-built>
    G-02 (startup-prime → candidate set survives restart), G-03 (in-band HMAC WS-kill
    on a running process, no restart), G-04 (process:rss_kb on /health) are all
    deployed to prod L2. The chaos-l2-inj4-throughput recipe now: primes candidates
    on boot, flips the kill via `make chaos-ws-kill ON=1` on the running 60-asset
    process, reads RSS from /health process:rss_kb, clears via `ON=0`.
  </what-built>
  <how-to-verify>
    1. Pick a HIGH-activity calendar window if possible (events/election day) — per
       planner A2 / MEMORY warning, a low-activity window can select ~0 markets and
       degrade the test to a bootstrap-equivalent. If only a low-activity window is
       available, proceed but record actual N (documented-deferral is NOT a failure
       per D-06.4).
    2. Run: `make chaos-l2-inj4-throughput`
       (it self-aborts if /healthz != 200 or ws:subscribed_count <= 3 — the latter
       now confirms G-02 startup-prime worked across the deploy restart).
    3. Watch the recipe emit T1 (baseline), wait 5min, T2 (baseline rate), STORM
       (in-band kill via chaos-ws-kill ON=1), wait 60s, T3 (recovery), CLEANUP
       (chaos-ws-kill ON=0). RSS at each T comes from /tmp/inj4t-t{1,2,3}.json
       process:rss_kb (G-04). Final /health should be overall=pass / HTTP 200.
    4. Observe Pitfall 4 (D-06.3): because the process is NOT restarted (G-03), the
       same 60-asset process lives across the storm boundary — watch flyctl logs /
       /health ws:connection_state for whether the watchdog FALSE-TRIPS (declares
       RECONNECTING / forces a reconnect) during the healthy baseline window (T1→T2)
       BEFORE the storm. Record what you see (false-trip observed? frequency? or
       clean?).
    5. Compute the 3 D-06 indicators from the raw numbers:
       (1) frame_rate_recovery >= baseline * 0.90 ?
       (2) ws_state == WAITING_FOR_EVENT within 60s of cleanup ?
       (3) rss_recovery <= rss_baseline * 1.30 ?
    6. Record into the NEW file 04.1-SOAK-LOG.md (D-06.4): the run timestamp,
       candidate set N (subscribed_count), the three indicators with PASS/FAIL each,
       the overall D-06 verdict, the Pitfall 4 watchdog observation, and any
       low-activity-window caveat (actual N). Mirror the 04-SOAK-LOG.md §G-* style.
  </how-to-verify>
  <resume-signal>
    Type "verdict recorded" once 04.1-SOAK-LOG.md contains the D-06 verdict (3
    indicators) + the Pitfall 4 watchdog observation. If the window was low-activity
    and N degraded, type "deferred: N=<actual>" with the recorded caveat (still a
    valid close per D-06.4). Describe any indicator that FAILED so we decide whether
    a follow-up fix is needed.
  </resume-signal>
</task>

</tasks>

<verification>
- 04.1-SOAK-LOG.md exists with: deploy proof, candidate N, 3 D-06 indicators with
  PASS/FAIL, overall verdict, Pitfall 4 watchdog observation.
- The chaos run used the in-band endpoint (no machine restart) — confirmable from
  the recipe output (chaos-ws-kill calls, no `flyctl secrets set POLYARB_WS_TEST_KILL`).
- RSS numbers in the SOAK-LOG came from /health process:rss_kb (G-04).
</verification>

<success_criteria>
- A real D-06 verdict is recorded (or a documented N-degraded deferral per D-06.4).
- The Pitfall 4 watchdog behaviour over a healthy window is observed and recorded.
- Phase 04.1 closes the throughput-verdict loop that Phase 04 left DEFERRED.
</success_criteria>

<output>
After completion, create
`.planning/workstreams/m1-perception/phases/04.1-d01-restart-robustness-chaos-redesign/04.1-04-SUMMARY.md`
(reference the verdict recorded in 04.1-SOAK-LOG.md).
</output>
