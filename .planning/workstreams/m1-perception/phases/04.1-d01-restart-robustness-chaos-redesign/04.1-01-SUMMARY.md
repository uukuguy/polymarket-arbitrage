---
phase: 04.1-d01-restart-robustness-chaos-redesign
plan: "01"
subsystem: daemon
tags: [l2-daemon, candidate-refresh, startup, catchup, websocket, asyncpg]

# Dependency graph
requires:
  - phase: 04-candidate-set-l2-throughput
    provides: "G-01 cold-start debounce trap fix (_last_refresh_at_s init sentinel)"
  - phase: 04-candidate-set-l2-throughput
    provides: "_dispatch_on_snapshot helper + catchup_from_cursor call pattern"
provides:
  - "Eager startup-prime: unconditional _dispatch_on_snapshot after catchup envelope in l2_main.py"
  - "G-02 fix: candidate set populated on every cold start regardless of missed snapshot count"
  - "Unit tests: 7 tests covering all three catchup outcomes + structural guards"
affects:
  - "04.1-04 chaos re-run: G-02 prime means ws:subscribed_count > 3 bootstrap ids post-restart"
  - "D-06 throughput verdict: reliable candidate scale on every cold start enables accurate verdict"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Eager startup-prime: sentinel payload (snapshot_id=-1) dispatched through existing helper after catchup envelope"
    - "Additive dispatch: prime is additive over replay loop — does NOT replace bootstrap or replay paths"
    - "Sentinel-safe dispatch: on_snapshot_complete uses snapshot_id only for log lines, never row read"

key-files:
  created:
    - tests/m1-perception/test_l2_startup_prime.py
  modified:
    - src/polyarb/daemon/l2_main.py

key-decisions:
  - "Prime placement: AFTER outer try/except closes (fires even on catchup-raises), BEFORE _listener_runner"
  - "Sentinel snapshot_id=-1: safe per 04.1-PATTERNS.md confirmation (on_snapshot_complete never reads snapshot row)"
  - "Additive only: replay loop (missed>0 path) and bootstrap fallback both preserved untouched"
  - "Fail-soft inheritance: prime failure propagates through existing on_snapshot_complete fail-soft envelope, no crash"

patterns-established:
  - "Catchup+prime pattern: any daemon that catchup-replays missed events should also dispatch a startup-prime to handle the zero-missed case"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-05-30
---

# Phase 04.1 Plan 01: G-02 Eager Startup-Prime Summary

**Unconditional startup-prime dispatch in l2_main.py forces markets_latest fetch after every cold start, fixing the cross-restart candidate-set starvation bug (04-SOAK-LOG §G-02)**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-05-29T17:13:00Z
- **Completed:** 2026-05-29T17:28:04Z
- **Tasks:** 1 (TDD: RED → GREEN)
- **Files modified:** 2 (l2_main.py + test file created)

## Accomplishments

- Added unconditional `_dispatch_on_snapshot({"snapshot_id": -1, "_startup_prime": True, "ts_s": time.time()})` after the catchup try/except closes in `l2_main.py` (line ~434)
- Prime fires on all three catchup outcomes: no-missed snapshots, missed>0, and catchup-raises (cursor table absent)
- Bootstrap fallback (D-04 `initial_assets=_bootstrap_ids`) and replay loop unchanged — additive change only
- 7 unit tests: 4 structural (source code guards) + 3 behavioural (all catchup paths dispatch the prime)
- TDD discipline: RED commit (tests fail) → GREEN commit (implementation) in proper sequence

## Task Commits

TDD task with two commits:

1. **RED — failing tests** - `9a01c5d` (test)
2. **GREEN — startup-prime implementation** - `7109689` (feat)

## Files Created/Modified

- `src/polyarb/daemon/l2_main.py` — Added 13-line startup-prime block after catchup try/except (lines 425-437)
- `tests/m1-perception/test_l2_startup_prime.py` — 7 tests: structural guards + all-catchup-path behavioural coverage

## Decisions Made

- **Prime placement after outer except**: ensures prime fires even when `catchup_from_cursor` raises (cursor table not yet migrated). Placement is AFTER `except Exception` closes and BEFORE `_listener_runner` definition — both consumer and watchdog tasks exist at this point so `asyncio.create_task` in `_dispatch_on_snapshot` has a running loop.
- **Sentinel snapshot_id=-1**: confirmed safe per 04.1-PATTERNS.md §G-02 reference analysis — `on_snapshot_complete` only uses `payload.get("snapshot_id")` for log lines (l2_candidate_refresh.py:361, 413), never to query a snapshot row.
- **No new imports**: `time` is already imported in l2_main.py (used at §311), so no import changes needed.

## Deviations from Plan

**Rule 3 — Auto-fix: `uv sync --extra dev` needed before async tests could run**
- **Found during:** GREEN phase verification run
- **Issue:** Worktree's fresh venv had `pytest-asyncio` not installed (only `pytest` in base sync). The `asyncio_mode = "auto"` config requires `pytest-asyncio>=0.23`. Tests failed with "async def functions are not natively supported".
- **Fix:** Ran `uv sync --extra dev` to install `pytest-asyncio==0.23.8` and other dev deps. Verified existing async tests (`test_alerts.py`) also failed before the fix — this is a worktree venv bootstrapping issue, not a code bug.
- **Files modified:** none (venv only)
- **Verification:** All 7 tests green after sync.

---

**Total deviations:** 1 auto-fixed (Rule 3 — blocking dev env issue)
**Impact on plan:** Minimal — one `uv sync` call. No scope creep.

## Issues Encountered

None beyond the dev-env deviation above.

## Known Stubs

None — implementation is complete. The prime dispatch wires directly to the existing `_dispatch_on_snapshot` helper and `on_snapshot_complete` function, both of which are production-grade with full fail-soft envelopes.

## Threat Flags

None — this change adds a dispatch call within an existing daemon function. No new network endpoints, auth paths, file access patterns, or schema changes introduced. The sentinel payload (`snapshot_id=-1`) is confined to log lines in `on_snapshot_complete` and never reaches any external query path.

## Next Phase Readiness

- G-02 fix is complete and ready for deployment alongside G-03 (in-band chaos endpoint) and G-04 (RSS sub-check)
- The prime fires on every cold start; chain-truth surface is via the existing `candidates:supabase_fetch_age_seconds` /health sub-check (D-01.5 — satisfied by `_record_fetch_success()` call on successful prime fetch)
- End-to-end verification of prime effectiveness deferred to 04.1-04 human-verify chaos re-run (D-06.1/D-06.2)

## Self-Check

- [x] `tests/m1-perception/test_l2_startup_prime.py` exists (7 tests, all green)
- [x] `src/polyarb/daemon/l2_main.py` contains `_startup_prime` marker at line 434
- [x] `grep -n "_startup_prime" src/polyarb/daemon/l2_main.py` → line 434 ✓
- [x] RED commit `9a01c5d` exists (test-only, prime absent, tests fail)
- [x] GREEN commit `7109689` exists (prime added, tests pass)

---
*Phase: 04.1-d01-restart-robustness-chaos-redesign*
*Completed: 2026-05-30*
