---
phase: 04.1-d01-restart-robustness-chaos-redesign
plan: 04
subsystem: infra
tags: [chaos-engineering, fly-deploy, websocket, watchdog, d-06, soak]

requires:
  - phase: 04.1-01
    provides: G-02 eager startup-prime (candidate set survives restart → subs=62)
  - phase: 04.1-02
    provides: G-04 process:rss_kb on /health (real process, not PID-1)
  - phase: 04.1-03
    provides: G-03 in-band HMAC ws-test-kill endpoint (kill on running process, no restart)
provides:
  - Real D-06 throughput verdict (PARTIAL — was BLOCKED in Phase 04)
  - Pitfall 4 watchdog false-trip OBSERVED + root-caused (GAP-401)
  - Prod proof all three fixes (G-02/03/04) live in deployed image
affects: [m1-perception, watchdog, ws-consumer, future-phase-watchdog-fix]

tech-stack:
  added: []
  patterns:
    - "Chaos re-run pre-flight: deployed image == latest main (verified via /health sub-check presence, not just deploy success)"
    - "In-band HTTP chaos (no machine restart) makes accumulated-state bugs observable across a healthy window"

key-files:
  created:
    - .planning/workstreams/m1-perception/phases/04.1-d01-restart-robustness-chaos-redesign/04.1-SOAK-LOG.md
  modified: []

key-decisions:
  - "Recorded D-06 as PARTIAL (1 PASS / 1 FAIL / 1 DEFERRED) faithfully — the FAIL is a real watchdog bug, not papered over"
  - "Post-chaos: restarted L2 machine to clear the reconnect storm-cap and restore healthy WS"
  - "Frame-rate indicator (1) DEFERRED per D-06.4 — low-activity window + /health lacks frame_count surface"

patterns-established:
  - "Pitfall-4 observability: only achievable with G-03 (no-restart kill) because restart resets the reconnect counter that the false-trips consume"

requirements-completed: []

duration: 25min
completed: 2026-05-30
---

# Phase 04.1 Plan 04: D-06 Throughput Verdict Re-run Summary

**Deployed G-02/G-03/G-04 to prod L2, re-ran the throughput chaos, and got the REAL D-06 verdict (PARTIAL) — surfacing GAP-401: the 30s staleness watchdog false-trips on healthy-but-quiet WS and reconnect-storms itself into REST-polling degradation.**

## Performance
- **Duration:** ~25 min (deploy ~3min + chaos ~7min + diagnosis/restore ~15min)
- **Tasks:** 2 (Task 1 auto-deploy + Task 2 human-verify chaos)
- **Files modified:** 1 created (04.1-SOAK-LOG.md)

## Accomplishments
- **All three fixes proven live in prod**: G-02 `subscribed_count=62` (was 3), G-03 HMAC endpoint 200 + in-flight kill, G-04 `process:rss_kb=241MB` numeric.
- **Real D-06 verdict computed** (Phase 04 left it BLOCKED): (1) frame-rate DEFERRED (low-activity), (2) ws-recovery FAIL, (3) RSS PASS (ratio 1.0000).
- **Pitfall 4 watchdog false-trip OBSERVED + root-caused** — the headline result. Watchdog reads 30s of legitimate market silence as "stale → RECONNECTING", burns the 11-reconnect/hr storm-cap during a healthy baseline window, then degrades to REST polling → stuck DISCONNECTED.
- **Prod restored** to healthy (WAITING_FOR_EVENT, subs=62) via machine restart after the run.

## Task Commits
1. **Task 1: Deploy + pre-flight verify** — `flyctl deploy --config fly-l2.toml --remote-only` → image `deployment-01KSWFMYBBAZYW7B7Q67HWJXK0` v21 (no code commit — deploy is an action)
2. **Task 2: Chaos re-run + verdict** — recorded in `04.1-SOAK-LOG.md`

**Plan metadata:** this SUMMARY + SOAK-LOG committed together.

## Files Created/Modified
- `04.1-SOAK-LOG.md` — D-06 verdict (3 indicators), raw T1/T2/T3 numbers, deploy proof, Finding 1 (G-03 validated), Finding 2 (GAP-401 watchdog false-trip), GAP-401 carry-forward.

## Decisions Made
- Recorded the ws-recovery FAIL honestly rather than re-running until "green" — the FAIL IS the finding this phase was built to surface.
- Restarted the L2 machine post-chaos (the storm-cap circuit-breaker had degraded it to REST polling for the hour; restart resets the counter and re-establishes WS).

## Deviations from Plan
None — plan executed as written. The `/health` lacking a `frame_count` surface (indicator 1) was anticipated by the plan's own note; handled as a documented proxy/deferral.

## Issues Encountered
- **WS stuck DISCONNECTED post-storm** — diagnosed via flyctl logs (chain-first): not a connectivity failure but the watchdog's deliberate reconnect-storm-cap degradation, itself caused by pre-storm false-trips. Root cause → GAP-401. Resolved operationally by machine restart.

## Next Phase Readiness
- **Phase 04.1 closes the D-06 loop** that Phase 04 left DEFERRED — verdict is now recorded, no longer blocked.
- **GAP-401 (NEW)** is the genuine bug to fix next: watchdog must distinguish healthy-but-quiet from genuinely-stale (WS ping/pong liveness, activity-aware threshold, or only count real-close reconnects against the cap). Candidate for a follow-up phase — NOT folded into 04.1.
- All three 04.1 fixes (G-02/03/04) are production-verified and stable.

---
*Phase: 04.1-d01-restart-robustness-chaos-redesign*
*Completed: 2026-05-30*
