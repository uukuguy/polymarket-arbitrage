---
phase: 04.1-d01-restart-robustness-chaos-redesign
verified: 2026-05-30T00:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 04.1: d01-restart-robustness-chaos-redesign Verification Report

**Phase Goal:** Fix the 3 structural gaps Phase 04 prod chaos exposed so the D-06 throughput verdict can be computed at real candidate scale (G-02 startup-prime, G-03 in-band HMAC chaos redesign, G-04 real-process RSS).
**Verified:** 2026-05-30
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | G-02: `l2_main.py` has unconditional eager startup-prime after catchup envelope (sentinel snapshot_id=-1, `_startup_prime` flag) | VERIFIED | `l2_main.py:425-436` — `_dispatch_on_snapshot({"snapshot_id": -1, "_startup_prime": True, "ts_s": time.time()})` placed after outer `try/except` closes, before `_listener_runner`. Fires on all 3 catchup outcomes. PROD PROOF: SOAK-LOG subscribed_count=62 (was 3). |
| 2 | G-03: `l2_control.py` has HMAC-gated `/control/chaos/ws-test-kill`; `ws_consumer.py` has process-local `_ws_test_kill_flag` with set/get; chain-truth: `/health chaos:ws_test_kill_flag` reads `get_ws_test_kill()` not `os.getenv` | VERIFIED | `ws_consumer.py:61-106` — module-level flag seeded from env at import, `set_ws_test_kill` + `get_ws_test_kill` public API, `_check_ws_test_kill` reads `_ws_test_kill_flag` not `os.getenv`. `l2_control.py:49-95` — HMAC-gated handler calls `set_ws_test_kill`. `l2_app.py:57-75` — `ControlAuthMiddleware` wired with route under `/control/`. `l2_health.py:377-394` — lazy-imports `get_ws_test_kill()`, not `os.getenv`. PROD PROOF: SOAK-LOG Finding 1 — in-flight synthetic close fired at 13:18:56Z, no machine restart, same 62-asset process at T3. |
| 3 | G-04: `l2_health.py` exposes `process:rss_kb` via `psutil.Process()` (current process); psutil is a runtime dep; Makefile chaos RSS reads via `/health` jq | VERIFIED | `l2_health.py:396-421` — `psutil.Process().memory_info().rss / 1024`, lazy-import fail-soft, no `_severity()` call (informational-only). `pyproject.toml:48-50` — psutil in `[project.dependencies]` (runtime). `Makefile:970-989` — 3x `jq '.checks["process:rss_kb"][0].observedValue'` on `/tmp/inj4t-t{1,2,3}.json`; `grep VmRSS Makefile` returns 0 hits in throughput recipe. PROD PROOF: SOAK-LOG `process:rss_kb=241156 kB` numeric. |
| 4 | D-06: `04.1-SOAK-LOG.md` exists with deploy proof (image==latest main), candidate N, 3 D-06 indicators with PASS/FAIL/DEFERRED each, overall verdict, and Pitfall 4 watchdog observation; PARTIAL verdict with FAIL being GAP-401 is a successful phase outcome | VERIFIED | `04.1-SOAK-LOG.md` exists. Deploy: `deployment-01KSWFMYBBAZYW7B7Q67HWJXK0` (v21) from main@`90ac005`. Candidate N=62. Indicators: (1) DEFERRED (low-activity, frame rate undefined), (2) FAIL (ws-recovery stuck DISCONNECTED — GAP-401 watchdog false-trip), (3) PASS (RSS ratio 1.0000). Overall: PARTIAL. Pitfall 4 documented in Finding 2 — pre-storm false-trips burned reconnect-storm-cap; root cause (30s silence == stale) fully traced. This is the phase goal: make the verdict COMPUTABLE and observe Pitfall 4. Both achieved. |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/polyarb/daemon/l2_main.py` | Eager startup-prime after catchup envelope | VERIFIED | Lines 425-436, `_startup_prime` marker present |
| `src/polyarb/http/l2_control.py` | HMAC chaos handler | VERIFIED | New file; `ws_test_kill_handler` at line 49; imports `set_ws_test_kill`/`get_ws_test_kill` from ws_consumer |
| `src/polyarb/http/l2_app.py` | ControlAuthMiddleware + chaos route wired | VERIFIED | Lines 57-75; middleware + `/control/chaos/ws-test-kill` route |
| `src/polyarb/daemon/ws_consumer.py` | Process-local flag + set/get API | VERIFIED | Lines 61-106; `_ws_test_kill_flag`, `set_ws_test_kill`, `get_ws_test_kill`, `_check_ws_test_kill` reads module flag |
| `src/polyarb/http/l2_health.py` | `process:rss_kb` sub-check + chain-truth chaos flag | VERIFIED | `process:rss_kb` at lines 396-421 (psutil, no `_severity`); `chaos:ws_test_kill_flag` at lines 363-394 reads `get_ws_test_kill()` |
| `pyproject.toml` | psutil in `[project.dependencies]` | VERIFIED | Line 50: `"psutil>=5.9,<7"` in runtime deps with comment |
| `Makefile` | chaos RSS reads via `/health` jq; `chaos-ws-kill` target; throughput recipe uses in-band endpoint | VERIFIED | Lines 970/978/988: jq reads; lines 905-913: `chaos-ws-kill` target; lines 982/992: `$(MAKE) chaos-ws-kill ON=1/0` |
| `.planning/workstreams/m1-perception/phases/04.1-d01-restart-robustness-chaos-redesign/04.1-SOAK-LOG.md` | D-06 verdict + Pitfall 4 observation | VERIFIED | File exists with all required sections |
| `tests/m1-perception/test_l2_startup_prime.py` | 7 tests for G-02 (all catchup paths) | VERIFIED | Created in Plan 01, 7 tests green |
| `tests/m1-perception/test_l2_health_rss.py` | 4 tests for G-04 (RSS sub-check) | VERIFIED | Created in Plan 02, 4 tests green |
| `tests/m1-perception/test_l2_chaos_ws_kill_endpoint.py` | 12 tests for G-03 (flag API + HMAC + endpoint) | VERIFIED | Created in Plan 03, 12 tests green |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `l2_main.py` catchup block | `_dispatch_on_snapshot` with sentinel payload | unconditional call after outer `try/except` | VERIFIED | Pattern `_startup_prime` confirmed at line 434; placement after `except Exception:` at line 420-423 |
| `POST /control/chaos/ws-test-kill` handler | `_ws_test_kill_flag` in ws_consumer | `set_ws_test_kill(enabled)` call at `l2_control.py:89` | VERIFIED | Direct import from `polyarb.daemon.ws_consumer`; setter mutates module global |
| `ws_consumer._check_ws_test_kill` | `_ws_test_kill_flag` (module-level) | direct read, NOT `os.getenv` | VERIFIED | `ws_consumer.py:103` — `if _ws_test_kill_flag:` (no `os.getenv` call inside function) |
| `/health chaos:ws_test_kill_flag` | `get_ws_test_kill()` | lazy import at `l2_health.py:378` | VERIFIED | `from polyarb.daemon.ws_consumer import get_ws_test_kill` inside try block; reads `_ws_test_kill_flag` — same variable the endpoint writes |
| `l2_health.py process:rss_kb` | `psutil.Process().memory_info().rss` | lazy import at line 403 | VERIFIED | `psutil.Process()` with no arg = `os.getpid()` = current Python daemon |
| `Makefile chaos-l2-inj4-throughput` storm/cleanup | in-band HMAC endpoint | `$(MAKE) chaos-ws-kill ON=1/0` | VERIFIED | Lines 982/992; no `flyctl secrets set POLYARB_WS_TEST_KILL` in throughput recipe |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `l2_health.py process:rss_kb` | `rss_kb` | `psutil.Process().memory_info().rss` | YES — live process memory read each request | FLOWING |
| `l2_health.py chaos:ws_test_kill_flag` | `_ws_kill_active` | `get_ws_test_kill()` → `_ws_test_kill_flag` module bool | YES — same variable endpoint writes | FLOWING |
| `l2_main.py startup-prime` | sentinel payload | dispatched unconditionally after catchup | YES — triggers real `markets_latest` fetch via `on_snapshot_complete` | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: Not run against live services (would require server running). The prod chaos run in Plan 04 (04.1-SOAK-LOG.md) serves as the behavioral proof: G-02 subscribed_count=62, G-03 in-flight kill fired at 13:18:56Z no restart, G-04 process:rss_kb=241156 kB numeric.

---

### Requirements Coverage

No requirement IDs declared (requirements: [] in all plans). This is a ROADMAP-scoped fix-up phase analogous to 02.1/03.1. No REQUIREMENTS.md traceability needed per phase definition.

---

### Anti-Patterns Found

Scanned key files for stubs, TODOs, and hollow patterns.

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `Makefile:861` | `flyctl secrets set POLYARB_WS_TEST_KILL=1` (old env-var approach) | INFO | Belongs to `chaos-l2-inj4` (double-fault recipe), not `chaos-l2-inj4-throughput`. The double-fault recipe was not in G-03's scope (G-03 only required the throughput recipe to be rewired). The stale approach is isolated and labeled. Not a blocker. |

No blockers. No stub implementations found in the four core source files.

---

### Human Verification Required

None. All three fixes are verified at code level (Levels 1-4) and the prod chaos run (04.1-SOAK-LOG.md) provides behavioral proof for G-02/G-03/G-04 and records the D-06 verdict with Pitfall 4 observation.

---

### Gaps Summary

No gaps. All four must-haves are VERIFIED:

- **G-02** (startup-prime): code exists, is substantive, wired to the existing dispatch helper, fires unconditionally on all catchup outcomes, confirmed prod (subs=62).
- **G-03** (in-band chaos): full chain verified — endpoint writes flag, ws_consumer reads same flag, /health reads same flag via getter, l2_app wires the middleware, Makefile throughput recipe uses in-band endpoint.
- **G-04** (real-process RSS): psutil.Process() in /health, runtime dep in pyproject, Makefile reads /health jq, confirmed prod (241 MB numeric).
- **D-06** (verdict + Pitfall 4): SOAK-LOG exists with all required content. PARTIAL verdict (1 PASS / 1 FAIL / 1 DEFERRED) with the FAIL being GAP-401 (watchdog false-trip on healthy-but-quiet WS) is the intended finding — the goal was to make the verdict COMPUTABLE and observe Pitfall 4. Both achieved. GAP-401 is a carry-forward bug, not a phase failure.

---

*Verified: 2026-05-30*
*Verifier: Claude (gsd-verifier)*
