# Phase 05.1 — Production Recovery Log

**Status:** RECOVERY CORE PASS — Plan 04 Task 3 remains open for natural quiet-edge proof
**Prepared:** 2026-07-18
**Minimum verified code SHA:** `889fab4`; deployed image and instance recorded below

This log is the evidence ledger for the no-restart recovery checkpoint. It
contains only non-secret IDs, timestamps, counts, ages, HTTP statuses, and
configuration values. Exact times or durations absent from the checkpoint
capture are explicitly marked `not captured`.

## Verdict Boundary

| Chain link | Verdict | Evidence |
|---|---|---|
| L2 process continuity | PASS | machine, instance, and creation anchors stayed unchanged |
| LISTEN reconnect | PASS | backend `610457` terminated; reconnect `0 → 1`; listener returned |
| Poll-only cursor recovery | PASS | snapshot/cursor `520 → 521` while notification anchor stayed exact |
| L1 publishing restoration | PASS | flag restored to `1`; a later notification advanced the anchor |
| Candidate projection | PASS | desired/active keyed rows `5/5`, stale/missing `0/0`, WS assets `5/5` |
| Strict quiet-market freshness | BLOCKED → Plan 04 | HTTP 503 from stale business-frame and TOB mirror ages despite a live socket |
| L3 strict N=5 | unchanged | `0/10`; recorded separately and never relaxed |

**Core recovery verdict:** `RECOVERY CORE PASS`.

Strict health is deliberately not converted to pass: after five valid markets
became quiet, transport pong liveness remained good while the business-frame
and mirror freshness checks aged out. Plan 04 owns an in-band
subscribe/initial-dump refresh. Phase validation remains unsigned.

## Acceptance Contract

| Invariant | PASS threshold / observed result |
|---|---|
| L2 process continuity | machine ID, instance/start anchor, and `created_at` unchanged — PASS |
| LISTEN recovery | returns to `listening`; reconnect count increases within 180s — PASS; exact elapsed not captured |
| Poll causality | cursor reaches newer snapshot within 60s — PASS; exact elapsed not captured |
| Notification exclusion | exact `last_notification_at` unchanged across poll transition — PASS |
| Stale grace | remains 180s; not relaxed |
| L1 cleanup | value `1` observed and later notification advances — PASS |
| Candidate projection | active keyed rows equal desired; WS equals unique desired assets — PASS |
| Final strict health | record exact truth without threshold changes — HTTP 503, routed to Plan 04 |

## Image-Aware Preflight

Authoritative deployed image:

```text
registry.fly.io/polyarb-l2:deployment-01KXSBYX2S8G4M21H8VHGT1C56
manifest sha256:97aede0c16055401cfeaf35920487c795cee5e78862ba3d74416e0ef7c01add2
Fly release v31
```

The authenticated deployed-image check succeeded. Its relevant matrix was:

```text
MISS pkill
MISS ps
MISS dig
MISS ping
```

The recovery harness uses operator-host Python, asyncpg, Fly APIs, and HTTP. It
does not invoke any missing container primitive.

## Automated Pre-Deploy Evidence

| Command | Result |
|---|---|
| Phase 05.1 validation suite | `84 passed in 1.00s` |
| Wider HTTP/event/daemon/storage/observation/chaos regression | `108 passed in 1.09s` |
| Ruff: new script, health source, event-chain test | PASS |
| Ruff import/undefined-name gate: chaos plan | PASS |
| Python compile check: harness + health | PASS |
| `make help` entry | `chaos-l2-listener-recovery` present |

## T0 — Post-Deploy Baseline

| Field | Evidence |
|---|---|
| timestamp UTC | not captured |
| deployed code | contained `886aeff`; exact deploy HEAD not captured |
| Fly release | `v31` |
| Fly image ref | `registry.fly.io/polyarb-l2:deployment-01KXSBYX2S8G4M21H8VHGT1C56` |
| image digest | `sha256:97aede0c16055401cfeaf35920487c795cee5e78862ba3d74416e0ef7c01add2` |
| L2 machine ID/name | `85e647c4eed598` / `divine-mountain-5812` |
| L2 instance/start anchor | `01KXSC1DK9W0P5JQSK22N417MJ` |
| L2 machine `created_at` | `2026-05-24T22:30:06Z` |
| latest snapshot / committed cursor | `520 / 520` |
| cursor lag | `0` |
| listener state | `listening` |
| reconnect count | `0` before listener fault |
| exact `last_notification_at` | `1784337253.5297902` before poll proof |
| reconciliation age | fresh; exact age not captured |
| desired / active candidate keys | `5 / 5` |
| unique desired / WS assets | `5 / 5` |
| baseline strict `/health` | not captured |
| L3 count | `0/10` |

## T1 — LISTEN Fault and Recovery

The harness selected the one backend whose last statement exactly matched
`LISTEN "snapshot_complete"`; no machine was stopped or restarted.

| Field | Evidence |
|---|---|
| fault timestamp UTC | not captured |
| identified LISTEN backend PID | `610457` |
| pre-fault listener/reconnect count | `listening / 0` |
| post-fault listener/reconnect count | `listening / 1` |
| recovery duration seconds | exact duration not captured; harness 180s gate passed |
| pre/post machine identity | `85e647c4eed598`, instance `01KXSC1DK9W0P5JQSK22N417MJ`, created `2026-05-24T22:30:06Z` — unchanged |
| verdict | PASS |

## T2 — Poll-Only Cursor Recovery

L1 publishing was disabled and verified inside L1. L2 was never stopped or
restarted. The exact notification anchor did not move while the durable poll
advanced the cursor.

| Field | Evidence |
|---|---|
| disabled-setting observation | `POLYARB_EVENT_BUS_ENABLED=0` |
| baseline latest/cursor | `520 / 520` |
| baseline exact `last_notification_at` | `1784337253.5297902` |
| new normal L1 snapshot | ID `521`; timestamp not captured |
| cursor transition | `520 → 521`; harness 60s gate passed, exact elapsed not captured |
| post-poll exact `last_notification_at` | `1784337253.5297902` — exactly unchanged |
| reconciliation age after catch-up | fresh; exact age not captured |
| pre/post L2 machine identity | unchanged |
| verdict | PASS |

## T3 — Mandatory L1 Restoration

| Field | Evidence |
|---|---|
| restored-setting observation | `POLYARB_EVENT_BUS_ENABLED=1` |
| later snapshot ID/timestamp | not captured |
| later exact `last_notification_at` | `1784337831.508485` — advanced |
| final L2 machine identity | unchanged |
| verdict | PASS |

## T4 — Projection and Final Health

Candidate database identity is `(asset_id, recipe_name)`; WS identity is unique
`asset_id`.

| Field | Evidence |
|---|---|
| `markets_latest` rows | `1944` |
| desired keyed candidates | `5` |
| active DB keyed candidates | `5` |
| stale / missing active keys | `0 / 0` |
| unique desired assets | `5` |
| WS subscribed assets | `5` |
| final reconciliation age | fresh; exact age not captured |
| final cursor lag | `0` |
| final listener/reconnect count | `listening / 1` |
| final strict `/health` | HTTP `503` |
| failing freshness samples | `ws:last_event_age_seconds=716.9`; `mirror:l2_tob_age_seconds=790.4` |
| transport truth | `WAITING_FOR_EVENT`; socket open; pong liveness proven |
| L3 status/count | `0/10`; unchanged |
| projection verdict | PASS |
| strict-health verdict | BLOCKED → Plan 04 quiet-market refresh |

## Checkpoint Result

**RECOVERY: CORE PASS**

- L2 identity stayed unchanged through the entire LISTEN and poll proof.
- LISTEN reconnect count moved `0 → 1` and connection returned to `listening`.
- Snapshot/cursor moved `520 → 521` through polling with exact unchanged
  notification anchor `1784337253.5297902`.
- L1 publishing was restored to `1`; a later notification advanced the anchor
  to `1784337831.508485`.
- Candidate keys and WS assets converged exactly.
- Final HTTP 503 is not a core recovery failure: it is the truthful
  quiet-market freshness blocker assigned to `05.1-04-PLAN.md`.

Phase 05.1 remains open and validation remains unsigned until Plan 04 proves
strict health without weakening freshness or L3 thresholds.

## Plan 04 Production Checkpoint — 2026-07-18

Plan 04 implementation through shared reconnect gating was deployed from
`889fab4d58c94dfe36a18d065f2387b1ddf5fdb2` with:

```text
image tag    registry.fly.io/polyarb-l2:deployment-01KXSMPKM78105Q9JZG58GBZYP
image digest sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d
machine ID   85e647c4eed598
instance ID  01KXSMS80B5AX2FGT5EPRC6V82
created_at   2026-05-24T22:30:06Z
```

The machine ID and creation anchor remained unchanged, while the deploy created
the new instance ID above. After stabilization at 03:37:45Z, the same instance
passed the required >=180-second main-chain window: bootstrap subscription,
candidate fetch/convergence to five active assets, real book processing and
Supabase mirror writes remained live. Overall health was `warn` only because L3
remained at its unchanged `0/10` gate.

The exact deployed-digest primitive matrix was:

```text
PRESENT kill
PRESENT which
PRESENT curl
PRESENT python
MISSING pkill
MISSING ps
MISSING dig
MISSING ping
```

No package was installed and no image/tool threshold changed.

### Quiet-refresh verdict boundary

**QUIET REFRESH FAIL / Task 3 OPEN** as of 04:07:55Z does not mean the refresh
chain fired and failed. The new instance received natural business frames
throughout an additional ten-minute read-only monitor; `ws_age` never reached
45 seconds, so the required natural 60-second quiet trigger never occurred.
An active market that never becomes quiet cannot validate the quiet edge, but
that is not evidence against the refresh path.

Task 3 requires a later read-only observation on this exact instance showing,
in order, the natural quiet trigger, real book/mirror processing, and the final
evidence log. A send/request log by itself remains insufficient. If the instance
ID changes, the >=180-second baseline must be rebuilt before quiet-edge evidence
can be accepted.

At session close, the temporary `codex-phase-05-1-quiet-refresh` Fly organization
token was revoked, the organization-token page reported `No organization tokens`,
no token value was stored, and both authenticated shell sessions were closed.

### Read-only checkpoint window — 2026-07-18 07:25:44Z–07:36:20Z

A later credential-free monitor forced all public requests to machine
`85e647c4eed598` and collected 60 samples over 10m36s. All probes returned HTTP
200. The observed maxima were `ws_age=3.2s`, `mirror_age=74.2s`,
`reconciliation_age=60.1s`, and `candidate_age=60.2s`; listener remained
`listening`, cursor lag remained `0`, WS assets remained `5`, and L3 remained at
the unchanged `0/10` gate.

**Task 3 remains open.** Organic frames kept the subscription active, so no
natural 60-second quiet trigger occurred. Ordinary mirror resets are not
quiet-refresh evidence. Public health probes can target the expected machine but
cannot expose exact instance incarnation or the required Fly log sequence;
read-only Fly authentication was unavailable and no token was created. Task 4,
validation signing, and Plan 04 SUMMARY remain blocked.

### Authenticated same-instance checkpoint — 2026-07-18 07:53:56Z–08:08:31Z

After local Fly login, `flyctl auth whoami` succeeded without printing a secret.
Identity matched the prior proof exactly before and after monitoring: machine
`85e647c4eed598`, instance `01KXSMS80B5AX2FGT5EPRC6V82`, creation
`2026-05-24T22:30:06Z`, start anchor `2026-07-18T03:37:28Z`, image
`deployment-01KXSMPKM78105Q9JZG58GBZYP`, digest
`sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d`,
release `v33`. No baseline rebuild was required.

The bounded window collected 72/72 HTTP-200 samples over 13m32s. Maximum
WS/mirror/reconciliation/candidate ages were `3.0s/84.8s/59.6s/59.7s`;
cursor lag stayed `0`, listener stayed `listening`, WS assets stayed `5`, and
L3 stayed at its unchanged `0/10` gate. Exact log counts were
`quiet_sending=0`, `quiet_evidenced=0`, and `quiet_failed=0`. Logs showed only
ordinary active-market HTTP 201 mirror writes, which are not quiet-refresh evidence.

**Task 3 remains open solely because the natural 60-second quiet trigger did not
occur.** Authentication and exact-instance uncertainty are no longer blockers.
No thresholds, candidates, secrets, cloud state, deployment, or chaos controls changed.

### Repeated same-instance checkpoint — 2026-07-18 08:07:30Z–08:26:37Z

The available rolling Fly buffer was searched from `08:07:30.237Z` through
`08:13:39.135Z`: 82 rows, quiet `sending/evidenced/failed = 0/0/0`, and 24
ordinary mirror successes rejected as evidence. A new 60-sample monitor then ran
from `08:15:34Z` through `08:26:37Z`; 60/60 health probes returned HTTP 200.
Maximum WS/mirror/reconciliation/candidate ages were
`2.6s/66.2s/59.9s/60.0s`. Listener stayed `listening`, cursor lag stayed `0`,
WS assets stayed `5`, and L3 stayed `0/10`.

Monitor logs from `08:15:40.579Z` through `08:26:26.322Z` again contained
quiet counts `0/0/0`; 15 ordinary mirror successes were rejected. Exact
machine/instance/image anchors were unchanged at the end. Because Fly exposes a
rolling buffer, no claim is made for the gap `08:13:39.135Z`–`08:15:40.579Z`.
Task 3 remains open solely for the absent natural trigger; no cloud or code state changed.

### Same-instance chain-truth checkpoint — 2026-07-20 04:25:40Z–04:52:31Z

Authenticated read-only inspection confirmed machine `85e647c4eed598`, instance
`01KXSMS80B5AX2FGT5EPRC6V82`, start `2026-07-18T03:37:28Z`, release `v33`,
image `deployment-01KXSMPKM78105Q9JZG58GBZYP`, and digest
`sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d`
were unchanged.

The available 100-row rolling log buffer covered `04:25:40Z`–`04:51:36Z` and
contained quiet `sending/evidenced/failed=0/0/0`, received-book debug `0`, and
TOB/trade mirror success `0/0`. Candidate projection naturally converged to
three active assets; no candidate mutation was performed.

The forced-instance strict `/health` at `04:52:31Z` returned HTTP 503 overall
fail. WS age was `0.2s` with three subscribed assets, while mirror age was
`6306.2s` and failed. Listener remained `listening`, cursor lag `0`,
reconciliation age `54.3s`, candidate age `54.4s`, and L3 remained `0/10`.

**Task 3 remains open.** No natural quiet transaction exists, and the first
current broken link is mirror freshness despite fresh WS activity. This window
does not justify another short monitor: first diagnose which received frame
types advance WS freshness without a valid TOB/trade mirror write, then restore
strict main-chain freshness before accepting any future quiet proof. No deploy,
restart, scale, secret, config, threshold, candidate, chaos, code, or cloud-state
mutation occurred.

### Natural quiet-refresh closure — 2026-07-20 05:31:41Z–05:37:47Z

The previously missing external trigger occurred naturally on the exact same
machine, instance, creation/start anchor, release, image, and digest. The valid
transaction sequence was:

| UTC | Evidence |
|---|---|
| `05:31:41Z` | Supabase `l2_top_of_book` POST returned HTTP 201, followed by `ws quiet refresh: evidenced assets=3` |
| `05:32:41.823Z` | `ws quiet refresh: sending assets=3` |
| `05:32:42.567Z` | Supabase `l2_top_of_book` POST returned HTTP 201 |
| `05:32:42.684Z` | `ws quiet refresh: evidenced assets=3` |

The second transaction is the complete buffer-visible
`sending → mirror HTTP 201 → evidenced` proof. Evidence resolution remains
same-generation and book-only: the sender itself does not mutate WS or mirror
freshness.

A forced-instance read-only acceptance window ran from `05:34:29.106Z` to
`05:37:47.356Z` (198 seconds). Ten of ten strict `/health` requests returned HTTP
200. Maximum observed WS age was `50.1s` (`<120s` fail), maximum mirror age was
`162.2s` (`<600s` fail), cursor lag remained `0`, listener remained
`listening`, reconciliation remained fresh, and subscribed assets remained `3`.
Overall body status remained `warn` only because the unchanged Phase 05 L3 gate
was `0/10`.

Start/end anchors were byte-identical for the acceptance fields:

```text
machine_id   85e647c4eed598
instance_id  01KXSMS80B5AX2FGT5EPRC6V82
created_at   2026-05-24T22:30:06Z
updated_at   2026-07-18T03:37:28Z
image        deployment-01KXSMPKM78105Q9JZG58GBZYP
digest       sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d
```

Final local verification covered 139 Phase 05.1/quiet-refresh tests. Focused Ruff
and `compileall` passed. The image-aware Make target was updated for current and
legacy Fly JSON shapes under a RED→GREEN test; the exact deployed digest again
showed present `kill/which/curl/python` and missing `pkill/ps/dig/ping`. Temporary
Docker registry credentials were removed immediately after the read-only pull.

**QUIET REFRESH MECHANISM PASS.** This window proves the natural quiet transaction
without L2 restart, redeploy, threshold change, candidate mutation, chaos
injection, secret/config change, or production write beyond the daemon's own
natural mirror path. The later regression below invalidates Phase closure while
preserving this window as historical mechanism evidence.

### Closure-invalidating empty projection regression — 2026-07-20 05:51:57Z+

A final fresh strict probe returned HTTP 503: WS age `613.5s`, mirror age
`834.3s`, candidate set `0`, cursor lag `0`, and L3 `0/10`. L2 logs showed
`markets_latest` first returning 1939 rows for snapshot 573 and later returning
zero rows; the zero response was recorded as a successful fetch, candidate
projection changed `3→0`, and the empty desired union made further quiet refresh
impossible.

Read-only database and local test inspection confirmed:

- snapshot 573 remains `status=ok`, `market_count=1939`;
- `markets_latest` exact count is zero for both REST service-role and direct SQL;
- the table has no user trigger, pg_cron job, or cascading delete;
- the only repository writer is `SupabaseMirror.push_snapshot`, whose current
  protocol is non-transactional `snapshot upsert → DELETE all → chunked INSERT`.
- `Settings` loads project `.env`; M1 orchestrator fixtures did not override
  cloud credentials, so the earlier full `make test` auto-enabled real Supabase/
  R2 adapters after Fly snapshot 573. Local HTTP calls explain why the final
  destructive request is absent from Fly logs.

Two local fail-closed invariants were added under observed RED→GREEN tests:

1. an empty L1 `market_rows` projection is rejected before any remote mutation;
2. an empty L2 live projection does not update LKG/fetch freshness, compute
   candidates, mutate WS membership, or advance reconciliation.
3. all tests disable cloud/event/alert adapters by default; cloud-path tests opt
   in only with explicit localhost/dummy settings.

Focused mirror/candidate tests pass 42/42. The complete 27-test orchestrator file
passed with production Postgres write counters identical before and after. The
repository-wide suite then passed all 1413 collected tests (one expected xfail,
one skip) with the same five write counters unchanged. Vanilla-Postgres Alembic
fixtures now seed Supabase's `anon` role and target their own revisions instead
of drifting with `head`. No deploy occurred. Plan 04 Task 3 and Phase 05.1
closure are reopened pending current-chain recovery and production proof under
the unchanged thresholds.

### Natural current-chain recovery and re-acceptance — 2026-07-20 06:38:03Z–06:49:07Z

No production mutation, deploy, restart, scale, secret, or configuration change
was performed. The next natural L1 tick restored the projection and the existing
L2 process consumed it:

| UTC | Evidence |
|---|---|
| `06:38:03.876Z` | L1 persisted valid snapshot 574 with 1942 markets |
| `06:38:04.614Z` | Supabase `markets_latest` POST returned HTTP 201 |
| `06:38:05.098Z` | L1 published `snapshot_complete` for snapshot 574 |
| `06:38:05.869Z` | L2 fetched 1942 rows from `markets_latest` |
| `06:38:05.940Z` | L2 converged candidate `+3 -0`, total 3; cursor later observed at zero lag |

Direct read-only database facts agreed: latest snapshot was 574/`ok`/1942,
`markets_latest` contained exactly 1942 rows, and every row belonged to snapshot
574. This proves the empty state was an invalid projection, not a real
zero-market universe.

A fresh strict acceptance window ran from `06:42:59Z` to `06:47:17Z`, spanning
258 seconds. Ten of ten final requests returned HTTP 200 after bounded curl
transport retries. Maximum WS age was `10.2s`, maximum mirror age was `565.4s`,
cursor lag remained `0`, listener remained `listening`, subscribed assets
remained `3`, and L3 remained separately `0/10`. The mirror clock then crossed
the edge of that window safely: a real `l2_top_of_book` POST returned HTTP 201 at
`06:48:06.245Z`, and the `06:49:07Z` strict probe remained HTTP 200 with mirror
age reset to `61.7s`. No probe or control-send path advanced that clock.

Start/end Fly anchors were unchanged:

```text
machine_id   85e647c4eed598
instance_id  01KXSMS80B5AX2FGT5EPRC6V82
created_at   2026-05-24T22:30:06Z
updated_at   2026-07-18T03:37:28Z
image        deployment-01KXSMPKM78105Q9JZG58GBZYP
digest       sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d
```

The local fail-closed guards were not deployed and are not represented as
production-proven runtime behavior. They are defense-in-depth against recurrence.
The actual regression source was the local test runner inheriting production
credentials; the repository-wide test isolation fixture removes that source
without a production deployment. The existing deployed daemon naturally
recovered once the next valid L1 projection arrived.

**CURRENT CHAIN PASS / PHASE 05.1 ACCEPTED.** Historical quiet-refresh mechanism
evidence, current natural projection recovery, the 258-second strict window,
the post-window real mirror reset, full local regression, and unchanged instance
identity jointly close Plan 04 without relaxing any health or L3 threshold.
