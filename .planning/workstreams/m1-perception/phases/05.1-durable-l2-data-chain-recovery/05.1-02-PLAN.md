---
phase: 05.1
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/polyarb/storage/l2_supabase_mirror.py
  - src/polyarb/observation/l2_candidate_refresh.py
  - src/polyarb/events/reconciliation.py
  - src/polyarb/http/l2_health.py
  - src/polyarb/daemon/l2_main.py
  - tests/storage/test_l2_supabase_mirror.py
  - tests/observation/test_l2_candidate_refresh.py
  - tests/http/test_l2_event_chain_health.py
autonomous: true
requirements: [PHASE05.1-R03, PHASE05.1-R04, PHASE05.1-R05]
must_haves:
  truths:
    - "Supabase active candidate history converges by (asset_id, recipe_name), including across cold starts and repeated inclusion cycles"
    - "WS candidate assets are the unique asset projection of desired rows and required convergence failure blocks cursor advancement"
    - "Health reads live listener/pump state and fails strict health for stale reconciliation or cursor lag after grace"
    - "A quiet but caught-up event bus remains healthy; notification age alone is diagnostic"
  artifacts:
    - path: "src/polyarb/storage/l2_supabase_mirror.py"
      provides: "key-aware active candidate read/close/insert reconciliation surface"
    - path: "tests/http/test_l2_event_chain_health.py"
      provides: "chain-truth strict health proof"
  key_links:
    - from: "src/polyarb/observation/l2_candidate_refresh.py"
      to: "src/polyarb/storage/l2_supabase_mirror.py"
      via: "desired keyed rows reconcile against durable active keyed rows"
      pattern: "recipe_name|asset_id"
    - from: "src/polyarb/events/reconciliation.py"
      to: "src/polyarb/http/l2_health.py"
      via: "success-only runtime timestamps and cursor lag"
      pattern: "last_reconciliation|cursor_lag"
---

<objective>
Make candidate history and health surfaces converge to business reality, then bind that convergence to cursor success.

Output: idempotent keyed projection reconciliation, explicit refresh outcomes, and strict event-chain health based on live state rather than cached intent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RESEARCH.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-VALIDATION.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-01-SUMMARY.md
@.planning/threads/market-observation-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — specify keyed candidate projection and required success semantics</name>
  <read_first>
    - src/polyarb/storage/l2_supabase_mirror.py
    - src/polyarb/observation/l2_candidate_refresh.py
    - tests/storage/test_l2_supabase_mirror.py
    - tests/observation/test_l2_candidate_refresh.py
    - alembic/versions/003_l2_tables.py
  </read_first>
  <action>
    Add tests for active candidate key reads and reconciliation with identity `(asset_id, recipe_name)`: close stale keys on cold start; insert missing keys; retain unchanged active rows; distinguish two recipes for one asset; preserve remove/re-include history cycles; empty desired set closes all active keys; read/update/insert failures return false and do not masquerade as success.

    Add candidate-refresh tests proving desired rows, not process-local history, drive the DB diff; WS receives unique asset IDs; false/exception from required WS subscribe/unsubscribe or mirror reconciliation yields an explicit failed refresh outcome consumed by the pump. Preserve the L3 set and existing fallback behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/storage/test_l2_supabase_mirror.py tests/observation/test_l2_candidate_refresh.py -x</automated>
    Expect RED on new keyed reconciliation and failure propagation contracts.
  </verify>
  <done>Tests fail only because keyed convergence and explicit outcomes are absent.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement idempotent durable candidate convergence</name>
  <read_first>
    - tests/storage/test_l2_supabase_mirror.py
    - tests/observation/test_l2_candidate_refresh.py
    - src/polyarb/storage/l2_supabase_mirror.py
    - src/polyarb/observation/l2_candidate_refresh.py
  </read_first>
  <action>
    Add narrow mirror methods to fetch active candidate rows and reconcile desired candidate dicts. Compute active and desired key sets using `(str(asset_id), str(recipe_name))`. Close stale keys with both filters and `removed_at_ts IS NULL`; insert only missing desired keys; leave unchanged rows untouched. Plain inserts preserve repeated cycles. Treat any REST failure as false and emit existing dual-anchor log/Sentry metadata without secrets.

    Refactor `on_snapshot_complete` to return an explicit success result suitable for cursor commit. Compute desired rows first. Converge the WS candidate projection using unique asset IDs while preserving `_l3_active_set`; check boolean results from live subscribe/unsubscribe calls. Reconcile durable candidate history every successful refresh, including cold start with no process-local diff. Required WS or candidate projection failure must return failure. Keep candidate calculation fallback visible and do not advance success freshness for failed fetch/convergence.

    Ensure retry idempotence: if a prior attempt completed one side effect but failed later, the next attempt must safely converge the remaining difference.
  </action>
  <verify>
    <automated>uv run pytest tests/storage/test_l2_supabase_mirror.py tests/observation/test_l2_candidate_refresh.py tests/m1-perception/test_l2_reconciliation_pump.py -x</automated>
  </verify>
  <done>Cold-start drift closes, unchanged history remains stable, repeat cycles work, and convergence failure retains the cursor.</done>
</task>

<task type="auto">
  <name>Task 3: RED/GREEN — expose live chain-truth health</name>
  <read_first>
    - src/polyarb/http/l2_health.py
    - src/polyarb/http/l2_app.py
    - src/polyarb/events/reconciliation.py
    - src/polyarb/daemon/l2_main.py
    - tests/http/test_l2_candidates_fetch_health.py
    - .planning/threads/market-observation-architecture.md
  </read_first>
  <action>
    Create `tests/http/test_l2_event_chain_health.py` first. Cover: connected/listening pass only after actual LISTEN; disconnected status; last notification age is informational; reconnect count is surfaced; fresh caught-up reconciliation passes; stale reconciliation fails after `event_reconcile_stale_seconds`; positive cursor lag warns within grace then fails; quiet notifications with zero lag and fresh polls remain pass; missing DSN/config is explicit; no output leaks secrets.

    Then replace the cached listener check with checks backed by the shared runtime state. Add separate keys for connection state, notification age, reconciliation age, cursor lag, and reconnect count. Mutate timestamps/lag only at the real listener and pump write sites defined in Plan 01. Apply strict severity to stale reconciliation and aged positive lag, not notification quietness alone. Keep `/healthz` platform semantics unchanged and preserve all existing L2/L3 checks.
  </action>
  <verify>
    <automated>uv run pytest tests/http/test_l2_event_chain_health.py tests/http/test_l2_candidates_fetch_health.py tests/m1-perception/test_l2_health_l3_subchecks.py -x</automated>
  </verify>
  <done>Strict health identifies dead/stalled business processing and does not false-fail a quiet caught-up chain.</done>
</task>

</tasks>

<threat_model>
- Candidate filters use client parameter APIs; never compose untrusted recipe/asset values into SQL strings.
- Health exposes counts, ages, IDs, and states only—never DSNs, JWTs, or payload bodies.
- Projection retries are idempotent and cannot multiply unchanged active rows.
- `removed_at_ts` updates are key-scoped so one recipe cannot close another recipe's history.
</threat_model>

<verification>
`uv run pytest tests/storage/test_l2_supabase_mirror.py tests/observation/test_l2_candidate_refresh.py tests/m1-perception/test_l2_reconciliation_pump.py tests/http/test_l2_event_chain_health.py tests/http/test_l2_candidates_fetch_health.py tests/m1-perception/test_l2_health_l3_subchecks.py -x`
</verification>

<success_criteria>
- Active DB keys equal desired keyed rows after every successful refresh.
- Required convergence failure prevents cursor commit.
- Health is driven by the exact state mutated by listener/pump success and failure paths.
- Create `05.1-02-SUMMARY.md` immediately after the code commit, then run `make planning-status` before Plan 03.
</success_criteria>

