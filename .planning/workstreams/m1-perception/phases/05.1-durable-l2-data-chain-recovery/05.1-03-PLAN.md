---
phase: 05.1
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - Makefile
  - tests/chaos/test_l2_chaos_plan.py
  - .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RECOVERY-LOG.md
  - .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-VALIDATION.md
  - docs/learning/19-NOTIFY门铃与游标账本.md
  - docs/learning/00-INDEX.md
autonomous: false
requirements: [PHASE05.1-R01, PHASE05.1-R02, PHASE05.1-R03, PHASE05.1-R04, PHASE05.1-R05, PHASE05.1-R06]
must_haves:
  truths:
    - "A documented Makefile command can trigger/observe LISTEN-loss recovery without container-only tools"
    - "Production proves a new L1 snapshot reaches the committed L2 cursor through polling while L2 is never restarted"
    - "Production active candidate keys and WS candidate assets converge and strict upstream health recovers"
    - "Phase 05.1 closes with summaries, signed validation, teaching material, and Phase 05-06 still retaining strict N=5"
  artifacts:
    - path: ".planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RECOVERY-LOG.md"
      provides: "timestamped production fault/recovery evidence"
    - path: "docs/learning/19-NOTIFY门铃与游标账本.md"
      provides: "30-second model, code map, tradeoffs, self-checks, and FAQ increment section"
  key_links:
    - from: "Makefile"
      to: "tests/chaos/test_l2_chaos_plan.py"
      via: "stable make target and structural contract"
      pattern: "chaos-l2-listener-recovery"
    - from: "05.1-RECOVERY-LOG.md"
      to: "05.1-VALIDATION.md"
      via: "evidence-backed Nyquist sign-off"
      pattern: "cursor|restart|projection|health"
---

<objective>
Prove the repaired chain in production without restarting L2, then leave durable operational and teaching artifacts.

Output: image-aware Makefile chaos entry, automated contract tests, production recovery log, signed validation, teaching chapter, and planning closure that unblocks (but does not execute or relax) Phase 05-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RESEARCH.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-VALIDATION.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-01-SUMMARY.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-02-SUMMARY.md
@docs/dev/chaos-toolkit.md
@docs/learning/10-L2-跟踪.md
@docs/learning/00-INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image-aware recovery diagnostic and automated chaos contract</name>
  <read_first>
    - Makefile
    - tests/chaos/test_l2_chaos_plan.py
    - docs/dev/chaos-toolkit.md
    - fly-l2.toml
  </read_first>
  <action>
    Extend the chaos-plan test first to require one Makefile target named `chaos-l2-listener-recovery`, listed by `make help`. The target must use Python/asyncpg, existing HTTP probes, or Fly machine APIs—never assume `pkill`, `ps`, `dig`, `ping`, or `which` exist in the slim image. It must expose two separately evidenced modes:

    - `mode=listener`: print pre-fault machine identity and listener/reconnect health, terminate only the identified LISTEN backend, then prove reconnect count increases and the same L2 machine remains running.
    - `mode=poll`: print latest snapshot/cursor plus `last_notification_at`, guide the operator to temporarily set L1 `POLYARB_EVENT_BUS_ENABLED=0`, trigger a normal L1 snapshot, and poll L2 for at most the explicit grace window. PASS requires the new snapshot ID to become the committed cursor while `last_notification_at` remains byte-for-byte unchanged, proving the periodic poll—not a reconnected listener—caused recovery. The harness must then require restoration to `POLYARB_EVENT_BUS_ENABLED=1` and verify a later notification is observed.

    Keep destructive scope narrow: no database table deletion, no app-wide machine stop, no credential echo. If production connection termination cannot be safely automated via the current privileges, implement the target as an evidence-gathering harness with one clearly labeled browser/SQL human action and automated before/after assertions.

    Update `make help` and command comments. Run `make chaos-l2-fly-image-check` and preserve its result for the recovery log.
  </action>
  <verify>
    <automated>uv run pytest tests/chaos/test_l2_chaos_plan.py -x &amp;&amp; make help | grep -F "chaos-l2-listener-recovery"</automated>
  </verify>
  <done>The repository exposes one stable, image-compatible recovery proof entry.</done>
</task>

<task type="auto">
  <name>Task 2: Run full pre-deploy verification and prepare evidence log</name>
  <read_first>
    - .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-VALIDATION.md
    - .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-01-SUMMARY.md
    - .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-02-SUMMARY.md
  </read_first>
  <action>
    Run the full Phase 05.1 suite from VALIDATION plus the repository's standard lint/type tests relevant to changed files. Create `05.1-RECOVERY-LOG.md` with T0 baseline fields: git SHA, Fly image/machine ID, latest snapshot, committed cursor, cursor lag, listener state, reconnect count, reconciliation age, candidate desired/active keyed counts, WS candidate asset count, strict `/health` status, and image-check output. Include empty fault and recovery sections with exact acceptance thresholds (poll <=60s, stale grace <=180s, no L2 restart).
  </action>
  <verify>
    <automated>uv run pytest tests/events/test_listener_catchup.py tests/m1-perception/test_l2_reconciliation_pump.py tests/storage/test_l2_supabase_mirror.py tests/observation/test_l2_candidate_refresh.py tests/http/test_l2_event_chain_health.py tests/chaos/test_l2_chaos_plan.py -x</automated>
  </verify>
  <done>All automated checks are green and the live evidence template is ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Deploy and prove no-restart production self-heal</name>
  <what-built>
    Phase 05.1 code has passed local verification. This checkpoint deploys the L2 image, establishes a healthy baseline, terminates only the LISTEN connection, produces a newer L1 snapshot, and proves the timer-driven pump commits it without restarting L2.
  </what-built>
  <how-to-verify>
    1. Deploy the verified git SHA to `polyarb-l2` using the existing authenticated Fly dashboard/CLI path and record image plus machine ID.
    2. Wait for a caught-up healthy baseline; record latest snapshot/cursor, zero lag, listener connected, `last_notification_at`, fresh reconciliation, reconnect count, active candidate keyed count, and WS asset count.
    3. Run `make chaos-l2-listener-recovery mode=listener`. Terminate only the LISTEN backend. Prove reconnect count increases, listener reconnects, and L2 machine ID/start time remains unchanged.
    4. Run `make chaos-l2-listener-recovery mode=poll`. Through the existing authenticated Fly control surface, temporarily set L1 `POLYARB_EVENT_BUS_ENABLED=0` (an L1 restart is acceptable; do not restart/stop L2), then trigger one normal L1 snapshot. Record its new snapshot ID.
    5. Within 180 seconds prove the committed L2 cursor reaches that snapshot and reconciliation freshness resets while L2 `last_notification_at` is exactly unchanged from the mode=poll baseline. This unchanged notification anchor is the required evidence that the timer path, not NOTIFY, advanced the cursor.
    6. Immediately restore L1 `POLYARB_EVENT_BUS_ENABLED=1`, trigger/wait for a subsequent snapshot, and prove `last_notification_at` advances again. Record the restored L1 setting so the test cannot leave publishing disabled.
    7. Prove active DB keys equal desired keys, WS assets equal unique desired assets, and upstream strict health returns HTTP 200 (pre-existing L3 status is recorded separately).
    8. Paste all timestamps/queries/probes into `05.1-RECOVERY-LOG.md`. A failure is a failed checkpoint; retain Phase 05-06 blocked and diagnose rather than weakening grace or L3 thresholds.
  </how-to-verify>
  <resume-signal>Return `RECOVERY PASS` only with the unchanged L2 machine identity, reconnect transition, poll-only cursor transition with unchanged notification timestamp, restored L1 publish flag plus a later notification, projection counts, and final health evidence. Otherwise return `RECOVERY FAIL` plus the first broken link.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Teach, sign validation, and close Phase 05.1 planning state</name>
  <read_first>
    - .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RECOVERY-LOG.md
    - docs/learning/10-L2-跟踪.md
    - docs/learning/00-INDEX.md
    - src/polyarb/events/listener.py
    - src/polyarb/events/reconciliation.py
    - src/polyarb/http/l2_health.py
  </read_first>
  <action>
    Create `docs/learning/19-NOTIFY门铃与游标账本.md` in Chinese using the mandated format: 30-second mental model; code/data flow; current `file:line` excerpts; why NOTIFY is a doorbell and cursor the ledger; at-least-once/idempotence tradeoffs; chain-truth mapping; production incident and recovery evidence; five adversarial self-check questions; FAQ increment section. Add it to `00-INDEX.md`.

    After RECOVERY PASS only, update `05.1-VALIDATION.md` to status signed and `nyquist_compliant: true`, with evidence links. Update ROADMAP/STATE to mark all three Phase 05.1 plans complete and route next work to Phase 05 Plan 06 strict N=5 soak without changing its thresholds. Update JOURNAL and relevant observation thread with incident/root cause/recovery learning. Create `05.1-03-SUMMARY.md` immediately after the implementation commit and run planning-status.
  </action>
  <verify>
    <automated>test -f docs/learning/19-NOTIFY门铃与游标账本.md &amp;&amp; grep -q "19-NOTIFY" docs/learning/00-INDEX.md &amp;&amp; grep -q "nyquist_compliant: true" .planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-VALIDATION.md &amp;&amp; make planning-status</automated>
  </verify>
  <done>Phase 05.1 is evidence-backed, taught, summarized, drift-free, and Phase 05-06 is the next action.</done>
</task>

</tasks>

<threat_model>
- Fault injection targets one LISTEN connection only; never delete tables or stop the L2 machine.
- Diagnostic commands redact credentials and avoid shell expansion of secrets.
- Evidence captures IDs/counts/ages, not JWTs, DSNs, or notification bodies.
- Failed production proof cannot be converted to pass by relaxing stale grace or L3 N=5.
</threat_model>

<verification>
Automated Phase 05.1 suite GREEN; `make chaos-l2-fly-image-check` GREEN; live RECOVERY PASS proves unchanged machine identity and cursor catch-up; `make planning-status` has no drift.
</verification>

<success_criteria>
- Production self-heals from listener loss without L2 restart.
- Candidate DB and WS projections match desired state.
- Validation and all three SUMMARY files exist.
- The next command is Phase 05 Plan 06 execution/soak, with strict N=5 unchanged.
</success_criteria>
