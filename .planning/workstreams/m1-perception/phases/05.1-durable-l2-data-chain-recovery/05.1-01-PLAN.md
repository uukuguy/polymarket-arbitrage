---
phase: 05.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/polyarb/config.py
  - src/polyarb/events/listener.py
  - src/polyarb/events/reconciliation.py
  - src/polyarb/daemon/l2_main.py
  - tests/events/test_listener_catchup.py
  - tests/m1-perception/test_l2_reconciliation_pump.py
autonomous: true
requirements: [PHASE05.1-R01, PHASE05.1-R02, PHASE05.1-R03]
must_haves:
  truths:
    - "A terminated asyncpg LISTEN connection clears live connection state and enters bounded reconnect without restarting L2"
    - "NOTIFY and the 60-second timer only wake one serialized reconciliation pump; concurrent refresh is impossible"
    - "The pump processes the newest durable snapshot and commits its cursor only after awaited business success"
    - "A failed refresh or cursor write leaves the old cursor retryable; shutdown and CancelledError remain prompt"
  artifacts:
    - path: "src/polyarb/events/reconciliation.py"
      provides: "single-owner durable reconciliation pump and cursor store"
    - path: "tests/m1-perception/test_l2_reconciliation_pump.py"
      provides: "RED/GREEN proof for coalescing, polling, serialization, and cursor semantics"
  key_links:
    - from: "src/polyarb/events/listener.py"
      to: "src/polyarb/events/reconciliation.py"
      via: "NOTIFY callback sets wake_event; it never directly refreshes candidates"
      pattern: "wake|notify"
    - from: "src/polyarb/events/reconciliation.py"
      to: "l2_event_cursor"
      via: "parameterized read/latest/commit queries with updated_at mutation"
      pattern: "last_snapshot_id|updated_at"
---

<objective>
Replace the fragile notification task fan-out with a durable, serialized consumer.

Output: real asyncpg termination-driven reconnect, a NOTIFY-or-timer reconciliation pump, success-only cursor commit, and l2_main wiring that removes premature startup replay and sentinel-prime dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-CONTEXT.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RESEARCH.md
@.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-VALIDATION.md
@docs/superpowers/specs/2026-07-18-m1-durable-data-chain-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — specify real listener lifecycle and durable pump semantics</name>
  <read_first>
    - src/polyarb/events/listener.py
    - src/polyarb/daemon/l2_main.py
    - tests/events/test_listener_catchup.py
    - tests/m1-perception/test_l2_startup_prime.py
  </read_first>
  <action>
    Extend `tests/events/test_listener_catchup.py` with async tests proving: connection termination completes a termination future and causes a second `asyncpg.connect`; connected state is set only after LISTEN succeeds and cleared before reconnect; reconnect count increments; stop cancels pending helper tasks; `CancelledError` propagates.

    Create `tests/m1-perception/test_l2_reconciliation_pump.py` with fakes for a cursor store and awaited refresh callback. Specify these cases: initial wake catches up; timer catches up without NOTIFY; many notifications coalesce; a second wake during refresh never overlaps; only latest snapshot is processed; refresh false/exception retains cursor; cursor commit exception retains cursor; success commits snapshot and `updated_at`; caught-up polling records reconciliation success; shutdown is bounded.

    Tests must fail for missing behavior, not syntax or fixture errors. Do not change production code in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/events/test_listener_catchup.py tests/m1-perception/test_l2_reconciliation_pump.py -x</automated>
    Expect RED on termination-state/pump behavior.
  </verify>
  <done>New tests collect successfully and fail for the intended missing contracts.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement termination-aware LISTEN and serialized cursor pump</name>
  <read_first>
    - tests/events/test_listener_catchup.py
    - tests/m1-perception/test_l2_reconciliation_pump.py
    - src/polyarb/events/listener.py
    - alembic/versions/003_l2_tables.py
  </read_first>
  <action>
    In `listener.py`, add a narrow mutable listener-state protocol/dataclass or callbacks owned by the caller. After successful `add_listener` and LISTEN, register `add_termination_listener` and race termination against `stop_event`. Set `is_connected` only after LISTEN success, clear it on every exit, increment reconnect count on retry, and use bounded exponential backoff capped at the existing operational scale. Cancel and await race tasks in `finally`; explicitly re-raise cancellation. Keep malformed JSON fail-soft and never log DSN/payload bodies.

    Add `events/reconciliation.py` containing:

    - a cursor store using short-lived asyncpg connections and parameterized SQL to read `(cursor, latest_snapshot)` and commit with `updated_at=now()`;
    - a runtime state object for last notification, last reconciliation success, latest snapshot, committed cursor, cursor lag, last error, and reconnect count/connection state shared with health later;
    - a pump whose only inputs are `wake_event`, `stop_event`, cursor store, and `async refresh(payload) -> bool`;
    - initial wake, periodic wait timeout (Settings default 60s), NOTIFY coalescing via `asyncio.Event`, and one sequential loop;
    - newest-snapshot processing because refresh reads `markets_latest`;
    - cursor commit only after refresh returns true; exceptions retain cursor and update error state;
    - a caught-up pass that advances reconciliation freshness without candidate work.

    Do not hide missing cursor tables as success in production mode. Preserve explicit fail-soft logging while health retains failure truth.
  </action>
  <verify>
    <automated>uv run pytest tests/events/test_listener_catchup.py tests/m1-perception/test_l2_reconciliation_pump.py -x</automated>
  </verify>
  <done>Listener termination reconnects and all pump/cursor tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire l2_main to one pump and remove premature replay</name>
  <read_first>
    - src/polyarb/daemon/l2_main.py
    - src/polyarb/config.py
    - src/polyarb/events/reconciliation.py
    - tests/m1-perception/test_l2_startup_prime.py
  </read_first>
  <action>
    Add explicit Settings `event_reconcile_poll_seconds` default 60 and `event_reconcile_stale_seconds` default 180 with positive validation consistent with existing Settings patterns.

    In `l2_main`, replace `_EventListenerWrapper`, `_dispatch_on_snapshot`, the startup `catchup_from_cursor` fan-out/cursor write, and sentinel `snapshot_id=-1` prime with one shared runtime state and wake event. The listener callback validates/records notification metadata and sets the wake event only. The pump awaits `on_snapshot_complete`; adapt that callback boundary so a real successful refresh is distinguishable from debounce/failure. Start listener and pump as separate tasks; periodic reconciliation must continue when LISTEN is disconnected. Include the pump in bounded shutdown.

    Update or replace startup-prime tests to assert the initial pump wake provides cold-start refresh without sentinel IDs and that no direct task fan-out remains. Do not change L3 promoter thresholds or Phase 05 behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/events/test_listener_catchup.py tests/m1-perception/test_l2_reconciliation_pump.py tests/m1-perception/test_l2_startup_prime.py -x</automated>
  </verify>
  <done>l2_main has one cursor owner, initial and periodic reconciliation work, and shutdown remains bounded.</done>
</task>

</tasks>

<threat_model>
- Parameterize all cursor SQL and reject negative/non-integer snapshot IDs.
- Never log DSNs, credentials, or raw notification bodies.
- Coalescing and single ownership bound notification-storm work amplification.
- Cancellation cannot be swallowed by fail-soft exception handlers.
</threat_model>

<verification>
`uv run pytest tests/events/test_listener_catchup.py tests/m1-perception/test_l2_reconciliation_pump.py tests/m1-perception/test_l2_startup_prime.py -x`
</verification>

<success_criteria>
- Connection death causes reconnect without process restart.
- Polling recovers missed work without a NOTIFY.
- Cursor never passes failed business processing.
- Exactly one refresh executes at a time.
- Create `05.1-01-SUMMARY.md` immediately after the code commit, then run `make planning-status` before Plan 02.
</success_criteria>

