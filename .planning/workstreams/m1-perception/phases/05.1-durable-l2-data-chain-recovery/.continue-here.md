---
context: phase
phase: 05.1-durable-l2-data-chain-recovery
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-07-18T08:08:31Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through production failure or independent review.
> Acknowledge them before changing code or cloud state.

- [ ] DUPLICATE SUBSCRIBE IS NOT RESNAPSHOT — only a real unsubscribe→subscribe edge may request an initial dump; do not restore the old duplicate-subscribe design.
- [ ] SEND IS NOT DATA EVIDENCE — only a same-generation received `book` whose production mirror write returned true may satisfy quiet refresh.
- [ ] RECONNECT BUDGET IS SHARED — initializer, quiet, candidate, L3, watchdog, and ordinary `ConnectionClosed` paths must not bypass the shared 10/hour gate.
- [ ] PRODUCTION CHECKPOINT IS STILL OPEN — active markets never entered a natural 60-second quiet window; do not sign validation or create Plan 04 SUMMARY without the missing edge evidence.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Duplicate subscribe as refresh | Production showed `subscribe(existing, initial_dump=true)` can send successfully without returning a book. | blocking | Preserve the locked unsubscribe→subscribe transaction and require generation/mirror evidence. |
| Transport truth treated as chain truth | A successful WebSocket send was previously logged as recovery despite no mirror write. | blocking | Accept only receive-path `book` plus `push_top_of_book(...) is True`. |
| Per-branch reconnect gate | Review probes found initializer and dynamic-control branches could separately bypass the storm budget. | blocking | All reconnect entry points must retain the shared retry-after gate and cancellation tests. |
| Implicit Fly config | `fly deploy --app polyarb-l2` once selected the L1 `fly.toml`. | advisory | Always deploy with explicit `-c fly-l2.toml --app polyarb-l2`. |

<current_state>

Phase 05.1 Plan 04 Task 3 is open. Code through `889fab4` is deployed and independently reviewed APPROVED. The durable startup chain passed on machine `85e647c4eed598`, instance `01KXSMS80B5AX2FGT5EPRC6V82`: live candidate fetch converged to five assets, WebSocket subscribed five assets, real book frames wrote Supabase, cursor lag stayed zero, listener stayed listening, and an unchanged-instance health window exceeded 180 seconds.

The remaining production gate did not trigger. During an additional ten-minute read-only monitor, `ws_age` never reached 45 seconds, so there was no natural 60-second quiet window and no valid `sending → mirror push → evidenced` quiet transaction. This is an external trigger absence, not a protocol failure.

A second read-only window from `07:25:44Z` to `07:36:20Z` collected 60
HTTP-200 samples forced to machine `85e647c4eed598`. Maximum observed
WS/mirror/reconciliation/candidate ages were `3.2s/74.2s/60.1s/60.2s`;
listener stayed `listening`, cursor lag `0`, WS assets `5`, and L3 `0/10`.
Production remained too active to trigger quiet refresh. Credential-free probes
do not expose exact instance incarnation or Fly logs, so no PASS could be signed.

After the user completed `flyctl auth login`, an authenticated window confirmed
the exact same machine/instance/start/image anchors at both ends. From
`07:53:56Z` to `08:07:28Z`, 72/72 health probes returned HTTP 200 and max
`ws_age` was only `3.0s`. Exact log counts were
`quiet_sending/evidenced/failed = 0/0/0`; only ordinary active-market mirror
writes occurred. The access blocker is resolved, but the natural trigger remains absent.

</current_state>

<completed_work>

- Tasks 1–2: quiet refresh implemented as a serialized unsubscribe→subscribe transaction with bounded control sends, generation-scoped waiters, mirror-success evidence, cancellation compensation, latest-union reconnect, caught-up candidate maintenance, and a shared reconnect budget.
- Independent review: APPROVED after closing initializer cancellation, initializer retry, dynamic-control storm, and real production handler-test gaps.
- Local verification: fresh 123-test relevant suite passed; Ruff and `compileall` passed.
- Task 3 partial: deployed `889fab4d58c9`; exact image digest `sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d`; >=180-second main-chain proof passed.
- Exact image matrix: `kill/which/curl/python` present; `pkill/ps/dig/ping` absent. No packages installed.
- Credential cleanup: Fly organization-token page showed no remaining tokens; authenticated shells closed; Docker registry logged out.
- Later monitor: 60/60 public probes returned HTTP 200 with max `ws_age=3.2s`;
  no cloud state changed and no authentication token was created.

</completed_work>

<remaining_work>

- Task 3: on the same instance, observe a natural 60-second quiet period followed by `ws quiet refresh: sending assets=5`, a real Supabase mirror write, and `ws quiet refresh: evidenced assets=5`.
- If instance identity changes, first rebuild the full >=180-second baseline before accepting quiet evidence.
- Task 4: only after Task 3 PASS, create teaching chapter 19, sign validation with `nyquist_compliant: true`, update ROADMAP/STATE/JOURNAL/thread, create `05.1-04-SUMMARY.md`, and route to Phase 05 Plan 06 strict N=5.

</remaining_work>

<decisions_made>

- Preserve all WS, mirror, candidate, and L3 thresholds; no threshold relaxation is permitted.
- Keep Task 3 open because active traffic cannot prove a quiet-only branch.
- Do not use the chaos WS-kill endpoint, candidate mutation, or process suspension as substitute evidence for a natural quiet window.
- If production again exposes a code defect, return to RED→GREEN and independent review before redeployment.

</decisions_made>

<blockers>

- External: production subscription remained active and never satisfied the natural quiet trigger during either observation window. Workaround is read-only monitoring; there is no safe synthetic substitute authorized by the plan.
- Access: resolved. Authenticated Fly CLI confirmed exact instance identity and
  exposed logs without printing or creating a token.

</blockers>

## Required Reading (in order)

1. `.planning/CURRENT.md` — cross-workstream current position and production boundary.
2. `.planning/workstreams/m1-perception/STATE.md` — workstream status and resume guardrails.
3. `.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-RECOVERY-LOG.md` — complete production evidence timeline.
4. `.planning/workstreams/m1-perception/phases/05.1-durable-l2-data-chain-recovery/05.1-04-PLAN.md` — Task 3 acceptance and Task 4 closure gates.
5. `.planning/threads/market-observation-architecture.md` — protocol edge, mirror evidence, and reconnect-gate learning.

## Critical Anti-Patterns (do NOT repeat these)

- Do not infer resnapshot from `ws.send()` success; inspect the receive/mirror chain.
- Do not mark Task 3 PASS from ordinary active-market mirror writes; require the quiet transaction log sequence.
- Do not deploy without `-c fly-l2.toml`.
- Do not create Plan 04 SUMMARY or sign validation while Task 3 remains open.

## Infrastructure State

- Fly app: `polyarb-l2`
- Machine: `85e647c4eed598`
- Instance: `01KXSMS80B5AX2FGT5EPRC6V82`
- Machine creation: `2026-05-24T22:30:06Z`
- Image: `registry.fly.io/polyarb-l2:deployment-01KXSMPKM78105Q9JZG58GBZYP`
- Digest: `sha256:ec90d98e20c6ffe7ee48c899c939dab7a67addf45c28adda6695d13ed6269c4d`
- Latest authenticated window at 07:53:56Z–08:07:28Z: 72/72 HTTP 200;
  max WS age 3.0s; max mirror age 84.8s; listener listening; cursor lag 0;
  WS assets 5; L3 0/10; exact instance/image anchors unchanged at both ends.
- No background monitor, authenticated shell, temporary Fly token, or registry login remains active.

<context>

The engineering and main-chain recovery work is complete enough for production operation, but the plan deliberately makes the quiet-only branch evidence-gated. Resume as an observer first. A highly active market can postpone this checkpoint indefinitely without implying failure. Preserve evidence honesty and avoid inventing a synthetic PASS.

</context>

<next_action>

Resume `$gsd-execute-phase 05.1 --ws m1-perception` when monitoring another natural quiet window. Verify exact identity first and require the complete quiet transaction. If identity changed, rebuild the >=180-second baseline first.

</next_action>
