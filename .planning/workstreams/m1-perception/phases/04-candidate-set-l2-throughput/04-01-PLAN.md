---
phase: 04-candidate-set-l2-throughput
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/004_add_yes_token_id.py
  - src/polyarb/storage/supabase_mirror.py
  - tests/alembic/test_004.py
  - tests/storage/test_supabase_mirror.py
autonomous: false   # [BLOCKING] D-07 alembic push requires live POLYARB_SUPABASE_DB_DSN (operator confirm)
requirements: []    # ROADMAP-scoped workstream (no REQ-IDs); covers D-07
user_setup: []      # uses existing POLYARB_SUPABASE_DB_DSN from .env — no new secret

must_haves:
  truths:
    - "markets_latest table has a nullable yes_token_id column live in Supabase Postgres"
    - "narrow_market_row() output dict includes yes_token_id (None when source row lacks it)"
    - "Existing markets_latest rows get NULL for yes_token_id (add-only, no data loss)"
    - "Migration 004 chains after 003 and contains no DROP in upgrade()"
  artifacts:
    - path: "alembic/versions/004_add_yes_token_id.py"
      provides: "Add-only migration: markets_latest.yes_token_id nullable TEXT"
      contains: "op.add_column"
    - path: "src/polyarb/storage/supabase_mirror.py"
      provides: "yes_token_id in _NARROW_MARKET_COLUMNS + narrow_market_row passthrough"
      contains: "yes_token_id"
    - path: "tests/alembic/test_004.py"
      provides: "Migration static checks (chain to 003, no drop, revision id 004)"
      exports: ["test_down_revision_chain_to_003", "test_no_drop_in_upgrade", "test_revision_id_is_004"]
  key_links:
    - from: "src/polyarb/storage/supabase_mirror.py narrow_market_row"
      to: "markets_latest.yes_token_id column"
      via: "REST insert chunk dict key"
      pattern: "yes_token_id"
    - from: "alembic upgrade head"
      to: "live Supabase markets_latest"
      via: "make supabase-migrate (POLYARB_SUPABASE_DB_DSN)"
      pattern: "alembic upgrade head"
---

<objective>
D-07 投影 gap 收尾: 给 Supabase `markets_latest` 补一个 nullable `yes_token_id` 列, 并让 L1 mirror 的 narrow projection 写入它。这是 Plan 02 临时库能填 yes_token_id 给 watchlist slug 解析用的前提 (闭环: D-07 让 markets_latest 有列, D-01 临时库才能填它)。

Purpose: markets_latest 当前 10 列窄投影缺 yes_token_id (Phase 02 mirror 投影 gap)。Plan 02 的 watchlist 路径 + CandidateRow 把 yes_token_id 当 asset_id 用, 没这列就拿不到。
Output: Alembic 004 add-only migration (已 push 到 live Supabase) + supabase_mirror.py 11 列投影 + migration/mirror 测试。

**[BLOCKING] 这个 plan 包含一个 alembic push 任务 (Task 2), 必须用 live `POLYARB_SUPABASE_DB_DSN` 跑 `make supabase-migrate` AFTER migration 文件写好 BEFORE 任何"列已在 live DB 存在"的验证。** Build/import 检查在没 push 时也会过 (列在 migration 文件里, 不在 live DB), 会造成 false-positive。这个 plan 因此 `autonomous: false` — operator 需确认 DSN 在场。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/m1-perception/STATE.md
@.planning/workstreams/m1-perception/ROADMAP.md
@.planning/workstreams/m1-perception/phases/04-candidate-set-l2-throughput/04-CONTEXT.md
@.planning/workstreams/m1-perception/phases/04-candidate-set-l2-throughput/04-RESEARCH.md
@.planning/workstreams/m1-perception/phases/04-candidate-set-l2-throughput/04-PATTERNS.md

<interfaces>
<!-- markets DDL source-of-truth (schemas.py) — yes_token_id already exists in LOCAL markets table -->
From src/polyarb/storage/schemas.py (markets table, ~line 105):
  market_id TEXT PRIMARY KEY, condition_id TEXT NOT NULL, slug TEXT, question TEXT,
  yes_token_id TEXT, no_token_id TEXT, mid_price REAL, ... fetched_at_ms INTEGER NOT NULL, ...

From src/polyarb/storage/supabase_mirror.py (lines 31-61) — CURRENT 10-column narrow projection:
  _NARROW_MARKET_COLUMNS = ("market_id","question","slug","event_slug","mid_price",
    "liquidity_usd","volume_usd","end_time_ms","snapshot_id","question_zh")
  narrow_market_row(full_row): loop over _NARROW_MARKET_COLUMNS;
    special-case `event_slug` → full_row.get("event_slug") or full_row.get("event_id");
    default branch → out[col] = full_row.get(col)   # None if absent → nullable correct

From alembic/versions/003_l2_tables.py — header pattern:
  revision = "003"; down_revision = "002"; branch_labels = None; depends_on = None

From normalizer.py:107 — yes_token_id source:
  yes_token_id = str(token_list[0]) if len(token_list) > 0 else None   # nullable legit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 004 add-only migration + static-check tests</name>
  <files>alembic/versions/004_add_yes_token_id.py, tests/alembic/test_004.py</files>
  <read_first>
    - alembic/versions/003_l2_tables.py (header boilerplate + add-only discipline — copy structure)
    - alembic/versions/001_initial_dashboard_schema.py:52-67 (markets_latest current 10-col schema — confirm yes_token_id absent)
    - tests/alembic/test_003.py (static-check test structure — copy verbatim, adapt for 004)
    - 04-RESEARCH.md Q3 + § "Alembic Add-Only Migration Structure" (the exact upgrade/downgrade shape)
  </read_first>
  <behavior>
    - Test test_down_revision_chain_to_003: migration file text contains `down_revision = "003"`
    - Test test_no_drop_in_upgrade: upgrade() body (text between `def upgrade(` and `def downgrade(`) contains no `op.drop_`
    - Test test_revision_id_is_004: file text contains `revision = "004"`
  </behavior>
  <action>
Create `alembic/versions/004_add_yes_token_id.py` — add-only migration adding `yes_token_id` nullable TEXT column to `markets_latest`. Copy header structure from 003_l2_tables.py. Concrete content:

```python
"""Add yes_token_id nullable column to markets_latest (Phase 04 D-07)

Revision ID: 004
Revises: 003
Create Date: 2026-05-28

Phase 04 D-07: markets_latest.yes_token_id (nullable TEXT).
Alembic add-only discipline (Phase 02 L15 / Phase 01.1 P7): upgrade() uses only
op.add_column. No DROP / RENAME / ALTER in upgrade(). downgrade() reverses with
op.drop_column for replay-test safety only.
"""
from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "004"
down_revision = "003"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "markets_latest",
        sa.Column("yes_token_id", sa.Text, nullable=True),
    )


def downgrade() -> None:
    op.drop_column("markets_latest", "yes_token_id")
```

Then create `tests/alembic/test_004.py` — copy `tests/alembic/test_003.py` static-check structure verbatim, adapt MIGRATION_PATH to `alembic/versions/004_add_yes_token_id.py` and the three static assertions (chain to 003, no op.drop_ in upgrade body, revision id == 004). ALSO copy the `pg_dsn` / `_run_alembic` / `_q` helpers + the `@pytest.mark.slow test_004_up(pg_dsn)` live-DB column-existence test from test_003.py (asserts column_name='yes_token_id' present + is_nullable=='YES' in information_schema). The slow test is gated behind Docker/pg_dsn fixture — it will skip in CI without a live DSN; that is acceptable for Task 1 (the REAL live verification is Task 2's push against prod Supabase).

Do NOT run `alembic upgrade` in this task — that is Task 2 (the [BLOCKING] push). This task only writes the file + static tests.
  </action>
  <verify>
    <automated>uv run pytest tests/alembic/test_004.py -x -k "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - File alembic/versions/004_add_yes_token_id.py exists
    - `grep -c 'down_revision = "003"' alembic/versions/004_add_yes_token_id.py` >= 1
    - `grep -c 'revision = "004"' alembic/versions/004_add_yes_token_id.py` >= 1
    - `grep 'op.drop_' alembic/versions/004_add_yes_token_id.py` appears ONLY inside downgrade() (verify: no `op.drop_` between `def upgrade(` and `def downgrade(`)
    - `uv run pytest tests/alembic/test_004.py -x -k "not slow"` exits 0
  </acceptance_criteria>
  <done>Migration file written with add-only upgrade; static-check tests green; live-DB slow test present but not yet run (Task 2 pushes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: supabase_mirror.py narrow projection + [BLOCKING] alembic push to live Supabase</name>
  <files>src/polyarb/storage/supabase_mirror.py, tests/storage/test_supabase_mirror.py</files>
  <read_first>
    - src/polyarb/storage/supabase_mirror.py:31-61 (_NARROW_MARKET_COLUMNS + narrow_market_row — exact modification point)
    - 04-PATTERNS.md § "src/polyarb/storage/supabase_mirror.py" (the 10→11 column diff + no-special-case-needed note)
    - 04-RESEARCH.md Pitfall 3 ("D-07 migration requires correct DB DSN auth" — verify current_user before push)
    - tests/m1-perception/test_supabase_mirror.py OR tests/storage/test_supabase_mirror.py (whichever exists — narrow_market_row test pattern; create file if absent)
    - Makefile:340-345 (supabase-migrate target — `uv run alembic upgrade head` against POLYARB_SUPABASE_DB_DSN)
  </read_first>
  <action>
**Step A (code):** In `src/polyarb/storage/supabase_mirror.py`, add `"yes_token_id"` as the 11th entry to the `_NARROW_MARKET_COLUMNS` tuple (after `"question_zh"`). Add inline comment: `# D-07: nullable; source = normalizer.py:107 clobTokenIds[0]`. NO change needed to `narrow_market_row()` — the existing default branch `out[col] = full_row.get(col)` handles yes_token_id correctly (returns None when key absent → nullable correct). Per 04-PATTERNS.md, no special-case branch is required.

**Step B (test):** In `tests/storage/test_supabase_mirror.py` (create the file if it does not exist; use `tests/m1-perception/test_supabase_mirror.py` as analog if that is where the existing mirror tests live — match the actual repo location), add `test_narrow_includes_yes_token_id`: call `narrow_market_row({...full row with yes_token_id: "YES-123"...})` and assert the output dict has key `yes_token_id` == "YES-123"; also call with a row LACKING yes_token_id and assert output dict has `yes_token_id` is None (passthrough nullable).

**Step C — [BLOCKING] alembic push (autonomous: false — operator must confirm POLYARB_SUPABASE_DB_DSN in .env):**
This step CANNOT be skipped or replaced by an import/build check. The yes_token_id column does NOT exist live until pushed. Run:
```
psql "$POLYARB_SUPABASE_DB_DSN" -c "SELECT current_user"   # Pitfall 3: confirm service_role/postgres can ALTER
make supabase-migrate                                       # = uv run alembic upgrade head (auto-loads .env)
```
Then VERIFY the column is live (not just in the file):
```
psql "$POLYARB_SUPABASE_DB_DSN" -c "SELECT column_name, is_nullable FROM information_schema.columns WHERE table_name='markets_latest' AND column_name='yes_token_id'"
```
Expected: one row, is_nullable = 'YES'. If `POLYARB_SUPABASE_DB_DSN` is not present in env/.env, STOP and surface a checkpoint to the operator — do NOT mark this task done on import-check alone.

If `psql` is unavailable, run the `tests/alembic/test_004.py::test_004_up` slow test against the live DSN as the verification substitute, OR query via supabase-py information_schema. Record the actual live-column-exists evidence (psql output or test pass) in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/storage/test_supabase_mirror.py -x 2>/dev/null || uv run pytest tests/m1-perception/test_supabase_mirror.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c yes_token_id src/polyarb/storage/supabase_mirror.py` >= 1 (in _NARROW_MARKET_COLUMNS)
    - narrow_market_row test asserts yes_token_id key present AND None-passthrough — test exits 0
    - LIVE EVIDENCE (recorded in SUMMARY): `psql "$POLYARB_SUPABASE_DB_DSN" -c "...information_schema..."` returns one row with is_nullable='YES' — proving the column exists in live Supabase, NOT just the migration file
    - `uv run alembic current` (or migrate log) shows head at revision 004
  </acceptance_criteria>
  <done>narrow projection writes yes_token_id; migration 004 pushed to live Supabase and column existence verified via information_schema query (not import check); mirror test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| L1 daemon → Supabase Postgres (Alembic DSN) | DDL ALTER TABLE crosses here; service_role/postgres credential required |
| L1 mirror → Supabase REST (service_role) | narrow_market_row insert payload crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | POLYARB_SUPABASE_DB_DSN / service_role key during migrate | mitigate | DSN/key already in .env (gitignored, secrets-hygiene memory). `make supabase-migrate` loads from .env; NEVER echo the DSN to logs or SUMMARY (record only the information_schema RESULT, not the credential). yes_token_id is public market metadata — no PII. |
| T-04-02 | Tampering | yes_token_id as uint256 string overflow via mirror payload | accept | normalizer already coerces to str (RESEARCH Q3); stored as TEXT; no numeric parse downstream in this plan. Low risk. |
| T-04-03 | Elevation of Privilege | Alembic ALTER TABLE with insufficient role | mitigate | Pitfall 3: run `SELECT current_user` before push; same DSN used for 001-003 (proven service_role/postgres DDL access). |
</threat_model>

<verification>
- `uv run pytest tests/alembic/test_004.py tests/storage/test_supabase_mirror.py -x -k "not slow"` green (or tests/m1-perception/ variant)
- LIVE: information_schema query proves yes_token_id column exists in Supabase markets_latest, is_nullable='YES'
- `alembic current` at head 004
- No `op.drop_` in 004 upgrade() body
</verification>

<success_criteria>
- markets_latest has live nullable yes_token_id column (verified via information_schema, not file-only)
- supabase_mirror narrow projection is 11 columns incl. yes_token_id
- Migration is add-only, chains 003→004
- Existing markets_latest rows unaffected (NULL backfill)
</success_criteria>

<output>
After completion, create `.planning/workstreams/m1-perception/phases/04-candidate-set-l2-throughput/04-01-SUMMARY.md` (template: ~/.claude/get-shit-done/templates/summary.md). MUST include the live information_schema query result as the D-07 push evidence (per [BLOCKING] requirement). Do NOT paste the DSN/service_key into the SUMMARY.
</output>
