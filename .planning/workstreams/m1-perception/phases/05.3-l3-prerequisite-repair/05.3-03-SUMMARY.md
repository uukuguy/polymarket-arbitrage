---
phase: "05.3"
plan: "03"
status: complete
subsystem: m1-perception/l3-pair-resolution
tags: [supabase, yes-token, fail-closed, websocket, chain-truth, tdd]
dependency_graph:
  requires:
    - "05.3-01: durable Yes/No projection"
    - "05.3-02: representative Yes-side books"
  provides:
    - "Yes-keyed markets_latest token-pair lookup"
    - "complete distinct-pair validation without fallback"
  affects:
    - "05.3-04: mutation-free planning/apply boundary"
key_files:
  modified:
    - src/polyarb/observation/l3_promote.py
    - tests/m1-perception/test_l3_promoter.py
decisions:
  - "l2_top_of_book.asset_id and l2_candidates.asset_id are treated as the Yes token ID."
  - "A pair is accepted only when both tokens are non-empty, distinct, keyed by the selected Yes ID, and not claimed by another pair."
  - "Missing identity under-fills l3:active_count; the old asset fallback was deleted."
metrics:
  focused_passed: 4
  regression_passed: 32
  red_failures_observed: 4
---

# Phase 05.3 Plan 03: Yes-Keyed L3 Pair Resolution

Corrected the production schema assumption that had made five-to-ten expansion
structurally unreachable. The PostgREST query now selects real columns and
filters by `yes_token_id`; its returned map is keyed by the same Yes token carried
in TOB and candidate rows.

## TDD Evidence

RED:

```text
4 failed: query selected asset_id; happy path produced 5 fallback tokens;
incomplete/duplicate pairs were accepted; diff omitted all No tokens
```

GREEN:

```text
focused pair contract: 4 passed
promoter + L3 health + candidate/L3 protection: 32 passed
Ruff touched-file check: pass
```

Five complete pairs produce ten distinct tokens. Incomplete, self-paired, and
cross-pair duplicate identities are rejected as whole pairs, so health remains
truthfully under-filled. No production read/write, deployment, or migration was
performed.
