---
phase: "05.3"
plan: "02"
status: complete
subsystem: m1-perception/l3-observation-seed
tags: [candidate-recipe, l3, deterministic-selection, makefile, tdd]
dependency_graph:
  requires:
    - "05.3-01: durable Yes/No projection"
  provides:
    - "BUILTIN_RECIPES l3-seed capped at 100"
    - "make scan-l3-seed"
  affects:
    - "05.3-03: representative Yes-side TOB input"
key_files:
  modified:
    - src/polyarb/observation/recipes.py
    - tests/observation/test_l2_candidate_refresh.py
    - Makefile
    - tests/test_makefile.py
decisions:
  - "The seed uses only snapshot facts: non-null Yes ID, mid 0.1..0.9, liquidity >=500."
  - "Limit 100 follows the successful 100-book/86-qualified read-only sample."
  - "The seed is registered before specialized recipes so an overlap retains the specialized label."
metrics:
  focused_passed: 3
  regression_passed: 106
  red_failures_observed: 2
---

# Phase 05.3 Plan 02: Representative L3 Seed

Added a bounded observation-coverage recipe that supplies L2 with liquid
mid-price books while leaving the L3 promotion recipe unchanged. The candidate
continues through the existing union, watchlist precedence, global 500 cap,
reconciliation, and WS membership path.

## TDD Evidence

RED:

```text
test_l3_seed_is_bounded...: expected 100, got 0
test_scan_l3_seed_make_entry...: target must exist
```

GREEN:

```text
focused: 3 passed
candidate/scanner/Make regression: 106 passed
```

The overlap test proves a market matching both `l3-seed` and `near-end` retains
the more specific `near-end` attribution. No network, production mutation,
threshold change, or deployment occurred.
