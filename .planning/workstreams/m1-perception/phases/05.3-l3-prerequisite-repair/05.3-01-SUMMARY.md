---
phase: "05.3"
plan: "01"
status: complete
subsystem: m1-perception/l3-token-projection
tags: [alembic, supabase, token-identity, temp-sqlite, tdd]
dependency_graph:
  requires:
    - "05.3-CONTEXT: approved L3 prerequisite repair"
  provides:
    - "Alembic 006 nullable markets_latest.no_token_id"
    - "12-column L1 Supabase market projection"
    - "L2 temp projection preserving both outcome tokens"
  affects:
    - "05.3-03: Yes-keyed L3 pair resolution"
key_files:
  created:
    - alembic/versions/006_add_no_token_id.py
    - tests/alembic/test_006.py
  modified:
    - src/polyarb/storage/supabase_mirror.py
    - src/polyarb/observation/l2_temp_db.py
    - tests/storage/test_supabase_mirror.py
    - tests/observation/test_l2_temp_db.py
decisions:
  - "Revision 006 is add-only and chains from the already deployed OHLC revision 005."
  - "No-token identity uses the same nullable passthrough semantics as Yes-token identity."
  - "The L2 adapter maps no_token_id from the durable projection instead of manufacturing NULL."
metrics:
  focused_passed: 27
  environment_skipped: 1
  red_failures_observed: 6
---

# Phase 05.3 Plan 01: Durable Yes/No Token Projection

Added the missing durable half of the binary outcome identity chain. The L1
normalizer already emitted `no_token_id`; Alembic 006 and the narrow Supabase
mirror now retain it, and the L2 temporary SQLite adapter passes it through to
the existing full `markets` schema.

## TDD Evidence

RED:

```text
6 failed: revision 006 missing; mirror key absent; temp no_token_id was NULL
Docker replay: Can't locate revision identified by '006'
```

GREEN:

```text
uv run pytest tests/alembic/test_006.py tests/storage/test_supabase_mirror.py \
  tests/observation/test_l2_temp_db.py tests/m1-perception/test_supabase_mirror.py -q
...........................s. [100%]
27 passed, 1 environment-declared skip
```

## Boundary

The migration exists and replayed against the local testcontainer. It was not
run against production Supabase, and no service was deployed or restarted.
