# Plan 05.5-01 Summary: Production neg-risk opportunity feed

**Status:** Complete — local quality gates, exact deployment, three-run
production continuity, health, and operator documentation verified.

## Delivered

- Added an in-process L1 quote worker with immediate first collection,
  120-second sequential cadence, fail-soft retry, bounded runtime state, and
  cancellation propagation.
- Kept the worker disabled by default and explicitly enabled it in `fly.toml`.
- Centralized the 300-second quote and 50,400-second universe SLA constants.
- Added strict L1 health checks for durable complete-run age and collector
  process state. Missing/older-than-300-second quote truth fails health.
- Wired worker construction, app state, startup, cancellation, and bounded
  shutdown into `polyarb.daemon.main`.
- Updated the living M1 manual and added learning document 23.

## TDD and local verification

- Worker module existence: RED then GREEN.
- Worker behavior: five expected RED failures, then 6/6 passed.
- Health/lifecycle/route/store/collector focused suite: 86/86 passed.
- Full repository pytest: passed with the existing one xfail and one skip.
- Changed-file Ruff, compileall, M1 manual contract, planning status, and
  `git diff --check`: passed.
- Full repository Ruff still reports 229 pre-existing findings outside the
  changed files; no unrelated bulk rewrite was made.
- Docker image `polyarb:quote-worker` built as local image
  `sha256:64214e3e825d…`; image smoke proved worker import, default disabled, and
  interval 120.

## Production evidence

- Before the capacity run, the public route returned HTTP 503
  `quote run unavailable`.
- Snapshot 711 contained 1,278 eligible tokens in 254 neg-risk groups.
- Authorized capacity run 1 completed 1,278/1,278 responses in 1.013 seconds.
- The route returned HTTP 200 immediately after that complete run.
- Exact implementation `bef53d3…` deployed through GitHub workflow
  `30182327847` as Fly L1 release 130.
- Automatic runs 2→3→4 completed 1,278/1,278 responses at approximately
  121-second start intervals. Repeated `make diagnose-arb-feed-prod
  min_edge_bps=0` calls returned HTTP 200 `available-opportunities`.
- Sampled quote ages were 36.7, 17.9, and 20.3 seconds; quote worker and
  snapshot checks stayed pass. Python RSS was about 400 MB then 356 MB on the
  1 GB machine, with no rising-memory signal.
- A RED/GREEN workflow follow-up bound `POLYARB_RELEASE_ID` to `GITHUB_SHA`.
  Exact SHA `cb0ba9c54d79ed741f847c9db08ebeda098c5342` deployed via workflow
  `30183038209` as release 131; strict health reports that SHA, automatic run 6
  completed after startup, and the opportunity feed remained HTTP 200.

## Safety boundary

This feature performs public CLOB reads and local quote-run persistence only.
It does not use a wallet, sign or place orders, calculate net-after-fee
profitability, or authorize real-money execution.
