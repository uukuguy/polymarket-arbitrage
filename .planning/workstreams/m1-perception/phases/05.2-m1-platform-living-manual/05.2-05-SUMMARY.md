---
phase: "05.2"
plan: "05"
status: complete
subsystem: m1-perception/operator-documentation
tags: [living-manual, makefile, production-health, verification, handoff]
dependency_graph:
  requires:
    - "05.2-01 through 05.2-04: checked manual, staged guard, and canonical entry points"
  provides:
    - "verified M1 living-manual closure and durable recovery evidence"
    - "read-only strict L2 production health Make entry"
  affects:
    - "M1 daily production walkthroughs"
    - "future M1 public-contract changes"
key_files:
  created:
    - ".planning/workstreams/m1-perception/phases/05.2-m1-platform-living-manual/05.2-05-SUMMARY.md"
  modified:
    - "Makefile"
    - "docs/M1-市场感知平台使用手册.md"
    - "tests/m1-perception/test_m1_manual_contract.py"
    - ".planning/JOURNAL.md"
decisions:
  - "All operator-executable strict L2 health reads go through a read-only Make target; /healthz remains reachability only."
  - "Production warn/fail/zero-opportunity results are recorded as timestamped evidence rather than rewritten as documentation failures."
  - "Phase 05.2 closure does not close Phase 05.1 or confirm H-007."
requirements_completed: [PHASE05.2-R05]
metrics:
  focused_tests: 108
  walkthrough_commands: 5
  production_mutations: 0
completed: 2026-07-18
---

# Phase 05.2 Plan 05: Verified Living-Manual Closure

Closed the M1 living-manual delivery with a command-complete strict L2 health
workflow, proportional verification, a timestamped read-only production
walkthrough, and durable session recovery evidence.

## Delivered Contract

- `make smoke-l2-health-prod` remains the always-200 `/healthz` reachability
  probe and is explicitly not a business-health conclusion.
- `make smoke-l2-health-strict-prod` performs only a GET of the production L2
  strict `/health`, prints the HTTP status and response body, and exits nonzero
  unless HTTP is 200. Its recipe has no flyctl, scale, deploy, POST, restart,
  secret, schema, or chaos operation.
- The manual's capability matrix, five-minute daily flow, production read-only
  flow, candidate-chain flow, safety index, and auditable sync log all point to
  the Make entry instead of asking operators to execute a direct URL workflow.

## Manual Product and Operations Role

[`docs/M1-市场感知平台使用手册.md`](../../../../../docs/M1-市场感知平台使用手册.md)
is the stable M1 product-description and daily-operations entry point. It
separates `已验证可用`, `有条件可用`, and `尚不可用` capabilities,
routes volatile production truth to `.planning/CURRENT.md` and strict health,
and explicitly states that M1 evidence does not authorize real-money orders.

The maintenance protocol requires any M1 Make/CLI/health/Dashboard/schema or
readiness-contract change to update the manual in the same change, run
`make docs-m1-check`, and satisfy the scoped staged pre-commit guard. Readiness
changes must update CURRENT evidence before changing the stable manual.

## TDD Evidence

RED, before adding the target or rerouting the manual:

```text
uv run pytest tests/m1-perception/test_m1_manual_contract.py \
  -k 'smoke_l2_health_strict_prod or routes_l2_strict_health' -q
FF                                                                       [100%]
FAILED ... strict L2 production health target must exist
FAILED ... expected `make smoke-l2-health-strict-prod` in daily workflow
```

GREEN after the minimal Make/manual implementation:

```text
..                                                                       [100%]
```

`make -n smoke-l2-health-strict-prod` showed a temporary response file, one
curl GET to `https://polyarb-l2.fly.dev/health`, HTTP/body printing, and the
HTTP-200 exit gate. `make help` listed both L2 production health targets.

## Proportional Verification

Run after GREEN:

```text
make docs-m1-check
M1 manual contract: OK

uv run pytest tests/m1-perception/test_m1_manual_contract.py \
  tests/m1-perception/test_makefile_contract.py tests/test_makefile.py -q
........................................................................ [ 66%]
....................................                                     [100%]

bash -n .githooks/pre-commit
# exit 0

make planning-status
✓ no drift detected — every shipped plan has a SUMMARY.

git diff --check
# exit 0
```

The pre-summary planning run correctly reported 05.2-05 as `NOT-STARTED`; the
post-summary/final run is the closure gate.

## Read-only Production Walkthrough

No deploy, restart, scale, secret/schema change, POST, chaos, or other
production mutation occurred. `make smoke-test` was deliberately not run.

| UTC window | Command | Actual result |
|---|---|---|
| 11:25:50Z | `make status` | exit 0; reported 05.1 quiet-edge and final soak still open |
| 11:25:55Z–11:25:56Z | `make smoke-health-prod` | HTTP 200, overall `warn`, snapshot/Supabase pass, R2 warn; exit 0 |
| 11:26:00Z–11:26:01Z | `make smoke-l2-health-prod` | `/healthz` HTTP 200, overall `warn`; exit 0 |
| 11:26:07Z–11:26:08Z | `make smoke-l2-health-strict-prod` | strict HTTP 200, overall `warn`; cursor lag 0, listener listening, subscribed 5, mirror/candidate fresh, L3 0/10 and book-level write never observed; exit 0 |
| 11:26:12Z–11:26:13Z | `make scan-arb-live min_edge_bps=0` | HTTP 503 from production opportunity endpoint; make exit 2 |

The final 503 is truthful production evidence and a follow-up concern, not a
failure of the documentation contract. No secrets were printed or retained.

## Files Changed

- `Makefile` — added the strict read-only L2 health entry and clarified the
  existing reachability-only entry.
- `docs/M1-市场感知平台使用手册.md` — routed daily, candidate, and
  read-only workflows through Make and appended the sync log.
- `tests/m1-perception/test_m1_manual_contract.py` — asserted target safety,
  HTTP-200 gating, and manual routing.
- `.planning/JOURNAL.md` — recorded verification, production evidence, open
  boundaries, and the exact resume command.
- This SUMMARY — provides the durable Plan 05.2-05 recovery anchor.

## Boundaries, Limitations, and Next Step

Phase 05.1 remains independently open. The natural 60-second quiet-edge
receive→mirror proof has not occurred, and Phase 05 Plan 06's strict soak has
not started. This plan neither changes those gates nor repairs the observed
opportunity-feed 503.

The controller will evaluate H-007 after task review. No climb hypothesis,
runs ledger, session state, or research tree was changed, and H-007 is not
claimed confirmed here.

Next session starts with:

```bash
/gsd-resume-work --ws m1-perception
```
