---
phase: "05.2"
plan: "01"
status: complete
subsystem: m1-perception/living-manual-contract
tags: [documentation-contract, offline-validator, staged-impact, pytest]
dependency_graph:
  requires:
    - "05.2-CONTEXT: living M1 platform manual contract"
  provides:
    - "scripts.check_m1_manual.validate_manual"
    - "scripts.check_m1_manual.classify_staged_impact"
    - "scripts.check_m1_manual.main"
  affects:
    - "05.2-02: real M1 manual content"
key_files:
  created:
    - scripts/check_m1_manual.py
    - tests/m1-perception/test_m1_manual_contract.py
decisions:
  - "The checker is pure filesystem/text validation and requires no network or credentials."
  - "Content-based staged impact is path-bound to explicit M1 production surfaces; tests, internal tools, scripts, and log-only changes do not trigger the manual gate."
  - "Staged Git paths use a NUL-separated bytes protocol with os.fsdecode so the canonical Chinese manual path survives core.quotePath settings exactly."
  - "The nested m1-perception test adds the repository root to sys.path before importing scripts.check_m1_manual because its shared conftest dependency chain preloads a third-party scripts namespace under the repository's pytest invocation."
metrics:
  focused_tests: 9
  contract_behaviors: 8
---

# Phase 05.2 Plan 01: Offline Manual Contract Checker

Implemented the first H-007 slice: a credential-free checker for the future M1
living manual. The public interfaces are:

- `validate_manual(root: Path, text: str) -> list[str]`
- `classify_staged_impact(paths: list[str], diff: str) -> bool`
- `main(argv: Sequence[str] | None = None) -> int`

The fixture suite covers the six base contract areas plus two staged-change
regression areas across nine focused tests:

1. all ten required numbered headings and verification/current-state metadata;
2. readiness-label allowlisting and seven-column capability rows;
3. Make target existence and local Markdown-link resolution;
4. health marker name/source agreement;
5. dashboard filesystem route/source agreement;
6. narrow staged-impact classification, including negative refactor/test cases;
7. per-file path binding for health, CLI, route, Make, migration, and dashboard changes;
8. NUL-separated filesystem-safe staged-path parsing and Unicode manual dispatch.

The validator reads only repository files. It performs no HTTP calls, cloud
operations, credential lookup, or production mutation. The real manual is
deliberately deferred to Plan 05.2-02.

## TDD Evidence

RED:

```text
uv run pytest tests/m1-perception/test_m1_manual_contract.py -q
E   ModuleNotFoundError: No module named 'scripts.check_m1_manual'
1 error during collection
```

GREEN:

```text
uv run pytest tests/m1-perception/test_m1_manual_contract.py -q
.........                                                                [100%]
```

## Staged Classifier Patterns

- Alembic paths under `alembic/versions/`.
- Next.js route pages under `dashboard/app/**/page.tsx`.
- Added/removed M1 operator-facing Make help contracts.
- Added/removed `checks["..."]` health registrations.
- Added/removed Typer command/option/argument declarations.
- Added/removed Starlette `Route("/..."` declarations.

Content patterns are evaluated only within the corresponding unified-diff
file chunk and explicit production-path allowlist. Staged filenames are read
via `git diff --cached --name-only -z` as bytes, then decoded with
`os.fsdecode`; Git display quoting never enters the contract decision.

## Deviations

The plan example imported `Sequence` from `typing`; Python 3.12 Ruff rule
UP035 requires `collections.abc.Sequence`, which preserves the exact public
annotation while satisfying repository lint. The fixture also needs a local
`sys.path` bootstrap before the specified `scripts.check_m1_manual` import due
to the preloaded third-party namespace described above.
