---
phase: "05.2"
plan: "03"
status: complete
subsystem: m1-perception/operator-documentation
tags: [living-manual, pre-commit, staged-contract, planning-hygiene]
dependency_graph:
  requires:
    - "05.2-01: path-bound Unicode-safe staged manual checker"
    - "05.2-02: M1 product and operations living manual"
  provides:
    - "pre-commit enforcement for staged M1 public-contract synchronization"
    - "auditable manual no-operator-impact path"
  affects:
    - "all later changes to M1 public operator contracts"
key_files:
  created:
    - ".planning/workstreams/m1-perception/phases/05.2-m1-platform-living-manual/05.2-03-SUMMARY.md"
  modified:
    - ".githooks/pre-commit"
    - "docs/M1-市场感知平台使用手册.md"
    - "tests/m1-perception/test_m1_manual_contract.py"
decisions:
  - "Run the offline staged M1 manual checker immediately after set -euo pipefail, before commit-message parsing and every SUMMARY-related early exit."
  - "A staged public-contract change is satisfied only when the manual is staged in the same change; operator-impact updates change the explanation/matrix, while no-impact updates append an auditable Section 10 sync-log reason."
  - "Keep the Task 1 classifier narrow: unrelated, test-only, and log-only changes remain an immediate no-op."
metrics:
  focused_hook_tests: 7
  full_contract_tests: 20
---

# Phase 05.2 Plan 03: Staged M1 Manual Synchronization Guard

Integrated the existing `scripts/check_m1_manual.py --staged` contract into the
repository pre-commit hook without changing the existing plan SUMMARY guard.

## Hook Placement and Behavior

The hook invokes:

```text
uv run python scripts/check_m1_manual.py --staged
```

immediately after `set -euo pipefail`. This placement is intentionally before
commit-subject parsing and `PHASE_PLAN=` so a non-plan-scoped commit or a hook
invocation without a message cannot bypass the staged M1 guard.

The integration preserves the Task 1 classifier and therefore has three
auditable outcomes:

- a staged M1 public-contract change without a staged manual update is blocked;
- the same contract change with a valid staged manual update is allowed;
- unrelated, test-only, and log-only changes exit successfully without manual
  validation output.

Section 10 of the manual now records that enforcement is active. Public changes
with operator impact update the relevant explanation or readiness matrix. A
public change with no operator impact stages a dated sync-log entry explaining
why the workflow is unchanged.

## TDD Evidence

RED, after adding hook-order, real staged-index, and manual-status assertions but
before changing the hook/manual:

```text
uv run pytest tests/m1-perception/test_m1_manual_contract.py \
  -k 'precommit or reviewed_operator_safety' -q
FFF...F                                                                  [100%]
FAILED ... test_precommit_invokes_staged_m1_manual_check_before_summary_exit
FAILED ... test_precommit_blocks_staged_public_contract_without_manual_sync
FAILED ... test_precommit_allows_staged_public_contract_with_manual_sync
FAILED ... test_manual_keeps_reviewed_operator_safety_facts
```

GREEN after the minimal hook and Section 10 truth update:

```text
uv run pytest tests/m1-perception/test_m1_manual_contract.py \
  -k 'precommit or reviewed_operator_safety' -q
.......                                                                  [100%]
```

Full verification:

```text
uv run pytest tests/m1-perception/test_m1_manual_contract.py -q
....................                                                     [100%]

make docs-m1-check
M1 manual contract: OK

bash -n .githooks/pre-commit
# exit 0

make help | rg 'docs-m1-check:'
  docs-m1-check: Offline verification of the M1 platform manual's commands, links, routes, health names, and readiness matrix
```

`make planning-status` reported no shipped-plan drift before this SUMMARY was
created.

## Scope and Production Impact

This plan changed only the hook integration, focused repository contract tests,
the manual's living-truth status, and this SUMMARY. It did not alter the
existing SUMMARY guard, the staged classifier, Task 4 entry points, runtime
code, dependencies, production/cloud state, Phase 05/05.1, or climb state.
