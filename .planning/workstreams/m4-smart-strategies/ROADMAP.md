# Roadmap: M4 Smart Strategies (LLM-driven)

> 能力线，不是里程碑。
> Phase 由 `gsd-tools phase add "..."` 动态长出，不预先列。

## Overview

加入需要价值判断的策略：weather edge（基于集合预报）、news-driven（事件解读）、MM rebate。
LLM agent 在此能力线真正参与价值判断，前面（M1/M2/M3）都是确定性代码。

依赖 m1-perception 数据沉淀够 + 评估框架（不直接信 LLM 输出）。

## Phases

(等 m1 数据 + 评估基线)

---

*Workstream: m4-smart-strategies*
