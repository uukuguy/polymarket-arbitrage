---
context: default
phase: m1-perception-scoped-upstream-fault-control
task: 8
total_tasks: 8
status: paused
last_updated: 2026-07-31T03:03:25.210Z
---

# M1 Scoped Upstream Fault Control — Integrated Handoff

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failures. Acknowledge them
> explicitly before proceeding.

- [ ] CONSTRAINT: Worktree closure is part of completion — A reviewed local
  branch was incorrectly left awaiting a user disposition. Required mitigation:
  merge locally, verify the merged tree and test suite, preserve user state,
  remove the owned worktree, delete the merged branch, and record the result.
- [ ] CONSTRAINT: Image-aware targets can invoke cloud fallback —
  `make chaos-l2-fly-image-check` began read-only Fly status/SSH discovery
  before it was terminated. Required mitigation: inspect target expansion and
  use only explicitly authorized local-only paths before running any target
  that may invoke Fly or another external service.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Pausing before integration | Treating a verified feature branch as complete while its worktree remains open. | blocking | Close the branch locally unless the user explicitly requests PR/push retention. |
| Implicit cloud fallback | A local image check transparently reached a live machine. | blocking | Read the Make target and prohibit its cloud fallback without exact authorization. |
| Relabelling local evidence | Synthetic/local qualification can be mistaken for production qualification. | advisory | Preserve `NOT RUN` until exact release and evidence scope are authorized. |

<current_state>

The scoped upstream-fault-control plan is locally complete and integrated into
`main`. Merge commit `b4c9ee3` preserved both histories; its tree exactly
matched the independently reviewed feature tree. The final local integration
record is `d245919` before this handoff commit.

`fix/l3-continuity-repair` and its owned worktree have been removed. The only
remaining local worktree belongs to the independent M2 position-persistence
branch and is out of scope.
</current_state>

<completed_work>

- All eight scoped upstream-fault-control tasks were implemented, reviewed,
  remediated twice, documented, and summarized.
- Final source/evaluator/finalizer evidence chain is fail-closed: cleanup
  authority uncertainty freezes local injection, rejected intent envelopes
  cannot claim or create source evidence, and authority timestamps are
  monotonic.
- Final local verification at feature HEAD: 3,695 repository tests collected,
  3,693 passed, one skip, one expected xfail; Ruff, docs, planning, and local
  qualification passed.
- After merge onto `main`, the same 3,695-test suite, local qualification,
  docs gate, and planning gate passed again.
</completed_work>

<remaining_work>

- Production qualification is blocked pending human authorization for one exact
  deployed release and a new evidence directory.
- No deployment, feature enablement, production fault injection, wallet,
  signing, order, or trade action is authorized by this handoff.
</remaining_work>

<decisions_made>

- Four-role source/evaluator/finalizer capability separation remains mandatory.
- Local `qualify-perception-local` proves only local consistency; it cannot be
  promoted to production evidence.
- User-owned dirty files are preserved unstaged on `main`; never stage or edit
  them as part of this work.
</decisions_made>

<blockers>

- Human authorization is required for production qualification. Required inputs
  are an exact deployed release identity, a fresh evidence directory, and
  explicit read-only collection permission. Fault mutation needs additional,
  fault-specific authorization.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — project contract, workstream rules, and production boundary.
2. `.planning/JOURNAL.md` Sessions 121-122 — implementation closure and local
   integration evidence.
3. `docs/superpowers/plans/2026-07-30-scoped-upstream-fault-control-SUMMARY.md`
   — all task decisions, verification, and external-gate disclosure.
4. `docs/dev/perception-fault-runbook.md` — four-role capability and
   qualification contract.
5. `.planning/threads/market-observation-architecture.md` §2.17 — write-side
   truth, cleanup authority, rejected-envelope, and monotonic-time lessons.

## Infrastructure State

- Branch/worktree: `main` at the repository root; l3-continuity-repair is gone.
- No local server, watcher, or test process is running.
- The uncommitted files are exactly:
  `.superpowers/sdd/progress.md`, `.superpowers/sdd/task-2-report.md`,
  `.superpowers/sdd/task-7-brief.md`, `findings.md`, `progress.md`, and
  `task_plan.md`. They were restored byte-for-byte after worktree cleanup and
  must remain unstaged.
- Production qualification is **NOT RUN**. The earlier unintended read-only Fly
  check was terminated and disclosed in the plan SUMMARY; it is not PASS.

<context>

The implementation loop is closed. The next task is not more fault-control
coding by default: first re-establish current planning state, then select the
next authorized local work item. Treat production qualification as an external
authority boundary, not an automatic next command.
</context>

<next_action>

Start with:

```bash
/gsd-resume-work --ws m1-perception
```

Then run:

```bash
make planning-status
```
</next_action>
