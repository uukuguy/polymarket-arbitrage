---
context: default
phase: m1-perception-opportunity-watcher
task: 3
total_tasks: 6
status: paused
last_updated: 2026-07-27T12:51:33.365Z
---

# M1 Neg-risk Opportunity Watcher — Resume Here

## Current State

Design is approved and committed:

1. `docs/superpowers/specs/2026-07-27-neg-risk-opportunity-watcher-design.md` at `48a2b6e`.
2. `docs/superpowers/plans/2026-07-27-neg-risk-opportunity-watcher.md` at `829644f`.

Implementation is local only; nothing in Fly production has been changed or deployed.

Completed commits:

- `8fd425b`: `GroupAssessment` separates `observe`, legal `no-edge`, and `unavailable` within one complete certified Quote projection.
- `b623e31`: SQLite opportunity master, append-only observation, and notification outbox; enter and close transitions.
- `916c55f`: 25bps material edge-change outbox rule.
- `00a2537`: delivered/failed notification audit transitions that leave opportunity facts unchanged.

## Completed Work

- Scanner regression test proves `no-edge` is distinct from incomplete Quote evidence.
- Ledger tests prove first entry writes master + observation + pending card atomically; a complete below-threshold observation closes it; a +25bps change creates one follow-up card; delivery removes only the pending notification.
- Relevant scanner/ledger/quote-store regressions and Ruff passed before commits.
- `make planning-status` reported no plan-summary drift before handoff.

## Remaining Work

1. Finish ledger behavior: unavailable/invalidation, capacity-change notifications, replay queries, and 30-day compaction.
2. Create `OpportunityWatcher`; reconcile only successful `CompleteQuoteProjection` instances and deliver durable outbox cards through existing Telegram transport.
3. Add focused identity-bound CLOB tracker (15 seconds) without inserting false global Quote runs.
4. Add cloud market-map/watcher APIs, HMAC triggers, and Make command entries.
5. Change cadence/docs, deploy exact verified commit, and collect honest production evidence.

## Required Reading

1. `docs/superpowers/specs/2026-07-27-neg-risk-opportunity-watcher-design.md` — approved product boundary.
2. `docs/superpowers/plans/2026-07-27-neg-risk-opportunity-watcher.md` — exact TDD task sequence.
3. `src/polyarb/routing/opportunity_scanner.py` — new assessment contract.
4. `src/polyarb/routing/opportunity_ledger.py` and `tests/routing/test_opportunity_ledger.py` — current durable facts.
5. `src/polyarb/daemon/quote_worker.py` and `src/polyarb/daemon/alerts.py` — next integration points.
6. `.planning/JOURNAL.md` SESSION 113 — latest project timeline.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Overwriting an existing test file | The first new scanner test accidentally replaced an existing 513-line tracked test file; it was restored before any commit. | advisory | Before creating a test path, run `test -e <path>` or `rg --files tests`; append/update an existing test file instead of replacing it. |
| Placeholder-count mismatch in SQL | First ledger INSERT used 13 values for 12 columns; the red test caught it before commit. | advisory | Count named INSERT columns against values and run the focused SQLite test before any broader test suite. |

## Infrastructure State

- Branch: `fix/l3-continuity-repair` in an already isolated linked worktree.
- Production: unchanged by this implementation session. Existing L1/L2 state must be re-read before any deploy claim.
- No local server, daemon, watcher, or browser automation is running for this work.
- Preserve unrelated dirty user files: `findings.md`, `progress.md`, `task_plan.md`.

## Summary Placeholder Scan

The workflow’s broad placeholder scan matched many historical PLAN/SUMMARY/research files. `make planning-status` is the authority for current summary drift and reported no drift; do not treat those historical textual matches as a blocker for this watcher work.

## Next Action

Start with:

```bash
uv run pytest tests/routing/test_opportunity_ledger.py -q
```

Then write a failing `OpportunityWatcher` test first. The first implementation boundary is: a watcher receives a successfully certified `CompleteQuoteProjection`, calls `assess_certified_neg_risk_quote_projection`, persists ledger transitions, and attempts outbox delivery. A collection failure, collecting run, or Telegram failure must not mutate the certified global feed into a false market result.
