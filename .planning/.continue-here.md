---
context: research
phase: m1-perception-task-8
task: 5
total_tasks: 7
status: paused
last_updated: 2026-07-29T13:12:22.670Z
---

# M1 Task 8 Production Fault Qualification — Resume Here

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: No silent long-running work — the user repeatedly had to ask whether tasks were running or failing — announce each task start, failure, fix, and commit; send progress at least every 60 seconds during long gates.
- [ ] CONSTRAINT: Discuss architecture before changing direction — reactive implementation changes caused the user to object that local comments were moving the main line — state the tradeoff and preserve the live plan before changing the implementation boundary.

**Do not proceed until both boxes are acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Silent background verification | Long-running tests/reviews produced no visible progress and the user had to poll repeatedly. | blocking | Announce every task transition and report percentage/output at intervals under 60 seconds. |
| Reactive main-line switching | A concrete comment was treated as authorization to redirect the architecture without first resolving the collection/opportunity-first design. | blocking | Explain the system-level implication and update the stable plan before editing. |
| Treating truncated output as success | One Candidate regression result was lost during context compaction. | advisory | Rerun and obtain an explicit exit code before any completion claim. |

<current_state>

Active workstream is `m1-perception`, branch `fix/l3-continuity-repair`, linked
worktree `.worktrees/l3-continuity-repair`. Task 8 runtime evidence is complete:
all 16 fault plans name real durable Incident/coverage behavior and no
`not-wired` marker remains. Only three production adapters are executable:
`candidate-exit`, `discovery-exit`, and `reconciliation-stall`. The other 13
still reject before filesystem/network mutation.

HEAD before this WIP handoff commit is `a5aa19e`.
</current_state>

<completed_work>

- `a820e97`: early `child-stalled` Reconciliation detection, authenticated idle
  heartbeat, exact SIGSTOP/SIGCONT recovery.
- `c0a3186`: Gamma timeout/malformed/cursor durable incidents and exact
  Discovery/Reconciliation writer proof.
- `77aedb0`: Candidate missing-leg/429/latency group-scoped incidents; reserved
  lanes start as a bounded concurrent batch.
- `567295d`: SQLite BUSY/LOCKED becomes `candidate:<group>/sqlite-busy`, not the
  false `child-failed` expectation.
- `1f88de9`: env-configured disk-free/load-per-CPU Resource sensors,
  `resource-disk-pressure` / `resource-contention`, legacy-v1 replay.
- `1147379`: exact-outbox `telegram-delivery-failed` lifecycle and restart-safe
  delivered-attempt convergence.
- `a5aa19e`: SESSION 119 and architecture-thread checkpoint.
- Full `tests/perception + tests/m1-perception` passed 100% after `379495e`;
  subsequent Candidate/Resource/Notification/HTTP proportional regressions,
  Ruff, M1 manual, diff, and 82-plan no-drift gates passed.
</completed_work>

<remaining_work>

1. Write and critique a scoped upstream fault proxy contract for
   Gamma/CLOB/Telegram. It must bind exact release/fault/target, affect only the
   selected call class, expose intent, and guarantee finally cleanup.
2. Implement those adapters under RED tests without enabling production.
3. Implement separate bounded primitives for SQLite lock, disk filler, host
   load, daemon restart, deploy interrupt, and HTTP recovery proof.
4. Run the full local/read-only qualification, Dashboard build/typecheck, docs,
   planning, code/security review, then write Task 8 evidence/handoff.
5. Only after exact user authorization: deploy exact SHA, enable flags in
   audited stages, inject one production fault at a time, and collect the final
   continuous window.
</remaining_work>

<decisions_made>

- Runtime detection and mutation capability are separate deliverables.
- Logs never replace durable Incident events or recovery receipts.
- Upstream API adapters may share a scoped proxy contract; host/store/runtime
  primitives must remain separate because cleanup and identity effects differ.
- Resource sensor thresholds live in environment variables. Old v1 history
  treats missing additive sensor fields as unknown and keeps original hashes.
- Telegram `delivered` proves the API writer only, not handset display/read.
</decisions_made>

<blockers>

- Local implementation is not blocked.
- Production deploy/flags/injection/cutover require exact-SHA and
  fault-specific user authorization.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — project role, workstream, planning, teaching, Makefile, image,
   and chain-truth rules.
2. `.planning/JOURNAL.md` SESSION 119 — exact latest timeline and boundary.
3. `docs/superpowers/plans/2026-07-28-m1-opportunity-first-rollout.md` Task 8 —
   acceptance contract.
4. `docs/dev/perception-fault-runbook.md` — current fault matrix and safety.
5. `docs/learning/41-fault-qualification-matrix.md` — mental model and recovery
   boundaries.
6. `scripts/perception_chaos.py` — adapter framework and fail-closed routing.
7. `.planning/threads/market-observation-architecture.md` — chain-truth design.

## Critical Anti-Patterns (do NOT repeat these)

- Do not claim “production-ready” from local tests or synthetic PASS.
- Do not enable an adapter merely because its runtime Incident now exists.
- Do not use SSH/Fly ad-hoc injection to bypass adapter intent/cleanup/evidence.
- Do not stage or edit user-owned dirty `findings.md`, `progress.md`,
  `task_plan.md`.
- Do not touch wallet, signing, balances, orders, or trades.

## Infrastructure State

- Worktree: `/Users/sujiangwen/sandbox/hacker2026/PolyMarket/polymarket-arbitrage/.worktrees/l3-continuity-repair`
- Branch: `fix/l3-continuity-repair`
- No local server, test process, watcher, or browser session remains running.
- No cloud deployment, feature flag, secret, database, fault, wallet, order, or
  trade state changed in the latest implementation sequence.
- Production must be re-read before any new external-state claim.

## Summary Placeholder Scan

The broad workflow grep matched historical SUMMARY files that contain literal
example words such as placeholder/TBD. `make planning-status` is authoritative
and reported all 82 plans clean; there is no active SUMMARY drift.

## Pre-Execution Critique Required

- Design artifact: create a focused scoped-upstream-fault-proxy design next to
  the existing Task 8 plan/runbook.
- Critique focus: exact target selection; no secret leakage; one active fault;
  cleanup on timeout/cancel/process exit; image availability; incident MTTD;
  authentic recovery writer; immutable evidence; unchanged release/boot.
- Gate: do not enable a Gamma/CLOB/Telegram adapter until the critique is
  complete and its RED contract tests pass.

<context>

The runtime side is now coherent. The next risk is mutation architecture, not
another Incident class. Prefer the smallest reusable upstream proxy boundary,
but resist abstracting disk/load/restart/deploy into it. Keep user-visible
progress frequent and preserve the stable Task 8 sequence.
</context>

<next_action>

Start with:

```bash
make planning-status
```

Then acknowledge the two blocking constraints, read SESSION 119 and the
runbook, and write/critique the scoped upstream fault proxy contract before
touching adapter implementation.
</next_action>
