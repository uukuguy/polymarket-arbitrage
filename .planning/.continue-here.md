---
context: default
phase: 05.6-self-healing-structure-production
task: 0
total_tasks: 0
status: paused
last_updated: 2026-09-02T11:12:49.571Z
---

# M1 Business-Research Closure — Handoff

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual production failures.

- [ ] CONSTRAINT: Capacity is a write-admission prerequisite — A full detail mirror pushed the Free Supabase database into its critical state. Do not resume the coordinator or any range/batch lane until a tested capacity admission and post-publication physical-space reuse path exists.
- [ ] CONSTRAINT: A zero must be lineage-current — The opportunities projection can be `available` with zero rows while pointing at an older Quote generation. Do not present it as a current market conclusion unless Quote generation, parent Structure generation, and certified pointer all align.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Full-detail database mirroring | Mirroring every Structure/Quote record exhausted database capacity and hid useful business state. | blocking | Keep authoritative full artifacts in R2; materialize bounded research indexes only, and gate new work before admission. |
| Treating stale zero as no opportunity | A historical certification pointer carried a zero count after Quote advanced. | blocking | Require generation lineage equality in API, Dashboard, daily brief, and any conclusion. |
| Dashboard summary mistaken for business research | Analysis only held aggregate source counts; candidate/reject facts were never published. | advisory | Persist bounded funnel facts and rejection taxonomy before calling the funnel complete. |

<current_state>

Structure and Quote business research pages are deployed and visually verified through the authenticated Dashboard. Structure shows event-level market intelligence; Quote shows market, executable YES ask/depth, execution state, event, and neg-risk evidence.

The M1 business chain is not complete. The current production overview says Analysis is `available` only for Structure/Quote counts and carries `candidate-reject-detail-not-published`. The certified opportunities projection is `lagging`: it has a historical zero-row pointer for Quote generation `quote:d5a198...`, while the current quote research generation is `quote:58d5e2...`. Execution qualification is paused as `not-yet-qualified`.

The coordinator Machine `e82d1220b2d138` in `polyarb-control-worker-m1` was intentionally stopped to prevent another database-capacity incident. Structure and Quote view data remain readable, but new Quote admission/cadence is not running.
</current_state>

<completed_work>

- Production migration 046 and bounded Structure intelligence projection were deployed. Current Structure generation `structure:0bc58cc52d5d57a55b2c86c56fa8eee6fe0aaa7d1a12aeda988d2ce6ca62c094` exposes 22,236 events, 175,979 markets, 108,116 tags, and 10,472 group-truth records through useful business views.
- The Quote research API regression that produced `Detailed Quote index unavailable` was repaired in `49b60c00`; the business Quote page was redesigned and deployed in `5446f60b`.
- Quote index physical space was reclaimed after write lanes stopped. This prevented another immediate outage but is not an automatic recurring control.
- Read-only audit confirmed the current Analysis/Opportunity gaps above; this is an implementation and lineage gap, not merely a lack of market data.
</completed_work>

<remaining_work>

- Design and implement a pre-admission capacity guard plus bounded, post-publication Quote space reuse; verify it before restarting the coordinator.
- Persist a bounded Analysis funnel: candidate counts, rejection taxonomy, and no-edge/reject evidence tied to Structure/Quote generations.
- Publish certified opportunities for the current Quote generation, enforce lineage-current semantics, and render actual opportunity rows/evidence in the Dashboard.
- Update the business operator guide and required plan summaries after the corresponding implementation plans land.
</remaining_work>

<decisions_made>

- R2 is the complete immutable source; Postgres holds capacity-bounded indexes and research projections only.
- Coordinator stop is an intentional safety state, not proof of healthy continuous business operation.
- `available + count=0` is only a verified no-opportunity conclusion when its projection is current for the active Quote and parent Structure lineage.
- Do not make production mutations merely to inspect state; begin new work with read-only status/brief evidence and a scoped M1 objective.
</decisions_made>

<blockers>

- No human action is currently required to inspect production state.
- Restarting write lanes is blocked by missing automatic capacity admission and space-reuse controls, not by credentials or a lack of operator access.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — project contract and production-mutation boundaries.
2. `.planning/JOURNAL.md` Sessions 398–399 — latest capacity/recovery facts.
3. `.planning/workstreams/m1-perception/STATE.md` — official workstream state; treat historical next-actions as context, not authorization.
4. `docs/superpowers/plans/2026-09-02-quote-business-view.md` — completed Quote-view contract.
5. `src/polyarb/control_plane/quote_admission.py` — admission point for the next capacity control.
6. `dashboard/app/business/analysis/page.tsx` and `dashboard/app/business/opportunities/page.tsx` — current incomplete business views.

## Infrastructure State

- Fly control-plane API: `https://polyarb-control-api.fly.dev`.
- Coordinator deliberately stopped: app `polyarb-control-worker-m1`, machine `e82d1220b2d138`, process `coordinator`.
- Quote and Structure range Machines remain started; current data is readable.
- Dashboard is Vercel Access protected. Reuse the existing Playwright browser session; do not open a new browser instance that forces another login.
- Existing user/unrelated dirty content must remain unstaged: `.planning/JOURNAL.md`, `.wrangler/`, and existing `output/` artifacts.

<context>

The next session should not continue polishing individual tables. The main business objective is to make the Structure → Quote → Analysis → Certified opportunity chain both capacity-safe and truthfully observable. Start by turning the proven capacity incident into an admission control, then add the missing persisted funnel facts and current-lineage certification.
</context>

<next_action>

Start with:

```bash
/gsd-resume-work --ws m1-perception
```

Then run:

```bash
make planning-status
make smoke-control-plane-prod
make control-plane-business-brief
```

Before any production mutation, use a fresh bounded objective and design the capacity-admission contract; do not restart the coordinator as an observation step.
</next_action>
