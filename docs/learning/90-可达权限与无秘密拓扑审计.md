# 可达权限与无秘密拓扑审计

## 30 秒心智模型

权限不是一张平面表，而是一条可达链：

`能连接数据库 → 能进入 schema → 对象 ACL 允许操作`。

对象上写着 `PUBLIC SELECT`，不等于角色真的能读；如果缺少 schema
`USAGE`，路径在上一跳已经断了。相反，运维工具看到 provider JSON
也不等于应该把它打印出来：先在内存里缩成闭合字段，再输出。

这次两个问题其实是同一种设计：**只传播真正可达、明确允许的信息**。

第三个生产差异也属于同一心智模型：角色创建后，provider 可能留下
一条不传播权限的 creator membership。要比较的是完整 option tuple，
不是看到“有 member”就粗暴放行或拒绝。

## 代码地图

- `alembic/versions/026_m1_runtime_scoped_roles.py:330`：先跨全部非系统
  schema 验证 namespace 权限，再枚举角色可达的对象。
- `src/polyarb/control_plane/db_role_contract.py:608`：daemon 启动前重复同一
  权限模型，防止迁移后权限漂移。
- `src/polyarb/control_plane/fly_topology_audit.py:44`：只允许两种 Fly
  只读命令，并强制 Keychain 身份。
- `src/polyarb/control_plane/fly_topology_audit.py:178`：原始 provider JSON
  在内存中收缩成精确 Machine 输出。
- `alembic/versions/026_m1_runtime_scoped_roles.py:62`：定义 Supabase
  delegated creator 和 grantor，并只承认精确的 PG16 option tuple。
- `src/polyarb/control_plane/db_role_contract.py:552`：daemon 启动验证同样
  区分 non-effective creator 和 effective application login。

## 关键代码片段

迁移先检查所有 schema：只有 `public USAGE` 可以成立，其他非系统
schema 的 `USAGE/CREATE` 都拒绝（`alembic/versions/026_m1_runtime_scoped_roles.py:330`）。
对象枚举随后加上可达条件（同文件 `:355`）：

```sql
AND pg_catalog.has_schema_privilege(
        '{role}', namespace.oid, 'USAGE'
    )
```

这不是“忽略 extensions 权限”，而是准确表达 PostgreSQL 的访问链。
如果未来角色真的得到 `extensions USAGE`，schema 总门会先拒绝；不会把
provider ACL 偷偷加入允许集。

Fly 审计的边界在 `src/polyarb/control_plane/fly_topology_audit.py:44`：

```python
child_env = os.environ.copy()
child_env["FLY_API_TOKEN"] = ""
return subprocess.run(
    command, capture_output=True, text=True, timeout=30, check=False, env=child_env
)
```

关键不是 `capture_output` 本身，而是 raw body 此后只能进入严格 parser；
provider 失败统一变成 reason code，stderr 和解析异常正文都不会传播。

PostgreSQL 16 的 membership 不能只记成“A 属于 B”。对这次的
Supabase creator，精确 tuple 是：

```text
member=postgres, grantor=supabase_admin,
admin=true, inherit=false, set=false
```

`inherit=false + set=false` 意味着它不是 daemon 能力的传播路径。真正的
application login 则必须是 `admin=false, inherit=true, set=true`。
两者的语义不可交换。

## 设计取舍

### 为什么不硬编码 Supabase 的两个扩展视图？

硬编码 `pg_stat_statements*` 只是给今天的 provider 形态开洞。可达性规则
来自 PostgreSQL 语义，能覆盖未来任意不可达扩展对象，同时仍拒绝真正
可访问的额外权限。

### 为什么不直接撤销 PUBLIC ACL？

那会改变 Supabase 和四个既有应用共享的数据库环境，扩大生产变更面。
revision 026 的目标是隔离两个新进程，不是重写 provider 默认权限。

### 为什么 env key 也会触发拒绝？

只隐藏 value 仍可能让密码继续以普通 Machine env 存在。审计遇到
`DSN/PASSWORD/TOKEN/SECRET/...` 类普通 key 直接失败，迫使 credential
回到 Fly hidden secret；错误可以指出 key，但绝不打印 value。

### 为什么允许 creator membership 不是权限放宽？

因为允许集不是“`postgres` 这个名字可以存在”，而是 member、grantor
和三个 PG16 option 全部精确匹配。任何一位变为可继承、可 `SET ROLE`
或来自其他 grantor，都会立即拒绝。

## 自检题

1. `has_table_privilege(role, object, 'SELECT') = true`，为什么仍不能单独证明
   role 能执行 `SELECT`？
2. 如果给 PUBLIC 增加 `USAGE ON SCHEMA extensions`，revision 026 会在哪一层
   fail-closed？
3. provider 返回非 JSON，为什么错误消息不能附带前 200 字符帮助诊断？
4. 为什么安全审计仍要比较“实际 Machine 集合 == 预期 Machine 集合”，而不只
   输出找到的目标？
5. `ADMIN=true` 为什么不能单独证明 creator 已获得 capability 的运行权？

## FAQ 增量

暂无。后续答疑只追加在这里；同一问题出现三次后再提升到正文。
