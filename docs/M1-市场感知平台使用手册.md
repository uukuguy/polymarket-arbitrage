# M1 市场感知平台使用手册

> 最后核验：2026-07-27
> 动态状态：以 [`.planning/CURRENT.md`](../.planning/CURRENT.md) 和生产 L1/L2 `/health` 为准。
> 定位：产品说明 + 日常运维；设计学习请进入 [`docs/learning/`](learning/00-INDEX.md)。

## 1. 30 秒心智模型

市场发现 → L1 Structure（5 分钟）→ Quote（2 分钟）→ 候选集 → L2 实时订单簿 → L3 观察证据 → 人工/paper 决策。
M1 的职责是看清市场和暴露数据质量，不构成真实资金下单授权。

三种事实必须分开：本地运行只证明当前 checkout 和本地数据；“候选”是 L1 筛出并送往 L2 的市场候选集，其收敛证据只证明数据链跟上；生产事实必须由当前生产实例的 strict `/health`、读模型和可审计时间戳共同证明。任何一层的绿灯都不会自动升格成下单许可。

## 2. 功能完成度矩阵

| 能力 | 状态 | 用途 | 数据源 | 验证方法 | 已知限制 | 禁止用途 |
|---|---|---|---|---|---|---|
| L1 市场发现与快照 | 已验证可用 | 每 5 分钟建立可供 M2 绑定的市场结构，并按需保留历史切片 | Structure: Gamma markets/events；Archive: CLOB/Parquet | `make snapshot-status`; 生产跑 `make smoke-health-prod` 与 `make smoke-market-truth-prod` | 最新 Structure 尝试必须完整发布；Archive 独立 warn；动态状态看 CURRENT | 把 HTTP 成功、旧组合快照或本地行数当市场全集 |
| 观察配方与单市场追踪 | 已验证可用 | 筛候选、看漂移、管理 watchlist | 本地 SQLite/Parquet | `make overview`; `make list-recipes` | 结果依赖最近一次本地快照 | 把筛选命中当可成交机会 |
| L1→L2 候选链 | 有条件可用 | 将最新候选送入实时跟踪 | Supabase + durable cursor + NOTIFY/poll | `make smoke-l2-health-prod`; `make smoke-l2-health-strict-prod` | Phase 05.1 quiet-edge 证据仍以 CURRENT 为准 | 忽略 cursor lag 或 freshness fail |
| L2 WebSocket 与 top-of-book mirror | 有条件可用 | 观察实时盘口和镜像新鲜度 | Polymarket WS + Supabase | L2 `/health` 的 `ws:*` 与 `mirror:*` | 安静市场需区分活连接和业务帧新鲜度 | 把连接在线等同盘口可成交 |
| L3 深度、OHLC 与连续证据 | 已验证可用 | 为单市场深度/K 线和 24h 连续性提供证据 | `l2_book_levels` + OHLC views + 五张 L3 evidence 表 | `make l3-evidence-status`; `make ohlc-spot-check URL=https://polyarb-l2.fly.dev`; 完整窗口用 `make l3-soak-verify` | production 007、隔离凭据与 exact-SHA boot 已就位；A1–A6 是拒绝/诊断证据，A7 T0/T6/T12/T18/T24 与 final raw-row verify 已 PASS | 用一次当前绿灯推断未来永不故障，或把观察资格当下单许可 |
| Dashboard | 有条件可用 | 浏览候选、盘口、成交、L3 和状态 | Supabase read models | `make smoke-l2-dashboard`; L3 用 `make smoke-l3-dashboard asset_id=...` | canonical URL 是 `https://polymarket-arbitrage-jiangwen-su-s-projects.vercel.app`；Vercel Auth 后还需检查真实页面和时间戳 | 仅凭登录页或 UI 绿色绕过 `/health` |
| Neg-risk 可成交 ask 机会输入 | 有条件可用 | 只把完整 standard neg-risk 组的同轮新鲜 ask 作为 M2 评估输入 | complete published snapshot membership + 只读 CLOB + L1 SQLite；生产入口 `/arbitrage/opportunities` | `make diagnose-arb-feed-prod min_edge_bps=0`; 必须看到 `coverage=verified-standard-neg-risk` | 可供 M2 只读评估；需持续运行证据，augmented 只拒绝不出候选；gross-before-fees | 把旧 1,278/1,278 容量证据、HTTP 503 或 augmented 子集当合法套利输入 |
| 自动真实资金执行 | 尚不可用 | 交易所下单、成交和风控 | 尚无 live adapter | 无 | 认证、allowance、限额、kill switch 未完成 | 任何真实资金运行 |

## 3. 五分钟日常巡检

### 自动报警与本机临时检查

生产主监控部署在 Fly `polyarb-l1` 的独立 `cron` 机器上，每 2 分钟检查
L1、机会 feed、L2/L3 和 Dashboard。故障通知进入 Telegram 与
[@polyarb_l1_bot](https://t.me/polyarb_l1_bot) 的私人聊天：首次或变化后的故障
立即通知，同一故障持续 30 分钟提醒一次，恢复后通知一次；全绿时不发送例行消息。
GitHub Actions 是异地兜底，不作为准点时钟。

四个监控面（L1、机会 feed、L2/L3、Dashboard）各自拥有独立的故障与恢复状态：
同一轮可以合并发送多个新故障，但 L1 恢复不会再等待仍故障的 L2 一起恢复。Telegram
投递失败时，只保留投递失败的那个组件，下一轮继续补发，不会把其他组件重新标为故障。

每次新的 Structure 发布后，旧 Quote 会被立即拒绝，匹配的新 Quote 会在同一个 300 秒 SLA
窗口内重算。这个窗口中 health 会标记“正在追上”，Polywatch 不报警；机会接口可短暂返回
503，含义是“不要使用旧报价”，绝不等同于零机会。超过 300 秒、采集失败或 collector 停止
才是必须处理的故障与 Telegram 告警。

在本机临时检查，直接复制：

```bash
make polywatch-healthz-dry
make polywatch-resident-status
```

若你在本机运行过 L1 daemon，想看该 SQLite 中**最新一次快照尝试**的原始结果，运行：

```bash
make snapshot-attempt-status
```

它只读本地数据库，输出 `latest` JSON；`failed` 的 `failure_kind` 可直接区分可能 OOM、
子进程失败或取消。它不连接 Fly、不发送 Telegram、不创建表，也不能替代生产 strict
`/health`。生产问题仍先跑 `make smoke-health-prod`，再用 Fly/Telegram 证据定位。

- `make polywatch-healthz-dry`：立即用正式监控的同一套规则读取四个生产面，但
  **不会发送 Telegram**、不会 unpause 或修改生产。`noop` 表示该面正常；
  `push` 表示正式巡检会报警；`unpause+push` 表示快照调度故障会触发窄化恢复和报警。
- `make polywatch-resident-status`：读取 Fly 上最近的自动巡检和状态文件，需本机
  `flyctl` 已登录。`active_keys` 为 `[]` 表示当前没有未恢复故障；
  `last_seen_at_s` 应约每 2 分钟推进。

不要用 `make polywatch-healthz` 做普通临时检查：它是正式模式，可能真实发送
Telegram，并在 L1 snapshot stale 时执行受控 unpause。

当前 release 75 的修复后 L3 连续性窗口还多一层**本机证据定时器**。它不是故障
报警器，而是每 5 分钟判断 T+6/T+12/T+18/T+24 是否到点；到点后调用既有
`make l3-soak-checkpoint`，只提交 PASS 报告。查看它：

```bash
launchctl print "gui/$(id -u)/com.polyarb.l3-soak-autocheckpoint" \
  | grep -E "state =|runs =|last exit code|run interval"
tail -50 "$HOME/Library/Logs/polyarb-l3-soak-autocheckpoint.log"
```

正常的未到点输出形如
`idle next=T+6@2026-07-26T14:51:13.206077Z`；`last exit code = 0`
表示最近一次预检成功。Mac 睡眠期间不会丢市场证据：生产数据库仍每 30 秒记账，
唤醒后定时器补生成累计报告；但如果 Mac 一直关机，报告会延后生成。实时故障仍看
Telegram/Fly Polywatch，不要把这个本机定时器当告警主链。

1. 先读 [`.planning/CURRENT.md`](../.planning/CURRENT.md)，确认当前已关闭和未关闭的质量门。CURRENT 是仓库当前结论，不是生产实例的实时心跳。
2. 跑 `make smoke-health-prod`，再跑 `make smoke-market-truth-prod`。两者都只读 L1 strict `/health`，不调用 flyctl。除整体 `pass` 外，还必须看 `market_truth:coverage=complete/pass`；它回答最新一次 snapshot 尝试是否完整发布。再看 `snapshot:latest_attempt` 和 `snapshot:failure_counter`：前者专门报告调度器最近一次尝试（即使旧完整快照仍新鲜，也会暴露新的 OOM、子进程失败或取消）；`running` 超过 240 秒会直接报错，并由调度器终止该子进程，避免卡死占住后续 5 分钟周期；重启时会先从 SQLite 的完整认证 Quote 重建紧凑机会 feed，再采集新 Quote；若重启中断 Quote 子进程，已确认退出后会把唯一 `collecting` run 标成 `collector-cancelled`，不留下阻塞新实例的租约；Structure 在 Quote 采集途中换代时，旧 Quote 会被拒绝并立即重试，不必等待完整 120 秒周期；后者是连续失败计数，`0` 为正常、`1–4` 为预警、`5+` 为失败/暂停。`never-started` 仅表示实例尚未写入新尝试记录，兼容已有旧快照，不能当作一次成功。`market_truth:last_complete_age_seconds` 只回答最近一次完整发布有多旧，不能替较新的失败尝试开绿灯。
3. 跑 `make smoke-l2-health-prod` 先确认 L2 Fly 路由可达，再跑 `make smoke-l2-health-strict-prod` 读 strict `/health`。前者只检查 always-200 `/healthz`；后者打印 HTTP 与 body，非 200 退出非零。不得用 `/healthz` 代替 strict 结论。
4. 在 L2 strict body 中联合看 `event_bus:cursor_lag`、`ws:last_event_age_seconds`、`mirror:l2_tob_age_seconds` 和 `l3:active_count`。冷启动的 `null`、`WAITING_FOR_EVENT` 或 L3 低于 10 都是“证据不足”，不是已验证的稳定性。
5. 若要检查本地 quote-run 合同，先确认已有最新 snapshot，再跑 `make collect-neg-risk-quotes db=data/state.db`；它对 CLOB 只读、但会写本地 SQLite，既不是生产 cron，也没有部署或下单。随后跑 `make scan-arb-quotes db=data/state.db`。只有 quote 与 universe 两个时钟都在 SLA 内，它才输出完整 run 的 gross-before-fees 候选；缺 run 或任一 stale 均非零退出。该 known-universe 覆盖不含下一次 membership snapshot 前新挂出的组。
6. 最后跑 `make diagnose-arb-feed-prod`（实战加 `min_edge_bps=0`），并在 L1 strict `/health` 联合查看 `market_truth:*`、`quote_feed:last_complete_age_seconds` 与 `quote_feed:collector_state`。诊断输出只有在 success payload 明确包含 `coverage=verified-standard-neg-risk`、合法 snapshot/universe/run 身份和逐候选 provenance 时才 exit=0。`available-zero` 才是合法零机会；HTTP 503 或 invalid response 不是零机会。旧的 1,278/1,278 报价容量证据建立在被截断的 market universe 上，已撤销其 M2 readiness 含义；即使历史采集跨周期稳定，仍不代表机会 feed 已准备就绪。

<!-- m1-contract: health=snapshot:last_success_age_seconds file=src/polyarb/http/health.py -->
<!-- m1-contract: health=snapshot:latest_attempt file=src/polyarb/http/health.py -->
<!-- m1-contract: health=snapshot:failure_counter file=src/polyarb/http/health.py -->
<!-- m1-contract: health=market_truth:coverage file=src/polyarb/http/health.py -->
<!-- m1-contract: health=market_truth:last_complete_age_seconds file=src/polyarb/http/health.py -->
<!-- m1-contract: health=event_bus:cursor_lag file=src/polyarb/http/l2_health.py -->
<!-- m1-contract: health=ws:last_event_age_seconds file=src/polyarb/http/l2_health.py -->
<!-- m1-contract: health=mirror:l2_tob_age_seconds file=src/polyarb/http/l2_health.py -->
<!-- m1-contract: health=l3:active_count file=src/polyarb/http/l2_health.py -->

## 4. 三种使用方式：生产巡检、本地验证、Dashboard

### 生产巡检（只读）

- `make smoke-health-prod`：纯 GET L1 strict `/health`，失败时不要继续将 L1 当新鲜输入。
- `make smoke-market-truth-prod`：在同一只读 health body 中强制验证最新 snapshot 尝试已完整发布，并打印 release、覆盖计数和最近完整发布年龄。
- `make smoke-l2-health-prod` 只证明 L2 `/healthz` 可达；`make smoke-l2-health-strict-prod` 才以只读 GET 给出 strict `/health` 的 HTTP/body 和业务健康结论。
- `make diagnose-arb-feed-prod min_edge_bps=0`：一次只读 GET 后分类 M1→M2 机会 feed。成功还会验证 `verified-standard-neg-risk`、source snapshot、universe hash、quote run 与逐候选身份；任何缺失都是 `invalid-response`，不能把 feed 升格为 ready。

### L1→L2 市场候选链（只读观察）

**候选不是独立 staging 部署**；在本项目中，它指 L1 根据最新市场快照筛出、写入 Supabase，再由 durable cursor + NOTIFY/poll 驱动 L2 订阅的市场集合。

前置条件：L1 `make smoke-health-prod` 不是 fail；L2 `make smoke-l2-health-prod` 证明 `/healthz` 可达；操作者有读取生产 health 和 Supabase read model 的权限；要跑 L3 dry-run 时，本地 `.env` 需有现有 Supabase 读取配置。

1. 跑 `make smoke-health-prod` 确认 L1 快照链的 strict 状态，再跑 `make smoke-l2-health-prod` 确认 L2 路由可达；后者不是 strict 通过证明。
2. 跑 `make smoke-l2-health-strict-prod` 读 L2 strict `/health`，确认 `event_bus:cursor_lag=0`、`event_bus:last_reconciliation_age_seconds` 在阈值内，以及 `candidates:supabase_fetch_age_seconds` 为 pass。这三项分别证明账本追平、轮询仍在工作、候选源数据取得成功。
3. 查看 `ws:subscribed_count` 与当前 candidate 数；要观察哪些市场符合 L3 推广条件时，跑 `make l3-promote-dry-run`。该 target 可读生产 Supabase 并计算 `proposed_active`，但显式禁用 WS add/remove、`l3_promoted_at_ts`、LKG、module active state 与 freshness anchor 的全部 mutation；输出是提案，不是“运行后 active state”。

停止条件：L1 或 L2 strict health 为 fail；cursor lag 超过 grace 后仍不归零；reconciliation 或 candidate fetch stale/fail；候选数与 WS 订阅持续不收敛。命中任一条都停止将候选链当作新鲜输入，先按第 7–8 节诊断。

### 本地验证

- `make sync-structure-local` 会从 Gamma 写入一版完整 Structure；这是本地 mutation，但**不**请求 CLOB、不写 Parquet/R2，也不产生可交易价格。它是本地验证 M1→M2 市场成员关系的最短路径。
- `make archive-markets-local` 才会做完整 CLOB/Parquet 历史归档；这是明确的研究/审计 mutation，可能耗时和占用磁盘。无论成功或失败，都不会替换当前 Structure 的 `markets` 视图。
- 旧的 `make snapshot-markets-v` 保留给历史兼容工具；它产出的 `legacy_combined` 不能作为切换后的 M2 输入证据。
- `make overview` 读最近一次本地 SQLite；零行时先检查本地快照而不是猜测生产异常。
- `make scan-l3-seed` 只读当前本地 snapshot，按 non-null Yes token、mid 0.1–0.9、liquidity ≥500、liquidity 降序/market ID 升序列出最多 100 个代表性 L3 seed；它不等于 promotion，也不改变生产候选或订阅。
- `make daemon-run-local` 在本地启动 L1，`make daemon-l2-run-local` 在本地启动 L2；它们都会持续运行并可写本地状态，Ctrl-C 停止。

### 本地 known-universe quote 首次路径

```bash
# CLOB 只读；仅写本地 data/state.db，不是 cron 或生产任务
make collect-neg-risk-quotes db=data/state.db

# 仅扫描同一 complete run；任一时钟 stale/unavailable 都非零退出
make scan-arb-quotes db=data/state.db min_edge_bps=0 \
  max_quote_age_s=300 max_universe_age_s=50400
```

`collect-neg-risk-quotes` 的 universe 必须来自最新 complete、published snapshot，
并且只收入 exact event membership 已匹配的 standard neg-risk 组。故新上市的
neg-risk 组要等下一次 snapshot 才会进入下一轮收集；它不会补读或混用别的 run。`scan-arb-quotes`
只报告 gross-before-fees，不是 order instruction。HTTP 入口若返回 503，表示 run、quote 或
universe 不可用/过期，绝不是“0 条机会”。2026-07-26 的 1,278/1,278 报价运行
只证明对当时本地子集的采集能力；后来证实旧 offset 分页在第 2,100 行 HTTP 422
后把截断误认成结束，因此这组历史证据不再证明 M2 可用性。

旧链路的问题不是“少几行无所谓”：Michigan 事件 `111080` 在截断视图中只剩两条腿，
实际事件结构有 33 个成员且属于 augmented neg-risk。新链路使用 markets/events
keyset 的终止 cursor 证明全集，再以 event 内嵌成员生成 exact membership hash；
augmented 组保留在拒绝统计中，原因
`augmented-neg-risk-not-supported`，绝不进入 standard buy-all 候选。

生产 feed 的每个候选必须同时带：

- `event_id` 与 `membership_hash`：证明属于哪个完整事件结构；
- `source_snapshot_id` 与 `universe_hash`：证明来自哪次完整发布和哪套腿；
- `quote_run_id`：证明各腿报价属于同一原子 run。

缺少任一身份，或顶层 coverage 不是 `verified-standard-neg-risk`，诊断命令都拒绝。

### Opportunity-first 按组盯盘（Slice B，尚未生产启用）

仓库现已具备按一个完整 neg-risk 组采集 top books 的 Candidate Watcher。它在请求前后
读取同一组的 `membership_hash`，只有成员未变化、全部有序 Yes token 都返回可执行
ask/size 时，才发布一个原子 `quote_batch_id`。成员变化或缺腿只影响该组，不再要求
一次十分钟级的全市场采集成为在线机会的统一闸门。

该能力由 `POLYARB_OPPORTUNITY_FIRST_WATCHER_ENABLED` 控制，默认是 `false`。
当前没有新增 operator 命令，也不得仅因代码存在就把它打开或描述为已经生产切换。
后续还需要滚动 Discovery、checkpoint Reconciliation、incident/资源隔离、公开 API、
Dashboard 和故障资格门。旧 `/arbitrage/opportunities` 与 known-universe Quote 路径
在这些门完成前保持不变。

Candidate 的初始控制输入是
`POLYARB_CANDIDATE_HIGH_INTERVAL_S`、`POLYARB_CANDIDATE_NORMAL_INTERVAL_S`、
`POLYARB_CANDIDATE_EXPLORE_INTERVAL_S` 和
`POLYARB_CANDIDATE_QUOTE_HARD_STALE_S`。每轮工作上限、lower-lane 保留槽、
per-group timeout 和 supervisor retry 分别由
`POLYARB_CANDIDATE_CYCLE_MAX_GROUPS`、
`POLYARB_CANDIDATE_RESERVED_NON_HIGH_SLOTS`、
`POLYARB_CANDIDATE_GROUP_TIMEOUT_S`、
`POLYARB_CANDIDATE_HIGH_BURST_GROUPS` 和
`POLYARB_CANDIDATE_LOWER_LANE_MAX_WAIT_S`，以及
`POLYARB_CANDIDATE_SUPERVISOR_RETRY_S` 控制；scheduler 空转轮询由
`POLYARB_CANDIDATE_SCHEDULER_POLL_S` 控制。所有时间值必须是正有限数，high interval
不能大于 hard-stale。默认每轮 12 组中保留 3 槽，且始终要求至少
`ceil(cycle_max_groups * 20%)` 给 normal/explore。顺序确定为“首段 high（默认 1 组）
→ 保留 lower lanes → 其余已选工作”，所以 hot 至少先服务一组，排队 high 不能先耗光
lower 的时间预算。`high_burst * group_timeout` 必须严格小于 lower 最大等待；该等待可
调严但不能高于生产验收的 120 秒。high burst 还不能超过 high CLOB worker 数。
normal/explore 基准周期可以高于 high hard-stale，它们不代表 high freshness SLA。

同步 CLOB SDK 的资源隔离由 `POLYARB_CANDIDATE_HIGH_CLOB_WORKERS`（默认 2）和
`POLYARB_CANDIDATE_LOWER_CLOB_WORKERS`（默认 1）控制。high 与 normal/explore 使用
不同的有限线程池，不占用 SQLite/Structure 的默认 executor。per-group timeout 会让
scheduler 继续，但不能强杀已经运行的同步 SDK 调用；关闭时只取消未开始的 future，
运行中调用仍受 CLOB HTTP timeout 约束。

有效间隔、原因和下一次到期时间会随每次终态写入 SQLite；它们是后续 API/Dashboard
的事实源，不是仅存在内存里的日志。source/cycle supervisor 与 per-group degraded
状态分开统计：单组只有在同组下一次成功时才记 recovery，其他组成功不会清除它。

### Bounded Discovery（Slice C，尚未生产启用）

滚动 Discovery 每次只读取一个最多 100 events 的 Gamma keyset 页面。它复用现有
event/market normalizer；只有完整 standard neg-risk membership 同时具备每腿
market ID、condition ID、Yes token 和标题时，才在同一事务发布 certified revision、
priority schedule、promotion、coverage sample 和 opaque next cursor。身份不完整或
unsupported 的组会留下有界拒绝原因但不 promotion；传输、normalization 或写入失败时
整个页面回滚，cursor 不前进。

`POLYARB_OPPORTUNITY_DISCOVERY_ENABLED` 默认 `false`；页面大小和周期分别由
`POLYARB_DISCOVERY_PAGE_LIMIT`（1-100）与 `POLYARB_DISCOVERY_INTERVAL_S` 控制。
首次 Candidate 启动承诺由 `POLYARB_DISCOVERY_CANDIDATE_MAX_WAIT_S` 控制，默认且最大
均为 60 秒。complete-supported 组先认证并进入 durable queue；只有满足
`poll + bounded selection + capacity × attempt-start SQLite write + (high burst + prior
admitted slots) × (group timeout + terminal-write busy budget) ≤ 60s` 的 factless
集合才 admission/promotion。所有预算按毫秒向上取整；默认 effective capacity 为 1、
effective start bound 为 47 秒。超量组保持 unpromoted，
按 queue deadline、score、group ID 等待，前一组写入终态事实时同事务 admit 下一组。
attempt-start 不是 scheduler 的前置推测：它是 watcher 入口第一条取消安全事务，
绑定 event/membership/promotion/deadline；若已超时则同事务写 unavailable、释放槽位并
跳过 Structure 与 books。
每次真实 admission 会同步追加 audit（身份、promotion/deadline、完整 capacity proof）；
状态页只接受能 exact join audit 且 revision 在 promotion 前已认证的 attempt。
首次 incomplete/unsupported sample 合法地没有 revision，状态校验不会误报；只有
complete-supported sample 必须提供 batch 时点前的精确 revision。
所有 sample 同时写 append-only schedule evidence；状态按 batch/time/identity/
quality/reason/promotion exact join，因此历史拒绝样本即使字段表面合法也不能改成 ghost。
promotion 使用 normal/explore 保留容量且排在 genuine high burst 之后，不会被重标为
global high。degraded 探针周期由
`POLYARB_DISCOVERY_DEGRADED_PROBE_EVERY_CYCLES` 控制：N-1 轮 yield 后最多放行一个
有界页面，phase/streak/modulus 持久化，重启不能重置。即使打开，也只会
对“独立 bootstrap authority + 已有 durable Candidate fact 的 current certified
authority + admitted promotion”
的 matching complete Quote 做一次 durable p95/missing snapshot；尚未 watch/admission
的认证排队组不制造 missing-Quote degradation，unavailable fact 不刷新 Quote age。
缺 Quote 或 p95 接近 hard-stale 时先 yield。Discovery
promotion 与 legacy active masters 组成去重并集，接入不会丢掉旧 hot candidates；
首次 Candidate 终态前按持久化 Decimal score 排序，之后由 durable due time 接管。

本地只读巡检：

```bash
make perception-discovery-status db_path=/path/to/state.db
```

输出完整 cursor/sweep receipt chain、最近 batch、promotion queue、effective
admission capacity/60 秒启动上界、attempt-start receipt/deadline breach readiness、
degraded streak/modulus/decision、最老 visit，
以及 15/30/60 分钟 raw 和 liquidity-weighted active-known coverage。低覆盖是有效
业务事实，命令仍返回 0；
数据库不可读、schema 无效或 cursor/promotion/Decimal/coverage 语义链损坏才非零。
所有字段来自一个 SQLite read transaction，不混合并发提交前后的事实。该覆盖是滚动
统计，不是“任何新套利组都不漏”。
通用 `init_schema()` 只做 additive/idempotent schema 与 receipt sequence migration，
不会在 active Settings 生效前擅自设 capacity 或 demote。只有启用的 Discovery/
Candidate wiring 调用显式 admission configure transaction，才按真实配置持久化 proof
并确定性 reconcile legacy factless promotions。
### Checkpointed Full Reconciliation（Slice D，尚未生产启用）

Full Reconciliation 是低优先级校准作业，不是在线发布门。每次 batch 最多读取一个
Gamma event 页面，并在一个事务写 staging groups、batch receipt、window counts 和
opaque next cursor。只有 upstream terminal 空页才把 window 标为 `complete`；随后在
一个 `BEGIN IMMEDIATE` 事务发布 added/changed/closed/unchanged/rejected diff。
`open` 窗口永不修改在线 group revision。进程在 terminal receipt 后死亡会留下可恢复的
`complete`，重启先幂等 apply，不重新从第一页抓取。
窗口开始事务会保存 exact current-certified baseline；change/closure 都要求 apply 时
current 仍精确匹配该 baseline，不依赖可能同毫秒或回拨的 observed timestamp。
baseline 另有带 domain/version/count 的 canonical digest；等行数替换也会 fail
closed，旧无 digest 窗口新建重扫而不猜测回填。terminal receipt 还要求
event/candidate/sample 与 observed/unique/update/duplicate/rejected 全部为零。跨页
重复 group 写 append-only samples：相同事实 de-dup，变化 latest-wins；receipt 保留
observed/unique/update/duplicate/rejected 计数。cursor 指回当前或历史位置时窗口持久化
为 `failed/cursor-loop` 且绝不 apply，下一轮从新窗口第一页恢复，不会永久卡在同一
checkpoint。

```bash
# 本地 mutation：推进一个 bounded page
make reconcile-market-map

# 只读 checkpoint/window/counts
make reconciliation-status db_path=/path/to/state.db
```

`POLYARB_OPPORTUNITY_RECONCILIATION_ENABLED` 默认 `false`。旧 universe-sized
Structure loop 由独立的
`POLYARB_LEGACY_STRUCTURE_RECONCILIATION_ENABLED=false` 控制；其历史尝试和自适应
时长证据保留，但默认不再运行。health 的
`perception:reconciliation_progress` 与
`perception:reconciliation_checkpoint_age_seconds` 直接读取 checkpoint 和最后一条
receipt；它们不改变 Candidate/整体 availability。并发 Discovery 发布了更晚 revision
时，reconciliation 计 unchanged 而不覆盖；closure 只作用于 baseline 中、完整窗口
未观察且 current 仍 exact-match 的 certified group。incomplete/unsupported identity
记 rejected；只有 group/event 能绑定 baseline 才能保守抑制 closure，attacker event
不能用相同 group ID 掩盖真实缺失。apply 同事务写逐组
added/changed/closed/unchanged/rejected evidence，绑定 baseline、最终 staging 和实际
result revision；validator 从 evidence 重算五项计数。applied rejected 表示最终
rejected group 数，不是跨页历史 rejected observation 总数。

### Opportunity-first 异常与进程隔离

Slice E 仍默认关闭。开启
`POLYARB_OPPORTUNITY_PRODUCER_SUPERVISOR_ENABLED=true` 后，Candidate、
Discovery、Reconciliation 使用三个独立 child process；HTTP 只读持久化结果。
严格健康页新增 `perception:open_incidents` 和
`perception:resource_mode`。恢复必须由 recovery 开始后的真实 writer mutation
证明；child exit code 0、等待一段时间或任意 JSON key 都不能关闭 incident。

```bash
make run-perception-worker component=candidate
```

这是单 child 调试入口，仍受 `.env` 双层 default-off flag 约束。生产由 supervisor
启动。Slice F 的公开 API/Dashboard 与生产资格门仍未完成，因此这里仍是
default-off 本地/代码资格证据，不是上线声明。

**本地数据不代表生产状态**。同理，分支上的验收通过只是部署前证据，未部署之前不能当成生产事实；这和上述“市场候选集”是两个概念。

### Dashboard

`make dashboard-dev` 启动本地 Next.js Dashboard。生产 canonical URL 是 `https://polymarket-arbitrage-jiangwen-su-s-projects.vercel.app`；旧短域名不再是入口。`make smoke-l2-dashboard` **只覆盖 candidates、asset TOB/trades 和 signals** 的生产路由可达性，Vercel Auth 页返回 200 也会被计为可达；它不检查 `/status` 或 L3。L3 本地 smoke 默认 localhost；检查生产必须显式跑 `make smoke-l3-dashboard asset_id=<known-id> URL=https://polymarket-arbitrage-jiangwen-su-s-projects.vercel.app`。该 curl 不携带 Vercel Auth 会话，因此 HTTP 200 可能只是登录页；必须再用已认证浏览器查看 `/l3/<known-id>` 内容和 `/status`。页面能打开只证明路由/认证链存活；数据新鲜度仍由 strict `/health` 和行时间戳判定。

<!-- m1-contract: route=/status file=dashboard/app/status/page.tsx -->
<!-- m1-contract: route=/candidates file=dashboard/app/candidates/page.tsx -->
<!-- m1-contract: route=/signals file=dashboard/app/signals/page.tsx -->
<!-- m1-contract: route=/l3/[asset_id] file=dashboard/app/l3/[asset_id]/page.tsx -->

## 5. L1/L2/L3 数据与新鲜度契约

L1 是广覆盖、低频的市场事实切片。`snapshot:last_success_age_seconds` 表示最后一次
成功发布快照的年龄；“请求成功”或“有行”都不等于市场全集。它与
`snapshot:latest_attempt` 必须一起看：前者说明市场事实多久未更新，后者说明最新一次
调度尝试的结果；因此新 OOM 不会被旧的成功快照掩盖。`snapshot:failure_counter` 显示
连续失败趋势，达到 5 时调度器进入暂停保护。`market_truth:coverage`
读取最新尝试，分页失败、membership 不完整或未发布都会 fail；即便
`market_truth:last_complete_age_seconds` 仍能指出一个较旧的完整版本，最新失败也不会
被它掩盖。`snapshot:schedule` 显示 Structure 当前真正使用的 timeout/cadence、
成功样本数、p95 和调整原因；health 对 running attempt 的超时判断读取同一份持久化
effective timeout，而不是静态默认值。Supabase 镜像和 R2 归档是独立故障域。

只有标记为 **Structure** 的完整发布版本才会被 strict health、报价运行和 M2 输入认可。
它只读取 Gamma 并保留最终 event/member reconciliation；CLOB 价格必须由独立 Quote run 提供。
此前的 `legacy_combined` 快照仍可用于历史研究，但不能证明当前市场结构。`archive` 是显式的
Parquet/R2 研究产品；它失败时 health 会显示 `archive:*` warning，但不会阻断 Structure、Quote
或 M2。生产尚未部署这次切换，所以生产状态仍以 CURRENT 和生产 health 为准，不能把本地
Structure 测试说成已迁移的生产版本。

L1→L2 有两个互补机制：**NOTIFY 是门铃**，只告诉 L2 “可能有新快照，来看一眼”；**durable cursor 是账本**，记录已处理到哪个 snapshot。门铃丢了可以靠 poll/reconciliation 补读，但 cursor lag 持续增长意味着账本没跟上，不能因为 listener 还连着就忽略。

L2 还要分开两种“活着”：WS transport liveness 是 TCP/PING/PONG 和连接状态；business frame freshness 是最近一个真实市场业务帧的年龄。前者可以在后者冻结时仍显得正常，因此必须同看 `ws:connection_state` 和 `ws:last_event_age_seconds`。`mirror:l2_tob_age_seconds` 再证明业务帧确实写到了镜像，而不是只被接收。

L3 是覆盖最小、频率最高的深度证据层。`l3:active_count` 的严格目标是 5 个市场×2 tokens = 10；`0/10` 是 under-filled/证据不足，不能用 OHLC 页面外观代替 active set 和 book-level freshness 的闭环。更深设计见 [10-L2 跟踪](learning/10-L2-跟踪.md) 和 [11-L3 K 线](learning/11-L3-K线.md)。

### L3 24h R08 精确门

R08 不是“五个检查点都看起来绿”，而是对同一个 manifest 和同一个 boot 的完整区间判定：

- `sample interval = 30s`，实际相邻/边界 `sample gap ≤ 75s`；
- `promoter interval = 300s`，每次 `promoter start gap ≤ 360s`，且 tick 连续、结果均为 success；
- 每市场 `book / OHLC age < 120s`；
- 固定 `5 markets / 10 tokens`，每个过程样本必须保持 `10/10/10 desired/committed/evidenced`；
- 精确窗口覆盖 `all 10 book tokens` 和 `all 5 Yes OHLC`；
- 同一 boot、release、machine/version、image digest、mapping 和 `AcceptanceConfig`；`schema revision 007`、recipe/code identity 不得漂移；
- 默认拒绝 `watchdog_stale`、`reconnect_failed`、`subscription_control_failed`、`evidence_writer_failed`，判定只看 event kind，不受可变 severity 影响；
- 最终窗口必须恰好 24 小时，五张证据表的 `recorded_at` 和原始行 hash 都要能重查；`retention ≥ 30 days`。

L2 release 37 只证明旧实现曾达到 10/10 和真实深度写入。由于当时的六小时人工检查无法覆盖中间的 promoter、membership、writer 和五市场 freshness，release 37 已明确降级为 `diagnostic-only`，不能绑定新 manifest，也不能作为 R08 strict PASS。

当前 production 已是 Alembic 007，runtime/retention 使用两个隔离角色。最终
release 72 / boot `9eeab4d5…` 的 A7 精确窗口已经完成 T0/T6/T12/T18/T24：
2,880 个 health row、14,400 个 market row、288/288 promoter tick 均通过，
final verifier 重新查询原始行并复现相同 hash。

随后 release 73 在真实 promoter 边界暴露了重连期间的部分证据采样问题，因此不能
用旧 A7 替代当前代码的连续性证明。修复后 release 75 / boot `d029c2ea…` 已通过
真实 promoter、quiet refresh 和新窗口 T0；其 T+6/T+12/T+18/T+24 由上述
`launchd` 定时器自动收集。最终边界是
`2026-07-27T08:51:13.206077Z`，此前不得宣称修复后的 24 小时窗口完成。

## 6. 如何解读结果与实战边界

一条 neg-risk 机会记录只是当时可观察的 gross-before-fees 输入。从“有 edge”到“可部署策略”至少还有六个缺口：

- **fee**：交易费、gas/转账和其他成本尚未从 gross edge 中完整扣除。
- **slippage**：显示的 best ask 不代表整个仓位都能以该价成交。
- **fill**：多腿不保证同时成交，部分成交会留下方向性暴露。
- **oracle**：结算规则、数据源和争议可以使“数学闭环”不等于“可兑付闭环”。
- **risk/capital approval**：认证、allowance、单市场/单日限额、kill switch 和人工资金批准都尚未成为 live gate。
- **sustainable return**：一次或少量命中不能证明机会频率、容量、存活时间和回撤后的可持续收益。

因此，本手册中的 M1 输出、健康绿灯、Dashboard 结果和 paper 证据均**不构成真实资金下单授权**。详细验收步骤链接到 [端到端验收指南](E2E_ACCEPTANCE_GUIDE.md)，不在这里复制一份会漂移的流程。

## 7. 故障排查地图

| 现象 | 先看 | 解读与下一步 |
|---|---|---|
| 本地 `overview` 零行 | `make snapshot-status` | 确认本地 DB/快照路径与时间；需要新数据时才跑 `make snapshot-markets-v`，不推断生产为空。 |
| L1 `warn` | 每个 `checks` 子项 | 识别是快照年龄、Supabase 镜像还是 R2；R2 warning 可以只表示归档降级。 |
| L1 `fail`/HTTP 503 | `snapshot:last_success_age_seconds` 与 scheduler | 停止依赖该 feed，先做只读诊断，再进入受控恢复。 |
| snapshot 期间 `/health` 超时、机器重启或日志出现 OOM | Fly machine instance/event log、snapshot phase 与最近 CLOB chunk | 这不是“市场全集还没完成”的正常 503；正常采集期间 HTTP 必须持续可响应。保存 OOM RSS、phase、chunk 和 instance，停止依赖 feed，并按内存峰值缺陷修复。完整全集采集只保留每个 book 的 top ask/bid；全局 CLOB 失败只留一条有界错误，不能展开成逐 token 缺失。修复后必须重测采集期间和完成后的 `/health` 响应延迟。 |
| `market_truth:coverage` fail | `make smoke-market-truth-prod`，联合看最新尝试计数与 `last_complete_age_seconds` | 表示最新尝试没有证明 markets/events 全集或未获准发布；旧完整快照只能辅助定位，不能继续供应 M2。Polywatch 即使 scheduler/worker 绿色也会用稳定原因报警。 |
| `archive:last_attempt` warn | 看 `archive:last_success_age_seconds` 和本地 `make archive-markets-local` 日志 | Archive 是研究/审计产品：`never-run`、CLOB/R2 失败或长期未归档都要记录，但不应让 Structure/Quote/M2 停止。若需要历史文件，再单独修 Archive，不要重启线上 Structure。 |
| `quote_feed:last_complete_age_seconds` warn/fail | 报价年龄与 `quote_feed:collector_state` | 240–300 秒表示已经错过正常 120 秒节拍但旧 run 尚可读；超过 300 秒或无 complete run 时机会入口应 503，不能回退到旧 snapshot ask。 |
| `snapshot:latest_attempt=running` 且 `snapshot-subprocess-timeout-exceeded` | Structure 子进程超过 `snapshot:schedule.effective_timeout_s` 仍未返回 | 这是实际故障；调度器会终止子进程并持久化失败。先保存 `snapshot:schedule` 的样本数、p95、effective timeout/cadence 与 reason；下一轮会使用持久化新值，最新 timeout 会立即有界退避，普通调整有三次尝试冷却。若连续失败计数升至 5，仍按暂停故障处理，控制器不会自动 unpause。 |
| `source-snapshot-refreshing` | 新 Structure 在 Quote 采集途中发布 | 旧 Quote 被安全拒绝，worker 立即以新 Structure 重试；这是一段有界刷新窗口，M2 仍不可读旧 Quote，若没有很快回到 matching complete run 再按真实 Quote 故障排查。 |
| L2 `WAITING_FOR_EVENT` | `ws:last_event_age_seconds` 和订阅数 | 冷启动/安静窗口下可为 warn；没有业务帧证据时不宣称 WS 数据面通过。 |
| cursor lag | `event_bus:cursor_lag` 和 reconciliation age | 宽限内 warn 可观察；持续超过阈值的 fail 表示 durable ledger 没跟上。 |
| stale mirror | `mirror:l2_tob_age_seconds` | 区分从未写入、配置缺 key 和超阈值；连接正常不能覆盖 mirror fail。 |
| L3 `<10/10` | `l3:active_count` 和 promote/book-level ages | 当作 under-filled 证据不足；不用空 K 线推断市场没活动。 |
| `l3:membership_convergence` fail | `make smoke-l2-health-strict-prod` 后跑 `make l3-evidence-status` | 表示 mapping 身份、WS desired/committed 或 durable evidence 不一致，不是单纯市场安静；保存 release/generation/count 后再定位。真实 reconnect 收 initial dump 时 live health 会立即 fail，但 durable sampler 会等 exact 10/10/10；若迟迟不收敛，`l3:evidence_sample_age_seconds` 会继续失败，不能把等待当成无监控。 |
| `l3:worst_market_freshness` fail | strict body 的 5 个 market row 与 `make fly-l2-logs` | 至少一个持久化 market 输入超过 120 秒；其他市场或全局 WS 有新帧不能覆盖该失败。先查写入链，不盲目 restart。 |
| `diagnose-arb-feed-prod` → `available-zero` | exit=0、`count=0`、payload 可解析 | 这才是“当前无满足条件的 gross edge”；不要放宽风控去制造信号。 |
| `diagnose-arb-feed-prod` → `available-opportunities` | exit=0、`count>0`、payload 可解析 | 只表示观察到 gross-before-fees 输入；仍需费用、滑点、成交、oracle 和资金门控。 |
| `diagnose-arb-feed-prod` → `stale-snapshot` | HTTP 503 body 的快照年龄 | 上游快照超过 SLA；停止依赖该 feed，保存证据并排查 producer cadence。 |
| `diagnose-arb-feed-prod` → `feed-unavailable` | HTTP 503 非快照错误或其他非 200 | endpoint 不可用，不是 0 条；保留 body 后按 H-008 只读 chain-truth 诊断。 |
| `diagnose-arb-feed-prod` → `invalid-response` | HTTP 200 但 JSON/schema 不合法 | 不把意外 200 降格成零机会；保存 body，排查 endpoint 合同。 |
| `diagnose-arb-feed-prod` → `transport-error` | curl 本身失败、固定 JSON、exit=2 | 网络/DNS/TLS 等传输失败；没有 HTTP 语义，不能推断机会数量。 |
| `scan-arb-quotes` 非零退出 | `quote run unavailable`、quote age 或 universe age | 不回退到 snapshot best ask，也不输出零机会；先检查本地 snapshot、complete run 和两个时钟。 |
| Dashboard 显示登录页/401 | Vercel Auth 会话与白名单 | Polywatch 接受 canonical URL 的 302/307 SSO 外门，只证明 deployment 存在；登录后仍要检查应用内容和数据时间戳。 |
| Polywatch opportunity 报警 | L1 `market_truth:*`、`quote_feed:*` 和 `/arbitrage/opportunities?min_edge_bps=0` | market truth 不完整、endpoint 不可达或 verified coverage/response 合同错误会报警；只有完整 verified payload 的空列表才不会报警。 |
| Polywatch L3 报警 | `l3:active_count` 与四个 strict evidence 子检查 | 必须 10 tokens 且 evidence、ledger、membership、worst freshness 全 pass；单独 `WAITING_FOR_EVENT` 且数据新鲜不会报警。 |
| Polywatch Dashboard 报警 | canonical URL 首个响应 | 200 或 Vercel SSO 302/307 健康；`DEPLOYMENT_NOT_FOUND`、5xx 或传输失败报警。 |
| Polywatch 没有按 GitHub `*/15` 准点 | `make polywatch-resident-status` | Fly cron 每 2 分钟是生产主监控；GitHub schedule 只作异地兜底。检查 cron machine、最近 tick 和状态文件，不能把 workflow 配置当成实际执行 SLA。 |
| R2 warning | L1 `r2:upload_recent_success` | 归档路径降级不自动否定 SQLite 快照，但必须记录并恢复冷存储保护。 |

原理和长链路故障案例请链接阅读 [07-观察市场](learning/07-观察市场.md)、[08-生产化部署](learning/08-生产化部署.md) 和 [09-生产化运维](learning/09-生产化运维.md)。

## 8. 运维与恢复

**永远从只读诊断开始**：保存 strict `/health` body、release/instance 识别、首次异常时间，再读日志和上游新鲜度。先确定是本地路径、候选版本、还是当前生产实例；先定位故障域，再选恢复动作。

restart、deploy、修改 secrets 和 chaos 注入都是**生产 mutation**，需明确的目标 app/instance、授权、当前证据保存、回滚/清理路径和事后 strict `/health` 复验。不要因为 `/healthz` 仍为 200 就跳过这些门。

受控恢复顺序是：固化证据 → 确定单一故障域 → 选最小 mutation → 记录操作者/时间/对象 → 等待数据新鲜度窗口 → 同一 strict 检查复验 → 记录结果。仅路由恢复但 cursor/mirror 仍 stale 不算恢复完成。

## 9. 命令安全分级索引

### 日常只读

- `make perception-status`、`make perception-groups limit=100`、`make perception-incidents limit=100`：从云端机会优先读模型读取稳定 JSON。每次读取最多 500 项、HTTP 最长 10 秒；`opportunities.status=available,count=0,reason=no-certified-edge` 才是有效零机会，HTTP 503 / `unavailable` 表示 worker 或历史证据链不可用，不能解释成零机会。这些命令不读取 HMAC secret，也不调用生产者。
- `make status`：前置为仓库 checkout；成功表示读到当前文档与 checkout，不表示云服务健康。
- `make snapshot-status`、`make snapshot-attempt-status` 和 `make overview`：前置为本地快照 DB；成功只解读本地事实。`snapshot-attempt-status` 是 JSON 格式的最新一次调度尝试，`latest: null` 表示无记录，不表示成功。
- `make collect-neg-risk-quotes db=data/state.db`：CLOB 请求只读、但会把一个 complete-or-failed run 写进本地 SQLite；不是 production cron、部署或订单命令。`make scan-arb-quotes` 只读该本地 run；两者都不证明生产容量或 readiness。
- `make smoke-health-prod`、`make smoke-market-truth-prod`、`make smoke-l2-health-prod`、`make smoke-l2-health-strict-prod`、`make diagnose-arb-feed-prod min_edge_bps=0`：前置为网络可达；分别解读 L1 strict health、L1 最新市场全集发布、L2 `/healthz` 可达性、L2 strict health 和 verified 机会 feed。最后一个只有完整 verified payload 的 `available-zero`/`available-opportunities` 才 exit=0；其余状态均不是零机会。
- `make fly-l2-status` 和 `make fly-l2-logs`：前置为 flyctl keychain auth；是诊断性读取，日志可能含运维元数据，不对外粘贴。
- `make polywatch-resident-status`：前置为 flyctl keychain auth；只读主监控 cron machine、最近日志和告警/恢复状态。生产主监控每 2 分钟运行，GitHub 定时器不是准点 SLA。
- `make smoke-l2-dashboard`：前置为网络；200 只表示 candidates、asset TOB/trades 和 signals 路由或 Auth 页可达。L3 生产检查显式跑 `make smoke-l3-dashboard asset_id=<known-id> URL=https://polymarket-arbitrage-jiangwen-su-s-projects.vercel.app`，再用已认证浏览器验证内容并手工检查 `/status`。
- `make l3-evidence-status`：只读最新 boot、身份、membership 和证据连续性锚点；缺证据或不一致必须按 NOT-CLOSED 处理。
- `make l3-soak-checkpoint manifest=<path> start=<ISO> end=<ISO> output=<unique-path>`：只读精确 `[start,end)` 数据并以 O_EXCL 新建一个 canonical checkpoint；不会覆盖报告，也不会绑定 manifest。
- `make l3-soak-verify manifest=<path> start=<ISO> end=<ISO>`：只读重载 manifest 声明的 T+0/T+6/T+12/T+18/T+24 五份报告，逐份重查 raw rows/hash，最后要求精确 24h PASS。
- `make l3-evidence-retention-check`：只读五张证据表的 server `recorded_at` 范围与行数，验证配置和已成熟历史没有早于 30 天被删除。
- `make l3-runtime-credential-check expected_ref=<ref>`、`make l3-retention-operator-check expected_ref=<ref>` 和 `make supabase-prod-revision expected_ref=<ref> expected_revision=<rev>`：对应 DSN 必须显式带唯一的 `sslmode=require|verify-ca|verify-full`；检查还会用当前 PostgreSQL session 证明 TLS 已实际启用。runtime/retention 检查枚举递归角色成员关系、public 全部表/视图/序列有效权限和相关函数显式 ACL，任何额外 capability（包括 `pg_read_all_data`）、额外 public 对象权限、RLS 关闭或 restrictive cursor policy 都返回 NOT-CLOSED。输出只含目标、角色、布尔 TLS 证明和 sslmode，不含 DSN、cipher 或 secret。

### 本地 mutation

- `make snapshot-markets-v`：需 Polymarket 网络和本地磁盘；会写 SQLite/Parquet，成功标准是 snapshot 流程和 validator 完成，再用 `make snapshot-status` 确认产物。
- `make sync-structure-local`：会写本地 Gamma-only Structure，供本地 Quote/M2 合同验证；不连接 CLOB、不写 Parquet/R2。`make archive-markets-local`：会写本地 CLOB/Parquet 历史归档并可在已配置时上传 R2；它绝不发布 online `markets`，也不是生产定时任务。
- `make daemon-run-local` 和 `make daemon-l2-run-local`：需本地端口、配置和可写数据路径；成功启动只说明本地进程存活，还要读本地 strict `/health`。
- `make dashboard-dev`：需 dashboard 依赖和本地 Supabase 环境；启动 dev server 不改生产，但会创建本地 build/cache 状态。
- `make l3-soak-manifest start=<ISO> end=<ISO> output=<unique-path>`：读取当前 runtime identity，但 mutation 仅限以 O_EXCL 创建一个新的本地 manifest 文件；它不绑定数据库，也不授权生产。

### 生产 mutation

- `make queue-discovery`、`make queue-reconciliation`：需要 `POLYARB_SCAN_SHARED_SECRET`，使用时间戳、nonce、路径和精确 body 绑定的 HMAC；认证与队列操作都写入可完整重放验证的 append-only hash chain。只有组件启用、没有 active incident、producer 未 paused/unavailable、resource decision 仍有效且未 shed 时，才会写入一个可合并的 durable wake-up。它们不会直接调用 producer、改变市场事实或绕过单 worker 并发；重复业务请求返回 `already_queued`，认证 replay 返回 401。消费方先 peek 精确 nonce，只有正常串行 Discovery/Reconciliation attempt 写下 terminal evidence 后才原子消费；进程 crash/cancel 会保留 wake-up 供重试，所以命令成功不等于采集完成。
- `make smoke-test`：仅用于 post-deploy；`scripts/deploy_smoke.sh` 在 flyctl 可用时可执行 `scale count`归一化 L1 进程规模，因此不属于日常只读巡检。需 flyctl 目标与生产 mutation 授权。
- `make deploy`：需 flyctl keychain auth；会把当前 checkout 的精确 Git SHA 注入 L1 `releaseId`、部署 L1，并以非交互方式调整进程规模；必须有发布授权、回滚点和事后 strict health 证据。部署后 `make smoke-health-prod` 返回的 `releaseId` 必须与部署前 `git rev-parse HEAD` 完全一致，否则该发布不合格。
- `make dashboard-deploy`：需 Vercel 登录；会推广到生产，成功部署后仍要验证 Auth 内数据而不是只看 200。
- `make unpause-prod`：需 `POLYARB_SCAN_SHARED_SECRET`；会改变 L1 scheduler 状态，只在根因已处理且已授权时使用。
- `make l3-soak-manifest-bind manifest=<path>`：独立的 pre-T0 生产 append。只有完成 migration、runtime/retention credential、revision 和部署审批后才可执行；它写入唯一 `soak_manifest_bound`，不能由 status/checkpoint/verify 隐式调用。
- `make l3-evidence-retention-cleanup cutoff=<ISO> protected_start=<ISO> protected_end=<ISO> approve=DELETE_EXPIRED_L3_EVIDENCE`：生产删除操作，只能使用独立 `L3_RETENTION_DSN` 和 `l3_retention_operator`，没有 daemon DSN fallback；protected window、30 天 cutoff、显式 approval 和单独授权缺一不可。
- 任何 Fly restart 或 secrets 变更：需明确 app 和审批；它们会改生产运行态/配置，不属于“快速诊断”。

本地测试、文档检查、image gate 或 deploy-path contract 通过都只证明当前 checkout 可进入后续审批。**LOCAL PASS DOES NOT AUTHORIZE PRODUCTION**；它不授权 migration、credential provisioning、secret mutation、deploy、restart、manifest bind、retention deletion、chaos 或交易。

### chaos

- `make chaos-l2-baseline`：先固化基线；需 flyctl auth，并且必须在任何注入前通过。
- `make chaos-l2-fly-image-check`：完整打印当前 Fly image 的工具矩阵，默认只把 `python` 当作 required gate；optional MISS 是替代设计证据而非失败。需要更严格的 plan 可传 `required="python curl"`；结果仍是注入前置证据，不是业务 health。
- `make chaos-l2-inj1` 等注入 target 会主动扰动生产，只能在批准窗口中运行；注入后必须跑 `make chaos-l2-cleanup`、复验 strict `/health` 并保留审计记录。

## 10. 持续同步协议与变更记录

事实层级从高到低是：**当前生产 strict `/health` + 实际读模型时间戳** → [`.planning/CURRENT.md`](../.planning/CURRENT.md) 的最新已审计结论 → 当前仓库的实现/测试/候选发布证据 → 历史 SUMMARY/JOURNAL/学习文档。低层证据不能覆盖更新的高层事实；生产数据已过时时，strict health 的 fail 优先于仓库里的“已完成”。

每次修改 M1 Make 命令、CLI、health 字段、Dashboard 路由、schema/migration 或功能就绪结论时：

1. 同一个 change 中更新本手册对应解释、矩阵状态或 sync log。
2. 跑 `make docs-m1-check`，确认命令、链接、路由、health marker 和就绪标签仍对应仓库事实。
3. 从 05.2-03 起，**staged guard 已在 pre-commit hook 生效**：已暂存的 M1 public contract 变更必须在同一 change 中暂存本手册更新才能通过。有 operator impact 时同步解释或矩阵；无 operator impact 时在本节追加可审计 sync log 理由。unrelated、test-only、log-only 变更不触发该 gate。
4. 需要修改产品就绪性时，先更新 CURRENT 的动态证据，再同步手册；不用手册反向“宣布”生产已就绪。

### 可审计 sync log 格式

`YYYY-MM-DD | commit/release | changed contract | operator impact or no-operator-impact reason | evidence command/artifact | author/reviewer`

- `2026-07-26 | 05-06 CI baseline | repository-wide Ruff safe fixes and formatting | no operator impact; public contracts unchanged | uv run ruff check src/ tests/ + uv run pytest -q tests/ | Codex`
- `2026-07-27 | L1 continuity repair | added machine-readable child-process output and isolated snapshot/quote collection from the HTTP process | no operator workflow impact; existing Make/CLI defaults and health/feed schemas are unchanged, while internal cancellation and responsiveness are bounded | focused subprocess contract tests + full pytest + make docs-m1-check | Codex`
- `2026-07-27 | L1 resource-priority repair | lowered the internal snapshot child priority and extended the renewable quote-run lease to 180 seconds | no operator workflow impact; public Make/CLI defaults and response schemas are unchanged, while quote freshness can survive single-vCPU snapshot contention below the 300-second SLA | focused scheduler/CLI/lease tests + full pytest + production overlap proof | Codex`
- `2026-07-27 | L1 OOM headroom repair | retained only compact certified quote metadata plus precomputed opportunities after each full projection proof and classified snapshot SIGKILL as a possible cgroup OOM | no operator workflow impact; health/feed identity gates and public schemas are unchanged, while full run legs, quotes, and source-universe objects are released before the scheduler wait | weak-reference retention regression + signal classification test + full pytest + production RSS/snapshot proof | Codex`
- `2026-07-27 | L1 allocator headroom repair | ran fail-soft GC and Linux malloc_trim after releasing each full certified projection | no operator workflow impact; collection, health, and opportunity contracts are unchanged, while unused allocator pages are returned to the 1GB cgroup before snapshot/quote overlap | release-order + fail-soft trim tests + full pytest + production cgroup/RSS proof | Codex`
- `2026-07-28 | neg-risk watcher cloud controls | added public observer map/status/history reads and HMAC-gated normal-cycle Structure/Quote queue controls | operator impact is bounded: reads and observer cards remain no-order/no-execution, controls fail closed without local HMAC authority and never revive PAUSED Structure; full workflow and teaching expansion is deferred to Task 6 | Task 5 focused HTTP/Make/scheduler/quote tests + make docs-m1-check | Codex`
- `2026-07-28 | neg-risk watcher truth-chain repair | bound current observer cards and legacy-feed IDs to the newest complete Structure membership and fresh matching complete Quote run; shared the global-reconciling watcher with status | no new operator action: stale observer facts stay replayable but are no longer advertised as current/executable inputs, and status now reports the reconciliation owner; all outputs remain no-order/no-execution | Task 5 regression + full focused verification + make docs-m1-check | Codex`
- `2026-07-28 | adaptive Structure schedule | derived bounded timeout/cadence from the latest 30 durable attempts and exposed the persisted effective schedule in strict health | operator impact: inspect snapshot:schedule instead of assuming a fixed 240s/300s deadline; timeout failures back off immediately, ordinary changes cool down for three attempts, limits remain 180-600s timeout and 300-900s cadence, and PAUSED still requires explicit recovery | adaptive policy/scheduler/storage/health RED-GREEN + make docs-m1-check | Codex`
- `2026-07-28 | opportunity-first Slice C | added default-off one-page Discovery with atomic certification/revocation, actual-Candidate durable Quote freshness, capacity-proven 60-second admission, bounded bulk selection, attempt-start/breach receipts, complete cursor history, rolling coverage, and one-snapshot local perception-discovery-status | operator impact is local/read-only until later cutover: low coverage exits zero as a measured fact, a deadline breach is non-ready evidence, invalid authority/state exits nonzero, and no code or UI claims zero-miss discovery | Task 3 review-remediated focused/regression tests + make perception-discovery-status fixture + make docs-m1-check | Codex`
- `2026-07-28 | opportunity-first Slice D | added default-off checkpointed Full Reconciliation with page receipts, exact restart cursor, terminal completion proof, atomic concurrency-safe diff, scoped health and Make status/run entries; legacy Structure loop is independently default-off while history remains | operator impact is local-only before deployment: reconcile-market-map mutates only the selected local SQLite one bounded page at a time, reconciliation-status is read-only, and incomplete/stalled reconciliation never gates Candidate availability | Task 4 focused/proportional tests + fixture status + make docs-m1-check | Codex`
- `2026-07-28 | opportunity-first Slice E | added default-off append-only incident lifecycle, component-specific post-recovery writer proof, deterministic resource shedding with cooldown, and shell-free isolated producer supervision | operator impact remains local-only before qualification: no deploy/trade authority; strict health exposes open incidents/resource mode and a successful restart alone cannot claim recovery | Task 5 lifecycle/resource/subprocess/health fault matrix + full regression | Codex`
- `2026-07-28 | opportunity-first Slice F API/control candidate | added bounded read-only perception status/group/history/discovery/reconciliation/incident HTTP projections plus replay-resistant coalescing producer wake-ups | operator reads now distinguish valid zero from unavailable evidence; authenticated controls only enqueue enabled serial producers and remain default-off/no-deploy/no-trade before Task 7/8 qualification | Task 6 HTTP/control/Make adversarial tests + docs-m1-check | Codex`
- `2026-07-28 | opportunity-first Slice F review remediation | made each GET a bounded single-snapshot proof, strengthened Candidate row/membership authority and recursive secret redaction, and changed operator auth/queue/consume to deadline-safe append-only hash chains with incident/resource/producer authority | controls now fail closed for active incidents, paused/unavailable producers, shed/expired resources or corrupt history; crash/cancel before terminal attempt evidence preserves the exact wake-up for retry | 19 focused + 321 proportional + 2596 full collected tests + docs/planning gates | Codex`
- `2026-07-28 | opportunity-first Slice F formal re-review remediation | added absolute read-worker convergence, full Group/Quote/fact as-of replay, auth-before-64KiB body rejection, one shared bounded auth+queue deadline, replay-window nonce pruning with retained queue auth proof, and atomic/idempotent legacy proof migration | operators receive fail-closed 409/503 instead of valid-zero or a late mutation when history, capacity, migration, timeout or producer terminal evidence is invalid; failed/yielded/cancelled runs preserve the exact wake-up | 41 focused + 343 proportional + 2618 full collected tests + docs/planning gates | Codex`

### 变更记录

- `2026-07-18 | Phase 05.2-02 | initial ten-section living manual | operator entry point established; no production mutation | make docs-m1-check + focused acceptance tests | Task 2`
- `2026-07-18 | Phase 05.2-02 review | corrected read-only health, candidate-chain meaning, hook timing, secret, and Dashboard coverage | operator safety facts tightened; smoke-health-prod added | focused RED/GREEN + make docs-m1-check | Task 2 follow-up`
- `2026-07-18 | Phase 05.2-03 | activated staged pre-commit guard for M1 public contracts | contract changes require a staged manual explanation/matrix update or an auditable no-operator-impact sync-log entry; unrelated, test-only, and log-only changes remain no-op | focused hook integration tests + make docs-m1-check + bash -n .githooks/pre-commit | Task 3`
- `2026-07-18 | Phase 05.2-05 | added smoke-l2-health-strict-prod read-only Make entry for strict L2 health | daily, candidate-chain, and read-only workflows no longer require a direct URL command; smoke-l2-health-prod remains /healthz reachability only | focused RED/GREEN + proportional suite + read-only walkthrough | Task 5`
- `2026-07-18 | whole-branch review | downgraded opportunity-feed readiness, hardened staged surface/sync checks and read-only health curl, replaced post-commit amend with pre-commit generated-tree review | operators now distinguish HTTP 503 from valid zero opportunities; contract and climb state updates fail safely | focused contract/climb tests + make docs-m1-check + planning-status | Final review remediation`
- `2026-07-18 | re-review | made staged validation index-exact, relaxed unchanged climb projections, corrected Dashboard production URL | MM index/worktree splits no longer bypass contract checks; deletion and byte-identical projection paths are covered | focused MM/hook tests + proportional suite + docs/planning checks | Re-review remediation`
- `2026-07-19 | H-008 Task 3 | diagnose-arb-feed-prod read-only feed diagnosis | one curlrc-isolated GET retains HTTP bodies and distinguishes zero, available, stale, unavailable, invalid, and transport states; no producer repair or readiness promotion | focused RED/GREEN + make docs-m1-check | Codex`
- `2026-07-19 | H-009 Task 4 | local known-universe quote operator path | added local collector/complete-run scan and fixture-only evaluator documentation; production remains conditional/not ready pending separately authorized deployment/scheduling, timestamped read-only capacity observation, repeated complete runs, and immutable evidence | focused RED/GREEN + make eval-local profile=opportunity-feed-cadence-sla + make docs-m1-check | Codex`
- `2026-07-20 | Phase 05.1 Plan 04 closure | chaos-l2-fly-image-check accepts current and legacy flyctl JSON image-ref shapes | no operator workflow or safety class changed; the existing image-aware read-only command now resolves the documented deployed image across flyctl versions | focused Makefile RED/GREEN + deployed-image matrix + make docs-m1-check | Codex`
- `2026-07-20 | Phase 05.3 Plan 04 | added scan-l3-seed and made l3-promote-dry-run fully mutation-free | operators can inspect deterministic observation coverage and proposed ten-token membership without changing WS, Supabase, LKG, active, or freshness state; local PASS does not imply deploy/10-of-10/soak | Phase focused + full pytest + make docs-m1-check + planning-status | Codex`
- `2026-07-20 | L1 release 127 / L2 release 37 | Alembic 006 token pairs, price-ranked book projection, newest-row-per-asset selection, and pre-LIMIT Yes-side identity filtering held exact 10/10 across two real ticks with book writes | L3 moves from unavailable to conditional/soak-ready; two ticks still cannot replace the 24h strict gate | exact Fly instance + promoter log + HTTP health + Supabase depth evidence + full pytest | Codex`
- `2026-07-23 | Phase 05.4 Plan 01 final review | defined l2_runtime_db_dsn / POLYARB_L2_RUNTIME_DB_DSN but did not wire it in Plan 01 | no operator impact: current operator commands and production behavior are unchanged; Plan 02 will wire the setting and Plan 05 separately gates credential provisioning and deployment | focused RED/GREEN + real PostgreSQL suite + make docs-m1-check | Codex`
- `2026-07-23 | Phase 05.4 Plan 04 Task 2 final credential review | tightened L3 runtime/retention credential proof, production revision TLS proof, exact manifest binding, and consumer-scoped cursor RLS | hosted proof DSNs require explicit strong sslmode and an encrypted live session; checks reject recursive extra roles, any unexpected public object/effective privilege or explicit function ACL, disabled RLS/restrictive policy, and any forged manifest identity/detail/time field | test_l3_evidence_cli + test_007 real PostgreSQL roundtrip + Ruff/compile + make docs-m1-check | Codex + review remediation`
- `2026-07-23 | Phase 05.4 Plan 04 Task 3 review-loop | separated the complete image tool observation matrix from plan-specific required primitives | default image check now passes when Python is present despite documented optional MISS results; operators can preserve stricter fail-closed gates with required="python curl" | focused Make/chaos tests + authenticated make chaos-l2-fly-image-check + make docs-m1-check | Codex + review remediation`
- `2026-07-23 | Phase 05.4 Plan 04 Task 4 | documented L3 exact-window read surfaces, separate manifest/retention mutations, R08 and release-37 boundaries | operators can audit local evidence without confusing reachable deploy paths or local PASS with production authorization | focused/full pytest + docs/Ruff/compile/image/planning gates | Codex`
- `2026-07-24 | Phase 05.4 Plan 05 production gate | chaos-l2-fly-image-check resolves current digest-based Fly machine config images as well as legacy tag/image-ref shapes | no safety class changed; the existing read-only image-aware gate now verifies the actual deployed digest instead of failing before inspection on current flyctl JSON | focused Makefile RED/GREEN + authenticated make chaos-l2-fly-image-check + make docs-m1-check | Codex`
- `2026-07-24 | Phase 05.4 Plan 05 A5 active | production 007, isolated runtime/retention roles, exact-SHA L2 identity, OHLC source-observation freshness and startup sampler gate are deployed; A5 T0 plus T0+546s audit pass | operators may inspect the active immutable window but must keep L3 conditional until T+6/T+12/T+18/T+24 and final verify all pass | A5 manifest/T0 report + SOAK-LOG + production Fly/DB audit + make docs-m1-check | Codex`
- `2026-07-26 | Phase 05.5 / L1 release 131 | enabled the in-process 120-second neg-risk quote worker, durable quote health, and exact release SHA in /health | operators can use the production opportunity feed as a known-universe gross-before-fees discovery input after current health passes; it remains non-trading | runs 2→3→4 1,278/1,278 + repeated make diagnose-arb-feed-prod + release 131 health | Codex`
- `2026-07-26 | Phase 05 Plan 07 pre-deploy | added generation-scoped L3 target evidence, atomic 10/10/10 publication, sampler transition lock, and timeout compensation | membership_convergence and worst_market_freshness now have explicit operator meanings; inspect strict health, resident state, logs, and durable evidence before any restart | full pytest + scoped Ruff + make docs-m1-check + planning-status | Codex`
- `2026-07-26 | Phase 05 Plan 07 release-73 rejection | real promoter/quiet boundaries proved destructive timeout compensation caused generation churn and persisted two 8/10 samples; corrected repair reuses unchanged target evidence, preserves control-consistent sockets, and gates durable sampling during reconnect convergence | release 73/boot 60c3b70b… are rejected; live health still alerts immediately and stale sample age remains the no-convergence alarm | production runtime-event/promoter/sample SQL + 232 focused tests + full pytest + scoped Ruff | Codex`
- `2026-07-26 | Phase 05-06 resident Polywatch | documented Telegram destination plus copy-paste dry-run and resident-status checks near the daily workflow | operators no longer need to reconstruct alert reception or local inspection from the troubleshooting/index sections | focused manual contract + make docs-m1-check + live dry/status reads | Codex`
- `2026-07-26 | Phase 05-07 release 75 continuity | documented the local launchd checkpoint scheduler, log/status commands, sleep behavior, and its separation from Fly/Telegram live monitoring | T+6/T+12/T+18/T+24 evidence no longer depends on a foreground terminal wait, while production alerting stays resident | launchctl preflight + not-before guard + live Polywatch check + make docs-m1-check | Codex`
- `2026-07-27 | M1 verified market truth pre-deploy | replaced truncated offset assumptions with complete keyset coverage, exact event membership, supported/augmented classification, provenance-bound quote runs, market_truth health and Polywatch alarm | M1→M2 use is paused until current production proves coverage=verified-standard-neg-risk; historical 1,278/1,278 capacity runs no longer certify the universe | focused RED/GREEN + full local qualification + L1-only production gate | Codex`
- `2026-07-27 | M1 verified market truth deploy repair | make deploy binds the exact checkout SHA into L1 releaseId and makes app/cron scale convergence non-interactive | a local deploy that reports releaseId=dev or waits for scale confirmation is rejected; operators must compare production health releaseId to the pre-deploy HEAD | focused Makefile RED/GREEN + make -n deploy + post-deploy strict health | Codex`
- `2026-07-27 | M1 verified market truth OOM repair | snapshot CLOB fetch projects each chunk to top ask/bid, releases reconciliation maps before CLOB, and bounds a global CLOB outage to one issue | complete-universe collection must keep HTTP responsive and fit the existing 1GB L1 machine; an OOM/restart or health timeout during collection rejects the release | focused RED/GREEN + 47,500-market/95,000-token RSS regression + production in-flight/final latency proof | Codex`
- `2026-07-27 | L1 attempt-truth health | added snapshot:latest_attempt and snapshot:failure_counter to strict health | operators can distinguish an old published snapshot from the latest scheduler failure: failed/cancelled is warn while market truth remains fresh, and five consecutive failures become fail/pause; never-started is compatibility-neutral, not success | scheduler/health RED-GREEN + make docs-m1-check | Codex`
- `2026-07-27 | L1 bounded Structure run | enforced a 240-second subprocess deadline and expose an overdue running attempt as strict-health failure | a CPU-bound or upstream-stalled Structure run cannot silently occupy all future 5-minute cycles; timeout is durable evidence and the following cycle can recover | scheduler/health RED-GREEN + production follow-up | Codex`
- `2026-07-27 | Quote Structure-handoff retry | classify the precise stale-Structure rejection from the isolated Quote collector and retry immediately | a valid rejection during a Structure publish no longer creates a predictable 120-second M2/Telegram gap; CLOB and other collector failures remain visible | quote-worker RED-GREEN + production follow-up | Codex`
- `2026-07-27 | Quote feed restart continuity | restore the compact opportunity feed only from a fully certified durable Quote run before fresh collection begins | a daemon restart does not discard a still-fresh, identity-bound M2 input; empty, stale or mismatched durable evidence remains unavailable | quote-worker RED-GREEN + production follow-up | Codex`
- `2026-07-27 | Quote lease shutdown handoff | atomically fail only collecting Quote runs after the stopped worker's child is terminated | a rolling restart cannot leave an orphan lease that blocks the replacement worker; complete certified runs remain immutable | quote-store/worker RED-GREEN + production follow-up | Codex`
- `2026-07-27 | Polywatch component incidents | split resident alert state into independent L1, opportunity, L2, and Dashboard incidents | each component now alerts and recovers independently; a continuing L2 incident cannot hide L1 recovery, and a failed Telegram delivery retains only that component for retry | watcher RED-GREEN + make docs-m1-check | Codex`
- `2026-07-27 | Structure/Archive identity foundation | added data-product and archive-status metadata to snapshot records | no operator workflow changes yet; legacy combined rows remain preserved as history until a separately verified Structure publisher and Archive worker are introduced | SQLite migration RED-GREEN + make docs-m1-check | Codex`
- `2026-07-27 | Structure-only consumer gate | strict market-truth health and Quote/M2 universe selection now require data_product=structure | after deployment a legacy combined snapshot remains historical evidence but cannot certify a current M2 input; the first new Structure publication is therefore an explicit production gate | health/quote RED-GREEN + make docs-m1-check | Codex`
- `2026-07-27 | Structure/Archive execution split | online scheduler now requests Gamma-only Structure; explicit local Structure and Archive Make commands, non-blocking archive health, and retention-safe no-archive marker are added | operators can create a certified local Structure without CLOB/Parquet/R2; Archive failure is visible but cannot replace Structure/Quote/M2 or make strict health fail | focused separation tests + 371 related tests + Ruff + make docs-m1-check | Codex`
- `2026-07-27 | mounted-ownership cron repair | removed volume-less cron snapshot and purge jobs; retained only resident Polywatch | production market publication is now owned only by the mounted app scheduler; cron remains the independent monitoring surface and cannot create false snapshot evidence | cron contract RED-GREEN + make docs-m1-check | Codex`
- `2026-07-27 | persisted snapshot status | wrote the orchestrator's OK/DEGRADED/FAILED result atomically with each snapshot and made health read that field | a complete-but-degraded Structure is now shown as `snapshot:last_status=DEGRADED/warn`, never inferred as OK from an empty failure note | health regression RED-GREEN + migration/health suite + make docs-m1-check | Codex`
