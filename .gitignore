# Vendored references (large, fetched separately)
3th-party/

# Research outputs (working notes, not source)
.research/

# Python — the canonical .gitignore for src code is rebuilt by Plan 1 T5
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/

# Env / secrets
.env
.env.*
!.env.example

# Data — generated, not source
data/state.db
data/state.db-wal
data/state.db-shm
data/state.db.bak-*
data/snapshots/
data/.cache/

# OS
.DS_Store

# polyarb (added by m1-perception phase 01)
build/
dist/
.pytest_cache/
.ruff_cache/
pytestdebug.log

# Claude Code harness state
.claude/

# Agent implementation worktrees (repository-local, isolated, disposable)
.worktrees/

# climb execution artifacts; canonical state is tracked in docs/status/climb/
runs/climb/

# gsd-tools ephemeral runtime state (e.g., _auto_chain_active flag set on every workflow start)
.planning/config.json

# Phase 1.1 user-local config (template watchlist.yaml.example committed; actual
# watchlist.yaml is per-user, sync via git only if user opts in).
watchlist.yaml

# Phase 1.1 scan outputs (large, regenerable from snapshot data).
data/scans/

# CodeGraph local index (per-machine, regenerable; tool has its own .gitignore inside).
.codegraph/


# Plan 02-09: host-specific memory-budget calibration baseline (not portable)
tests/m1-perception/fixtures/baseline_rss.txt
/tmp/02-09-step1-snapshot.log

# Phase 02 Plan 02-06: Vercel Next.js dashboard build/runtime artifacts
dashboard/.next/
dashboard/node_modules/
dashboard/.vercel/
dashboard/.env.local
dashboard/.pnpm-store/

# Playwright CLI/MCP browser session artifacts (snapshots, console logs)
.playwright-cli/
.playwright-mcp/
