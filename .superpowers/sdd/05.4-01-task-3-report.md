# Phase 05.4 Plan 01 Task 3 Report

## Status

Implemented the append-only asyncpg evidence boundary, exact-window snapshot reads,
diagnostic status and retention bounds, and the separately credentialed retention
operator. No planning state, daemon construction, configuration, deployment, or
production resource was changed.

## RED evidence

1. Initial store contract:
   - Command: `uv run pytest tests/m1-perception/test_l3_evidence_store.py -q -rs`
   - Result: collection failed with `ImportError` because
     `polyarb.storage.l3_evidence_store` did not exist. This was the expected missing
     feature failure before production code was added.
2. Nested immutable event detail:
   - Command:
     `uv run pytest tests/m1-perception/test_l3_evidence_store.py::test_event_append_serializes_nested_immutable_detail -q`
   - Result: failed with `TypeError: Object of type mappingproxy is not JSON serializable`.
3. Full fail-soft argument boundary:
   - Command:
     `uv run pytest tests/m1-perception/test_l3_evidence_store.py::test_one_shot_argument_failures_stay_inside_fail_soft_envelope -q`
   - Result: three expected `AttributeError` failures showed argument projection was
     outside the shared fail-soft envelope.

## GREEN evidence

- Nested immutable JSON regression plus bounded argument failures: `4 passed`.
- Focused store suite after the first GREEN/refactor cycle: `14 passed`.
- Final required acceptance command:
  `uv run pytest tests/m1-perception/test_l3_evidence_store.py tests/m1-perception/test_l3_evidence.py tests/alembic/test_007.py -q -rs`
  produced `44 passed`, exit code 0.
- Final required Ruff command over all six requested files produced
  `All checks passed!`, exit code 0.
- `git diff --check` produced no errors.

## Real PostgreSQL proof

- PostgreSQL image: `postgres:16-alpine` via testcontainers and the local Docker daemon.
- New real-store cases executed:
  - `test_real_postgres_appends_duplicates_atomicity_windows_and_bounds`
  - `test_real_postgres_retention_requires_separate_login_and_protected_function`
- Existing real migration replay executed:
  - `test_007_upgrade_downgrade_upgrade_roundtrip`
- Final collected tests: 44 total (`18` store, `23` immutable records, `3` migration).
- Final skipped tests: **0**. The `-rs` output contained no skip section.
- The real-store suite proved duplicate rejection, all four terminal promoter statuses,
  five-market rollback with no parent/siblings committed, `[start,end)` boundaries,
  referenced pre-window boot retrieval, SQL book/OHLC coverage, immutable status,
  exact five-table bounds, daemon-role rejection, dedicated LOGIN operator membership,
  protected retention, and eligible-row deletion.

## Files

- `src/polyarb/storage/l3_evidence_store.py`
- `tests/m1-perception/test_l3_evidence_store.py`
- `.superpowers/sdd/05.4-01-task-3-report.md`

## Self-review

- All inserts use `$N` parameters and short-lived `asyncpg.connect(dsn=self._dsn)`
  connections closed in `finally`.
- `append_sample` revalidates five distinct markets and ten distinct token IDs, then
  inserts the health parent and five market rows in one transaction.
- Non-cancellation append failures report only operation and exception type; they
  return `False`. `CancelledError` is re-raised. Runtime anchors are never mutated.
- `fetch_window` uses one read-only repeatable-read transaction for all five evidence
  tables and both SQL coverage aggregates. Raw rows retain generated IDs and
  `recorded_at`.
- `L3EvidenceStore` exposes no UPDATE/DELETE/finalization/cleanup/generic execution API.
- `L3RetentionOperator` verifies a LOGIN member of `l3_retention_operator` that is not
  a member of `service_role`, then invokes only
  `SELECT * FROM l3_retention_cleanup($1,$2,$3)` for mutation.
- Production source contains no DSN value in SQL, logs, breadcrumbs, typed exception
  text, or object representation.

## Concerns

- The isolated PostgreSQL fixture explicitly grants the daemon test role `SELECT` on
  `l2_book_levels`, `l2_top_of_book`, and `l2_ohlc_1m` so it can exercise coverage.
  The accepted migrations model Supabase role grants separately; deployment must
  continue ensuring the actual daemon DB credential can read those existing L2
  surfaces. No migration or production privilege was changed in this task.
