/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep config minimal — Phase 02 dashboard is read-only + 1 Edge route.
  // Future deferred (Phase 02.1): images, i18n, Sentry integration.
};

export default nextConfig;
